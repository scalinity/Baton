#!/bin/sh
set -eu
. "$(dirname "$0")/common.sh"
ending=$(hook_envelope api-error) || exit 0
ending=$(printf '%s' "$ending"|jq -c --argjson payload "$payload" '. + {error:($payload.error // "unknown"),error_details:($payload.error_details // "")}')
hook_publish "$ending"
