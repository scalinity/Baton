# Decision log

Concise, dated decisions with rationale. Newest at the bottom. Status is `active` unless
superseded (then link the superseding entry). Specs state the current design; this file holds the
history. D-001 to D-017 are seeded from the wayfinder tickets under `.scratch/baton/issues/`, each
naming its ticket; D-018 onward were made while writing the founding set; D-025 to D-030 were
made by M01, D-031 to D-037 by M02, D-038 to D-049 by M03, D-050 to D-059 by M04, D-060 to D-069 by M05,
D-070 to D-076 by M06, D-077 to D-084 by M07, D-085 to D-091 by M07-b, D-092 to D-094 by M07-c, D-095 to D-137 by M07-d and the V1.1 authoring, D-138 to D-140 by M17, D-141 to D-144 by M17-b, and D-145 to D-146 by M09.
The next free number is **D-147**.

| ID | Date | Decision | Rationale | Status |
|---|---|---|---|---|
| D-001 | 2026-09-11 | **Baton is a relay and embeds no model call** (ADR 0001, `docs/adr/0001-baton-never-calls-a-model.md`). Judgement is dispatched as a session and returns as an artifact; it is never made inline in the tick. Sessions decide at both ends — part 4 rulings in, handover dispositions out — and the person answers everything a session still asks. Reversal condition: a judgement point recurring in the dispatch log that a session cannot pre-decide and a person tires of answering. | Every design that seated a Claude session as orchestrator reports needing nudges, waiting for input and misreporting state; a question a session with the whole project in context still asked is not one a smaller call answers better; no quota is spent on supervision and the relay is testable without an API key. "Relay or conductor". | active |
| D-002 | 2026-09-11 | **The handover is a machine-readable artifact in `~/.baton/inbox/`, authoritative, printed verbatim last as a guarded fallback**, and the convention is a six-clause project contract (`CONTRACT.md`) that Reclaim implements first. `outcome` ∈ complete / asking / stopped; `merged_as` is verified, never trusted; every eligible milestone is listed with a disposition and a brief pointer; no prompt text, no model. Baton's state lives outside every target repository and is not versioned in git. | Of twelve Reclaim transcripts three end with a message that is not the handover and two carry the wait judgement in prose; the transcript format is documented as internal; a shared file inside a checkout conflicts on every parallel close-out. "What a project hands to Baton". | active |
| D-003 | 2026-09-11 | **A Stop gate is injected into every dispatched session** through `--settings`: it gates on the artifact file, blocks once with the reason, then writes a `no-handover` artifact itself; it stands down while `background_tasks[]` is non-empty. Hand-started sessions carry no gate. | The gate fires inside a `--bg` session and its block reaches the model as a `user` record; a gate that blocked a session waiting on its own subagent manufactured a false handover, so the stand-down rule was added from the prototype. "What a project hands to Baton"; "Watching a dispatched session". | active |
| D-004 | 2026-09-11 | **One launchd agent on `StartInterval` 60 runs a tick that remembers nothing**, reconciling from the dispatch log, `claude agents --json`, the inbox, the plan file and one git check; an atomic `mkdir` lock serialises firings against hand runs; `caffeinate -i -w <pid>` per dispatch and `-i -t` per timed wait, detached under `AbandonProcessGroup`. | Hooks fire only inside a running session so something outside sessions must own time; `WatchPaths` is discouraged by its own manual; a tick that reconciles from scratch makes recovery the normal path. "Relay or conductor" §4. | active |
| D-005 | 2026-09-11 | **Shell**: `/bin/sh` with `set -eu`, `jq -e`, `awk`, git and claude; no packages, no build step. Reversal: a structure `jq` cannot express, or a file past a few hundred lines, means a Swift command-line tool for that piece. | The runtime is what ships with the Mac plus claude; the Stop gate must start in milliseconds and is shell whatever the tick is; tests are scripts over fixtures. "Relay or conductor" §6. | active |
| D-006 | 2026-09-11 | **Testing without quota**: one seam per outside thing (claude, the clock, caffeinate), a claude shim, hook fixtures piped on stdin, scenarios as fixture directories diffed after a tick, idempotence as the recovery test; launchd out of the tests; replayed transcripts and `-p` fixtures rejected. | The tick parses no transcript under the contract; `--bg` cannot combine with `-p`; the one live proof stays with the prototype. "Relay or conductor" §5. | active |
| D-007 | 2026-09-11 | **A fixed stops taxonomy with two bits per class**, every ending arriving as a file; API errors split by `error` alone; the wait is a fifteen-minute stop-then-resume spending no attempt, with ceilings that notify and never stop retries; the ladder is resume, redispatch, escalate; two continuation templates; `api-error` written by the injected StopFailure hook; the archive name suffixed with the consumed-at time. | A wrong label costs one refused request per interval, never a night; no transcript record carries a reset time as a field; limits reset at clock times so a fixed interval loses at most one interval. "What stops a session, and what happens next". | active |
| D-008 | 2026-09-11 | **Stall** is thirty minutes of unchanged transcripts, the session's and its subagents', on a live non-waiting row with no artifact — notify once, never touch. **Long-running** is six hours since the attempt's latest dispatch, resume or takeover — notify once. **Crash** is `pid: null` (while `state` may still read `working`) with no artifact, confirmed over two ticks, then stop, resume under the id if a transcript exists, else redispatch. A sleep leaves the row byte-identical. | `/review-2` parks the parent on one tool call for twenty minutes while its own transcript does not move; the longest legitimate gap measured was 1,368 s; `state: failed` arrives up to seventy seconds after the process dies. "What stops a session" §3, §4, §6; "Watching a dispatched session" items 23, 24. | active |
| D-009 | 2026-09-11 | **The permission mode is `bypassPermissions` on the flag, plus a per-project allowlist and two deny classes in the `--settings` file Baton composes**; no `ask` rules; three hooks only (Stop, StopFailure, statusLine). The target repository is untouched. | `auto` is unreachable in a `--bg` session (accepted, recorded as `default`, three ways), so the classifier design cannot be delivered; `default` parks on the first uncovered command and a night that parks has not run; deny rules are enforced under bypass, including the `osascript` phrase inside a quoted `-e` argument. The cost — nothing prompts, so the channel carries only endings — is accepted. "Where an escalation goes" §1–§3 as re-decided by "Watching a dispatched session" and "Dispatching more than one at once" §4. | active |
| D-010 | 2026-09-11 | **Escalation policy**: a notification is kept working past, an escalation parks; both reach the Mac as a `display notification` and a log event; the message is address, content, verb; scope is a property of what failed (three project-scope failures plus a dead service); a parked lane unparks by ruling, answer in place, or edit — never by timeout; `baton answer <milestone>` resolves across projects and refuses ambiguity; the ruling label quotes the question; Remote Control only on a per-dispatch `Remote: yes`; `baton allow` writes a typed rule and never composes one. | A channel that buzzes for what cannot be acted on stops being read; a self-sent iMessage raises no iOS alert; standing Remote Control would store every transcript on Anthropic's servers nightly; a rule that matches nothing looks identical to one not yet needed. "Where an escalation goes"; "Watching a dispatched session" addendum. | amended by D-081 |
| D-011 | 2026-09-11 | **The plan file is the milestone table in the target's `docs/MILESTONES.md`**, located by header cells, read by column name, cells holding tokens, plus a gates table with a `Cleared` D-number; a parse failure refuses to dispatch for the project; `Status` is `done` by the close-out and `held` by a person; `baton plan` validates; `project.json` registers a project; `config.json` holds Baton's numbers (`cap` 2, `fableReserve` 80, `stallMinutes` 30, `longRunningHours` 6, `retryMinutes` 15, `caffeinateMaxHours` 6, `models`). | Two hand-maintained sources is the M27-in-no-row failure with two chances to happen; a table people already edit and a script parses is the only shape that cannot drift; a typo should stop dispatch rather than dispatch on a guess. "Dispatching more than one at once" §1, §6. | active |
| D-012 | 2026-09-11 | **Full Disk Access for a Baton-owned copy of the shell**, `~/.baton/bin/sh`, granted once by hand, as the launchd job's executable; every tick starts with a self-check (`cat` the plan file, `git rev-parse HEAD`) whose failure parks the project. | A launchd job under `/bin/sh` cannot execute a script under `~/Documents`, read a file there or run git there, while `stat` succeeds — a readability guard passes and the read fails; TCC records grants per executable; mirroring the plan into `~/.baton` would trust the session for the one fact Baton verifies, and moving projects out of `~/Documents` rewrites every absolute path in Reclaim. "Dispatching more than one at once" §2. | active |
| D-013 | 2026-09-11 | **Every milestone runs in `../<Project>-M<nn>` on branch `m<nn>`, created by Baton from `main` and reused on redispatch; the session merges its own branch, runs the project's standing check on `main`, fixes it or writes `main-broken`, and removes its worktree; the tick prunes a leftover only on a verified merge.** Baton detects no collisions. | The project's own convention, run by M10–M13 themselves; worktree isolation gates only Write and Edit, so the conflict check stands on the briefs; a `merge-failed` lane's worktree is the work and must not be pruned on `Status` alone. "Dispatching more than one at once" §5. | amended by D-078 |
| D-014 | 2026-09-11 | **A global cap of two sessions**, counting live rows and parked prompts, no per-model cap; **quota reactive**, Opus carrying on through a Fable hold, a Fable milestone never downgraded, `--fallback-model` never passed; **`fableReserve` 80** on the seven-day window as the one predictive number; **`model_not_found` escalates at once** and a plan edit redispatches. | Two Xcode builds is a property of the Mac; the status feed's windows are account-wide with no per-model window, so they cannot see the Fable limit coming; a flagless resume restores the saved `--model`, so no retry can pick up a corrected cell. "Dispatching more than one at once" §6, §7. | active |
| D-015 | 2026-09-11 | **One append-only `~/.baton/log.jsonl`** written only by the tick under the lock, a five-field envelope with absent-never-null fields and `at` with offset, eighteen event kinds, `attempt` and the resume count derived and stamped, every count keyed on `(project, milestone, attempt)`, prompt bodies in sidecars with a hash, the takeover rule as a comparison against the newest typed record across the whole milestone, consumption idempotent by the move, `~/.baton/last-tick` as the tick's clock, `status` as one whole-file view. | The log must be able to say a typed record is not one of Baton's; every project has an M01; a copy fork's transcript is a byte-for-byte copy of its parent's; a quiet tick writes no event, so the newest event cannot be the clock. "The dispatch log". | active |
| D-016 | 2026-09-11 | **Dropped, with revival conditions**: the permission-denial event and its two notification keys; the `permission` escalation class; the iMessage send. `prompt-lost` survives for the question case only and its ruling carries no dropped-call sentence. Revival: a dispatch that chooses `default`, or `auto` becoming reachable in `--bg`; a phone channel that raises an alert. | Both events need a `--bg` session to hold a permission prompt, which `bypassPermissions` makes impossible; a self-sent iMessage arrives on the phone and raises no alert on three sends. "The dispatch log" §2 footer; "Watching a dispatched session" item 33. | active |
| D-017 | 2026-09-11 | **Sessions are named `Baton · <project> · <milestone>`**, the name being the only ownership marker; the model is not in it. The agents view plus `Claude.app` is the window; no tab per session. **Rulings are delivered flagless**; an allowlist is widened by editing the settings file in place. The `<project>` is the basename of the canonical checkout. | `kind: "background"` does not separate Baton's rows from a person's; the model is restored on resume and visible in the status feed; any flag on `--bg --resume` forks a copy; `claude logs` is an ANSI replay with no newlines. "Watching a dispatched session"; "Dispatching more than one at once" §1. | amended by D-036 |
| D-018 | 2026-09-11 | **The running relay is the copy installed under `~/.baton/bin/` by `sh install.sh`**, which launchd runs and every dispatched session's hooks call. A merge on `main` changes nothing until a person runs the script. | A launchd job cannot execute anything under `~/Documents`, so a copy is needed in any case; making the copy explicit means a Baton milestone landing at 3 a.m. cannot break the tick that dispatched it, and a bad install is undone by checking out an earlier commit and installing again. The person installs after reading the merge, which is also when the bootstrap's next hand-run dispatch happens. | amended by D-079 |
| D-019 | 2026-09-11 | **Eight milestones, bootstrapped.** M01 (plan reader, hand-run dispatch, the log's dispatch-side half, the hooks, the seams, install) is built by hand on `main`; M02 (the inbox, the whole event table, the fifteen derivations, `status`) onward are dispatched by the Baton that exists so far — by hand with `baton dispatch` until M03 lands the tick, by the tick after that. M01 to M03 are watched at the keyboard; M04 is the first milestone a tick dispatches, still watched; **M06 is the first that runs unattended overnight**, because M04 (waits, the ladder) and M05 (escalations, `answer`) put recovery and the channel underneath it first. The event table lands in M02, not M03, because the whole schema must exist no later than the milestone that lands the tick. | Each milestone needs only what earlier ones built in order to be dispatched: M02's dispatch needs a plan reader and a dispatcher; M03's needs consumption so M02's `done` is trusted through `merged_as`; the tick's first act is to reconcile from the log, so the schema precedes it. Running unattended before the ladder and the channel exist would leave a stopped session unnoticed until morning with no rule to act on it. "The dispatch log" §12; this document set. | active |
| D-020 | 2026-09-11 | **Model per milestone**: M01 on Fable; M02 to M08 on Opus; `Effort` blank throughout. `/review-2`'s reviewers are pinned to Fable on M02 and M03 and left to the session's model elsewhere. | The allocation rule: the scarce model where there is no precedent and later work inherits. M01 sets every idiom the rest copy — the shell style, the seams, the fixture layout, the log append, the hooks. The derivations (M02) and the tick (M03) are what every later rule reads, so the scarce model reviews them rather than authors them. Baton is a shell relay whose design is decided; the remaining milestones implement written rules. The map's model note (2026-09-11) and the global allocation rule. | active |
| D-021 | 2026-09-11 | **Baton drives only itself and fixture projects until every Baton milestone is done; Reclaim comes after.** M08, Reclaim onboarding, is the last milestone and is held by a gate, `Reclaim migrated`, cleared by the D-number of the entry that records the migration commit. The migration commit in Reclaim is a person's act in an interactive Reclaim session, never a Baton session's: it adds the `Model`, `Effort`, `Remote` and `Status` columns and the gates table to `docs/MILESTONES.md` with every built milestone marked `done`, the slot line and the two exact headings in every brief that can still be dispatched, the close-out step in `CLAUDE.md` ("What a kickoff prompt contains" part 7 and "Handing over"), names the plan file in its Documents table, and takes Reclaim's next free D-number at the moment it is written. It happens after M14 closes, never while a milestone is in flight. The first Reclaim milestone Baton drives is whichever the person's gate clears first; M19 is recommended over M15 because it changes no real files. | Stated on 2026-09-11 while resolving "Baton's plan and its M01 prompt": Baton does not take over Reclaim until it is complete, and runs on test content until then. Reclaim's plan and `CLAUDE.md` are a person's documents under the contract (gates are cleared by a person's edit), and M14 was in flight at the time of writing with nothing allowed beside it. | active |
| D-022 | 2026-09-11 | **The contract's text lives once, in `CONTRACT.md` at Baton's root**; `docs/SPEC.md`'s REQ-CONTRACT family binds Baton to it and a target project's `CLAUDE.md` cites it by path. | A third project adopts it by citation rather than by copying a section out of a specification; one text cannot drift from itself. "What a project hands to Baton" deferred the location here. | active |
| D-023 | 2026-09-11 | **Baton keeps no `docs/STATUS.md`.** The plan file's `Status` column, each brief's completion evidence and `baton status` are the state; `CLAUDE.md`'s start-of-session list points at those. | A status file beside the plan file is a second source for the one thing that must not drift, and Baton's own verb already prints the state from the record. | active |
| D-024 | 2026-09-11 | **Repository layout**: `bin/baton` dispatches verbs only; each verb's work lives in a file under `lib/`; hooks under `hooks/`; `tests/run.sh` is the standing check over `tests/scenarios/` and `tests/payloads/`; `install.sh` at the root. Baton's own `permissions.json` allows `sh tests/run.sh` and `jq` and denies the two classes. | Keeps every file under the few-hundred-line reversal condition of D-005; the standing check must exist before M02 runs, because the close-out runs it on `main`. | active |
| D-025 | 2026-09-11 | **The hash normalisation is: strip exactly one trailing newline, nothing else** (`prompt_normalise` in `lib/log.sh`), applied to the sidecar file before hashing and, from M03, to a transcript record's text before comparing. The sidecar is written with a trailing newline as a text file; the prompt is passed on the command line without one, and the CLI stores the argument as given. | Item 45, measured offline on the prototype's two pairs: the kickoff prompt file `prompt-B.txt` normalised hashes `30a57496…`, equal to the typed record at 09:14:43 in both `5b7ffe74` and its copy `ea4b650c` (the raw file hashes `c1432fc6…`); the RULING line as passed inline hashes `b8841472…` through `sidecar_write`, equal to the records at 09:19:39 (`ea4b650c`) and 09:20:02 (`5b7ffe74`). Confirmed again on the live trial dispatch (`2491f994…` on both sides). | active |
| D-026 | 2026-09-11 | **The deny-rule forms for Baton's state paths** are `Read|Edit|Write(//<absolute path>)` per named file or `<dir>/**`, plus `Bash(*.baton/<name>*)` for every named path but `bin` (a session runs `~/.baton/bin/baton status` from its start-of-session rules, and a Bash rule cannot tell a run from a write) and `inbox`; the list `install.sh` writes names `log.jsonl`, `archive`, `rejected`, `prompts`, `settings`, `projects`, `bin`, `status`, `lock`, `config.json` and `last-tick`. A directory the list does not name is not denied, and a Bash fragment is never complete (a `cd ~/.baton && … >> log.jsonl` slips past it); the class stays "by named path" and the rail is against the honest attempt, not an adversary. A `permissions.json` with no deny rules fails the `settings` stage rather than dispatching without the rail. | Item 12, measured in the trial session under `bypassPermissions`: `Write` on `log.jsonl` and on `archive/probe.json` refused with "File is in a directory that is denied by your permission settings"; the Bash form refused `printf … >> ~/.baton/log.jsonl` with "Permission to use Bash with command … has been denied"; a Bash write into a new `~/.baton/probe-dir/` ran. The `//` form is the one `permission_suggestions` proposed for `~/.aws/**` in the prototype. | active |
| D-027 | 2026-09-11 | **A dispatch that backgrounds a worker no row ever carries is a `dispatch_failed`, classified from the service's own log.** `row_for_id` polls `agents --json` for thirty seconds and, each pass, reads `bg settled <id> (crashed): <detail>` from `~/.claude/daemon.log`; the detail is classified like stderr, so a missing `--settings` file is `stage: settings` within a beat. The log's path is the fifth seam, `BATON_DAEMON_LOG`. | Item 47, measured: `claude --bg … --settings <missing>` printed `backgrounded · 2a55c38b · …` on stdout, nothing on stderr, exit 0; the service logged `bg settled 2a55c38b (crashed): exit 1 before init — Error: Settings file not found: …` forty milliseconds later; no row, no transcript. The four known strings are therefore matched wherever they appear; only the stderr half of REQ-DISPATCH-08 was a guess the capture corrected. | active |
| D-028 | 2026-09-11 | **The composed hook commands carry `BATON_HOME`** beside `BATON_PROJECT` and `BATON_MILESTONE`, so the hooks reach the home the relay that dispatched them uses. | Without it a hook always reads `~/.baton`, so a session dispatched from a scratch home would write its artifact into the real inbox; with it the live proof ran wholly under the scratchpad and the real `~/.baton` held nothing but the install when M02 was dispatched. The seams reach the hooks through the only channel a hook has, its command line. | active |
| D-029 | 2026-09-11 | **No `register` verb.** `install.sh` writes Baton's own `project.json` and `permissions.json` if absent, with `path` the main worktree of the repository the script sits in; another project is registered by writing the two files by hand (M08 for Reclaim). The hand-run `dispatch` reads the brief at `docs/milestones/<milestone>.md` under `## Copy-ready session prompt` by convention, because no handover pointer exists at the bootstrap. | A verb the install script can express is a verb not added; two files written once per project is the whole registration. | active |
| D-030 | 2026-09-11 | **The library convention**: every fallible function prints its result on stdout on success and its failure detail on stdout with a non-zero status, so a caller captures once and branches on the status; multi-value results are one JSON object; `log_event` takes the envelope's four optional fields as positional arguments (empty means absent) and the event's own fields as one JSON argument. The test runner recomputes a `dispatch` event's `prompt_sha256` from its sidecar rather than freezing it, and `BATON_TESTS_FREEZE=yes` regenerates `expected/` from a run that has been read and judged right. | Globals do not survive `$(…)`, and two channels for one result is where a caller drops one; a frozen hash over text that holds a temporary path can never match, while a recomputed one proves the sidecar rule on every run. | active |
| D-031 | 2026-09-11 | **The derivation contract.** Every one of the fifteen recovery derivations is one function in `lib/derive.sh` printing exactly one JSON object on stdout, its result under a named key and a list result as an array under that key; a field it has no value for is absent, never null. The rows arrive as a JSON argument and the transcripts through a seam, so every fixture is a directory and no fixture starts a process. An empty `<project>` argument means every registered project, for the seven that scan across projects. All time arithmetic goes through `iso_epoch`, which converts an ISO 8601 string in `awk`, and derivation 1 names both halves of its one join, `in_flight` and `no_row`. | `status` and every later rule read a field rather than parse a line, and a derivation whose output is text is a derivation every reader re-parses differently. Converting a string Baton itself wrote is not an outside thing, and the date seam answers the scenario's `now` whatever format it is asked for, so it cannot also convert. The crash rule is exactly the complement of derivation 1's intersection, and rebuilding it in M03 would replay the fork chain a second time from the same log. | active |
| D-032 | 2026-09-11 | **`BATON_TRANSCRIPTS` is the sixth seam**, default `~/.claude/projects`, the tree derivation 3 globs for `*/<session>.jsonl`. A scenario supplies its own `transcripts/` directory; a scenario without one gets an empty tree, which is a lane with no transcript. | Derivation 3 is the only reader of transcript content in Baton and it must be testable; without the seam the takeover fixture would have to start a session to make a transcript, which no fixture may do. It is the same move as `BATON_DAEMON_LOG` in M01 (D-027): one variable per outside thing, added when the thing is first read. | active |
| D-033 | 2026-09-11 | **`written_by` is derived from the reason and authorship is not decidable from the artifact.** `no-handover` reads `stop-gate`, `api-error` reads `stop-failure`, everything else reads `session`. The Stop gate and the StopFailure hook write files a session could write byte for byte, and no field marks the writer, so the reserved-reason rule is enforced only where it can be: an `api-error` carrying no `error` is rejected with the rule `reserved-reason`, because the contract gives a session no way to know that value. A `no-handover` is accepted and recorded as the gate's, which is the same handling either way — it is a failure ending on the ladder whoever wrote it. | REQ-ARTIFACT-04 asserted both that `written_by` is not stored on the artifact and that a session-written reserved reason is rejected; the two can only hold together if the test is a shape the writer alone can satisfy. Claiming a check Baton cannot make would be worse than naming the limit. | active |
| D-034 | 2026-09-11 | **A brief pointer that is not on `main` rejects that entry, not the file.** The file is consumed and archived; each dropped entry gets its own `rejected` event whose `milestone` is the entry's, not the artifact's, and whose `path` is the archived file, followed by a lane escalation for that milestone. The `rejected` event's `path` therefore means where the file came to rest — `rejected/` for a rejected file, `archive/` for a rejected entry. | The handover is still the session's own word about its own milestone; only the entry it cannot support is dropped, and the lane that loses its pointer is the lane that must hear about it. A reader following `path` finds the file either way. | active |
| D-035 | 2026-09-11 | **`status` prints a ninth line: what is waiting in the inbox**, one per artifact Baton has not acted on, with its outcome, and nothing when the inbox is empty. | The move is the consumption, so a file still in the inbox is a handover Baton has not read, and none of the other eight lines would say so — under the tick that is a window of seconds, but before the tick exists it is the only way to see a handover arrive. | active |
| D-036 | 2026-09-11 | **A session's name does not repeat the project key when the project is Baton**: `Baton · <project> · <milestone>`, and `Baton · <milestone>` when the key is `Baton` itself. `session_name` in `lib/templates.sh` composes it and the refusal to dispatch over a live row of the same name reads the same function. | The `Baton ·` prefix is the ownership marker and stays; when Baton drives itself the prefix has already named the project, and `Baton · Baton · M02` reads as a mistake in the one view a person scans for which lane is which. Amends D-017. | active |
| D-037 | 2026-09-11 | **A `merged_as` must be a commit id, not a name that resolves to one.** `merged_as_verify` requires hexadecimal of at least seven characters, resolves it, requires the resolved commit's full id to carry it as a prefix, and only then asks whether it is an ancestor of `main`. | git resolves `main`, `HEAD`, `@` and `main~0` as readily as a hash, and every one of them is an ancestor of `main` by definition, so the check as first written was satisfiable by a constant: an artifact claiming `merged_as: "main"` passed INV-03 while proving nothing about the session's own merge, and the next milestone would have been dispatched from a `main` that lacked the work. Only the prefix test excludes a name, because a name never spells its own commit. Found by the M02 review. | active |
| D-038 | 2026-09-12 | **The granted shell is ad-hoc re-signed, and `install.sh` does it.** `cp /bin/sh ~/.baton/bin/sh` alone produces a binary that cannot execute at all. `codesign --verify` passes on it, so the test for a usable copy is the ad-hoc flag (`codesign -d --verbose=2` reading `flags=0x2(adhoc)`) and nothing weaker; `install.sh` signs a copy it makes and re-signs one that lacks the flag. The existing Full Disk Access grant survived the re-signing on this Mac. | Measured while running live item 36: the launchd job exited with `last exit reason = OS_REASON_CODESIGNING` and wrote nothing at all, and running the copy from a terminal was killed with 137. A copy carries Apple's platform signature (`Identifier=com.apple.sh`, `Platform identifier=26`) and the kernel refuses to run a platform binary from outside its sealed location. Every part of the plan that rests on `~/Documents` rests on this copy running, and the failure is silent: an empty `launchd.out` and no log line. | active |
| D-039 | 2026-09-12 | **A stale lock whose holder is gone is cleared by the tick**, which then writes the `baton-unhealthy` escalation under the lock it takes. The two conditions are both required: the lock is older than the interval, and its pid answers no signal. A lock whose holder is alive is left alone and the verb refuses as before, with the lock's age printed first by `status` — before anything else, which is why `lock_stale_report` runs ahead of `lock_take` in every verb. | A verb runs for seconds and launchd fires every sixty, so a lock past the interval with a dead holder cannot belong to a live verb. Left alone it stops the relay entirely until a person notices, which is the one failure that costs a whole night; and the escalation the requirement asks for can only be written by something holding the lock, so a tick that never takes it could never write one.  Nothing is timed out into a decision: the holder being gone is a fact, not a guess. The escalation is resolved in the same tick, because the condition it names is over by the time the tick ends — Baton kept working past it, which is what separates a message from a park — so the pair in the log is the permanent record and nothing lingers holding every project. The break is a rename and not a removal, so two processes racing over one dead lock cannot both end up holding it. | active |
| D-040 | 2026-09-12 | **The crash rule carries three guards beyond `pid: null`.** A lane whose attempt already has a `consumed` event is not a crash candidate, because derivation 1's `no_row` also holds a session Baton itself stopped on an `asking` consume and one that ended with a declared stop. A sighting bearing this tick's own reading of the clock is one this tick wrote, so it neither confirms nor is written over. And a previous sighting counts only if it is no older than two intervals. | The first guard keeps one ending from being acted on twice, by the crash rule and by the routing the artifact already earned. The second is what makes the two-tick rule expressible at all: a scenario's two runs share one frozen clock, so "the previous tick saw this" and "I just wrote this" are otherwise the same event, and INV-05 requires the second run to change nothing; two real ticks are sixty seconds apart and never share a reading. The third stops a sighting from before a live row came back from pairing with one after it. | active |
| D-041 | 2026-09-12 | **A failed self-check is a project-scope escalation of the log's own class, raised once while it stands and resolved by the first self-check that passes.** A read or a git failure is `plan-unreadable`, a parse failure `plan-unparseable`; the `self_check_failed` event keeps the stage, the path and the cell beside it. `park_resolve` writes the `resolution` with `how: "edit"`. | The event table called this a park written every tick: written every tick it would be 1,440 identical lines a day through every derivation that reads the log whole, and the second run of a scenario would change the state, which INV-05 forbids. Keying on an open escalation instead is the same rule the live question already follows, and it is the only form `status` can print — `derive_parked` reads escalations, so an event alone left a project skipped every tick with nothing in the view to say so. The resolution matters as much: REQ-ESC-05's third route out of a park is a person's edit that the next tick re-reads, which only the tick can observe, and without it a fixed plan file stayed parked and a grant revoked, re-granted and revoked again would never notify twice. Found by the M03 review. | active |
| D-042 | 2026-09-12 | **A tick that cannot read `claude agents --json` reconciles nothing and dispatches nothing.** `rows_read` prints the array and returns non-zero when the listing could not be read, which `rows_json` could not express. | Read as an empty array, a listing the service failed to produce makes every in-flight lane a lane with no row — which is the crash rule's whole input — so two ticks later every lane would be a confirmed crash. And without the listing the tick cannot tell what is already running, so dispatching would be dispatching over a live session, which is the one mistake with no undo. | active |
| D-043 | 2026-09-12 | **`BATON_OSASCRIPT` is the seventh seam**, and every escalation and every notification is written through one of two functions in `lib/notify.sh` that raise the Mac message as they write the event. The body is composed from the event's own fields by `one_line`, the same function `status` prints from. | One variable per outside thing, added when the thing is first read (D-032's move again); the shim records the argv, so a scenario asserts on the line a person would have read. Routing both kinds through two writers makes "everything reaches the Mac" structural rather than a thing to remember at each call site, and one composer means the line on the Mac and the line in the view cannot drift apart. | active |
| D-044 | 2026-09-12 | **A composed message is built into a variable before the call that carries it**, and `escalation_write` and `notification_write` refuse fields that do not parse. | `/bin/sh` is bash 3.2 here, and it mistracks quoting when an apostrophe appears inside a double-quoted string inside a command substitution that is itself an argument: the quoted jq program is split into words, jq fails to stderr, the substitution yields the empty string, and `log_event` reads that as "no fields" and writes the envelope alone. Measured on the long-running notification, whose detail said "the attempt's". The failure is silent in a channel nobody reads, so the fix is both halves: keep the text out of the nesting, and make the next occurrence fail where it can be seen. | active |
| D-045 | 2026-09-12 | **A milestone Baton has an open attempt at is not a dispatch candidate**, and a caffeinate holder is armed only when no holder is watching that pid. | `Status` reads blank for a milestone in flight — in flight is the log's knowledge, never the column's — so without the first the tick would dispatch the same milestone again every sixty seconds for as long as its session ran. This is not the cap, which is M06's; it is what makes eligibility mean what it says. Arming a holder every tick would leave one live process per lane per minute, several hundred by morning, so the arm reads `ps` for the whole argv: under the tests that names a shim no live process carries, which keeps a fixture deterministic, and in the installed relay it names `/usr/bin/caffeinate`, which only Baton starts. | active |
| D-046 | 2026-09-12 | **A scenario that exercises a rule which stats a transcript owns the modification times**, through an `mtimes` file naming each path and its age in seconds before the scenario's `now`. | The stall rule compares the scenario's frozen `now` against a file's real modification time, which is the time `tests/run.sh` copied it, so the answer would depend on the machine's clock and the day the suite was run. Every other input a scenario has is a file it owns; this makes the last one so too. | active |
| D-047 | 2026-09-12 | **Baton does not dispatch unattended until live item 38's cold-start half has been run.** The half a session can run passed: a launchd tick under the granted shell dispatched a session into a project under `~/Documents` and that session read a file there. The half left is the same with the background service down beforehand, which no dispatched session can run, because the tick's own session is a worker of that service. The scaffolding and a one-command script are at `~/.baton/m03-probe/`. If it fails, M04 takes one of two named fallbacks: grant the `claude` binary as well, whose path carries the version so the grant rots on every update; or never let the tick start the service and treat a dispatch that printed `Starting background service…` as `dispatch_failed` with `stage: service`, a project-scope escalation that already exists. | It is the fact the whole `~/Documents` decision rests on, and the prototype's own launchd run did not test it. A night in which every dispatched session cannot read its own project is a night lost silently, so the proof gates the first unattended dispatch rather than the merge. | active |
| D-048 | 2026-09-12 | **Nothing a dispatched session can write becomes code the tick runs.** `worktree_ensure` passes `-c core.hooksPath=/dev/null`, and the launchd agent joins the deny list by name (`Edit`, `Write` and a `Bash` fragment on `~/Library/LaunchAgents/com.baton.tick.plist`). The `bin` exception of D-026 stands: its Bash fragment would refuse `baton status`, which every session is told to run, and the Read/Edit/Write rules already name the directory. | From M03 the tick is a launchd job whose shell has Full Disk Access, so what it executes is what a prompt injection would aim at. `git worktree add` runs the repository's `post-checkout` hook and a target's own `.git` is not in the deny list, which made INV-12 — "nothing under a target project is executed by the launchd job" — false in a way no test would have caught. The plist names the shell and the script the job runs, so a session that could rewrite it could choose both. Raised by the M03 review; the `bin` half was declined with D-026's reason, and the residual risk is the stated limit of the class — the rail is against the honest attempt, not an adversary. | active |
| D-049 | 2026-09-12 | **A tick that cannot read `claude agents --json` completes nothing and writes no marker**, returning 3; and **a dispatch that fails twice in a row for one milestone parks the lane** with a `dispatch-failed` lane escalation, after which the park is what stops the retry. | Every rule after the self-check reads "no live row" as a fact about a session, and a listing the service failed to produce would make that true of all of them at once: the consume would reject a handover a live session was mid-writing, an `asking` session would be recorded as already stopped, the crash rule would sight every lane, and a dispatch would run over something already running. Writing the marker on such a tick would say "a tick completed" when six of the eight steps did not run, and would advance the clock the gap is measured against — the only thing that would tell a person the relay had been blind all night. The dispatch bound is the event table's own rule: without it a permanent cause spawned a process and wrote a line every sixty seconds, and the double-run diff was never empty, which is INV-05. Both found by the M03 review. | active |
| D-050 | 2026-09-12 | **Everything Baton reads from the CLI's own output is stripped of colour first** (`cli_plain` in `lib/dispatch.sh`), and the claude shim can print a coloured id under a knob so a scenario holds the rule. | Measured while running live item 44: with `FORCE_COLOR` in the environment — which every process started from inside a Claude Code session inherits — `claude --bg` prints `backgrounded · <ESC>[36m82b69058<ESC>[39m · …` **even with stdout redirected to a file**, so the third field is not hexadecimal, `claude_bg` set no id, and a dispatch that had really started a session was recorded as a launch failure. The lane never opened, so the next tick would dispatch a second session for the same milestone — two live sessions on one branch, which is the one mistake with no undo. The launchd job sets only `LC_ALL` and never saw it, which is why M03's live proof passed; every hand-run `baton dispatch` from inside a session hit it. | active |
| D-051 | 2026-09-12 | **A copy fork's note names the job id, and `copy_fork.session` carries the session id it is the prefix of.** `fork_session` asks the row first and the transcript tree second. | Measured (item 46): a copy announced as `d2007634` has session id `d2007634-d17b-4b0d-9c67-a0cec9b98ff6`. Every count, join and glob in Baton is on the session id, so written short the event would point at no transcript and no row, `current_session` would hand the next resume an id the CLI does not know, and the attempt would be carried by nothing. The same capture found a copy's transcript filed under the slug of the *resuming* process's working directory rather than the original session's, which is why the transcript fallback globs the tree — the design §6.3 already had for its own reason. | active |
| D-052 | 2026-09-12 | **The wait's clock is the oldest `api-error` consume since the attempt's reset, and a hold lifts when the wait clears rather than on a delivered resume.** | Derivation 5 names the *newest* api-error consume, which is right for "is a wait active" and for the next retry. But a resumed session under a standing limit meets that limit again within seconds and the hook writes another artifact, so a new consume lands every fifteen minutes: read as the start, the elapsed time would reset every cycle and the two-hour ceiling would never arrive — the person would never hear that the night went to a limit. The continuous wait therefore begins at the oldest such consume since the reset, and that is the `since` every `wait_retry` carries, so derivation 5 reports it too from the first retry on. The hold follows the same fact: lifting on a delivered resume would open the model every fifteen minutes and the same tick would dispatch a new session onto it, which is the whole of what the hold exists to stop. M04's brief said "hold_lifted on a delivered resume"; REQ-STOP-13 and derivation 6 both say "when the wait clears", and they win. | active |
| D-053 | 2026-09-12 | **The two reasons that redispatch are bounded the same way: a second consecutive one escalates.** A second `unfinished` escalates `unfinished-twice` with both splits; a second `invalid_request` escalates `ladder-end` with both details. An `invalid_request` redispatch opens a new attempt whose ladder starts at zero. | REQ-STOP-12 states the `unfinished` bound and REQ-STOP-02 asks for the same shape from context overflow — "taking the ladder's redispatch step; the next failure ending escalates" — which derivation 9 cannot express, because a `dispatch` is a reset point and a new attempt's ladder is empty by construction. Without a bound of its own a context that overflows twice would redispatch nightly forever, which is the failure the ladder exists to stop; with one, the second occurrence says the fresh context was not the answer and the lane is handed to the person. Counting consecutive endings across attempts, rather than changing derivation 9, keeps the derivation the one sentence every other rule reads. | active |
| D-054 | 2026-09-12 | **A lane dispatched or resumed within two intervals is not a crash candidate.** | A resumed session does not carry a pid again the instant the resume returns, and `state: failed` was measured arriving up to seventy seconds after a process died — the same transition in reverse. Without the guard the tick that resumed a confirmed crash would sight that session on the next tick, the ladder would count its own remedy as the next failure ending, and a scenario's second run would write a sighting the first did not, which INV-05 forbids. Two intervals is the window a first sighting already expires in, for the same reason: it is how long a fact about a row takes to settle. Found by M04 the moment anything acted on a confirmed crash. | active |
| D-055 | 2026-09-12 | **Step 4 is two files, and the dispatch hold bites from M04 rather than M06.** `lib/waits.sh` holds the wait and the hold — the half that spends no attempt — and `lib/stops.sh` the taxonomy, the resume, the ladder and the declared stops; `stops_run` drives both and `holds_apply` runs once per tick before any project's step 4. | One file was 499 lines of code, half again the largest in `lib/` and past D-005's "a few hundred lines" reversal condition, and the seam was already in the milestone's own §4: a temporary stop that spends no attempt is a different subject from an ending that costs an attempt or a person. The hold moves because REQ-STOP-13 is M04's and a hold that does not withhold a dispatch is not a hold; `fableReserve` and the cap stay M06's. The hold is applied once per tick and not per project because a usage limit is a fact about the account: applied inside the project loop, whichever project ran second would spend the request the first had already learned was refused. | active |
| D-056 | 2026-09-12 | **A model the API refuses writes no artifact, so the `model_not_found` routing is not reachable from a refusal on a session's first turn.** The routing stands for the case that does produce one; what the person gets today is the stall notification, carrying `state: failed`. | Measured (item 44): `claude --bg --model claude-ghost-9` creates the session, which gets a row and a transcript and makes one request. The transcript's synthetic assistant record carries `"error":"model_not_found"` and `apiErrorStatus: 404`, and the row then reads `state: failed, status: idle` with a live pid — but neither the Stop gate nor the StopFailure hook wrote anything, though the settings file carried both. The tick reads a transcript's modification time and never its content (REQ-STOP-01), so the error value is not available to it; the only signal left is the row, and `state: failed` with a live pid is not a class the fixed taxonomy has. Recorded rather than invented: adding a class belongs to the milestone that owns them, and the lane is not silent in the meantime — thirty minutes of an unchanging transcript notifies a stall naming the row's state. | active |
| D-057 | 2026-09-12 | **A message implies a record: neither writer in `lib/notify.sh` raises the Mac message when `log_event` refused the line**, and a writer bounds its own fields rather than letting the 4 KB refusal be the first defence. `artifact_detail` cuts an archived artifact's `detail` to 500 characters. | Measured on an `unfinished-twice` escalation carrying two splits a session had really written: the line was 28,757 bytes, `log_event` refused it, no escalation event was written — and the notification went out regardless. Every once-only rule in Baton reads the log, so an escalation nothing recorded is a park that does not exist: the lane was never held, the retries never stopped, and the second tick repeated the first, which INV-05 forbids. The person would have had one Mac message a minute until morning with nothing in the record to answer. Both halves were needed, because either alone leaves the next unbounded field to find the same edge. Found by M04's hand pass, with the fixture `unfinished-twice-long`. | active |
| D-058 | 2026-09-12 | **The copy-fork test matches the family and not one phrasing**, and the new id is the 8-hex token after the last ` as `. `docs/ARCHITECTURE.md` §4.3's own rule — "the token after `started a copy as `" — is corrected with it. | §4.3 lists eight `note:` variants and seven read "…started a copy as <Y>", while the generic one reads "started a copy **of that conversation** as <Y>". A test on the literal string missed that one, and the section's claim that its rule survives every variant was false against its own list. The cost of missing one is the whole failure the classifier exists to prevent: the note is not seen, it is not the success line either, so the resume records `refused`, the original is never stopped a second time, no `copy_fork` is written, and two live sessions run on one branch while the ladder climbs on a resume that in fact succeeded. Both reviewers found it independently and neither could confirm the CLI emits that variant today — item 46 captured a different one — but it is a documented binary string and a rule that reads seven of eight is not a rule. Fixture `copy-fork-generic-note`. | active |
| D-059 | 2026-09-12 | **A copy fork writes its `copy_fork` event before it stops the original**, and the shim prints the `backgrounded` line the real CLI puts on stdout for every resume. | The two acts cannot be made atomic, so the order is chosen for what a recovery reads: until `copy_fork` exists, `current_session` still answers with the original, and a tick killed between the event and the stop would leave the next tick resuming a session the CLI had already replaced — the second live session again, by a different road. Writing the record first fails in the direction the next tick can recover from. The shim change is the same discipline applied to the fixtures: §4.3 records that stdout carries `backgrounded · <id>` whichever way a resume went, so a shim that printed only the note left every scenario blind to the ambiguity that forces the fork test to come first. It immediately earned its keep — with the line present, the refusal diagnostic was reporting `backgrounded · f2` as the reason a resume failed, so the refusal now reads stderr first, where the CLI says what went wrong. | active |
| D-060 | 2026-09-12 | **One writer for every escalation, and each park carries its own way out.** `escalate` in `lib/escalate.sh` replaces `escalation_write`: it checks the class and the scope, attaches re-read hashes to a lane park whose class an edit resolves (`ladder-end`, `unfinished-twice`, `blocked`, `model_not_found`, `disagreement`, `omitted`, `dispatch-failed`) or which no ruling can reach, writes the event, and only then raises the message (D-057). The hashes are `plan_rows_sha256` — the parsed rows of the milestone, of every milestone whose `Depends on` names it and of the blocker the carries names, row numbers removed, read from the plan the tick already parsed — and `brief_sha256`, the kickoff prompt read through `git show main:`. A ruling can reach a lane only when the park names a project, a session and an attempt (`ruling_target`), the same test the consume applies before it stops anything. | An edit is compared against what was read at the escalation, so a person's edit unparks without a second command. The whole parsed plan was hashed first and rejected in review: once an unpark leads to a redispatch (D-061), any other lane's close-out writing `done` in its own `Status` cell would have restarted every lane an edit resolves. An uncommitted brief edit is not one a session would receive. A park with no ruling route and no hashes would hold its milestone out of every dispatch indefinitely — a rejected artifact for a session Baton never dispatched was the case found. `asking`, `question`, `merge-failed` and `other` carry no hashes: an `asking` session was stopped on consume and only a resume reaches it, so an unpark on an unrelated edit would leave a stopped session with no park. | active |
| D-061 | 2026-09-12 | **A resolution ends a park rather than pausing it.** `person_acted` reads how the park an ending earned was resolved, by position in the log: after an **edit** the rule that parked the lane takes the step the edit was for — the ladder's end, two `unfinished`, a context that overflowed twice and a refused model redispatch the next attempt; a `blocked` lane is re-judged — and after a **ruling** the rule stands down until the session's next ending. A ruling's resolution is written only after a `delivered` or `forked` resume; a refused one leaves the park standing, and a ruling's own refused resume is not an ending. | Measured before the fix: a corrected `Model` cell unparked the lane and the same tick parked it again, naming the new model as refused, so REQ-STOP-02's redispatch never happened; a ruling on `unfinished-twice` was overruled a tick later. A resolution written before the resume closed the park when the CLI refused the resume, so `baton answer` then said nothing was waiting and the next tick resumed with the continue template, which does not carry the ruling. | active |
| D-062 | 2026-09-12 | **The three-part message is title and body**: the address (`notify_title`) is the title; the body is the content — a question with its options numbered and the recommendation marked, or the detail — then ` · ` and the verb. The content is cut so the verb always fits the 250-character cap. The option verb is `baton answer <M> <n>`, never the recommended number. `status` prints the same verb through `verb_for`. A notification keeps M03's one-line body; `prompt-lost` names its command in its detail. | `display notification` has two fields. The verb is the one part a person cannot reconstruct, and a long detail cut from the end removed exactly it. A copy-ready command carrying the recommendation makes that answer the one that costs nothing to give, on a decision Baton escalated because it cannot make it. Two tables for the verb would drift between the message and `status`. Details that restated the verb (`model_not_found`, `question`) were rewritten to say only what happened. | active |
| D-063 | 2026-09-12 | **`asking`, `merge-failed` and `other` are parked at the consume**, and the `asking` carries is bounded in UTF-8 bytes: question 600, recommendation 150, at most six options of 150, context reduced to `path` 300 and `heading` 150, a lone option read as a list of one. When the full carries cannot be built or written, a one-sentence carries naming the archive is written instead. An option number is expanded from the archived artifact. | The artifact is in hand at the consume and the consume is once by the move. Code-point cuts passed a 500-character Japanese question at 1,500 bytes, an excerpt in the context and a string `options` made jq fail, and the consume had already stopped the session, leaving no park, no message and no verb. The archive holds the options whole, so cutting the carries shortens the message and never the answer. | active |
| D-064 | 2026-09-12 | **A resume waits for its stop to land.** `stop_settle` polls the rows through `rows_read` until no row carrying the session has a pid, up to sixty listings half a second apart, treating a failed listing as not yet landed; past the bound the resume proceeds and the fork classifier still applies. The claude shim plays it: a stop removes the pid, `stop.linger` and `stop.fail` delay that, and a resume into a session whose row still has a pid prints the copy-fork note. | Measured live on the first `baton answer`: `claude stop` then an immediate flagless resume met a session still "already running in the background" and the ruling landed in a copy under a new id, filed under the resuming process's working directory, with a row named by the CLI rather than `Baton · …`. On the second run, with the wait, the same command delivered to the same session id. A session parked on a question is the one kind certain to be running when Baton speaks to it. `rows_json` answers a failed listing with `[]`, which reads as settled. | active |
| D-065 | 2026-09-12 | **A `question` park ends in place two ways, and one lane is parked once.** It is resolved `answered in place` when the row leaves `input needed` or when the session writes its own artifact after the park, whichever the tick sees first; a stopped session with the park open raises `prompt-lost` once per park. The row check parks nothing on a lane that already has an open lane park or was dispatched or resumed within two intervals. The ruling label for a `question` park quotes the call ("the question you put with AskUserQuestion…"), not the park's detail. | A row keeps its last state for up to seventy seconds after a stop: without the park guard an `asking` consume and the row check parked one lane twice, and without the settle window a ruling's own lingering row re-parked the lane and delivered the ruling twice; a session that answered in place and asked again through an artifact left two parks that `baton answer` could not act on in either form. The second live probe received `[Request interrupted by user for tool use]` on the stop and answered the ruling on the resume; the first probe's call was not in its transcript when the stop landed. | active |
| D-066 | 2026-09-12 | **The takeover hand-back is `baton answer <M> continue` on a lane that is taken over and not parked**: the continue template with class `handed back`, one `resume`, no `resolution`; any other text there is refused. A parked lane receives a ruling, whatever its text. | A takeover is not a park, so there is no escalation to resolve; the release is the takeover rule's own, because Baton's prompt becomes the newest typed record. The ruling label cannot be filled with no question and no escalation time, and the continue template says what the session needs: carry on, the handover is still owed. Prompting over a person in any other form is what INV-04 forbids. | active |
| D-067 | 2026-09-12 | **`baton allow` writes the rule as typed and refuses what it cannot state or finish**: the ask forms (live item 42 is unrun: what a `permissions.ask` rule does under `bypassPermissions` is unknown from the binary's strings and the documentation), anything that parses as JSON, a rule over 1 KB, a rule with a newline, and `--resume` on a parked lane. Both files are parsed before either is written; a `widening` is written only when a file changed; a rule the deny list also names is written and reported `denied`. The lane is the newest dispatch naming the milestone, per project. | Baton never composes a rule, and it does not write one whose effect it cannot state. A rule over the line limit would widen two files and then be refused by the log, leaving no provenance. `--resume` into a parked lane would wake a session behind its own park, which only a ruling that resumes the running session again could clear. A milestone never dispatched has no settings file and no lane; a person widens `permissions.json` by hand, which REQ-PERM-02 already allows. | active |
| D-068 | 2026-09-12 | **A row reading `state: failed` with a live pid earns no escalation class.** It surfaces as the stall notification, which names the row's state, thirty minutes after the transcript stops. | REQ-STOP-01 fixes the taxonomy and names the inputs every class is detected from; the one signal that distinguishes item 44's refused model is the error inside the transcript, which the tick never reads. A class keyed on `failed` alone would also catch every other failed row and route them all as refused models. Revisit if the stall is observed to be the only notice of a refused model in real use. | active |
| D-069 | 2026-09-12 | **The two verbs a person runs live in `lib/answer.sh`, beside `lib/escalate.sh`.** `escalate.sh` holds what Baton writes when it needs a person — the escalation, the resolutions, the message, the re-read and the question's resolutions — and `answer.sh` holds `answer` and `allow`. | One file would have passed D-005's reversal condition, and the seam is direction: the record and the way back through it are separate subjects that change for separate reasons. | active |
| D-070 | 2026-09-12 | **The declared stops leave `lib/stops.sh` for `lib/declared.sh`.** `declared_open`, `consecutive_run`, `splits_carries`, `blocker_state`, `declared_step` and `distant_wait_for_check` move unchanged; `stops.sh` keeps the taxonomy, the resume, the ladder and `stops_run`, which calls both halves. `main-broken`'s routing joins the new file, because it is the declared stop REQ-STOP-12 still owes. | `stops.sh` was 752 lines, the largest file in `lib/` and past D-005's reversal condition, and M04's review fixes named this seam for the milestone that next opened the file. The seam is REQ-STOP-12 itself: an ending the session declared, with a reason and a detail it wrote, is a different subject from an ending Baton detected and climbs a ladder over. The move is taken alone and first, so that the review reads one diff that changes nothing and a second that changes behaviour. | active |
| D-071 | 2026-09-12 | **The intersection's edges.** The disposition in force is the newest archived `complete` handover's entry that lists the milestone with `run`, `wait` or `held`; an entry with any other disposition does not list it. `omitted` is judged only for a project that has at least one archived `complete` handover, and is withheld while any dependency's session is mid-run — its newest `dispatch` or delivered resume later than its newest ending the session wrote itself, so a hook's `api-error` or `no-handover` artifact does not end the run; an omission held back that way neither raises a park nor clears one that stands. A `held` whose gate the plan does not name, on a milestone the plan makes eligible, is dispatched with a `plan_override` carrying no `cleared_by`. A `run` on a milestone that reads `held` or sits under an uncleared gate is a `withheld_over_run` override even when a dependency is also undone. A `plan_override` is written once per milestone, direction and gate since the milestone's newest dispatch, and the dispatched one is written after a dispatch that produced a `dispatch` event, so neither a candidate the cap holds back nor a dispatch that failed leaves one. An `omitted` or `disagreement` park is resolved with `how: edit` the tick its condition no longer holds; after an edit resolution an omitted milestone the plan still makes eligible is dispatched, and a disagreement is withheld silently. Steps 5 to 7 live in `lib/candidates.sh`. | A close-out writes `done` at step (c) and its artifact at step (d), and a tick between them would have escalated the successor on every normal finish; the session mid-run is the handover still coming. A project with no handover has not been handed to Baton, and every tick fixture that drives the plan without one would have escalated every eligible milestone. Neither class parks a session, so no ruling reaches it and the re-read hashes see only the plan rows and the brief: a handover that later lists the milestone, or a dependency that later reads done, would otherwise leave the park standing for good. The plan wins in both directions (D-014 and the ticket), so an unnamed gate holds nothing and a person's edit that did not hold an omitted milestone is the plan's word to run it. Putting steps 5 to 7 in `lib/dispatch.sh`, as the brief proposed, would have taken that file past D-005's reversal condition in the milestone that split the last one. | active |
| D-072 | 2026-09-12 | **The cap counts derivation 1 across every project and orders greedily.** In flight is every open lane, resolved through its copy forks, whose current session has a row with a pid. Step 7 runs once per tick after every project's steps 3 to 6: the holds drop their candidates, then each slot goes to the project with the fewest in flight counting the dispatches already ordered this tick, ties broken by the next candidate's plan row and then the project key; within a project the order is the in-force entries' handover recency and `eligible[]` index. A dispatch that writes no `dispatch` event takes no slot. A redispatch from step 4 is outside the cap. | The order is a question across projects, so it can only be asked once every project has answered, which moves step 8 out of `tick_project`. A count read once would give both slots to whichever project sorted first; the brief's own example needs one each. A question park's session is live and counted; a stopped `asking` session has no pid and is not. Counting rows by name would miss a copy fork, which the CLI names for itself (M05). A redispatch replaces a session its lane already held rather than adding one, and holding it for the cap would leave the lane with no session at all. | active |
| D-073 | 2026-09-12 | **The reserve's reading is the newest status file, by modification time, that carries `rate_limits.seven_day.used_percentage` as a number, whichever session wrote it — unless that file's own `seven_day.resets_at` has passed, in which case there is no reading.** `reserve_check` runs once per tick beside `holds_apply`, writes `hold` with `reading` and `status_file` when the reading is at or above `fableReserve` and none stands, and `hold_lifted` when the reading falls below it or none stands; neither carries a project, milestone or session. A reserve of 100 is off. The hold withholds every spelling of Fable (`is_fable`: the alias, its `config.json` value, `claude-fable-…`), in step 7 and in a redispatch alike. | The windows are account-wide, so any session's reading is the account's. The feed carries no time of its own and is rewritten every turn, so its modification time is the only freshness it has; a file from a session with no response yet holds no number and must not hide an older one that does. Without the expiry the reserve could not lift on a night nothing else ran — the hold stops Fable from starting, so nothing writes a new reading — and `resets_at` is the field's own statement that the window it measured is over, not an estimate of Fable's share, which the brief forbids. | active |
| D-074 | 2026-09-12 | **`main-broken` parks the project at the consume, holds new work, and resolves by a ruling that cascades or by `done` written by hand.** The consume writes a project-scope escalation naming the project, the milestone, the session and the attempt, so `baton answer <M>` reaches it. An open project park skips steps 5 to 7 for the project and holds every redispatch; steps 3 and 4 run on, so in-flight lanes resume and wait as before, while the milestone the park names is stood by in step 4 like any parked lane. A delivered or forked `main-broken` ruling is then delivered to every other open `main-broken` park of the project, oldest first, each resolved by its own resume; one that is refused keeps its park. `merge-failed` and `main-broken` parks carry the re-read hashes and `status_at_park`, the milestone's `Status` as it read at the escalation, and `edit_reread_check` resolves them only when `plan_rows_sha256` changed, the cell now reads `done`, and `status_at_park` is present and was not `done`. The verb is `fix main, then baton answer <M> "main fixed; finish the close-out from step (c)"`, and without a ruling target the close-out by hand. | Scope is what failed, and what failed is the tree every lane stands on. When several lanes merge onto a broken `main` each writes the park, their merges having landed, and what each still owes is step (c) onward — the same words for all, so one fix answers all, in turn because each resume waits for its own stop. The fallback the requirement names had to be one specific edit: both endings stop before step (c), so any edit but `done` leaves a session owing a close-out with no park to receive the ruling that would send it back (M05's reason for giving `merge-failed` no hashes). `done` must be new since the park, because a close-out that wrote `done` a step early and then failed its merge left the cell reading `done` over the only copy of the work. | active |
| D-075 | 2026-09-12 | **`worktree_prune` removes only `../<Project>-<milestone>` for a plan milestone, under three guards, with `git worktree remove` and never `--force`; a listed worktree whose directory is gone goes through the same guards and the same command, which drops the registration.** Guard 1: `Status` reads `done`. Guard 2: no open lane for the milestone whether or not its session has a pid, no live row named for it, and no live row whose `cwd` is the worktree or under it. Guard 3: the newest archived `complete` handover for the milestone has a `merged_as` `merged_as_verify` accepts, and no dispatch or consumed ending for the milestone is newer than it. Each guard reads its answer as text and refuses unless it is exactly `false`, so a question it could not answer refuses. A `done` worktree that is refused prints a line; one whose milestone is not `done` says nothing. It runs per project after step 4. The branch is kept. | Measured on git 2.54: `git worktree remove` on a registered path whose directory is missing exits 0 and removes that one registration; porcelain lists such an entry `prunable`, which is git's own verdict that nothing is left to protect. `git worktree prune` would act on every stale registration in the repository, some of which may be a person's, so the per-path command is the narrower act. Without `--force` git refuses a tree with modified or untracked files, a fourth guard for free; ignored build products are removed with the tree, which is the close-out's own intent. A live row in the worktree catches a person's own session there, which no name or lane would. | superseded by D-078 |
| D-076 | 2026-09-12 | **The fixture harness gains a second project and two small knobs.** A scenario's `other/` becomes a committed fixture project at `$tmp/Other` with its commit substituted as `@OTHERCOMMIT@`; an `mtimes` path under `home/` sets a Baton-home file's modification time; `BATON_TESTS_ONLY=<glob>` runs the matching scenarios only. | The cap, its order and the reserve are asked across projects, and two registrations of one checkout (M05's `two-projects-refusals`) cannot tell the projects' worktrees or attempts apart (INV-07). The reserve reads the freshest status file, so a scenario must own which one that is, exactly as D-046 made a scenario own its transcripts' times. The standing check takes about ten minutes, and a milestone that freezes one expectation at a time needs to run one scenario. | active |
| D-077 | 2026-09-12 | **The dispatched settings file writes `remoteControlAtStartup` as the plan's `Remote` cell, `false` as well as `true`.** `settings_compose` takes the cell; a flagless resume restores the file's path, so the value holds for every resume of the lane. | M07's own session, dispatched by the tick with `Remote` blank on Claude Code 2.1.270, recorded `remote_session_change` with a claude.ai session URL at its first turn boundary, nine seconds after dispatch, and a `bridge-session` record on every turn after it; M06's session, dispatched on 2.1.269, carries none. The 2.1.270 binary resolves the setting from the policy, flag and user sources in that order, ignores `true` from project and local settings, and otherwise returns `getCcrAutoConnectDefault()`, an account-side default — so leaving the key out left the choice D-010 made to the account. A `--settings` file is the flag source. Chosen over accepting the default, whose cost is that a session reads "not connected via Remote Control" in Claude.app and the phone unless its milestone is `Remote: yes`. | superseded by D-081 |
| D-078 | 2026-09-12 | **Milestone worktrees are kept, and the Stop gate and the StopFailure hook stand down on a closed lane.** A close-out removes its build products and leaves its worktree; `worktree_prune` and its twelve scenarios are deleted; no code path runs `git worktree remove`. Both hooks exit 0, writing and printing nothing, when `archive/<milestone>-<session>-*.json` holds `outcome: complete`; an archived `stopped` handover does not close the lane. CONTRACT clause 3(c) and REQ-DISPATCH-03 say so. | A finished session is worth going back to — to ask what it decided, or to continue by hand — and it cannot be resumed once its working directory is gone. Measured on 2026-09-12 in `~/.claude/daemon.log`: the service's restart for the 2.1.270 upgrade settled M04's and M05's idle sessions `(crashed): working directory no longer exists`, and M06's went the same way a minute after its own close-out removed `../Baton-M06`; Claude.app then shows each as "not connected" and a message to it does not send. Keeping the directory is the whole fix for that half, and the prune existed only to remove what is now kept. The other half: a resumed finished session still carries the gate, which would demand a handover already given and then write `no-handover`, and the tick would climb the ladder on a milestone that is done. The archive is the gate's own evidence that the tick acted on this session's handover. The cost is a worktree per milestone beside each checkout, bounded by removing build products. | active |
| D-079 | 2026-09-12 | **A Baton milestone's close-out runs `sh install.sh` itself**, from the canonical checkout on `main`, after the standing check has passed there and the `done` commit has landed, and before the handover artifact is written. It is authorised beside `sh tests/run.sh`, and it is the one write under `~/.baton/` outside `inbox/` a session makes. An installed agent that differs from `launchd/com.baton.tick.plist` is never replaced or reloaded by the session; the line `install.sh` prints about it is quoted in the completion evidence and the final message. | M06's handover asked the person to install before M07 was dispatched, and the tick dispatched M07 from the same handover in the same minute, so the ask could not be met in the window it named. The tick that consumes a handover is the one that dispatches its successor, so the install belongs before the artifact. D-018's guarantee is kept where it matters — a milestone cannot break the tick that dispatched it — and narrowed to "only the next tick, and only with a `main` the standing check passed"; a bad install is still undone by checking out an earlier commit and installing again. The deny list already permits the script, because D-026 leaves `bin` out of its Bash fragments. The plist is left to the person: `install.sh` copies the agent only when none is installed and leaves one that differs in place with a line saying so, because launchd reads an installed agent at every login, so a replaced plist would change what the tick runs, and with which shell, without a person choosing it (D-048, REQ-SETUP-04). | active |
| D-080 | 2026-09-12 | **`Remote: yes` is one command: the ordinary dispatch with `remoteControlAtStartup: true` in its settings file.** The two-step path (`claude_bg_remote`: an idle `--remote-control` start, `claude stop`, a flagless wake) is deleted, and `--remote-control` is never passed. | Live item 40 on Claude Code 2.1.270, fixture project Probe07: `claude --bg … --settings <file with remoteControlAtStartup: true> "<prompt>"` delivered the prompt as its first `user` record, the session acted on it, and its transcript recorded a claude.ai session URL; the control, dispatched with `false`, recorded none across eight tool results. The phone proof ran on this path through `baton dispatch`: the `AskUserQuestion` push reached the Claude mobile app, the answer (`alpha.txt`) arrived as the tool result at 21:45:50.694Z, and the row read `working/busy` within a second of it. Item 39 as written asked what a wake after an idle start restores; with no idle start the question that matters is whether a flagless resume keeps Remote Control, and it does: the woke line listed `-n, --effort, --permission-mode, --settings, --model`, and the resumed session recorded the same claude.ai session URL at its first tool result. The two-step path had been built and fixture-tested first, as the brief ordered; a mechanism seen working in one command replaces it. The URL record is written at the next turn boundary rather than on connecting, so times read from it are upper bounds. | active |
| D-081 | 2026-09-12 | **Every dispatched session is on Remote Control: the settings file writes `remoteControlAtStartup: true`, whatever the `Remote` cell says.** `Remote: yes` keeps only its escalation meaning — the row is not a park detector for that lane, its parks are artifact-borne, and a prompt nobody answers stalls. Project grouping in Claude.app and the lifecycle of a finished session's process move to M07-b. | Counted by `remote_session_change` records carrying a claude.ai URL, every `--bg` session Baton dispatched from M02 (2.1.268) to M07 (2.1.270) was connected by the account's default, which D-077 misread from `bridge-session` records; interactive and SDK sessions were not. Remote Control is what lists a session in Claude.app and on the phone: a fixture session started with the key `false` was absent from Claude.app's sidebar while its `true` siblings were listed, so D-010's "never automatic" would put every Baton session out of reach, including the finished ones a person goes back to (D-078). Writing `true` rather than leaving the key out keeps that reach from depending on the default. Measured beside it: an idle session's process holds about 450 MB whether connected or not (466 MB with the key false, 454 MB true) and one more HTTPS endpoint when connected, so the cost worth managing is the process, not the connection; `claude stop` makes claude.ai archive the session; Claude.app cannot wake a stopped session, but a message typed to it there is held and delivered as a `queued_command` when the session is resumed on the Mac; a session in a repository whose `origin` names `git@github.com:scalinity/probe07.git` still listed under Other, so the registration's `git_repo_url` alone does not group. M07 closes with this, and the lifecycle and grouping are split into M07-b, by the ruling on the question M07 parked on. | active |
| D-082 | 2026-09-12 | **The Mac message moves off Script Editor into a notifier applet with Claude's icon whose click opens the session, as milestone M07-c after M07-b.** The applet is built by `install.sh` with the system's `osacompile` from source in the repository, takes its icon from the installed Claude.app at install time, and opens `claude://code/session_<id>` for the session a message is about. A Swift helper is the fallback only if the applet cannot carry the icon or be relaunched by a click, and then only on the person's word, because it is D-005's named moment and a build step. | `osascript -e 'display notification …'` is attributed by macOS to Script Editor, so the message shows Script Editor's icon and a click opens a blank AppleScript document. Claude.app (`com.anthropic.claudefordesktop`) registers `claude://`, and its URL handler routes `claude://code/<id>` to a Code session when the id matches `^(cse|session)_[A-Za-z0-9_-]+$` (read from its `app.asar`), which is the claude.ai session id every Remote Control session's transcript records (D-081). A notification's icon is its sender's, so a sender of Baton's own is the only way to Claude's icon without a package; the icon is Anthropic's mark and stays out of the repository. After M07-b rather than beside it, because both edit `install.sh`. | active |
| D-083 | 2026-09-12 | **A handover delivered twice gets its own milestone, M07-d, after M07-c, and M08 depends on M07-d.** The plan is one lane again — M07, M07-b, M07-c, M07-d, then M08 behind its gate — so each close-out makes exactly one milestone eligible. | `dispositions_in_force` ranks archived handovers by consume order, so a handover consumed again becomes the newest in force for every milestone it lists and can dispatch one that a handover written after it held back or told to wait. It happened once on 2026-09-12 (M05's handover, consumed again at 15:32:12, harmless because M06 was already in flight), and Reclaim's lanes run in parallel, where no open lane stands in the way; so Reclaim's onboarding waits for the guard. With M08 depending on M07 it was eligible beside M07-b, and a handover listing only one of them would have had the other escalated as `omitted`. | active |
| D-084 | 2026-09-12 | **A finished session wakes when a message is sent to it; M07-b measures which mechanism does that before building, and a typed `baton wake` is at most the fallback.** In order: the background service's own Remote Control worker (the internal setting `autoAddRemoteControlDaemonWorker`), then a `claude remote-control` server environment per project, and only if neither works, keeping the processes of the most recently active finished sessions with a wake reachable from the app, put to the person first. Baton never polls claude.ai with the person's credentials. | Opening a terminal to type a command to continue a session already open in Claude.app is the friction the feature exists to remove. Measured in M07: Claude.app cannot wake a stopped `--bg` session, but a message typed to it is held on claude.ai and delivered as a `queued_command` when the session reconnects, so what is missing is only something on the Mac that notices the message. The 2.1.270 binary's server mode is exactly that: it polls claude.ai for work, spawns a session process when a message arrives, returns the session to idle when it finishes, and unarchives a reaped session before reattaching it. Speaking that protocol from shell would mean handling the person's tokens and a private API, which the relay's whole shape refuses. | active |
| D-085 | 2026-09-12 | **The takeover rule reads every message a person put into the session, not only the typed ones.** A message is a `user` record, not meta and not a compact summary, whose `promptSource` is `typed` or `queued` or whose `origin.kind` is `human`; or a `queued_command` attachment whose `origin.kind` is `human`, its text read from `attachment.prompt`. The newest such record is compared against the sidecars exactly as before, so Baton's own prompts, which arrive `typed`, still release a takeover. A peer session's message (`origin.kind: peer`), a task notification and the Stop gate's feedback are not a person and do not count. | Every dispatched session is on Remote Control (D-081), so a person types into one from Claude.app or the phone, and that input never arrives as `promptSource: typed`. Tallied over every transcript on this Mac on 2026-09-12: a message sent while a turn runs is a `queued_command` attachment with `origin: {kind: "human"}` (eleven in M07's session, and the wake message `hey` in Probe07 M01); one sent between turns is a `queue-operation` enqueue and dequeue followed by a `user` record with `promptSource: "queued"` and `origin: {kind: "human"}` (M03, M05 and M07 each hold two — M05's `the probe is finished.` waited two minutes in the queue for its turn to end); a slash command a person sends is a `user` record with `origin.kind: human` and no `promptSource`; `peer` messages (107) and task notifications (180) share the attachment shape with other origins. Before the change the fixtures `takeover-queued-command` and `takeover-queued-between-turns` parked a `question` on a lane whose newest input was the person's, which invites a ruling over them — the prompting INV-04 forbids. | active |
| D-086 | 2026-09-12 | **On Claude Code 2.1.270 no message wakes a stopped session, and Claude.app's grouping cannot be set by a `--bg` session; neither is built.** Of M07-b's three wake mechanisms, the background service's Remote Control worker does not exist in this build and a `claude remote-control` server cannot adopt a session Baton dispatched, so the design went to the person as an `asking` artifact (D-087). Grouping follows a session's `repoInfo`; Baton and Reclaim have no git remote, and a fixture whose `origin` was an `https` GitHub URL with `origin/HEAD` set still listed under Other. | The service's worker kinds are `{heartbeat, scheduled, remoteControl}` and the runner refuses every kind but `heartbeat` behind `isDaemonWorkerRegistryEnabled`, compiled as `function i7(){return!1}`; `claude daemon remote-control list` prints `'daemon remote-control' is not available in this environment.`; `autoAddRemoteControlDaemonWorker` has no reader, so no setting was asked for. `claude remote-control --session-id cse_REDACTED` (and the `session_` spelling) printed `Error: Session … has no environment_id. It may never have been attached to a bridge.`; a server also takes no prompt from the command line, refuses `--settings`, and spawns its children hydrated from claude.ai (`--resume=<sdkUrl>`), without the deny list or the hooks. Claude.app's bundled sidebar code (`ion-dist/assets/v1/shared-21-*.js`) keys a group on `repoInfo` from the session's `session_context` sources or a server environment's `git_repo_url` or `directory`, and uses "Other" without one; the live check put `Baton · Probe07b · M01` (no remote) and `Baton · Probe07g · M01` (GitHub origin) both under Other. The one lever that would group a Remote Control session, a claude.ai Project named at creation (`session_grouping_id`, persisted in the `bridge-session` record and reused on reconnect), is gated off in this build: the parser is called with `rcProjectGateEnabled:!1`, and a fixture `claude --bg … --project probe-grouping-test "<prompt>"` settled `exit 1 before init — error: unknown option '--project'`. Revisit when that start is accepted: the grouping is then a flag at dispatch that a flagless resume keeps. | active |
| D-087 | 2026-09-12 | **A finished session keeps its process while it is among the `keepFinished` (3) most recently active finished sessions with one; an older one is taken offline once idle for `idleStopMinutes`; the person reaches it through one always-on Remote Control session, `Baton · wake`, which runs `baton wake <milestone> "<their message>"`.** `offline_check` runs once per tick across every project, before the dispatch: a session whose `complete` handover was consumed and whose milestone was not dispatched since, whose row has a pid and reads `idle`, whose transcripts are older than `idleStopMinutes`, and which ranks past `keepFinished` by transcript modification time, gets `claude stop` and one `offline` event. `wake_session_ensure` starts the wake session after the first `offline` event exists, resumes it flaglessly when its process is gone, starts it again when a resume was refused, and tries at most once per `retryMinutes`; the session is known by the log's `wake` events, never by its name. `baton wake` with no milestone lists finished sessions; on a running one it names the thread rather than resuming, because a resume into a live session forks; on an offline one it resumes flaglessly with the person's text labelled as theirs and writes a `wake` event. `install.sh` writes `settings/wake.json`: Remote Control on and Baton's deny list. Two event kinds join the table, `offline` and `wake`. | The ruling on M07-b's `asking` artifact chose this over keeping every finished session running and over a wake reachable only from the Mac. A process costs 0.35–0.8 GB idle and about twenty more milestones lie ahead across Baton and Reclaim, so the bound is the point; the recent sessions are the ones a person goes back to, and they answer in their own threads with nothing else. The ranking is across projects because memory is the Mac's, as the cap is (D-014). Activity is a stat, never a read (REQ-STOP-01), and a woken session is recent again, so the edge moves rather than the woken one going straight back offline. The wake session exists only once something is offline, because before that it would be a process with no work, and a stopped session drops out of `claude agents --json`, so a tick that looked for it by name would start a new one after every stop. A flagless resume brings an archived session back active in Claude.app (measured on fixture `0801ecf4`), so a woken session's answer is readable in its own thread. | amended by D-090 |
| D-088 | 2026-09-12 | **`idleStopMinutes` is 60.** | Reading a session in Claude.app changes no transcript, so the offline rule cannot see a person reading; the wake is the undo, and the hour is the window in which someone who has just read a handover follows it up without needing one. It matches the supervisor's own documented idle stop for finished unattached sessions ("about an hour"), which does not act on Remote Control sessions on this Mac. It applies only past `keepFinished`, so the three most recent sessions are never timed out at all. | active |
| D-089 | 2026-09-12 | **A session asks the person in the session itself; the `asking` artifact is for a question asked as the turn ends.** `CLAUDE.md` and `CONTRACT.md` clause 5 say so: a question put with `AskUserQuestion` keeps the session running, Remote Control pushes it to the phone, the tick parks the lane as `question` with the Mac message, and the person's answer in Claude.app releases it in place. | M07-b's `asking` artifact was consumed by the tick, which stopped the session so that the ruling could resume it rather than a copy (REQ-ESC-06) — and `claude stop` archives a session on claude.ai. The Mac message then said a question was waiting while Claude.app showed only an archived session with nothing to answer, and the question and its options were readable only from the artifact's JSON; nothing else ran, because an `asking` handover is not a completion. Questions put in chat by the same session were answered in the app and resolved in place by the tick, with the session running throughout. | active |
| D-090 | 2026-09-12 | **The review of M07-b tightened the offline rule and the wake.** The offline rule acts only on sessions Baton dispatched — a `complete` handover consumed without an attempt is a person's own session, neither stopped nor counted against `keepFinished` — and records `offline` only for a stop the CLI took, retrying a refused one next tick. `bin/baton` drops the `CLAUDE*` variables before every verb, not only on the wake paths. Every flagless resume is classified by one function, `resume_classify`. A verb wake that forks records `copy`, stops the original, and later wakes address the copy; the wake session is resumed only after five seconds of listings without its pid, and a fork there records `from_session` and stops the original. The wake session runs in `~/.baton-wake/`, starts only when `settings/wake.json` exists and carries deny rules, is denied the `tick`, `answer`, `dispatch` and `allow` verbs, and passes the person's words as `baton wake <milestone> -` on a quoted heredoc. `status` gains a tenth section. | Two reviewers on Fable. The hand-started session was the one reachable harm: the consume already logs such a handover without an attempt, and its rule is that Baton stops only what it started. A stop recorded over a refusal suppressed every later attempt while the transcript stood still, which for an idle session is forever. The environment variables that broke Remote Control reach any verb run from inside a session, and a hand-run `baton dispatch` or `answer` is one. Two copies of the classifier had already diverged on the stdout fallback, which is the drift D-058 exists to prevent. Without the copy recorded the next wake forked again. The deny list's Bash rules spell Baton's state as `.baton/<path>`, which a relative path from inside `~/.baton` does not, and the list — not the standing prompt — is the rail against a misread message. A single-quoted argument broke on an apostrophe, and a model that switched to double quotes would have run a `$(…)` from the message (CWE-78). Fixtures shown failing first: `offline-refuses-hand-started`, `offline-stop-fails`, `wake-verb-forked`, `wake-session-forked`, `wake-session-unsafe-settings`, `wake-session-settings-missing`, `wake-verb-stdin`, `takeover-origin-not-an-object`. | active |
| D-091 | 2026-09-12 | **The close-out installs with the home named: `BATON_HOME=/Users/danny/.baton sh install.sh`.** `CLAUDE.md`'s handing-over step and M07-c's prompt say so. | M07-b's session was run by a background service first started by the M03 probe's launchd job, and inherited that job's environment, `BATON_HOME=/Users/danny/.baton/m03-probe/home` among it (M07 recorded the inheritance as a limitation). The close-out's `sh install.sh` therefore installed the merged relay into the probe's home, re-signing its copy of the shell and writing its `settings/wake.json`, and printed the probe path; the real `~/.baton/bin` still held M07's relay, so the tick consuming the handover would have dispatched M07-c from the old code. Run again with the home named, the installed `baton` and `lib/lifecycle.sh` compared equal to `main`. Every later session run by the same service inherits the same variable until the service restarts from a clean environment, so naming the home is the fix that does not depend on when that happens. | active |
| D-092 | 2026-09-12 | **The Mac message is posted by a notifier applet, `~/.baton/bin/Baton.app`, compiled by `osacompile` from `notify/Baton.applescript` at install, and a click opens the session through `claude://claude.ai/code/session_<id>`.** `notify` spools title, body and target to `~/.baton/notify/spool/` and launches the applet with `open -g` through the seam `BATON_OPEN`; the applet posts every spooled message with `display notification`, keeps the newest one's target, and on a launch with nothing spooled — a click — clears every Baton message from Notification Center (AppleScriptObjC, `NSUserNotificationCenter` `removeAllDeliveredNotifications`) and opens that target, or `claude://code/needs-input` when the newest message named no session. `install.sh` adds `CFBundleIdentifier com.baton.notify` and `LSUIElement`, copies Claude.app's `electron.icns` over both icon sources when it exists, signs ad hoc, and rebuilds only when the source or the icon differs. `osascript` stays the path when the applet is missing or will not launch. The icon is never committed. No Swift helper is built. | Measured live on this Mac with a probe applet (M07-c completion evidence): the banner showed the applet's name with Claude's icon; a click relaunched the quit applet, which found its spool empty; the first notification asked for no permission. `claude://code/session_<id>` brought Claude.app forward on the wrong view, logging `claudeURLHandler: code session deep link gated off`, because the handler reads flag `2143883161` only for the `code` host, while the `claude.ai`-host form reaches the same resolver without it and opened the session. `osacompile` writes no bundle identifier, which Notification Center keys a sender on, and keeps its icon in an asset catalog that wins over `applet.icns`. A `display notification` carries no data, so a click cannot name the message clicked; the ruling in the session chose the newest message's session and clearing every Baton message, since clearing only the clicked one is not possible, and `baton status` and Claude.app's sidebar still list older parks. Reading each field as a value from the spool rather than splicing it into script source removes the escaping the `osascript` path needs. The icon is Anthropic's mark and this is a personal tool on one Mac, so it is copied from the installed app. With (a) and (b) holding, D-005's Swift moment did not arrive. | active |
| D-093 | 2026-09-12 | **`session_url <session>` links a message to the newest claude.ai session URL among the transcript's `remote_session_change` attachments, and without one to the job state's `bridgeSessionId`** (`~/.claude/jobs/<first eight characters of the session id>/state.json`, `cse_<id>` read as `session_<id>`, through the seam `BATON_JOBS`). Only a well-formed claude.ai session id is used; a null url is skipped. SPEC §4 item 7 names both reads. The `question` park's verb reads `answer it in place in Claude.app, or baton answer <milestone> "<ruling>"`. | Live on M07-c's fixture `1d50e71b` (`Baton · Probe07c · M02`), whose first turn asked with `AskUserQuestion`: while it waited, its transcript held only `url: null` and its job state already held `bridgeSessionId: cse_REDACTED`; the URL `https://claude.ai/code/session_REDACTED` was written only after the answer, which is after a `question` park is raised — so from the transcript alone the park's click would have opened the needs-input view. With the fallback the park's message opened that session on the click. The transcript stays first because it records the thread's newest connection; the job state covers the turn that has not ended. `claude attach <job>` predates every session being on Remote Control (D-081), and the click now opens the thread in Claude.app, where the question is answered in place. | active |
| D-094 | 2026-09-12 | **The review of M07-c hardened the spool, the applet and its build.** A spooled name is never reused — the counter is padded and a name already on disk is skipped — and a tick launches the applet once when the spool still holds a message (`notify_flush`), while the applet posts on a reopen as well as a launch. The applet handles each message inside `try`, ends a pass that hit a failure, removes a message without a title unposted, opens nothing and shows nothing when `open` fails, compares the link with case considered, and writes the target aside and renames it. `install.sh` rebuilds the applet when its own checksum differs from the one the applet records in `built-by`, deletes each plist key before adding it, and swaps the bundle in by rename. The deny list names `notify` in its Edit, Write and Bash rules; `bin` stays out of the Bash rules. `session_url` stays in `lib/notify.sh`. | Two reviewers on Fable and a hand pass. `distant_wait_for_check` calls `notification_write` inside a `jq … \| while read` pipeline, so its counter increment was lost and the next message in the same second took the same name and replaced the unposted one; fixture `notify-spool-names`, shown before the fix keeping only `second` of two messages and listing eleven as `m9 m10 m11 m1 …`. A launch that reaches the applet as it quits is a reopen, which it did not handle, so a message could wait until a click posted it instead of opening a session; fixture `notify-spool-stranded`, shown before the fix leaving the message in the spool through both ticks. An applet shows an uncaught error as a dialog, and a file it could not remove would have been posted on every pass. AppleScript's `starts with` ignores case (`"CLAUDE://…" starts with "claude://…"` is true). A change to the build steps with the same source and icon never reached an installed applet. The notify directory is Baton state — its spool posts under Baton's name and its target decides what a click opens — so it is denied like every other path under the home but `inbox/`; the rule `Bash(*.baton/bin*)` would refuse a session running the installed `baton` by its path, as the wake session does, and `bin/**` is already denied to Edit and Write (D-026, D-079). `session_url` has one consumer, so it sits beside it; a second consumer is the moment to move it into `lib/derive.sh`. The installed `permissions.json` is written only when absent, so an install does not add `notify` to one that exists. Fixtures `notify-stall-session`, `notify-two-in-a-tick` and `install-rebuild` cover the notification writer's session, two messages in one tick, and the rebuild triggers. | active |
| D-095 | 2026-09-13 | **A file repeats a handover when it holds the same JSON value as the archived file of a `consumed` handover for the same milestone and session, and a repeat is archived with a `repeated` event and acted on not at all.** The value is compared with keys sorted, so spacing and key order do not count and every field, `written_at` included, does. The test runs first in step 2, before `artifact_check`; a repeat moves to `archive/` under the usual consumed-at name, and one `repeated` event carries `outcome`, `archive` and `repeats` — the first handover's archived path — under that handover's envelope; one line is printed. No check, stop, route, park, notification or `consumed` event follows. A consumed handover whose archived file is gone cannot be repeated, so such a file is acted on. A log the test cannot read leaves the file in the inbox for the next tick. `derive_consumed` claims a `repeated` event's archive, and its `unrecorded` join now names files at all: it compared each list with itself. Event kinds are twenty. | Every rule that acts on an ending reads `consumed` — in flight, the ladder's reset, the wait's end, the notification keys, `midrun`, `offline_check` — so a repeat recorded as one would have been acted on again whatever the ranking did: fixture `consume-repeat-asking` stopped a session working again after its ruling and parked it a second time, and `consume-repeat-older-word` dispatched M07 against the newer handover's `wait`; `consume-repeat-identical` and `-reserialised` consumed the same handover twice, which is what happened to M05's at 15:32:12 on 2026-09-12 (M07 completion evidence §6). The bytes would miss a repeat the Stop gate's printed fallback delivers, since it writes the fence's text rather than the session's file (`-reserialised`). The tuple of project, milestone, session, outcome and `written_at` would take a file whose content changed under the same `written_at` for a repeat and drop what changed; the whole value never skips anything new. `written_at` is kept because the gate's two `no-handover` endings for one session can carry the same `detail` and differ only there, and each is a failure ending the ladder counts (`consume-repeat-new-written-at`); REQ-ARTIFACT-01's rule is that it is never read for freshness, and here it is compared, never ordered. Rejection was not chosen, because a rejection parks a lane and a repeat asks nothing; the archive keeps REQ-ARTIFACT's rule that no file is skipped unrecorded. Testing before the checks means a transcript removed since the first consumption cannot reject a handover already acted on. | active |
| D-096 | 2026-09-13 | **Handovers in force rank by the order they were first acted on, and a repeat takes no place in it.** `dispositions_in_force` still ranks derivation 4's `consumed` list reversed, and `cap_order` still reads that rank; neither changed, because since D-095 that list holds each handover once. Two different handovers keep the order the tick consumed them in. | A repeated older handover was the one delivery order that could put an older word in force, and with no `consumed` event it has no rank (`consume-repeat-older-word`). For two different handovers no order Baton can read is right every time. `written_at` is the session's own claim, which the contract never reads for order ("What a project hands to Baton" §4: no ordinal), and it is wrong for the ordinary parallel close-out: lane A merges, lane B merges after it with A's merge in view, and A's slower standing check writes A's artifact last. The ancestry of `merged_as` is verified rather than trusted and gets that case right, but it cannot order a branch commit that another lane's merge does not contain, costs a git read of `main` for every project on every tick, and still misses a refresh commit made after the other lane's merge. The consume order is what the tick acted on, the plan wins on gates either way, `wait` needs every `wait_for` done, and a `run` the plan makes ineligible escalates; so a late different handover is a known limitation, not a rule: in the close-out above, A's older word is consumed after B's and is the one in force for any milestone both list. Reversal condition: a dispatch against a newer handover's word by a late, different handover, recorded in the log. | active |
| D-097 | 2026-09-13 | **The review of M07-d widened the repeat test to every archived copy and made the consume's moves fail loudly.** A file repeats a handover when its value matches any archived copy of it for the same milestone and session — the first file, or an earlier repeat's — and the match is recorded under the consumed event that copy repeats; `derive_consumed` lists `repeated` events with `archive_present` for it. `repeat_of` returns 0 for a repeat, 1 for none and 2 for a log it cannot read, compares only a file whose milestone and session are strings, and is derived per file because the loop writes to the log. `derive_consumed` claims archived names from every project's events. `archive_move` and `reject_move` check each step and refuse an existing destination; `consume_one`, `repeat_one` and `reject` write nothing when the move failed, and `consume_one` moves the file before it stops an asking session. The rule is stated as once while an archived copy of the handover stands. | Two reviewers on Fable and a hand pass; no critical finding. Once a person moved the first archived file aside, as `answer_options` already allows for, the next delivery was consumed again though a repeat's copy held the same value (fixture `consume-repeat-first-archive-moved`, consumed before the fix). With a project named, `unrecorded` called every other project's handovers unrecorded — hidden until M07-d's fix made the join name anything (`consume-repeat-derivation` with a second project, shown listing it). A non-empty stdout was the only sign of a log read failure, and in jq `null == null` matches an event missing the same field. The tick runs under `tick_run \|\| …`, where `set -e` stops nothing: with `archive/` not a directory the consume wrote a `consumed` event with an empty `archive` for a file left in the inbox, on every tick, and a name already taken was overwritten (fixtures `consume-archive-move-fails` and `consume-archive-name-taken`, shown doing both); an asking session would have been stopped each of those ticks. `consume-repeat-archive-gone` and `consume-repeat-log-unreadable` pin the two fallbacks D-095 describes. | active |
| D-098 | 2026-09-17 | **Unifying the two brief-on-main checks belongs to M17, not the dispatch-message patch.** `prompt_from_brief` in `lib/dispatch.sh` builds a session's prompt; `brief_pointer_check` in `lib/inbox.sh` validates a handover's brief pointer. They implement the same on-main invariant with near-duplicate messages and divergent case coverage: the former distinguishes a missing fenced block, the latter a missing heading; neither distinguishes both. | The inbox check's detail is written into archived handover JSON, making its text persisted state with a wider blast radius than a printed line. This patch changes dispatch messages and guards the session's own worktree only; the inbox check and its archived text stay unchanged. M17 owns unifying the checks and their presentation. | active |
| D-099 | 2026-09-17 | **The standing check depends on locale and the canonical checkout's location; neither assumption is changed here.** `tests/run.sh` neither pins nor asserts a locale: the `install` listing-order scenario failed under `C.UTF-8` and passed under `en_US.UTF-8`. The runtime sets `LC_ALL=en_US.UTF-8` in `launchd/com.baton.tick.plist`, with the reason in its comment, and dispatched sessions inherit it. Exactly two scenarios, `install` and `install-plist-differs`, run the real `install.sh` and assert the canonical absolute path `/Users/danny/Documents/Apps/Baton`. They work from linked worktrees because `install.sh` resolves the repository's main worktree, not the cwd (lines 45–47); a detached clone has its own main worktree and fails those assertions. A different clone basename also makes the fixture's `jq` read the wrong project directory and short-circuit an `&&` chain, skipping later output. A clone named `Baton` removed that shape difference and left only absolute-path differences. `statusline`, `stop-gate-fence` and `stop-gate-fence-active` use the hardcoded path as fixture data and passed in the detached clone. | Throwaway clones are used to verify history rewrites and other changes. Under `en_US.UTF-8`, the detached-clone run completed with 246 scenarios and exactly these two failures; a third failure must be treated as real, not excused by the location. **Investigated and disproved:** the hypotheses that these scenarios fail in every dispatched session's linked worktree, and that M09's relocation under `$BATON_HOME` worsens this, are both false because `install.sh` resolves the main worktree. Candidate fix: derive the expected name and path from the repository's main worktree instead of hardcoding them. That change, and pinning or asserting the harness locale, are deliberately not made here. | active |
| D-100 | 2026-09-17 | **Edit the repository's `AGENTS.md` and `CLAUDE.md` together until their equality is structurally enforced.** They are byte-identical independent regular files, not links. Both are startup instruction sources and carry the close-out and handover procedure, but nothing keeps them in step. | An edit to one can leave the other stale, causing Claude Code and Codex sessions in the same repository to hand over by different rules. That divergence would surface in the handover artifacts rather than as an error. Making `AGENTS.md` a symlink to `CLAUDE.md` is the candidate fix. It is deliberately not done here because these instructions govern every dispatched session's close-out and must not change immediately before a chain is started. | active |
| D-101 | 2026-09-17 | **Baton has no pause verb; standing down the relay for plan editing currently requires launchctl outside Baton.** The supported verbs are tick, answer, status, plan, dispatch, allow and wake. Record the gap; add no pause mechanism in M17 or M17-b. | An unattended relay needs a supported way for its owner to edit the plan without admitting work mid-edit. During brief authoring, the loaded tick held M17 as omitted. The owner authorized bootout before editing and bootstrap only after publishing, with the unchanged last-tick marker checked through status. D-102 explains why merely withholding a commit is not sufficient. No session was dispatched as a probe. | active |
| D-102 | 2026-09-17 | **An uncommitted plan edit is authoritative immediately, while a kickoff brief is read from main.** `plan_of_project` reads the registered working-tree file; `prompt_from_brief` uses `git show main:<brief>`. `reread_hashes` includes a milestone's own row and direct successors, and `edit_reread_check` can resolve its omitted park when those rows change; `dispositions_intersect` then admits it after that edit resolution. Record this asymmetry without changing either source of authority. | The installed code matched the inspected checkout. M17's live omitted park carried `plan_rows_sha256`; editing its successors could therefore release it before the new brief was committed, or even written. This is why D-101 matters. M17 addresses the other side by refusing unmet dispatch preconditions before worktree creation; it does not make plan/brief publication atomic. | active |
| D-103 | 2026-09-17 | **The original two-existing-library split threshold fragmented one rendering refactor into nine milestones.** The inspected allocation was M17: plan/dispatch; M17-b: status/plan; M17-c: dispatch/inbox; M17-d: answer/lifecycle; M17-e: notify/escalate; M17-f: rows/tick; M17-g: stops/declared; M17-h: waits/candidates; M17-i: lock/log. Each pair names `.sh` files under `lib/` and counts two; render.sh would be introduced. | The threshold was applied faithfully and was not relaxed by the authoring session. The owner first cut scope to preconditions and two rendered views rather than let the session amend its own constraint, then withdrew that deferral: a quarter-rendered surface would preserve the inconsistency. The original rule cannot accommodate an all-verb rendering layer in one milestone under its literal file count. This finding records the evidence, not a general mechanical-refactor exemption; D-104 records the owner's subsequent narrow amendment. Whether the rule should distinguish decision changes from other mechanical changes more broadly remains for independent review; this session does not pre-empt it. | active |
| D-104 | 2026-09-17 | **The owner amends the split rule only for rendering-call substitution.** Previously every existing lib file touched counted toward the two-file threshold. Now a file whose only change substitutes a rendering call for existing printing, with no change to what the verb decides or what an assertion verifies, does not count; any behavioural or decision change still counts. The existing natural-split-point mechanism bounds session size for this work. M17 owns two behavioural files; M17-b introduces render.sh and converts the full printing surface, with whole-file green checkpoints and an M17-c remainder brief only if it must stop. | This was the owner's deliberate call, not a session relaxing a constraint its own briefs benefit from. The two-file threshold proxies session size rather than risk; D-103's nine milestones for one refactor showed its poor fit for output substitutions. The measured 246-scenario suite asserts exact strings as well as state and calls, providing the safety net: reviewed presentation expectations may change, but no assertion is weakened or made to verify less. The exception does not extend beyond rendering-call substitution. A partial conversion is temporary, not a permanently deferred end state. First-occurrence precondition escalation moves to M15; D-098's unification remains deferred despite its original M17 assignment. | active |
| D-105 | 2026-09-18 | **Correct D-098's archive-persistence premise without unifying the brief checks.** D-098 claimed “The inbox check's detail is written into archived handover JSON”. At 4710d17, `artifact_check` returns the original `artifact` separately from `dropped`; `archive_move` moves the original input unchanged. The dropped-entry loop records a `rejected` event with the archive path and reason `brief-pointer`, then prints the diagnostic; it does not insert that diagnostic into archived JSON. | The `consume-blocked` fixture's “M01's reader is not on main yet” is the session's own `detail`, already present in its inbox file and unchanged in the archive. Divergent case coverage remains real: `prompt_from_brief` requires a complete fenced block under the heading, while `brief_pointer_check` checks the heading only. No archive migration is required to unify these checks. D-104's deferral still stands; this correction implements no unification and assigns no new work. | active |
| D-106 | 2026-09-18 | **Refreshing an in-flight sibling's prompt can make its own redispatch impossible; the refresh rule and M17's ancestry rule are in direct opposition for any parallel pair.** `docs/v1.1/SCOPE.md` §7 says “Refresh every listed prompt on main before writing the artifact”, and every V1.1 close-out lists “every other milestone the plan then makes eligible” (`docs/milestones/M09.md` §11). A milestone in flight still has a blank `Status`, so `plan_eligible` includes it and a closing sibling refreshes its prompt on `main`. `docs/milestones/M17.md` §4.3 then requires an existing milestone branch to contain “the latest commit on `main` that changed the milestone's brief (`git log -1 main -- <brief>`)” — and a refresh IS that commit. The refused redispatch's prescribed repair is `git -C <worktree> merge main`, which the precondition reader deliberately does not perform. No exemption for an in-flight milestone exists anywhere in the scope or the briefs. | Verified by inspection at f6c0c65, not inferred. It is not an M09/M10 accident: by construction every refresh by one sibling invalidates the other's existing worktree, so it bites any parallel pair and therefore contradicts acceptance item 5 and M13's whole purpose. The handover can name a sibling correctly and simultaneously strand it, with no error anywhere — an unattended chain would stop and say nothing until the precondition report is read. Fix by separating **naming a milestone in a handover** from **rewriting its active instructions**: do not refresh a milestone with an open lane, or defer that refresh until no attempt is active. Owner: the shared close-out rule plus M09's and M10's §11, before M09 and M10 run. Raised as F05 by the external V1.1 design review of 2026-09-18. | active |
| D-107 | 2026-09-18 | **Two recovery gaps make the ladder's promised final rung unreachable and can hide a crash after a resume.** (a) `crash_check` in `lib/rows.sh` skips a lane when the log holds any `consumed` event for `(project, milestone, attempt)`, with no time bound. So a `no-handover` ending consumed on attempt 1, followed by a resume, followed by the resumed process dying, is never sighted: the historical ending excludes the attempt. `ladder_position` in `lib/stops.sh` bounds the same question by a reset point (`dispatch`, or a session-written `consumed`, `last`); the two use different notions of “since when”, and that difference is the defect. (b) `ladder_position` counts failures only within `.attempt == $a`, and `next` is `resume` at one, `redispatch` at two, `escalate` at three. A redispatch starts a new attempt and writes a `dispatch` event, which is itself a reset point, so the count returns to zero. Fail-resume-fail-redispatch therefore repeats without ever reaching three failures in one attempt. | Verified by inspection at f6c0c65. The comment guarding (a) is right about the tick immediately after a consume — acting on one ending twice — and wrong about a failure that occurs after the resume has already acted on it. Together these mean an unattended overnight run can loop a lane indefinitely, resuming and redispatching, and never raise the `ladder-end` escalation the brief promises; other escalation classes still fire by their own routes, so the lane is not wholly unprotected, but the ladder's own end is unreachable by this path. Fix by making the ending guard relative to the newest delivered execution or resume, and by carrying an ineffective-recovery count across redispatches until a defined progress or resolution event clears it. Keep the takeover, wait and startup-grace protections. Owner: a small recovery unit over `lib/rows.sh` and `lib/stops.sh`, before M15-b, which promises this bound. Raised as F06 by the external V1.1 design review of 2026-09-18. | active |
| D-108 | 2026-09-18 | **Register of the external review's open findings, with the milestone that owns each, so none is lost to the shape of a later milestone.** F03: planning and judgment sessions have no lifecycle contract — `redispatch` needs a current plan row, and a guard session occupying an admission slot while its own result is awaited can deadlock the cap; settle request identity, role, result association and accounting before M11/M12. F07: `inbox_consume` moves an artifact through `archive_move` before appending `consumed`, so a tick killed between them leaves an archived file no event claims, with no advancement and a suppressed crash path; a recoverable receipt reconciled on the next tick is the fix, sized against M10's scope. F08: `install.sh` copies the runtime sequentially and `bin/baton` sources libraries before any verb takes the lock, so a tick or hook starting mid-install reads a mixture — stage a complete bundle and publish one atomic reference; matters most at M17-b, which adds a library while changing callers across the surface. F09: M10 does not settle the standing check's command, working directory, pinned revision, deadline or result-to-revision binding, and an inline implementation would hold the tick lock through an unknown project's test run. F10: the scope guard receives only the stored `goal`, and “run an unfamiliar project unattended” does not contradict “replace the controller with Swift and SQLite” — a goal-only guard would have passed V2, which is the single thing it exists to catch; the intent confirmation must capture the material constraints, in M11's contract, before M15-c is built on it. F11: nothing onboards or migrates Baton's own pre-existing registration, so M15-c can install and then meet its own close-out with no confirmed goal and correctly fail closed; assign legacy-registration handling to M11. | Recorded because these were set aside as a batch while four verified blocking findings were fixed in f6c0c65, and a batching decision is not a severity judgment. F05 and F06 were in that batch and are now D-106 and D-107, both verified. F10 is the one the owner and the reviewer rank differently: recorded here as owned by M11, and to be settled before M15-c rather than alongside it. F07 and F08 are consistent with §10's standing deferral of L34/L35/L41; F03, F09 and F11 need cross-milestone contracts rather than patches and are rightly held for the milestone that reaches them. None is fixed by M17 or M17-b. Source: external V1.1 design review of 2026-09-18 against 4710d17. | active |
| D-109 | 2026-09-18 | **Withhold a close-out prompt refresh while the listed milestone has an open lane or an open park; keep naming every eligible milestone with its correct disposition and brief pointer.** Amend CONTRACT clause 3(c), its REQ-CONTRACT restatement, the instruction twins, the standing plan/scope rules, the glossary/workflow summary and all V1.1 briefs' §11 and prompt close-out steps. Establish open lanes and parks with `baton status`, supplementing lanes from the dispatch log: the newest dispatch remains open until a later consumed complete for the same project, milestone and attempt, in log order. Status alone omits lanes without a live row. | Amended in the open-park follow-up because this is the same close-out restriction, not a new policy. A peer refresh also changes the parked milestone's prompt hash, which edit_reread_check treats as an edit resolution; person_acted then permits a redispatch even without a Model-cell change. Prevents both close-out mechanisms of D-106 without weakening M17's own-brief ancestry invariant or automatically merging main into a person's branch. Naming and refreshing are separate; dispositions, eligibility and artifact schema are unchanged. Known limitation: a redispatched milestone can read a brief one refresh behind, the same brief it was originally dispatched with, so no worse than today, but it may miss an interface a sibling introduced; repeated sibling close-outs can withhold further refreshes. Accept this cost without an owed-refresh record: a future safe refresh uses current interfaces, while tracking a debt would add lifecycle/schema work outside this correction. The hash-side defect remains outstanding: a person's unrelated brief edit can still release a park; reread_hashes is unchanged and its correction is a separate pass. Historical completed V1 evidence is unchanged. | active |
| D-110 | 2026-09-18 | **Bound both crash-ending guards by the newest delivered execution, carry ineffective recovery across attempts, and settle session identity before recovery.** In `lib/rows.sh`, `crash_check` is the only caller of `ended_on_disk`; its optional second argument `{at, archives}` bounds archive filenames by the newest dispatch or delivered/forked resume, while calls without it keep the old unbounded behavior. Log endings use event order. Equal-time archives are ignored only when a consumed event before that execution names the archive; equal-time archives without that proof, newer archives and archives whose time cannot be read still suppress a crash, preserving the INV-06 move-before-log window. Rejected files and events remain unbounded because rejection already parks the lane. In `lib/stops.sh`, `ladder_position` preserves within-attempt rungs and adds `ineffective_failures` across attempts since the last session-written consumed; the third failure takes existing `ladder-end`, before another resume/redispatch, only once the current attempt has a failure. `redispatch` finds the current session's job, stops and settles it before dispatch_try, and refuses a missing job, failed stop or unsettled process. Recovery uses job_of_session's optional any-state mode; the resume rung refuses a missing job too. Fork compensation re-reads rows by lane name and original session ID, without a pid filter, to avoid stopping its same-name copy. | Resolves D-107's log and ladder defects plus the archive guard the first fix specification missed, and the S1/B2 identity failures. Archive timestamps give the bound without another log read; explicit log-order evidence settles same-tick ties. Only a session's own handover proves progress, so a dispatch cannot erase failed recovery; existing person_acted/park handling still honors a person's resolution. The hand-dispatch live-row refusal is not on the automatic path, so it could not prevent outgoing processes accumulating on every redispatch (D-087 measured 0.35–0.8 GB per idle process). A missing pid is not proof of a missing process; a missing job now leaves recovery visibly refused until the listing identifies it, rather than resuming blind. No new class, event kind, log format, dependency or verb. The two existing libraries are the full counted scope; log_event's one-writer/4 KB bound, dispatch_failed's 2,000-byte cut, dispatch_try's separate two-failure bound, takeover, inbox, startup grace, sightings, waits and holds are preserved. D-108's unrecorded-archive reconciliation remains outstanding; suppression is protection, not reconciliation. | active |
| D-111 | 2026-09-18 | **Regenerate Baton's own permissions file on every install, using a temporary file, content comparison and rename only when changed.** Wake settings then inherit the current deny list. This restores the installer's allow and deny template, including on an existing registration; other projects' registrations are untouched. | V1 review B1: before this change the live registration held 37 deny rules while main generated 43. The absent-file guard never upgraded the six rules added for the plist and notifier state, and settings_compose copies the stale deny list verbatim. The owner's requested write-and-compare follows the existing wake-settings idiom. The live allowlist matched the two installer rules before remediation. The install-upgrade scenario starts with a shorter deny list, verifies the upgraded registration, dispatch settings and wake settings, and checks that an unchanged reinstall preserves both files' inodes and leaves no temporary file. Verification on main: 257 scenarios before, 259 after, zero failed under LC_ALL=en_US.UTF-8. The canonical install remediated the live registration from 37 to 44 deny rules and wake settings to 48; no rule was removed. The installed plist and last-tick checksums were unchanged, and launchd remained unloaded. | active |
| D-112 | 2026-09-18 | **Narrow D-026's Bash exception for bin to exclude bin/lib, adding Bash(*.baton/bin/lib*).** The owner ruled that the exception was drawn one level too wide. | V1 review M3: a session runs bin/baton but never runs the sourced bin/lib files as commands. The new fragment fences a named shell write into the tick's library set without refusing the installed status command. Edit and Write rules alone do not constrain a shell cp. The two deny classes and the stated named-path limitations remain; the generated deny count becomes 44. D-048's executable-set protection applies to these libraries too. | active |
| D-113 | 2026-09-18 | **Distinguish harness installs from production installs with BATON_INSTALL_TEST=1, set only in the scenario runner's environment by default.** Harness mode installs the tested checkout's bytes into its disposable home even from a linked worktree, while registration still names the canonical checkout. Production mode refuses before writes unless the script is in the canonical checkout with HEAD on main and its install inputs match committed main; tracked changes and untracked or ignored inputs the copy would pick up are refused. Source-guard scenarios explicitly disable the seam. | V1 review S8: source copies used the script's directory while registration named the canonical checkout. The first guard fixed that mismatch but broke D-099's linked-worktree verification property. The owner found the regression in review; a disposable linked worktree at a688685 reproduced 6 install scenarios with 5 failures, only install-source-guard passing, and was removed. Canonical-only verification had missed it. The explicit seam preserves identical behavior assertions wherever the suite runs; location-dependent expectations would test refusal instead of the installer in milestone worktrees. install-harness-linked proves branch-local fixture bytes install in harness mode, while production mode still refuses that linked tree and uncommitted canonical inputs. The seam is forgeable by a session like every other seam, not a security boundary; the deny rules cannot authenticate it. Source validation is not an atomic publication mechanism or a standing-check certificate; D-079's close-out order and D-108's F08 deferral remain. Completion verification: 277 to 278 full canonical scenarios, zero failed; the unchanged install subset plus the new boundary scenario passed 7/7 from both canonical and linked worktrees. Both disposable verification worktrees were removed. | active |
| D-114 | 2026-09-18 | **A failed dispatch may spend one cleanup budget per process, shared by every candidate in that tick or hand-run verb.** dispatch_one claims a shell variable before command substitution, so helper subshells cannot replenish the budget. The first unparsed successful or nonempty launch may poll its session_name for at most sixty readings and 59 half-second sleeps; a nonzero launch with no stdout gets one inspection. A row_for_id failure uses the known job id directly. dispatch_stop_jobs issues the admitted cleanup's stop commands and settles all successful stops together with at most sixty readings and sixty half-second sleeps, rather than one settle loop per job. Later candidates record "cleanup skipped; budget spent this tick" before the existing detail; refused, unidentified and truncated cleanup remains visible. No new clock is introduced: event timestamps still use BATON_DATE, never bare date. No event kind, envelope, writer, two-strike rule or verb changes. | V1 review S7's missing-stop finding holds on both paths; its capture claim was overstated because empty stderr already falls back to stdout. Preserve that fallback and retain both streams when stderr is also nonempty, without broadening cli_plain. The first correction passed 259 to 262 scenarios but left excessive lock holds. The proposed bound was wrong in three ways: the cap limits successful dispatches, so every remaining failed candidate can be tried; matching rows each previously bought another settle loop; sleep counts exclude CLI/read latency and are not wall-clock deadlines. Holding the lock prevents status and answer, excludes later ticks and delays crash, stall and takeover reconciliation. The owner rejected that trade and chose one shared admission. Added cleanup polling is now at most 119 half-second sleeps (59.5 seconds) and 120 listings per process, plus one stop call per identified job in the admitted cleanup. CLI and filesystem calls remain without timeouts; normal launch/row_for_id work still scales with candidate count, so total tick wall time remains unbounded and this is not a sixty-second deadline. Skipped launches may remain running and untracked; retry or parking follows the existing rules and does not prove eventual cleanup. The name lookup's authority depends on D-110 stopping outgoing sessions before redispatch, and the unquoted job-list expansion relies on hexadecimal ids having no glob characters. The shim's cursor prefix and delayed live-row visibility cover the two failure paths; dispatch-cleanup-budget exercises three failures with cap two in one real fixture tick, one cleanup and two visible skips, and dispatch-cleanup-shared-settle verifies three jobs share one settlement loop. Budget completion verification: 262 to 264 scenarios, zero failed under LC_ALL=en_US.UTF-8; the new budget scenario failed against the previous implementation. | active |
| D-115 | 2026-09-18 | **A tick retains an incomplete status when a top-level pass fails and withholds its marker.** tick_run accumulates status 3 across project reconciliation, dispositions, holds, reserve, offline, wake, dispatch and inbox failures; gap and park-resolution failures also prevent completion. It still lets independent work proceed and checks the gap against the previous marker. A clock read failure returns 3 immediately. A self-check refusal handled by parking its project remains a completed rule; best-effort notification delivery is unchanged. | V1 review S11 extends D-049's rows case: the prior guards printed an error then returned success, allowing last-tick to advance despite skipped work. Nine injected-pass scenarios exercise the failure boundaries, assert status 3 and the unchanged marker on both runs, and assert the lock was released. Except when the gap reader itself is the injected failure, the stale marker's notification appears once and stays keyed to that same marker. No new event kind, writer or retry policy is introduced. Pass C verification: 264 to 277 scenarios, zero failed under LC_ALL=en_US.UTF-8; 13 new scenarios and no existing expectation changes. Representative S11, S2, S3 and M1 regressions each failed against the prior implementation. Counted libraries: lib/derive.sh and lib/tick.sh only. | active |
| D-116 | 2026-09-18 | **Gap activity includes every open lane, including one with no live row, and compares closing events with the marker by epoch.** derive_gap uses lanes_open before the live-row intersection, keeps parks and waits, and converts closure timestamps through the existing iso_epoch. Equal instants do not count as after the marker. | V1 review S2: a process dying during an outage removed its lane from in_flight, silencing the gap until a completed tick permanently closed the window. gap-rowless-open pairs a stale marker with an open lane and no row, preserving the crash sighting and emitting the gap. M1: textual ordering can invert instants across UTC offsets. gap-mixed-offsets has no open lane to mask the result and distinguishes a later event that sorts earlier, an earlier event that sorts later, and an equal instant with another offset. | active |
| D-117 | 2026-09-18 | **An absent or unparseable lock timestamp falls back to the lock directory's own mtime in both stale reporting and rescue.** The fallback only reads metadata; age and the dead-pid condition still gate rescue, and rescue still claims by rename. The claim is revalidated against the observed pid and timestamp, plus directory mtime when that supplied the age. The fallback is labelled directory mtime in the existing held_since/detail fields; no lock metadata is repaired in place. | V1 review S3: a terminated lock_take or failed clock can leave a lock without a usable at, which both readers formerly ignored forever. lock-incomplete-metadata asserts dead-lock removal, live-holder pid/at/mtime preservation, a fresh incomplete lock retained, and an aged lock missing both fields reclaimed. These assertions run inside the fixture before tests/run.sh deletes lock directories from snapshots; the reviewed baseline had no scenario asserting lock contents. tick-stale-lock-incomplete also covers escalation, resolution and completed marker through the ordinary tick. No real session or launchd job is used. | active |
| D-118 | 2026-09-18 | **Recover a missing artifact identity from the longest filename prefix parse_id accepts that leaves a complete session id; refuse an unrecoverable pair before moving the file.** JSON identity fields remain first. Filename session suffixes are complete UUIDs or bare hexadecimal ids, including fixture ids such as f2; accepting an arbitrary UUID tail would mistake M02's first session group for a milestone suffix. Empty files also use the fallback. reject and the orphan sweep propagate identity refusal, leave the inbox bytes untouched and report the failure rather than inventing a lane. No third library is changed. | Correct the S5 coverage premise in the shape of D-105: the report named reject-missing-field among four fallback fixtures, but its JSON supplies milestone and session and lacks outcome, so it does not use filename identity. Inspection of shipped inbox inputs and their command paths confirms reject-unparseable, reject-orphan-tmp and consume-live-tmp, plus the omitted tick-inbox-not-a-stall, whose truncated M02-f2.json.tmp reaches the same helper through the tick. The subsequent correction by the owner also omitted tick-inbox-not-a-stall; inspection corrected both lists. Its hyphenated variant demonstrates destruction of a live session temporary artifact followed by a false stall about that same session, so S5 is a reproduced failure rather than only an argued risk. The finding stands regardless: the live archive contains four hyphenated-milestone artifacts, and the first-hyphen split misidentifies them when JSON cannot supply identity. All six new hyphen scenarios failed before the fix: the unparseable/orphan/live variants returned M07 with d-UUID, the tick variant returned M07 with d-f2, missing-milestone returned M07, and missing-session returned d-UUID. Both live temporary files were wrongly rejected. The tick variant updates the renamed kickoff text's receipt hash and asserts no takeover, so stand-off cannot mask the stall guard; the prior implementation then also emits a false stall. After repair the rejection events and per-project parks belong to M07-d and the full session, while live writers retain unchanged temporary files. artifact-ids-refuses-unknown checks that invalid names remain in the inbox without a guessed rejection event, and that an empty live file with a plain milestone retains its complete UUID. The review report itself is unchanged. Verification: 278 to 285 scenarios, zero failed under LC_ALL=en_US.UTF-8; seven new scenarios and no existing expectation changes. The unrecoverable-identity scenario also failed against the prior implementation. Counted library: lib/inbox.sh only. | active |
| D-119 | 2026-09-18 | **B4 remains unfixed because its class-input premise is contradicted by the code; the unattended instruction requires skipping that item.** reread_hashes accepts project, milestone, carries and optionally the plan. The class is a separate argument to escalate and a top-level escalation field; neither writer nor edit_reread_check passes it to the reader, and model_not_found carries contains model, plan and detail, not class. A class-specific reader needs an explicit interface change and a policy for existing hash receipts, rather than relying on the asserted input. | The proposed normalization of prompt parts 1 and 3–7 leaves the invariant slot paragraph, eliminating the brief signal. Requiring both plan and brief changes for every class breaks the existing ladder-end-edit brief-only release. The class-specific approach remains the proposed remedy, pending correction of its interface premise; no B4 runtime change is included. Preserve exact prompt hashing: unchanged bytes from touch, autosave or a content-restoring rebase cannot release a park, but a formatter that changes bytes can release a brief-based park. The owner's earlier formatter claim and the review's clean negative were too broad. That residual is accepted: whitespace, quoting, newlines and the anchored slot line change the delivered shell argument; the owner prefers a visible, recoverable release to silently ignoring a whitespace-only repair; prompt_normalise already supplies the single receipt/transcript normalization under REQ-LOG-05 and D-030, so a second normalization would create another divergent reading. Whitespace normalization also cannot fix substantive close-out refreshes. D-109 restricts refreshes of open lanes and parks but does not fix the hash join. This is a restart decision, not a completed finding. | open |
| D-120 | 2026-09-18 | **A nonempty baton answer ruling releases a sessionless lane-scope dispatch-failed park without resuming anything.** It writes the existing resolution with how ruling; the next tick still applies eligibility, cap and all normal admission rules. The message names the failed stage and asks for the ruling instead of promising an automatic dispatch. | B3: dispositions_intersect excludes a candidate with an open lane park, so the proposed successful-dispatch lapse is circular. A worktree or settings repair changes neither plan nor brief hashes. dispatch-failed-handback creates a real worktree-stage failure twice, repairs it, proves the next tick remains held, rejects an empty ruling, then gives a textual ruling and observes one dispatch with no resume. It does not seed a changed-hash sentinel. A read-only re-check of the recorded stage's precondition is the longer-term fix owned by M17, once its shared precondition reader exists; this pass does not build it. Session-backed rulings still close their parks only after delivered or forked resumes. Counted scope with D-121: lib/escalate.sh and lib/answer.sh. Verification: 285 to 288 scenarios; canonical and disposable linked-worktree full suites both report 288 scenarios, zero failed. The disposable worktree was removed. Existing expectations changed only the dispatch-failed message in dispatch-cleanup-budget, held-plan-dispatch-fails and tick-dispatch-fails-twice; the longer command shortens two content excerpts under the unchanged message limit. | active |
| D-121 | 2026-09-18 | **allow --resume checks takeover before widening either permissions file.** It uses the existing read-only derivation and refuses a taken-over, orphaned or unreadable lane, or a failed row read; the checked row snapshot supplies the eventual job lookup. Widening without resume and the explicit answer hand-back retain their existing roles. | M7 and INV-04: the old verb checked only parks and could stop a session the person was typing into. allow-resume-takeover asserts unchanged registration, settings and log, and no stop or resume; allow-resume-unreadable proves inability to check does not authorize interruption. The takeover fixture failed against the previous implementation by widening and resuming. No new normalization, event envelope, dependency or verb is introduced. | active |
| D-122 | 2026-09-18 | **An existing complete archive that cannot be read refuses disposition derivation; it is neither an empty handover nor permission to use an older word.** dispositions_in_force uses jq -e, counts a handover only after its successful read, and returns the unreadable path on failure. Missing archives retain the existing absence rule. | V1 review M6: the previous fallback replaced jq failure with an empty array but incremented the handover count. handover-unreadable pairs a valid older handover with a malformed newer one and also checks empty input; the old code returns the older run/held dispositions and fails the assertion. The fixture tick now returns 3 through D-115, preserves last-tick and the log, and creates neither dispatch nor false omission. Other tick readers may also print the malformed JSON diagnostic; that does not authorize disposition fallback. Counted scope with D-123: lib/candidates.sh and lib/templates.sh. Verification: 288 to 290 scenarios; full canonical and disposable linked-worktree runs both report 290 scenarios, zero failed. The disposable worktree was removed; no existing expectation changed beyond the finish-prompt path recorded in D-123. | active |
| D-123 | 2026-09-18 | **The finish continuation names the inbox under the effective BATON_HOME, as the Stop gate does.** It no longer hardcodes the default home. | V1 review M2 and D-091: an inherited foreign home can otherwise put the session's requested artifact and the gate's expected file in different places. finish-foreign-home uses a nondefault path containing a space and a hyphenated milestone, verifies that the prompt and blocking gate name the same path, then writes there and observes the gate release. It failed before the fix on the literal default path. The existing no-handover-ladder expected sidecar changes only that path to @TMP@/home; the sidecar/transcript normalization is unchanged. | active |
| D-124 | 2026-09-18 | **A refused ruling delivery is not a session failure ending.** derive_ladder and ladder_position now exclude resume_kind ruling, agreeing with person_acted and D-061. The latter's shared failure predicate covers both the attempt count and D-110's cross-attempt count. REQ-STOP-04, CONTEXT and the architecture name whose failure spends the remedies. | S4, by the owner's ruling: an intervention must never consume remedies meant for the session's failures. ladder-ruling-counts distinguishes two refused rulings plus one no-handover within and across attempts, while an ordinary refused continue still counts. tick-ruling-refusals previously parked ladder-end at three and now delivers exactly one finish resume for the one genuine ending. Both new scenarios failed before the fix. The remaining triplication across derive.sh, stops.sh and escalate.sh is a known concern; it is not unified because that would touch a third counted file. G is split into separately verified units: this one counts lib/derive.sh and lib/stops.sh; S10 is the next unit. Verification: 290 to 292 scenarios; full canonical and disposable linked-worktree suites both report 292 scenarios, zero failed. The disposable worktree was removed. No existing expectation changed. | active |
| D-125 | 2026-09-18 | **A delivered or forked resume re-arms the stall and long-running notification keys, and the long-running clock uses that same successful-resume boundary.** Refused resumes reset neither those keys nor that clock. Other classes retain their reset rules, so repeated wait retries do not re-arm wait-ceiling notifications. | S10: rows.sh restarted the elapsed clock on a resume while derive_key_spent still saw the notification before it as spent forever. The counted files are exactly lib/derive.sh and lib/rows.sh, a separate G unit after D-124. notifications-after-resume seeds prior notifications, a successful resume eight hours ago and a recent refused ruling: one new stall and long-running message must appear, the elapsed time must be eight hours, and repeated reads must not notify again. key-resume-boundary distinguishes delivered, forked and refused outcomes for both automatic continuations and rulings, and proves rate_limit stays spent. Both fixtures failed before the fix. The only existing expectation change is tick-long-running's message/detail saying successful resume, making the clock's boundary explicit. Stall retains its transcript-age threshold and existing remote-transcript movement rule. Verification: 292 to 294 scenarios; full canonical and disposable linked-worktree suites both report 294 scenarios, zero failed. The disposable worktree was removed. | active |
| D-126 | 2026-09-18 | **INV-05 remains stateless derivation from the five inputs; its stated enforcement is corrected, not the invariant weakened.** Review checks that a tick retains no process memory between runs. The harness runs each scenario twice in fresh shell processes against persisted fixture state, checks both runs' streams and exit statuses, then compares one final state snapshot with expected. It never takes or compares a snapshot after run one. The enforcement text in INV-05, REQ-TICK-02, SPEC section 6 and ARCHITECTURE sections 6.4 and 7 now says what is actually checked. | S9: an empty second-tick diff on every fixture was neither implemented nor a valid universal requirement. Dispatch's two-strike counter, crash confirmation, ladder steps and gap/notification keys can legitimately advance across ticks. In held-plan-dispatch-fails the second failed dispatch must create the park; an empty second-tick diff would be the bug. No impossible check is added, and the documents-only pass adds no scenario or runtime state. Verification remains 294 scenarios, zero failed, in both the full canonical and disposable linked-worktree runs. The disposable worktree was removed; no expectation changed. | active |
| D-127 | 2026-09-18 | **The snapshot harness excludes the lock directory; the reviewed baseline had no scenario asserting its contents. The claim that none does today is no longer true.** tests/run.sh removes got/home/lock before its final directory comparison. Pass C added direct assertions inside lock-incomplete-metadata, before snapshotting, for live pid/timestamp/mtime preservation, fresh incomplete locks and dead-lock reclamation; tick-stale-lock-incomplete also exercises the tick path (D-117). | The review's coverage statement applied at 4710d17. Repeating it as current fact after the S3 fix would contradict the new fixture's explicit reads and assertions. The unattended instruction therefore skips that current-tense premise and records the actual boundary: snapshot comparisons still cannot detect arbitrary lock-content regressions; the new command assertions cover only the cases they name. | active |
| D-128 | 2026-09-18 | **INV-01 has no direct no-model-call scenario; INV-11 as worded has no killed-mid-run or release-before-marker scenario; INV-12 has no scenario asserting the launch agent's execution contents.** These are recorded verification gaps, not evidence that the invariants are false. SPEC names the real review/fixture boundary instead of promising the nonexistent kill test. | Inspection of the runner and scenario commands found no model-call prohibition assertion, process-kill scenario or launchd ProgramArguments/StartInterval assertion. verb_tick does release the lock before its successful marker write, and D-115's injected failures assert an unchanged marker; those do not prove interruption behavior or write ordering under a kill. Installer scenarios inspect the notifier application's Info.plist and preserve a deliberately different launch-agent file, which is not an assertion of the shipped agent's execution paths or keys. launchd remains outside the harness and unloaded. | open |
| D-129 | 2026-09-18 | **Leave M4 and M5 as scoped concerns, without implementing either.** M4 needs a CLI compatibility feature: no version pin, binary fingerprint, canary or claude --version check protects prose-in-JSON readers such as the exact waitingFor value input needed. M5 remains a size/error-handling note: inbox field cuts use jq string length/codepoints, dispatch failure detail uses raw bytes, and log_event refuses a complete serialized JSON line at 4096 bytes including its newline. Several callers do not check that refusal explicitly, including consumed after the archive move. | M4 is a feature needing its own scope, rather than an incidental fix. M5's review estimated roughly fourteen unchecked sites and could not reach the line limit with a credible artifact; the differing units and unchecked consumed append are verified, but no reachable counterexample is established here and no log format/writer change is made. D-108's F03, F07, F08, F09, F10 and F11 remain outside this V1 repair pass and owned by later V1.1 milestones. | open |
| D-130 | 2026-09-18 | **Admission counts the executions the same tick created, and the launches it could not prove started nothing.** `dispatch_run` takes `<candidates> <plans>` and reads `claude agents --json` itself, after the reconciliation, instead of being handed the listing the tick opened with; with no candidate it returns before reading anything, and a listing it cannot read fails the pass rather than counting as empty. `lib/dispatch.sh` keeps a process-scoped `do_unresolved`, claimed in the caller's shell as `do_cleanup_spent` already is, raised by a launch whose worker was neither identified nor stopped — a skipped cleanup, a refused or unsettled stop, a listing that could not be read, or an acknowledged launch whose row never appeared — and `dispatch_run`'s loop condition adds it to the in-flight count. A launch that exited non-zero printing nothing at all, whose one fresh inspection found no row of the lane's name, raises nothing. No new event kind, clock, timeout, state file or verb; event timestamps still use BATON_DATE. | N1 and S7 are one defect, and N1 is not new: `docs/v2/01-findings.md` finding 24, "Sessions dispatched together receive incomplete concurrency context", records the same root cause over the same `lib/derive.sh:172–179` join, before the external review re-reported it. Derivation 1 reads the log fresh but joins it against the listing it is given, and steps 3 and 4 create sessions: `redispatch` opens a new attempt under a new id, and a resume that forked moves the lane onto the copy's. Against the opening listing those ids match no row, the lane counts zero, and with cap two and two other candidates the tick launches both beside the replacement — three workers. Re-reading closes both routes soundly, because `dispatch_one` records a session only once `row_for_id` has seen a row carrying its pid and `fork_session` adopts a copy the same way, so an execution this tick created is in the new listing. It does not close S7, where the worker may exist under no id at all; that needs occupancy rather than observation, and one tick's conservative count costs at most the rest of that tick because the next tick reads the listing from nothing. Four residuals are accepted rather than hidden: the count is process-scoped, so a worker leaked past the tick is invisible to the next one, no `dispatch` event naming it and derivation 1 keying on the log (INV-07); a fork whose id `fork_session` cannot resolve is recorded as the short eight-hex job id, which never matches a row's `sessionId`, so that lane still counts zero against any listing; and the non-zero-exit-with-no-stdout reading is treated as proof nothing started. The fourth is the half of finding 24 this pass does not close: `drn_rows` is read once before the candidate loop and handed to every `dispatch_one` in the batch, so the second milestone dispatched in one tick composes its slot line against a listing taken before the first one started and still under-reports its peers. `tests/scenarios/held-plan-wins` shows it — both expected prompts say nothing else is in flight while the scenario dispatches two — and both are unchanged here, which is what confirms the half is still open. Admission is nonetheless correct, because `drn_total` rises per successful dispatch inside the loop. That residual is prompt content rather than capacity, and closing it may need a third counted library, so it is left open and raised for assignment to a milestone. Closing it, or either of the first two, means changing `lib/derive.sh` or `lib/stops.sh`, a third counted library, which the split rule forbids in this pass. Counted libraries: `lib/tick.sh` and `lib/dispatch.sh` only. Verification: 294 to 297 scenarios, zero failed under LC_ALL=en_US.UTF-8, in both the canonical checkout and a disposable linked worktree. Twenty-six existing expectations were refrozen in `calls.log`: thirty-eight added lines and none removed, every one of them `claude agents --json` and nothing else — fifteen fixtures gained one, ten gained two and `dispatch-failed-handback` gained three, because their ticks reach admission more than once. No state, log, stream or status changed in any scenario. `cap-redispatch-counts`, `cap-copy-fork-counts` and `cap-unresolved-launch-counts` each failed against f1e32a2 at run one: the first two dispatched a further `Baton · Fixture · M07` beside the recovery, and the third launched a third candidate behind a worker whose cleanup had been skipped. | active |
| D-131 | 2026-09-18 | **The installed relay is published as one immutable library set per content, named by its digest and pinned into the copy of `bin/baton` beside it.** `install.sh` stages `lib/*.sh` in a directory of its own and publishes it by renaming that directory to `~/.baton/bin/lib/<digest>`, a name that does not yet exist; `<digest>` is sixteen hexadecimal characters over `bin/baton` and every `lib/*.sh`, and a name already present is a set already published and left untouched. The digest is written into the `baton_bundle=` line of the installed `bin/baton`, which is published by renaming over the old one — the one atomic reference change. `bin/baton` resolves `BATON_LIB` to `$here/lib/$baton_bundle` when that line is set, to `$here/lib` when it is not, and to `$here/../lib` in the checkout, so the checkout-versus-installed discovery is unchanged. The previous set is kept and older ones removed; a pre-bundle relay names no set and its flat `bin/lib/*.sh` is the previous set until one bundled install has been superseded. The three hooks are published the same way, and every file is compared before it is replaced. `~/.baton/bin/sh` is untouched by the publish, the `BATON_INSTALL_TEST=1` seam and the production source guard are unchanged (D-113), and the launchd agent's fixed `ProgramArguments` path is untouched — the reference lives under `~/.baton/`, never in the plist. | F08, pulled forward from D-108's register, whose assignment to M17-b was too narrow: the exposure is every unattended close-out running `sh install.sh` itself (D-079), which `docs/MILESTONES.md` schedules for M17-b and M09 to M16, not the addition of a library. Verified before implementing: `install.sh:34–37` copied file by file under no lock, `bin/baton:20–38` sourced eighteen libraries before `verb=` at :56 and before every `lock_take` in the case arms, and the agent fires every sixty seconds — so a tick starting during a copy sourced a mixture, and an installer lock could not have prevented it, the sourcing being over before the tick takes one. Only an immutable set plus one atomic reference change closes it. The atomicity is `rename(2)` through `mv`, measured on this Mac rather than assumed: renaming a directory to a name that does not exist succeeds atomically, and renaming a file over an existing one changes the destination's inode while a process holding the old inode open reads it unchanged to the end — which is exactly what a running `bin/baton` needs. The obvious alternative, a symlink flipped by `mv`, is wrong here and was measured to be: `mv new old`, where `old` is a symlink to a directory, does not replace the symlink but follows it and moves the new link inside the directory, leaving the old target in force and a stray link behind. Retention is the current set and the previous pin, which meets the requirement — the whole previous set or the whole new one — for one install at a time. Installs are not far apart by nature: this script takes no lock, and under D-079 every close-out runs it itself, so with a cap of two, two close-outs can run it in the same minute. Three accepted residuals follow, recorded rather than locked away. First, two concurrent installs can sweep a live reader's set: if Y reads `previous` before X publishes, Y keeps its own set and the pin Y saw, and deletes the set X published — which a tick started in between is pinned to and mid-source. Reachable at a cap of two. Second, when a pinned `bin/baton` names a set that has been swept, the first `.` at `bin/baton:45` fails under `set -e` and the tick dies whole having written nothing. That is a property of the design and not an accident of it: a loud total failure is the right direction to fail in, and it is the opposite of the silent mixture F08 was about. Third, `publish` writes its temporary file at `<destination>.tmp` with no pid, so two concurrent installs share `~/.baton/bin/baton.tmp`; this is the house idiom already used for `permissions.json.tmp` (install.sh:157) and `wake.json.tmp` (:180) and predates this pass, but those paths were never ones two sessions run at once, so it is named here rather than rediscovered later. A stage directory is deliberately never swept, for the same reason: a wildcard over `.stage-*` cannot tell an orphan from the directory a live install is filling, and deleting one aborts that install under `set -e` before it publishes anything, leaving a close-out's successor dispatched against the previous relay with nothing saying why. A successful run leaves no stage — its own is removed by pid before creation and consumed by the rename — so the only cost is a few unread kilobytes after a killed install. Counted scope: `install.sh` and `bin/baton`, neither a `lib/` file, so the split rule's library count is untouched and no `lib/` change was needed. Verification: 297 to 298 scenarios, zero failed under LC_ALL=en_US.UTF-8, in both the canonical checkout and a disposable linked worktree. `install-harness-linked` now reads the installed bytes through the published set's name and `install` masks the digest so a future library change does not churn its expectation; no other expectation moved. `install-bundle-flip` failed against 5af7f43 at run one with none of its three lines, and the reason was demonstrated directly there: installing a changed set overwrote in place the very files the already-installed relay reads, its `derive.sh` gaining the new content under a reader's feet. | active |
| D-132 | 2026-09-18 | **A fork's compensating stop is settled, and an original it cannot account for parks the lane; `allow --resume` refuses an attempt a consumed `complete` has closed.** B2: `resume_session` appends `copy_fork` first, so every derivation follows the copy from that moment, and then stops the original — previously `"$BATON_CLAUDE" stop "$rs_original" > /dev/null 2>&1 || true` at lib/stops.sh:305, with no settle and `rs_outcome` left at `forked`. It now settles that stop through `stop_settle`, exactly as the first stop is settled, and when no job in the listing identifies the original, the stop is refused, or its row still carries a pid, it writes a lane-scope `other` escalation carrying the original, the copy and what is unresolved, and adds `unresolved_original` to the document it prints. `verb_allow` reports that on its own line. M7: `allow_lane` now marks each candidate `closed` when a consumed `complete` names its project, milestone and attempt, and `verb_allow` refuses `--resume` on a closed candidate before anything is written; the rule alone is still written. | The first stop is allowed to fail — that is the fork classifier's whole design — but the compensating one is the last thing between a reported success and an untracked worker, and finding a session is not stopping it. The owner's ruling was that the lane must never report success while a worker it no longer tracks may be running, and that the survivor be made visible rather than hidden: a park is visible in `status`, reaches the Mac with the session to stop by hand, and stands Baton off the lane through `stops_standing_by`, which a warning on stderr does none of. The class is the existing `other`, the house catch-all M03 already uses for a transcript that cannot be scanned, so `class_or_fail`'s fixed list in `lib/notify.sh` is untouched and this pass stays at two counted libraries. M7's guard was correctly placed before `allow_write` but its domain was empty: verified by probe against a log holding a dispatch and a consumed `complete` for session f2 with a live row — `allow_lane` returned that candidate while `derive_in_flight` returned zero lanes and `derive_taken_over` returned zero in all three of its lists, so the guard passed on nothing. A finished session's process is kept while it is one of the few most recently active and is a person's to continue by hand (D-078), so resuming it would stop and re-prompt a conversation Baton no longer owns, with no way to have seen a person typing in it, which is what INV-04 forbids. Checking the selected target directly rather than widening the guard's domain keeps `lib/derive.sh` out of this pass. Counted libraries: `lib/stops.sh` and `lib/answer.sh` only. Verification: 298 to 300 scenarios, zero failed under LC_ALL=en_US.UTF-8, in both the canonical checkout and a disposable linked worktree; `copy-fork`, `copy-fork-generic-note` and `copy-fork-pidless` each gained one `claude agents --json` line in `calls.log` and nothing else, which is the settle succeeding where those stops do land. `copy-fork-original-unstopped` and `allow-resume-completed` each failed against a1c9857. Two stated limitations of the park this creates. Its actionable line is wrong: `escalation_verb` lists `other` in its no-ruling-target branch, which prints "fix what it names; the next tick re-reads the plan and the brief" — false here, because the park carries no `reread`, nothing in the plan or the brief bears on an unstopped process, and no re-read clears it; and with a reachable ruling target it falls instead to the default `baton answer <milestone> "<ruling>"`, which delivers a ruling into the copy, a session that is running and healthy. The `carries.detail` says the right thing, but the verb is the line `status` and the Mac message put in front of a person. `escalation_verb` is in `lib/escalate.sh`, a third counted library for this pass, so Pass 5 owns the correction under its own "report the actual evidence" requirement. Second, the park has no automatic way out: `class_unparks_by_edit` excludes `other`, `dispositions_intersect` clears only `omitted` and `disagreement`, `question_resolve_check` only `question`, and `park_resolve` only project scope — so only a person's ruling closes it, and `derive_gap` counts an open park in its open total at lib/derive.sh:499-501, so one never goes quiet again. That hazard is older than this pass rather than new to it: `escalate_rejection` at lib/inbox.sh:218-221 has always written the same shape — `other`, lane scope, no `reread`, ruling-only — for every rejected artifact, and its comment calls a rejection "the lane's own 'something else'". The two parks differ in kind, and that difference is the rule, not "`other`/lane parks should be clearable". A rejection's condition does not end by itself: a file was rejected and a person must decide something, so ruling-only is correct there and a park that stays open is the system working. A fork's condition does end by itself — the moment no row carries the original session with a pid, the worry is over and nothing needs a person — so ruling-only is a defect there and only there. The correction is to observe that end condition, and it belongs to **Pass 5**, not to M17: it needs nothing of M17's shared precondition reader, which is what made D-120's deferral right and leaves D-120's assignment untouched. `question_resolve_check <project> <rows json>` at lib/escalate.sh:490, called from lib/tick.sh:228 with the rows `tick_project` already holds, is the exact sibling — a class-filtered resolve check over state the tick has in hand — and a fork park's condition is one jq over those same rows, for no row whose `sessionId` is `carries.original` with a non-null `pid`. Pass 5 already counts `lib/escalate.sh` and already owes message truth, so the clearance and the actionable line are two halves of one correction and land together. Third, a stated dependency: when the copy completes, the milestone reaches `done` and `plan_eligible` selects `.status == ""`, so it is never listed in a later handover's `eligible[]` and D-109 never withholds a refresh for it. That escape is an accident of eligibility and not a design. M09 and M10 both touch completion; if eligibility ever widens to include a completed milestone, a lingering lane park begins withholding refreshes from every close-out that lists it. This pass also removes the `.stage-*` sweep D-131 added to `install.sh`: the script takes no lock and two close-outs can run it in the same minute, so a wildcard could delete the directory a live install was filling and abort it under `set -e` before it published anything, leaving a close-out's successor dispatched against the previous relay with nothing saying why — strictly worse than an unread orphan, which a successful run never leaves. | active |
| D-133 | 2026-09-18 | **The inbox pass carries every failure that left work undone, every token of a composed hook command is one shell word, and REQ-LOG-06's acceptance says what the harness checks.** S11: `inbox_consume` set `ic_status=0` and then called `consume_one` (lib/inbox.sh:364) and `repeat_one` (:360) without accounting for their status, and printed the unreadable-log branch (:362) without setting it, while `reject` (:375) and the `.tmp` sweep (:391, :398) were accounted; all three now set `ic_status=1`. N2: `settings_compose` quoted the assignment values and interpolated the executable path bare; a new `shell_word` helper renders each token as one single-quoted shell word, with an embedded quote closed, escaped and reopened, and the three commands are composed in the shell rather than inside the jq program. S9: docs/SPEC.md REQ-LOG-06's acceptance column said "the double-tick diff is empty" and now says what the harness does; REQ-VERB-02's "Fixture double-tick" was inspected and is accurate — the harness does run each scenario twice and that column claims nothing about a diff — so it is left as it stands. The requirement text of REQ-LOG-06 is unchanged and the harness is untouched. | S11 is the last of D-115's chain: the tick's marker guard can only be as honest as the status it is handed, and `consume_one` and `repeat_one` both return 1 when `archive_move` refuses (lib/inbox.sh:432, :341), which is a handover still in the inbox with nothing in the log. The existing `consume-archive-name-taken` proves the defect was live rather than theoretical: it starts with no `last-tick`, its expected home has one, and its inbox file survives — the marker advanced over a handover that was lost. It now ends at status 3 with no marker. `consume-archive-taken-marker-held` adds the form the review specified, starting from a marker and requiring it unchanged: the destination is occupied by a different artifact, so `archive_move` refuses at lib/inbox.sh:245 before moving anything. The unreadable-log branch is counted too: leaving the file for the next tick is the right act (D-095, D-097) but it is still work not done, and a tick that reports it as done advances the clock the gap is measured against. N2 was measured on both sides: the composed command with a spaced home executed and wrote its status file, and the same command with the path unquoted exited 127 with the path truncated at the space. `tests/scenarios/finish-foreign-home` invokes the hook source directly and never reaches that boundary; its proof of prompt/gate path agreement stands and is untouched. Counted libraries: `lib/inbox.sh` and `lib/dispatch.sh` only. Verification: 300 to 302 scenarios, zero failed under LC_ALL=en_US.UTF-8, in both the canonical checkout and a disposable linked worktree. | active |
| D-134 | 2026-09-18 | **A park is released only by the readings its own class designates, and a receipt written before that rule is taken afresh rather than converted.** B4. `reread_hashes` hashed the same two readings whatever the class — the parsed rows of the milestone, of every milestone whose `Depends on` names it and of the blocker it names, and the kickoff prompt on `main` — so a change to any of them released any park. It now takes the class as its third argument and prints an explicitly versioned receipt, `{version: 2, policy, hashes}`. `escalate` passes its own class argument (lib/escalate.sh:264) and `edit_reread_check` the parked event's top-level `.class` (lib/escalate.sh:572); neither infers it from `carries`, which is the interface premise D-119 stopped on. `class_reread_policy` gives `model_not_found` the policy `effective-model` — `model_sha256` alone, so no brief, sibling, downstream row, status, effort or topology change releases it; `blocked` gets `blocker-and-instructions` (`brief_sha256`, `work_plan_sha256`, `blocker_sha256`); `merge-failed` and `main-broken` get `close-out-status` (`status_sha256`); everything else, including a park of any class that no ruling can reach, gets `instructions` (`brief_sha256`, `work_plan_sha256`). `work_plan_sha256` is the lane's own execution fields and dependencies plus the ids and dependency edges of every milestone downstream of it, sorted by id, with row position and `Status` excluded and a downstream row's own `Model` and `Effort` excluded with them. Only a policy's own fields are compared, by presence and by value; the version and the policy say how to read the hashes and are never themselves evidence. A receipt with no `version` keeps the two things v1 can still speak for — its exact brief digest, presence and absence alike, and `status_at_park` — and those are compared first, so the brief-only release and a brief that first becomes readable both still work at once and no pending brief edit is erased; only when they say nothing are the readings v1 never held taken once into a `reread_baseline` event, which resolves nothing, leaves the park standing and prints a line saying an edit made before now cannot be proved. An unknown version, two baselines naming one park, a reading the plan will not give, and a failed append all leave the park exactly as it was, so an upgrade over unchanged inputs can never itself free a lane. `resolve` and the baseline append both have checked returns. The lines say what changed and never who changed it: `unparked … the effective model changed since the model_not_found park`, and the redispatch that follows reads `the effective model changed since the model was refused` rather than `the Model cell was edited`. | Measured at efdb005 before the change, not inferred. A lane parked `model_not_found` on M02, M02's own Model cell and brief untouched, and one `sed` on M03's Model cell — M03 depends on M02, so its row is in the projection — printed `unparked  Fixture/M02 · the plan rows it answers to changed since the model_not_found park`, then `redispatch Fixture/M02 · the Model cell was edited after the model was refused, so attempt 2 runs on it`, then a `dispatch` event carrying `"model":"opus"`: the identical refused model, under a claim no cell supported, which per D-056 writes no artifact and holds a cap slot until the stall rule speaks. The review found the same defect through the brief half, where CONTRACT clause 3(c) obliges a peer's close-out to refresh the parked milestone's prompt; D-109 withheld that refresh by instruction and said the hash join was still outstanding, and this closes it. A second release the same projection allowed was measured with it: writing `done` in a parked milestone's own `Status` cell released its `ladder-end` park and redispatched the milestone that had just been marked complete. That release is gone, which is the safe direction, and the residual is recorded rather than fixed — such a park now waits for a ruling, and a park nothing will dispatch stays in `derive_gap`'s open count (lib/derive.sh:499-501). `del(.row)` removed the row index but not the array order, so moving a row up or down changed the digest; the projections sort by id and that is gone too. Hashing is untouched: `prompt_normalise` and `prompt_sha256` are as they were, the brief still comes from `main` and the plan still from the registered working-tree file, and the accepted residual of D-119 stands — a formatter that genuinely changes a brief's bytes can release a brief-based park, which is preferred to silently ignoring a whitespace repair. Two limitations are stated rather than fixed. The rebaseline is said on the tick's line and not in a Mac message: a message needs a notification class, and `class_or_fail`'s fixed list is in `lib/notify.sh`, a third counted library. And `lib/declared.sh:98` still reads `the plan was edited after two unfinished endings`, the same authorship claim the two lines in `lib/stops.sh` gave up, for the same reason — it is a third library. `tests/scenarios/edit-unparks` was reseeded through the real reader, with the blocker held at the moment the park is taken, because its sentinel digest released the park on any reading at all and so proved nothing about the join; `edit-unparks-no-session` keeps its sentinels, since what it proves is that a park with no ruling route carries a receipt, not which reading ends it. Counted libraries: `lib/escalate.sh` and `lib/stops.sh` only — the two D-132 corrections need `lib/tick.sh` for their call site, so they are verified as their own half under the split rule. Verification: 302 to 306 scenarios, zero failed under LC_ALL=en_US.UTF-8, in both the canonical checkout and a disposable linked worktree. | active |
| D-135 | 2026-09-18 | **A fork park is answered by stopping the named original, and it ends when no row carries that session.** The two corrections D-132 recorded against itself, landed together because they are one park telling the truth about its own condition. `escalation_verb` tests `carries.original` before either of its routes: a park that names an original prints `stop the original session <id> by hand; the park clears itself once no row carries it`. Previously `other` sat in the no-ruling-target branch and printed `fix what it names; the next tick re-reads the plan and the brief`, which is false for a park carrying no `reread` and about a process, and with a reachable ruling target it fell to the default `baton answer <milestone> "<ruling>"`, which delivers a ruling into the copy — a session that is running with nothing wrong with it. `fork_resolve_check <project> <rows json>` sits beside `question_resolve_check` and is called from `tick_project` with the rows the tick already holds; it resolves `edit` for every lane-scope `other` park whose `carries.original` names no row with a non-null pid, and prints `unparked … no row carries the unstopped original <id> any more`. | The park's condition is a process that may be running under a session `copy_fork` has already moved every derivation off, and a process ends; the moment no row carries it there is nothing for a person to do, while a park that cannot end holds its milestone out of every dispatch and stays in `derive_gap`'s open count at lib/derive.sh:499-501 for as long as Baton runs. The copy completing is deliberately not the test: it is evidence about the copy and says nothing about whether the original died, and releasing a park on an act independent of its condition is the defect class D-134 closes. The filter is the field and not the class, because `escalate_rejection` at lib/inbox.sh:218-221 writes the identical `other`, lane-scope, no-`reread` shape for every rejected artifact and its condition does not end by itself — a file was rejected and a person must decide something — so ruling-only is correct there; a rejection carries `rule` and `path` and no `original`, and `copy-fork-park-clears` holds one open across both ticks, including the one that clears a fork park, to prove it. A listing read as empty is a listing and says the session is gone; a listing that could not be read never reaches step 3, because the tick fails on it first. `resolve`'s return is checked. Counted libraries: `lib/escalate.sh` and `lib/tick.sh` only — this is the second half of the pass D-134 opened, split because the clearance needs a call site in `lib/tick.sh` and the split rule counts at most two existing libraries. Verification: 306 to 307 scenarios, zero failed under LC_ALL=en_US.UTF-8, in both the canonical checkout and a disposable linked worktree. `copy-fork-original-unstopped`'s frozen Mac message carried the old verb verbatim — `… stop the original by hand and leave o… · baton answer M02 "<ruling>"` — and is the record of the defect being corrected. | active |
| D-136 | 2026-09-18 | **D-108's register, settled: F08 is done, F10 and F11 and F03 and M4 belong to M11, F07 leads M10, M5's remainder narrows to the execution-identity writes, and finding 24's open half and the V1-repair run's two deferrals have owners.** This supersedes the corresponding entries of D-108's register; D-108 stands as written, because a register is a record of what was set aside and editing it would erase that. **F08 is no longer deferred:** Pass 2 of the V1 repair run implemented it at `a1c9857` (D-131) — the relay is published as one immutable content-addressed library set with an atomic reference flip. Its D-108 assignment to M17-b was too narrow: the exposure was never the addition of a library at M17-b but every unattended close-out, because D-079 makes each close-out run `sh install.sh` itself while the tick keeps firing every sixty seconds. **F10 is owned by M11 and settled in M11**, not carried to M15-c as a reminder: the one intent confirmation writes a confirmed intent record holding the existing `goal` *and* explicit constraints and non-goals, and for Baton that record names the controller's POSIX-shell boundary and excludes a Swift/SQLite replacement. The independent guard receives that record and the work being evaluated, never the plan author's rationale. No second plan-approval gate is added: one confirmation, one record, a wider record. The regression M11 owes is one goal paired with two proposals — a permitted extension and the forbidden controller replacement — which the constraint record must distinguish and a goal-only check cannot. **F11 settles with F10, inside M11:** existing registrations migrate into the new intent representation without resetting progress, because `install.sh:131-133` writes `{path, plan}` only when `projects/<key>/project.json` is absent, so reinstalling over a registration writes nothing and can never supply a missing intent contract. **F07 comes before F09 in M10's lead-in:** recovery for an archived artifact whose `consumed` receipt is missing is established first, and only then does F09 settle the standing check's command, working directory, pinned revision, deadline and result-to-revision binding. **M4 moves into M11:** CLI compatibility checks for the shapes the new session roles actually consume. No present-day CLI incompatibility is verified — the concern is the missing detector as that interface widens. **M5's remainder narrows:** prioritise checked writes on the execution-identity paths — dispatch, resume, copy adoption — as those paths are repaired, rather than a blanket sweep of the logging callers. **F03 stays with M11 and is settled before M11's first judgment session:** request identity, terminal outcomes, restart behaviour and admission-slot ownership belong to the first special session, not to a later guard. **Finding 24's open half is M13's.** **The V1-repair run's two deferrals:** `lib/declared.sh:98` is M17-b's, and whether a re-read rebaseline earns a notification class is M15's. | Verified before writing, each claim in the code rather than from the register. The three execution-identity writes are all unchecked today: `log_event dispatch` at lib/dispatch.sh:427-435, with `echo "backgrounded · …"` as the very next statement, so a refused line leaves a worker already launched by `claude_bg` at lib/dispatch.sh:346 with nothing in the log naming it and nothing for the cap to count; `log_event resume` at lib/stops.sh:270-275; and `log_event copy_fork` at lib/stops.sh:290-291, whose loss would leave every derivation following the original after the CLI had replaced it. That is the meaningful failure — losing a required receipt while reporting success — and it is why the narrowing is to those three and not to a count of callers. The hard byte refusal already exists and needs nothing: `log_event` refuses a line at 4096 bytes at lib/log.sh:31-35 before any append. The three size units the review named are real and stay as they are: a codepoint slice at lib/inbox.sh:433, byte cuts at lib/dispatch.sh:193 and :269, and the whole JSON line at lib/log.sh:31 — three units because three different things are being bounded, and turning them into a project-wide abstraction would buy nothing a personal tool needs. Finding 24's open half is unchanged by D-130 and is prompt content, not admission: `dispatch_one` takes the rows as its fourth argument (lib/dispatch.sh:312) and derives `do_inflight` from them at :336, and `do_also` at :338-342 composes the slot line's "Also in flight" sentence from that snapshot, so a milestone dispatched second in one tick names no peer. D-130 deliberately left it: re-reading the listing per candidate is a second CLI call per dispatch inside the cap loop, and closing it well may need `lib/templates.sh` as a third counted library. It is M13's because M13's acceptance is the thing the defect breaks — "an independent pair overlaps" is worth nothing if neither session is told about the other. The same two lines carry a second fact M09 will need: :341 renders the *recorded* `worktree` field as a basename into that sentence, which is the only place outside `lib/derive.sh:137` that reads it at all, and it is display rather than a path anything resolves. `lib/declared.sh:98` still reads "the plan was edited after two unfinished endings", the authorship claim the two `lib/stops.sh` lines gave up in D-134; it was left because `lib/declared.sh` would have been a third counted library, and it is M17-b's because M17-b already passes over every output string — any earlier pass that already counts `lib/declared.sh` should simply take it. The rebaseline line is said on the tick's output and not in a Mac message because a message needs a notification class and `class_or_fail`'s fixed list is in `lib/notify.sh` (lib/notify.sh:141-154), which was likewise a third library; M15 owns that taxonomy, and it is a genuine question rather than a bug — a rebaseline is Baton telling a person that evidence was taken afresh, which may be worth a message or may be noise. | active |
| D-137 | 2026-09-18 | **The ten legacy sibling worktrees migrate into `$BATON_HOME/worktrees/<project>/<milestone>`, losslessly if M09's fixture proves a mechanism and otherwise at an accepted cost: hand-resume of ten finished, merged milestones.** This is the new decision entry `docs/v1.1/SCOPE.md` demanded before D-078's protection could be narrowed, and it narrows it only for **relocation of a closed set**. It supersedes three clauses explicitly: the scope clause at docs/milestones/M09.md that relocated a session-bearing worktree only after a proof, the matching paragraph in SCOPE.md §6 M09, and the §8 acceptance criterion built on grandfathering the recorded paths. The measured `daemon.log` evidence in SCOPE.md §6 M09 stays where it is: it is still true, and it is why the fixture is tried first. **The motive is tidiness, and what changed is the weighing, not the motive.** The value traded away is bounded and small: ten milestones that are `done` and merged, whose decisions are all in this file and whose completion evidence is in their own briefs, so the transcript is a second copy of reasoning the contract already forced into the record; the owner does not expect to resume them. **It licenses nothing else.** It does not license automatic relocation of a worktree whose milestone is not `done` and merged, and it does not license deleting anything; D-078's protection stands in full for every other case, and the proof obligation stands for a live or unmerged lane. If the fixture succeeds, this overturn is never exercised. M09 also records each migrated worktree's legacy absolute path beside its milestone, so the transcript directory stays findable by slug. | Both facts the ruling turned on were verified before it was written, read-only. **Transcripts, usage statistics and prompt history survive a relocation.** A real `git worktree move` was run in a disposable repository with `~/.claude/projects/` hashed before and after: 960 `.jsonl` files, every sha256 identical, excluding only this session's own live directory. The ten legacy slugs are plain directories of regular files — 152 files, about 83 MB — holding **zero** symlinks, so nothing in the store points into a worktree; the worktrees themselves hold no `.claude` state. `git worktree move` rewrote exactly two files, both git's: `.git/worktrees/<name>/gitdir` and the worktree's own `.git` pointer, leaving the admin directory under its old basename, which is cosmetic. `stats-cache.json` is aggregate-only — `dailyActivity`, `modelUsage`, `totalMessages` — with no path reference at all, so usage statistics cannot be affected. `history.jsonl` holds fourteen entries whose `project` field records a legacy path; like the `cwd` field inside a transcript record, it is stored as data and is neither deleted nor altered by a move. **The loss is resume-only, and the value at risk is the recorded working directory.** Baton's own resume runs with no `cd` — `"$BATON_CLAUDE" --bg --resume "$rs_s" "$rs_text"` at lib/stops.sh:245 — while a dispatch explicitly sets one at lib/dispatch.sh:240, so every resume Baton has ever performed ran from the launchd job's directory rather than the worktree: the harness resolves the session by id and restores the path it recorded, which is exactly what a move invalidates. The one observed failure names both conditions on that path — `[bg] bg settled 87ac61b1 (crashed): working directory no longer exists or is not accessible: /Users/danny/Documents/Apps/Baton-M04`, `~/.claude/daemon.log` at 2026-09-12T19:48:47Z — and it is emitted by the background daemon settling a job, so it fires at the next service start after a migration without anyone pressing resume. Eleven records under `~/.claude/jobs/*/state.json` name the ten legacy paths, every one with `pid: null` and a terminal state, which is why M09's single live-row guard permits all ten moves and why this decision is needed rather than the guard being enough. What could not be measured in this run is `claude --resume` itself, because measuring it means starting a session, which this repair run may not do — and that is precisely the gap M09's fixture exists to close, now that the work running it is a dispatched session. | active |
| D-138 | 2026-09-18 | **One read-only reader answers both "may this be dispatched?" questions, and it runs before anything is created.** `dispatch_preconditions <project> <milestone>` in the new `lib/preconditions.sh` returns one JSON result — `{project, milestone, checkout, brief, worktree, branch, references_inspected, failures: [{check, stage, path, detail}]}` — with status 0 whatever it found and status 1, with the detail on stdout, when the inspection could not be made at all. A failure's `path` is written the way the thing it names is written: relative to the checkout for a defect in the brief's own text, which is how the brief and the commands repairing it spell it, and absolute for the checkout, the worktree and the permission rules, which have no other spelling. `checkout` is in the result so a reader wanting the absolute form of the first kind joins the two instead of resolving the project a second time. `verb_plan` reports every plan-eligible milestone's result and `dispatch_one` refuses on it before `worktree_ensure`, recording the first failure in the result's fixed order through the existing `dispatch_failed` at that failure's existing stage. Each failure's `path` is the file its repair names, so a reader has somewhere to go without parsing prose. No new verb, event kind or stage; `dispatch_try`'s retry-then-escalate bound is untouched, so a precondition failure still retries once and parks on the second, and D-098's unification with `lib/inbox.sh` stays deferred with its archived strings. `worktree_of` was factored out of `worktree_ensure` so the branch and worktree naming is written once. | A report that were a second implementation of the same rules could say "ready" about a milestone a dispatch then refused, which is worse than no report. Collecting rather than short-circuiting is what lets one pass name every defect across every eligible milestone; taking the first of a fixed order for the one event keeps that event deterministic rather than whichever check ran first. A result that could not be collected is not an absence of defects, and reading it as one is exactly the mistake the check exists to prevent. | active |
| D-139 | 2026-09-18 | **A `docs/…` reference is checked when it is a concrete path, passed over when it is a template placeholder, and reported when it is neither.** `prompt_references` trims Markdown delimiters and sentence punctuation — including the dashes and the section mark this project writes in prose — from each whitespace-separated word of the fenced kickoff prompt, ends a word at a closing delimiter inside it, keeps the path's spelling otherwise, and deduplicates in first-seen order. An emphasis mark comes off the tail only for each one the head carried, so `**docs/SPEC.md**` loses both while `docs/adr/*` keeps the glob a person wrote and is read as shell text rather than as a concrete path that happens to be missing. A `#fragment` names a place inside a document and not another document, so the lookup drops it while the reference is still quoted back as written, and two spellings naming one file are one failure. A mark glued inside a word rather than at its end stays part of the spelling and is reported as written: that is a typo rather than a convention, and a diagnostic that shows it exactly is what makes it visible. A word holding `<` or `>` is a placeholder — `docs/milestones/<ID>.md` names a shape, not a file — and is not reported. A word holding `$`, `*`, `?` or `[` is reported as needing a concrete path, and finite brace alternatives are expanded while a range or a nested brace is not. Each surviving path is looked up with `git cat-file -e main:<p>`, one trailing `/` removed first, and a tree is as good as a blob. Links are not followed. | Evaluating the text would make a printed prompt into code the tick runs, which INV-12 forbids in a different costume; guessing what an unevaluated reference meant would report defects Baton has not seen. Silently dropping what it cannot resolve is the third option and the worst: it would make the report's silence mean two different things. A prompt that could not be read is said to be uninspected once, as `references_inspected: false`, rather than turned into a list of invented filenames. | active |
| D-140 | 2026-09-18 | **The changed expectations are the timing correction, and two working messages are kept as they are.** `dispatch-brief-absent`, `dispatch-brief-uncommitted`, `dispatch-failed-prompt`, `dispatch-no-slot-line` and `dispatch-worktree-missing-brief` no longer hold a `settings/` file, because the refusal now precedes `settings_compose`; `dispatch-worktree-missing-brief` asserts that the previously absent worktree is still absent instead of that it was created and stood clean. `prompt_from_brief`'s three messages from `7dd3ee6` are unchanged, and `worktree_ensure`'s "exists but is not a worktree" is reproduced verbatim by the precondition that now raises it, with no repair command: what to do with a directory Baton did not create and cannot read is a person's judgement, and scripting a move or a removal there would be advising a destructive act. New messages quote paths with `shell_word`; the older ones quote with double quotes, which covers the apostrophes and spaces they were written for. The slot and permission diagnostics did grow a need and a repair, which is what makes them usable in a report a person reads before touching anything. | An old expectation that recorded creation before a knowable refusal recorded the defect this milestone removes; refreezing it without saying so would have hidden the correction rather than proving it. Keeping the two messages is the other half of the same care: they already name what Baton needed and what it found, and rewriting them would have been preference wearing consistency's clothes. | active |
| D-141 | 2026-09-19 | **Plain is the rendering layer's default and its contract, and every call site names its stream.** `lib/render.sh` emits, for a stream that is not a decorated terminal, exactly the bytes the caller's own format string produces — no escape, no re-layout, no transliteration of a separator the caller wrote. Decoration is what a terminal adds on top. `render_init` reads the context once at the verb boundary in `bin/baton`; `render_token`, `render_hint`, `render_heading`, `render_row`, `render_lines` and `render_failure` each take `out` or `err` as their first argument, and a primitive reached before `render_init` initialises lazily. Set `NO_COLOR`, including set and empty, means plain; `FORCE_COLOR` is ignored; `TERM` unset or `dumb` means plain. | The tick's streams go to `launchd.out` and `launchd.err`, the harness diffs captured streams, and people grep both, so the plain shape is a contract with readers this repository cannot enumerate; making it the caller's own `printf` left all 319 existing scenarios byte-identical, which is what made the conversion provable rather than argued. The stream is an argument because `[ -t 1 ]` asked inside `$(…)` answers for that pipe and not for the stream the verb is writing to — `usage` alone is read on stdout by `baton help` and on stderr by every wrong-argument refusal. `FORCE_COLOR` is refused on the same evidence as D-050: every process started from inside a Claude Code session inherits it, and a relay that decorated because of an inherited variable would repeat the shape that once put colour into a parser's input. | active |
| D-142 | 2026-09-19 | **Below sixty columns a record's fields go onto continuation lines, split at separators outside a styled run.** The split is parameter expansion over the literal `" · "`, never a byte offset; a candidate field holding more escape introducers than resets ends mid-run, so the separator it ended on belongs to the value and is kept. Nothing is ever truncated: a path, an identifier, a timestamp or a command longer than the terminal overflows intact. A UTF-8 locale prefixes each continuation with the record's own separator and an unknown or C locale uses spaces. | `offline  <lane> · message Baton · wake, or baton wake <lane>` carries the wake session's name, which has the record's separator inside it; split naively it reads as `message Baton` and `wake, or …`, sending a person after a session that does not exist. A reset is itself an introducer, so counting introducers alone reads every closed run as still open — which made the narrow layout silently never trigger until it was measured. Truncation is refused because a command shortened to fit a width is a command that does not work. | active |
| D-143 | 2026-09-19 | **A helper with both a person's caller and a machine's takes the terminal as an argument and stays plain without it.** `plan_render <project> <inflight json> [<out\|err>]` prints the rows plain unless a stream is named. `takeover_check` and `dispositions_intersect` keep returning their human `lines[]` inside their result, and the tick renders them where they meet a stream. | The ineligible-dispatch refusal pipes `plan_render` through `awk '$1 == id'` to show the one row it refused; colour in that first field matches nothing and the refusal would name no row at all, which is the silent kind of wrong. A helper that asked the terminal for itself would answer for whichever pipe it happened to be inside, so the reading belongs to the caller that knows which use this is. | active |
| D-144 | 2026-09-19 | **`lib/declared.sh`'s redispatch after two unfinished endings says which readings changed, not who changed them.** It read `the plan was edited after two unfinished endings`; it now reads `the brief or the work plan changed after two unfinished endings`, the register `lib/stops.sh:461` and `:557` already use for the ladder's own redispatch and for a context overflow. D-136 assigned this line to M17-b, and this is the pass that takes it. | D-134 settled the rule for the other two libraries and left this one because `lib/declared.sh` would have been a third counted library then; M17-b passes over every output string, so it is the pass that can take it without a special case. What Baton has when `person_acted` reads `edit` is two digests that differ under the `instructions` policy — `brief_sha256` and `work_plan_sha256` — which is a fact about the brief and the work plan and not about anybody's hands. A line claiming authorship it cannot see is the same defect in the third library that it was in the first two, and telling a person Baton watched them do something it only inferred is how a person stops trusting the rest of the view. `lib/declared.sh` is this milestone's one counted existing library under D-104, whose exemption covers rendering-call substitution and not a changed word. | active |
| D-145 | 2026-09-19 | **A worktree's location is git's registration, never a path derived from the milestone's name; new ones are created under the managed root `$BATON_HOME/worktrees/<project>/<milestone>`; reuse is refused unless the directory is that project's repository on that branch; and the tick moves the legacy siblings in, under one guard.** `worktree_of` reads `git worktree list --porcelain` for the branch and falls back to the managed root only when the project has no worktree on it; `worktree_ensure` creates the managed parent, and refuses a reuse whose `--git-common-dir` is not the checkout's or whose `symbolic-ref HEAD` is not `refs/heads/m<nn>`, each refusal naming the repair. `worktree_migrate` runs per project between steps 6 and 7, `git worktree move`s each `../<Project>-M<nn>` still on `m<nn>` into the managed root, and writes one `worktree_moved` event (`from`, `to`, `branch`) and one line per move. The guard is single: refuse while any live row's `cwd` is that worktree or under it, a guard that cannot be answered refusing like one that answered yes. D-075's guard 1 (`Status` reads `done`) and guard 3 (a verified `merged_as` with no newer ending) are deliberately not restored. `~/.baton/worktrees/` stays unnamed in the deny list, and `tests/run.sh` drops `home/worktrees` from its snapshot as it already drops `home/lock`. | The managed root without the resolution change would have parked lanes rather than tidied a directory: `worktree_ensure` derived `$(dirname)/$(basename)-$id`, so a moved worktree was invisible to it — no directory at the derived path, the `worktree add` branch taken, and git refusing `fatal: '<branch>' is already used by worktree at '<new path>'`, exit 128 on git 2.54, reaching `dispatch_failed … worktree`, twice in a row parking the lane. Resolution is therefore what makes moving safe at all, and the two together are why this is one decision. The identity checks exist because the managed path is the one place a directory can sit that Baton did not put there: `worktree-wrong-repository` plants a *clone* of the fixture, which passes every check that reads the directory's contents — ancestry, a readable brief, the right branch name — and is still not the project's worktree, so a session dispatched there would commit into a history the close-out's merge never reaches. Guard 2's third clause is kept alone because its failure is the real one: the harness resolves a session by id and restores the working directory it recorded, and the one observed instance names both conditions on that path (`bg settled 87ac61b1 (crashed): working directory no longer exists or is not accessible: /Users/danny/Documents/Apps/Baton-M04`). The other two guarded a delete, which is irreversible; a move loses no bytes (D-137), so restoring them by analogy would only refuse moves nothing is at risk from. D-137 licensed the relocation of a closed set — ten finished, merged milestones — and not of a milestone that is neither, and the pass honours that by construction rather than by a guard: the legacy convention is retired, so no new sibling can be created, and every sibling that exists belongs to a milestone that is done and merged by the time its row goes. The one case the rule admits and the reasoning does not cover is a person's own `<checkout>-<id>` directory on branch `<id>`, made by hand in Baton's retired shape and left with no session in it; it would be moved, which costs a `git worktree move` back and is recorded rather than guarded against, because a guard here would have to read the plan and would refuse the very worktrees the migration exists for. The refusal is silent because a guarded worktree is one `status` already shows as a lane in flight, and a line a minute would bury the moves that happened. The deny list must not name `worktrees/` because that directory *is* the dispatched session's own repository; naming it would deny a session its own checkout. The snapshot exclusion follows `home/lock`'s precedent: what a scenario means to assert about a worktree is its path and whether it was reused, and both are in the `dispatch` event and the dispatch's stdout, which are diffed; the worktree itself is hundreds of fixture files whose `.git` pointer carries the temporary root. | active |
| D-146 | 2026-09-19 | **Claude.app's sidebar grouping is not a property of the worktree's path, and D-086's "both under Other" no longer describes this host.** Measured live on Claude Code 2.1.278 and Claude.app 2.2553.1, read from the sidebar by the owner. Reading one: Baton's five dispatched sessions, every one in a `~/Documents/Apps/Baton-M<nn>` sibling, all appear under a single heading **Baton**. Reading two, a controlled pair: two sessions of one disposable repository, one settings composition, differing only in where the worktree sat — `<home>/worktrees/Probe09/M01` under a managed root and `~/Documents/Apps/Probe09-M02` beside the checkout — appear **both under Other**. So the managed root can neither earn the heading nor cost it, and M09 claims no mechanism over it. What was not observed is the same repository at a managed path, because the sessions that are grouped are exactly the ones the move guard holds; the inference is that the move changes nothing, and its falsifier is the first milestone dispatched after the migration, which runs from the managed root and is visible in the sidebar at once. | The question had to be settled on this host rather than inherited: D-086 is from 2.1.270 and its mechanism was `repoInfo` derived from a git remote, which is not what this build does. `claude --help` on 2.1.278 still exposes no project designation — `--project` is not an option, and `claude project` offers only `purge` — so nothing at dispatch can set one. `-w/--worktree` is new and does record an `originCwd` the desktop app reads, but it hands worktree creation, placement (`<root>/.claude/worktrees/<name>`) and reaping (`cleanupStaleAgentWorktrees`, `reapJobWorktree`, and `claude rm`'s "and its worktree when that is safe") to the CLI, which is precisely what D-078 forbids, so it is not a lever Baton can take. A first probe under `~/.claude/jobs/<id>/tmp/` also read Other, but that location is a job scratchpad and the app drops `repoInfo` for a scratch workspace, so the reading was confounded and was re-run outside it; both probes are recorded because a confounded measurement thrown away silently is how a wrong conclusion survives. | active |
