# M15 — The escalation taxonomy

## 1. Identity

- **ID:** M15. **Objective:** resolve what Baton can resolve itself and replan when the plan is wrong.
- **User-visible result:** routine ambiguity no longer produces a Mac question; credentials,
  access and external facts still reach the person. Every class has an explicit disposition.
- **Size:** Medium, about 3–4 hours; high effort. Before implementation, the former Large
  6–8-hour brief is split: M15 owns taxonomy, AI/replan routing and L33; M15-b owns host-explained
  gaps and wake reconciliation; M15-c owns the independent goal guard. Nothing is implemented
  by this planning edit. Each implementation must recheck the plan's two-existing-library limit
  against the then-current M12 interfaces and split further before crossing it.

## 2. Dependencies and entry conditions

M12 complete and installed. Registration holds the confirmed `goal`; a subscription CLI session
can generate/adopt a plan and briefs from repository evidence. Completion records the dispatch
baseline, actual changed paths and Baton-produced standing-check outcome. M15 can start after
M12 independently of M13. Replan integrates with M13's continuation when available; otherwise
prove its request/result boundary with fixtures, without adding an entry dependency.

## 3. Required reading

`CLAUDE.md`; `CONTEXT.md`; `CONTRACT.md`; `docs/v1.1/SCOPE.md` §6 M15 and §7;
`docs/MILESTONES.md`; `docs/v2/01-findings.md` limitation 33;
`docs/SPEC.md` §2.4, §2.5, §2.8, §2.9, §5 and REQ-SETUP-07;
`docs/ARCHITECTURE.md` §4.1–4.3, §5.2, §6.2, §6.4 (derivation 15), §7, §10;
`docs/DECISIONS.md` D-078 and D-089; M12 completion evidence;
`lib/escalate.sh`, `lib/answer.sh`, `lib/notify.sh`, rejection routing in `lib/inbox.sh`,
`derive_gap` in `lib/derive.sh`, `gap_check` and crash checks in `lib/rows.sh`,
`caffeinate_rearm` in `lib/tick.sh`, and current planning/adoption code;
`tests/scenarios/answer-two-matches/` and `reject-unparseable/`.
Read M13's continuation interface if present; M15's dependency remains M12.

## 4. Requirements, scope, non-goals

- **Precondition failures:** classify known dispatch-precondition failures, including whether they should escalate on the first occurrence; M17 deliberately retains `dispatch_try`'s existing retry-then-escalate bound.
- **Covers:** SCOPE §6 M15's disposition routing and L33. The original guard acceptance moves
  to M15-c; the host finding below is implemented in M15-b.
- **Scope:** four dispositions: `AI-resolvable`, `human-required`, `replan`, and
  `HOST-EXPLAINED`. The fourth means the condition is real and correctly detected, the host
  explains it, and no action exists: record the event; send no notification. It is neither an
  AI task nor a human park. Preserve lane/project scope independently of disposition.
- **Non-goals:** implementing M15-b, M15-c or M15-d in this session, deleting existing machinery,
  weakening deny rules, inferring credentials/external facts, direct API calls or a new framework.

**The split the file-count rule forced, made before implementation.** §1 required this brief to
recheck the two-existing-library limit against the then-current M12 interfaces, and it does not
hold: both routing dispositions need libraries beyond the two this milestone can spend, and the
count was measured rather than estimated.

*Replan* cannot preserve completed work through the generation path as M12 left it.
`planning_validate` raises a defect for every row whose `Status` is not blank — "marks M05 done
before anybody has done it" — with the repair "leave every Status cell blank", and `planning_prompt`
tells the generating session the same thing twice and opens by asserting the project "has no plan
Baton can read". A replan of a project with `done` rows would therefore be refused row by row and
its next attempt instructed to erase exactly the work §10 requires it to preserve. Repairing that
is `lib/planning.sh`, a third counted library.

*AI resolution* needs a second judgment role, and M12 measured what one costs: `lib/dispatch.sh` for
a lane with no plan row, whose prompt is Baton's own text rather than a brief, and
`lib/completion.sh` for a declared scope that is Baton's own — plus `lib/inbox.sh` to turn the
judgment's artifact into a ruling. Three more.

The two are treated differently, and deliberately. Replan is bounded at one counted library, its
requirements are written out in §7.4 and §10 of this brief, and it is a milestone anybody can pick
up: it becomes `docs/milestones/M15-d.md`, with a row in the plan. AI resolution is not scheduled by
this session at all — it is recorded, with the measurement above, in `docs/DECISIONS.md` and in this
brief's completion evidence, and nothing dispatches it. Adding a runnable row for work nobody has
decided to do would put it in the queue by default, and the point of measuring its cost was to make
that a decision rather than a consequence.

What stays here is everything that needs no session to resolve it — the table itself, the re-read
rule, both L33 repairs and the silent record — which is coherent on its own, because a disposition
has to be assigned before anything can route on it. `AI-resolvable` therefore names a route that
does not exist yet, and says so honestly rather than quietly: a park carrying it still parks and
still reaches a person, which is what `disposition_notifies` returns for it.

### Verified finding — 2026-09-17 (planning evidence, not implementation acceptance)

The person reported seven `Baton · gap` messages (3m, 9m, 41m, 1h09m, 1h17m, 1h26m,
1h33m), each saying “and a lane was open through it,” with no understandable action.
Inspection found:

- `derive_gap` reports at **age >= 2 × BATON_TICK_SECONDS**, with a lane in flight, waiting
  or parked, including an ending after the marker. `gap_check` tests prior `notification`
  events with class `gap` and **key equal to the marker value**. ARCHITECTURE derivation 15
  agrees. These are correct missed-tick detection and deduplication, not cause attribution.
- `caffeinate_rearm` rearms idle-sleep assertions for current pids and bounded waits. Its comment
  and REQ-SETUP-07 explicitly say `caffeinate -i` cannot prevent lid-close sleep and Baton cannot
  lift this. No proposal here changes that hardware constraint.
- The real `/Users/danny/.baton/log.jsonl` had **16**, not seven, gap events dated September 17
  at inspection (through 18:11:24 EDT). All seven reported durations occur; 41m occurs more
  than once. The seven visible messages are not the complete event count. Notification delivery
  or which messages the person saw was not independently reconstructed.
- `/usr/bin/pmset -g log` exposes `YYYY-MM-DD HH:MM:SS ±HHMM` timestamps and actual `Sleep`,
  `DarkWake`, and `Wake` transitions. `Wake Requests` are scheduled requests, not wakes.
  A single capture had 960 transition lines (958 distinct timestamp/type records), reaching
  from September 11 through September 17. Native `/bin/date -j -f '%Y-%m-%d %H:%M:%S %z'`
  parsed their timestamps. Sort and deduplicate evidence; never count duplicated intervals twice.
- A temporary `launchctl submit` job using **Baton's installed shell**
  `/Users/danny/.baton/bin/sh` ran `/usr/bin/pmset -g log` as uid 501 with parent pid 1,
  exit 0 and empty stderr, without sudo or a new permission grant. The temporary job was removed;
  the real tick agent was not changed or invoked. This verifies the current user's LaunchAgent
  context, not every macOS version or a system daemon. Probe output is in
  `/tmp/baton-sleep-inspect.SJYDw2/` (temporary, not a durable fixture dependency).
- The host was **not continuously asleep for each whole marker-to-read interval**. Short dark
  wakes interrupt sleep and Baton sometimes ticks during them. Pairing Sleep with the next
  Wake **or DarkWake**, all 16 measured gaps were mostly sleep; the longest continuous awake
  remainder was 56 seconds. Examples below use the actual measured endpoint
  `epoch(marker) + gap_seconds`, not event-write time. The 1h09m event was written at 16:37:50
  but measured 4,152 seconds from 15:11:51, ending at 16:21:03; a later event timestamp must
  not silently enlarge the interval being explained.

| Reported duration | Gap seconds | Sleep seconds in measured gap | Longest continuous awake portion |
|---|---:|---:|---:|
| 3m | 190 | 130 | 50s |
| 9m | 543 | 483 | 56s |
| 41m (event at 10:22:20) | 2,511 | 2,455 | 16s |
| 1h09m | 4,152 | 4,105 | 29s |
| 1h17m | 4,657 | 4,605 | 10s |
| 1h26m | 5,160 | 5,118 | 10s |
| 1h33m | 5,614 | 5,568 | 12s |

**Feasibility:** affirmative, for retained, complete, parseable host history in this measured
LaunchAgent context. It is evidence for a conservative explanation, not a guarantee of a complete
history or proof of launchd health. Missing history, parse errors, read failure, contradictory
transitions, reboot boundaries or clock discontinuities cannot establish sleep coverage. They
must remain unknown and must not silently suppress a gap. M15-b defines that fallback and the
fixture boundary; no privileged helper, power assertion workaround or new liveness test is needed.

**Two other corrections matter to the design.** `status_render` currently re-derives the *active*
gap from `last-tick`; it does not show a historical gap after the marker advances. Therefore
preserving the log alone does not satisfy “baton status still shows it”: M15-b must render the
recorded latest gap, including a silent one. Also D-078 is evidence of a **service restart plus
missing working directory**, not evidence that sleep kills sessions. The current daemon log
confirms the 2.1.270 restart at 2026-09-12T19:48:32Z and M04/M05 settling at 19:48:47Z as
`(crashed): working directory no longer exists or is not accessible`. D-078 records Claude.app's
“not connected” and unsendable-message result; that UI state was not re-enacted in this inspection.

### Required behavior carried into M15-b

A host-explained gap retains its event, duration, marker key and status visibility but emits no
Mac message. A gap with complete history reporting no explanatory sleep is **UNEXPLAINED** and
still notifies: unloaded launchd, a stuck lock or a crashing tick are possible causes to investigate,
not diagnoses inferred from the gap alone. Unknown host evidence also retains the existing gap
notification, explicitly marked unknown rather than falsely claiming “no sleep.”

On waking, reconcile open lanes using the existing row, transcript, inbox and ending checks.
Sleep, unchanged rows/transcripts and Claude.app connectivity alone are not death evidence.
A confirmed dead session takes the existing recovery ladder; only an actionable unresolved
failure reaches the person. M15-b specifies the two-tick confirmation and fixture cases.

## 5. Expected files (proposed)

Every path this section names is between backticks, because a completion is proved by at least one
changed path falling inside this section's backticked spans, and a path named only in prose declares
nothing (D-150). The implementation session inventoried its edits before writing anything, the
two-existing-library rule fired, and the split it made is §4's last paragraph.

Introduced: `lib/disposition.sh` — the four-disposition table, the re-read rule and the silent
record. Wired into `bin/baton` and `tests/lib-load.sh`, neither of which is a counted library, and
carried by `install.sh`'s existing `lib/*.sh` glob without an edit.

Two counted libraries, and no more: `lib/answer.sh` for L33's qualified answer, and `lib/tick.sh`
for the project-less pass that closes an orphaned rejection park.

Fixtures: `tests/scenarios/disposition-table`, `tests/scenarios/disposition-silent-record`,
`tests/scenarios/answer-park-at`, `tests/scenarios/answer-park-at-unknown` and
`tests/scenarios/reject-orphan-park-clears`.

Documents: `docs/SPEC.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `docs/MILESTONES.md`, this
brief, and `docs/milestones/M15-d.md`, written here because the split needs somewhere to put what it
moved.

Three expectations already in the repository move with the behaviour they describe, and are named
here because a frozen expectation is evidence and not a by-product:
`tests/scenarios/answer-two-matches` for the refusal that now offers a park to name, and
`tests/scenarios/tick-stale-lock` and `tests/scenarios/tick-stale-lock-incomplete` for the one
delivery that becomes a silent record.

Replan's own file belongs to `docs/milestones/M15-d.md`: `lib/planning.sh`. The second judgment
role's — `lib/dispatch.sh`, `lib/completion.sh` and `lib/inbox.sh` — belong to no milestone yet, on
purpose. Host parsing and reconciliation belong to `docs/milestones/M15-b.md`, the goal guard to
`docs/milestones/M15-c.md`.

## 6. Interfaces

- **Introduces:** exhaustive four-disposition table; AI resolution and replan routes; an explicit
  record-without-delivery contract for HOST-EXPLAINED, ready for M15-b's evidence.
- **Consumes:** stored goal, existing CLI dispatch, escalation/answer/notifier records, M12's
  generation/adoption and M13's continuation seam when available. Use the shared admission seam
  so M14 pacing applies when installed. Drift remains human-required; M15-c produces it.

## 7. Implementation checklist (ordered)

1. Inventory all escalation/notification producers, including rejection paths. Assign disposition,
   rationale and resolution path; retain lane/project scope and explicit handling of unknown classes.
2. Reproduce both L33 cases: ambiguous project-qualified answers with multiple parks, and an
   unparseable artifact's park with no recoverable project/ruling target/re-read hashes.
3. Resolve genuine ambiguity through a fresh subscription CLI session and existing result records;
   make repeated ticks idempotent. Credentials, grants and external facts remain human-required.
4. Route replan through goal-based generation, preserve completed work and record the replacement;
   integrate with M13 when available or fixture its boundary. Do not add a person approval gate.
5. Establish the fourth disposition's recorded-but-silent delivery contract; do not classify real
   gaps as explained until M15-b provides host evidence. Run §8, document, review and close out.

**Split applied before implementation:** M15-b implements the host finding; M15-c takes the old
natural split after replan (independent guard and drift proof). Do not mark the original combined
scope delivered when M15 alone closes. M16 waits for the entire chain. This planning session writes
no `stopped` artifact because no implementation session is being stopped; if a later implementation
cannot finish its bounded part, record evidence and `stopped`/`unfinished` per the plan rule.

## 8. Fixtures, verification, acceptance

Fixture complete class coverage; AI ambiguity; human credential/access/external facts; replan;
both L33 repairs; repeated routing; HOST-EXPLAINED records without notifier calls. Run each twice,
then `sh tests/run.sh`. Acceptance: exhaustive mapping, usable recorded resolution of both L33
cases without notification, and replan through the proper boundary. Host coverage/dead-session
proof belongs to M15-b; cold independent guard sessions and drift notification belong to M15-c.

## 9. Definition of done and evidence

Ten-item handoff with class/disposition mapping, L33 before/after traces, replan boundary trace,
silent-record contract and test output. Identify any M13 integration proved only by fixture.
The verified finding above is investigation evidence, not a claim that implementation tests passed.

## 10. Risks and decisions this milestone settles

- A park needs a usable exit, not deletion of history.
- Replan preserves confirmed intent and completed work; it cannot clear a drift escalation.
- A silent event must remain visible and idempotent. HOST-EXPLAINED requires evidence, not a guess.
- More than two existing library edits triggers another split before implementation.

## 11. Handoff and next milestone

Apply `CONTRACT.md` clause 3(c) to every refresh below: run `baton status` and check the
dispatch log for open lanes, including lanes absent from status; status also lists open parks. Refresh only a listed
milestone with neither an open lane nor an open park; still name every eligible milestone with its correct disposition.

Direct successor: **M15-b**, `docs/milestones/M15-b.md`, heading `Copy-ready session prompt`.
M15-b then hands to M15-c; M16 depends on M14 and M15-c. Refresh the successor with actual
routing and silent-record interfaces. Re-read current Status cells, dependencies and gates:
list every direct successor even if not runnable, plus every other eligible milestone. An open
gate means `held`/`held_by`; otherwise unfinished dependencies mean `wait`/`wait_for`; otherwise
`run`. Do not assume sibling order, change dependencies to obtain a disposition or start successors.

## Recovery procedure

Inspect class inventory and unresolved resolution/replan records; resume the first unfinished item
without duplicating a resolution. Do not claim M15-b/M15-c acceptance as part of M15's completion.

## Completion evidence
### 1. What changed

- **Every escalation and notification class now carries a disposition**, and the mapping is
  exhaustive by construction rather than by a document. `disposition_of` in the new
  `lib/disposition.sh` assigns each of the twenty-six classes `class_or_fail` accepts — fifteen
  escalation, eleven notification — and refuses a class neither list holds, the same refusal
  `class_or_fail` itself makes and for the same reason.
- **Two clauses were settled before the four dispositions**, because both remove conditions from the
  table rather than placing them in it. Clause 1: a condition Baton can read for itself is re-read
  and closed rather than routed. Clause 2, which is the question D-167 left for this milestone: a
  condition whose repair is already a standing park's repair spends no new class and records an
  event under that park — the general rule, not the exception.
- **Limitation 33's two parks got the routes they never had, and neither notifies.**
  `baton answer <project>/<milestone>@<at>` answers one park of a lane that carries several, and the
  several-parks refusal now prints each candidate in that form instead of repeating the lane.
  `rejection_resolve_check` closes a rejection park that names no project once the file it names is
  gone or the session it names has had a handover consumed since.
- **The silent record exists and has one user.** `record_only` writes `escalate`'s event with
  `channel: ["record"]` and raises no Mac message, refusing any class the table does not call
  `HOST-EXPLAINED`. `baton-unhealthy` — the stale-lock break, whose own verb has read "nothing to do;
  Baton cleared it and carried on" since M05, and whose park never outlives the tick that raised it —
  is now recorded rather than delivered. That is the only delivery this milestone changes.
- **M15 was split a second time before any code was written** (D-182). Replan became
  `docs/milestones/M15-d.md`, with a row in the plan and its brief on `main`. The AI-resolution route
  was measured and deliberately left unscheduled.

### 1b. Limitation 33, before and after

**(a) The ambiguous qualified answer.** Before, on a lane carrying two parks:

```
$ baton answer Fixture/M03 "go on"
baton: Fixture/M03 carries 2 open parks at once, which one lane should never do; Baton will not
guess which the ruling answers. They are:
  baton answer Fixture/M03 · question · Baton · Fixture · M03 is waiting for input
  baton answer Fixture/M03 · other · the session stopped: a second park on the same lane
```

Both lines are the command that was just refused, so the way through led back to the refusal. After:

```
$ baton answer Fixture/M03 "go on"
baton: Fixture/M03 carries 2 open parks at once, which one lane should never do; Baton will not
guess which the ruling answers. Name the park:
  baton answer Fixture/M03@2026-09-12T11:10:00+01:00 · question · Baton · Fixture · M03 is waiting…
  baton answer Fixture/M03@2026-09-12T11:11:00+01:00 · other · the session stopped: a second park…

$ baton answer Fixture/M03@2026-09-12T11:11:00+01:00 "go on without the second opinion"
ruling    Fixture/M03 · f3 · other · delivered
```

and the log carries `resolution` with `escalation_at: 2026-09-12T11:11:00+01:00` — the park, not the
lane. The 11:10 park still stands. Sending the same ruling again finds nothing and says which park
is still open, so a repeat cannot double-deliver.

**(b) The orphaned rejection park.** Before, from `reject-unparseable`'s own expected log, an
escalation with **no `project` key at all**:

```json
{"at":"…","kind":"escalation","milestone":"M02","session":"1111…","class":"other","scope":"lane",
 "carries":{"rule":"unparseable","path":"…/rejected/M02-1111….json"},"channel":["notification"]}
```

No project means `escalate` attaches no re-read receipt, `ruling_target` is empty so no ruling can be
delivered, and `edit_reread_check` runs per registered project and never visits it. The park stood
for as long as Baton ran, counted as a parked lane in `status`, after the file had been dealt with.
After, with nothing typed at Baton:

```
settled   M04 · the rejection park raised at 2026-09-12T11:30:00+01:00 is closed · the file it names is gone
settled   M05 · the rejection park raised at 2026-09-12T11:35:00+01:00 is closed · the session it names has had a handover consumed since
```

and `status` no longer lists them. No notifier call is made in either run of the scenario.

### 1c. The replan boundary, and M13

**There is no replan boundary trace in this milestone, and that is the split, not an omission.**
§9 of this brief asked for one; the route moved to `docs/milestones/M15-d.md` (D-182) because
preserving completed work through M12's generation path requires `lib/planning.sh`, a third counted
library. What this milestone leaves M15-d is the entry point rather than the route:
`disposition_of` assigns `replan` to `unfinished-twice` and to `blocked`, and `disposition-table`
pins both.

**No M13 integration is claimed, by fixture or otherwise.** M13 completed and merged into `main`
during this session and was merged into this branch; nothing here consumes its interfaces, and
`lib/tick.sh` carries both its `drn_started` re-read and this milestone's two changes without either
touching the other. M15-d's §2 records what M13 does and does not settle for replan.
### 2. Files added / modified / deleted

Added: `lib/disposition.sh`; `docs/milestones/M15-d.md`; `tests/scenarios/disposition-table/`,
`tests/scenarios/disposition-silent-record/`, `tests/scenarios/answer-park-at/`,
`tests/scenarios/answer-park-at-unknown/`, `tests/scenarios/reject-orphan-park-clears/`.

Modified: `lib/answer.sh`, `lib/tick.sh` (the two counted libraries); `bin/baton` and
`tests/lib-load.sh` (one source line each); `docs/SPEC.md`, `docs/ARCHITECTURE.md`,
`docs/DECISIONS.md`, `docs/MILESTONES.md`, `docs/milestones/M15.md`; and the frozen expectations of
`tests/scenarios/answer-two-matches/`, `tests/scenarios/tick-stale-lock/`,
`tests/scenarios/tick-stale-lock-incomplete/` and `tests/scenarios/install/`.

Deleted: nothing. `install.sh` was not edited — its existing `lib/*.sh` glob carries the new file.

### 3. Interfaces introduced or changed

In `docs/ARCHITECTURE.md` §10, row M15. `lib/disposition.sh` introduces `disposition_of`,
`disposition_notifies`, `record_only` and `rejection_resolve_check`. `lib/answer.sh`:
`answer_resolve` gains a third narrowing (the park's `at`), `answer_candidates_print` a third
argument (whether to qualify each printed command with it), and `verb_answer` a third target form.
`lib/tick.sh`: the stale-lock escalation goes through `record_only`, and `rejection_resolve_check`
runs after `inbox_consume`. New requirements `REQ-ESC-12` and `REQ-ESC-13`; `REQ-ESC-05`,
`REQ-ESC-06` and `REQ-TICK-03` amended.


### 4. Checks run, with outcomes

**The standing check, `sh tests/run.sh`, on the branch: PASSED** — `405 scenarios, 0 failed`, run
after the merge from `main` and after the review fixes. The run is the whole
suite, each scenario run twice against a copy of its fixtures, with the seams pointed at the shims.

**The five new scenarios and the four whose expectations moved: PASSED**, each read and judged before
it was frozen rather than frozen and then trusted.

- `disposition-table` — the mapping, in both directions. The class list is extracted from
  `class_or_fail` in `lib/notify.sh` with awk and the arm list from `disposition_of` itself, then
  set-differenced, so neither a class added without an arm nor an arm left behind by a removed class
  can pass unseen. All twenty-six classes map; none is `REFUSED`. The `asking` discrimination is
  driven across seven carries shapes.
- `disposition-silent-record` — the guard. `baton-unhealthy` writes its event with
  `channel: ["record"]`; `asking`, `question`, `merge-failed`, `dispatch-failed`, `main-broken` and
  `unfinished-twice` are each refused, as are a bad scope, an unknown class and a carries that is not
  JSON. `calls.log` is empty: the notifier was not called at all.
- `answer-park-at` — the ruling reaches the named park and no other. The `resolution` names
  `escalation_at: 2026-09-12T11:11:00+01:00`, the `question` park at 11:10 still stands, and the
  second run writes nothing.
- `answer-park-at-unknown` — four refusals: an `at` matching no park lists the open ones; a truncated
  timestamp does the same because it still begins with a year; a tail that is not a timestamp is not
  read as a park id at all; and a lane with no park keeps the older refusal. A project-less park
  renders as `?/M03`.
- `reject-orphan-park-clears` — four orphan parks, one closing. M05 closes on a consumed handover for
  its own milestone; M04 stands while its file is there and closes on the tick after it is removed;
  M06 stands although its session had a handover consumed, because that handover names M01; M07
  stands although its `at` string sorts later than the park's, because its consumed event is earlier
  in log order. No `osascript` call in either run.
- `answer-two-matches` — the several-parks refusal now prints each park's own `@at`, so it leads
  somewhere instead of repeating the lane. The "parked in N projects" refusal is unchanged.
- `tick-stale-lock` and `tick-stale-lock-incomplete` — measured on both channels: the `osascript`
  line is gone and the event is identical except `channel: ["record"]`.
- `takeover-handback` — `baton answer M03@<at> continue` on a taken-over lane refuses and issues no
  stop and no resume; the bare `baton answer M03 continue` below it still hands the lane back.
- `install` — the installed library listing gains `disposition.sh`, one line.

**Two regressions were re-introduced on purpose to prove the fixtures are load-bearing**, and both
were caught. Restoring `.at > $a` in place of the log-order test made `reject-orphan-park-clears`
fail, closing both M07 and M06 wrongly. (The first freeze of `reject-orphan-park-clears` also flaked
on call ordering before its live row was removed — the detached-caffeinate race `tests/run.sh`
documents and D-172 priced; the scenario now has no live row and no caffeinate arm.)

**`/review-2` (one debugger, one code-auditor, both on Fable) plus a hand pass: run, and acted on.**
Neither reviewer raised a Critical. My own hand pass, before theirs, found two: `verb_answer` split
the target on the last `@` unconditionally, so a project key holding one parsed as a project nobody
named — the split now requires the tail to begin with a digit, which every `at` does; and
`answer_candidates_print` rendered a park with no project as the literal `null`.

The debugger's three warnings were all real and are all fixed: the consumed route ignored the
milestone, so a hand-run session's good `M01` artifact closed the park its bad `M02` artifact left;
the park's own index was found by its `at` alone, which an unrelated escalation in the same second
could stand in for; and a park id matching nothing fell through to the take-over hand-back, where
the word `continue` would have flaglessly resumed a session. Its three suggestions were applied or
recorded.

The auditor's most valuable finding was that the "log order, not `at`" rule was stated in a comment
and measured by nothing — the only fixture's consumed event was later in both orderings, so a
regression would have passed. That is now the M07 row. Its second warning was a doc comment claiming
status 1 where the code returns 2. Four of its five suggestions were applied; the fifth needs
`lib/escalate.sh` and is recorded in D-181.
### 5. Checks not run, and why

- **No live session, no live dispatch, no live Mac message.** Every check is the fixture harness
  against the claude, date, caffeinate and osascript shims. The one change of delivery —
  `baton-unhealthy` going silent — is proved by the absence of the `osascript` line in two scenarios'
  `calls.log`, not by watching Notification Center.
- **`baton status` against the real home was not captured at the time of the first attempt**: the
  tick held the lock for the whole window, which is the ordinary case since D-148 put the standing
  check under it. The clause 3(c) reading below was taken once the lock cleared; it is reported as
  read, not as assumed.
- **`sh install.sh` is run at close-out on `main`, after the standing check passes there**, and its
  result is recorded below rather than predicted here.
- **No M15-b, M15-c or M15-d acceptance is claimed.** Host evidence, the goal guard and the replan
  route are successor work and nothing here stands in for them.

### 6. Known limitations

- **`AI-resolvable` names a route that does not exist yet.** A park carrying it still parks and still
  reaches a person, which `disposition_notifies` returns correctly. The disposition was assigned
  ahead of the route on purpose — the route has to know which parks are its own before it can be
  written — but until something implements it, the table describes an intention on that one arm.
- **`escalation_verb` still tells an orphan rejection park "fix what it names; the next tick re-reads
  the plan and the brief", and for a park with no project there is no plan and no brief.** The
  sentence is visible in `reject-orphan-park-clears`' own expectation and in `baton status`. Named
  and not fixed: it is in `lib/escalate.sh`, a third counted library against a budget this milestone
  had already split to stay inside. Recorded in D-181 with the milestone that should take it.
- **`record_only` has no notification arm.** No notification class carries `HOST-EXPLAINED` and
  `gap` deliberately does not, so the arm would be unreachable and unfixturable today.
  `docs/milestones/M15-b.md` adds it with the evidence that earns it.
- **An orphan rejection park is still listed as an `answer` candidate** although no ruling can reach
  it. It now renders as `?/M04` rather than the literal `null` it printed before, which is how
  `status` renders one, but the deeper point — that listing an unanswerable park among answerable
  ones is misleading — is the same defect as the verb sentence above and belongs with it.
- **Two predicates decide whether an `asking` carries options** — this file's and `escalation_verb`'s —
  and they disagree on the shapes a session writes when it had none. Nothing the log carries today
  has those shapes, because `asking_carries` normalises before the event is written, so no delivery
  differs; but the comment claiming the two branch "on exactly this" is a rule nothing measures.
  One helper would settle it, in `lib/escalate.sh`, with the two findings above. Recorded in D-181.
- **Nothing here was proved against a live session.** Every check is the fixture harness.

### 7. Deviations from the brief, and the DECISIONS entries that record them

1. **The milestone was split again before any code was written (D-182).** §1 of this brief required a
   recheck of the two-existing-library limit against the then-current M12 interfaces; it did not
   hold. Replan went to `docs/milestones/M15-d.md`; the AI route was measured and left unscheduled.
   §4's last paragraph carries the measurement. This is the largest deviation: the brief's title
   scope was "taxonomy, AI/replan routing and L33", and what shipped is taxonomy, L33 and the silent
   record.
2. **§8's fixture list is delivered differently for the parts that moved.** "AI ambiguity" and
   "human credential/access/external facts" are proved by `disposition-table` as *assignments* —
   which classes carry which disposition, and the `asking`-with-options discrimination that decides
   the first — rather than by routing fixtures, because the routes are M15-d's. "Replan" and
   "repeated routing" moved with them and are listed in M15-d §8. "Complete class coverage",
   "both L33 repairs" and "HOST-EXPLAINED records without notifier calls" are delivered in full.
3. **One delivery changed, which the brief did not ask for in so many words** — `baton-unhealthy`
   goes through `record_only` (D-180). §7.5 said to establish the contract and not to classify real
   gaps as explained; it did not say to leave every producer alone. It was done because a table that
   described no measurable behaviour is the shape D-171 rejects, and because this is the one class
   whose own verb already said there was nothing to do. It is one line to revert.
4. **`record_only` has no notification arm**, against the brief's "ready for M15-b's evidence"
   (D-180). Ready means the shape is settled and the `channel` value fixed; an arm no caller could
   reach and no fixture could exercise would be speculative infrastructure.
5. **§5 of this brief was rewritten before any code was written**, as the prompt required, so that
   the declared scope names paths between backticks rather than in prose (D-150).


### 8. Commit information

On branch `m15`:

- `9747e42` — *Assign every escalation and notification class a disposition, and repair limitation 33*
- `33ace95` — *Merge branch 'main' into m15* (brings M13, merged to `main` mid-session; one conflict,
  in `docs/ARCHITECTURE.md` §10, where both sides appended an interfaces row — both kept, M13 first)
- `82d2880` — *Address the review: match the milestone, pin log order, and refuse before the hand-back*
- `@EVIDENCE@` — this evidence, D-179 to D-182, and the M15-d brief

Merged into `main` as **`@MERGE@`**, which is the handover artifact's `merged_as`.

### 9. Working-tree state as observed

`git status --short` in `/Users/danny/.baton/worktrees/Baton/M15`, after the branch commits and
before the merge:

```
(clean — every change is in the four commits above)
```

The worktree is left in place, per `CONTRACT.md` clause 3(c) and D-078: it is the working directory
that lets this session be resumed. It holds no build products — this project has no build step.
### 10. Exact next step, and the eligible milestones

The plan makes three milestones eligible once M15 reads `done`, and all three are `run`; the gates
table is empty, so no disposition is `held`. M15-c waits on M15-b and M16 on M14 and M15-c, so
neither is eligible and neither is named in the artifact.

- **M14** — `docs/milestones/M14.md`. Eligible because M13 completed and merged during this session.
  Not a successor of M15; named because clause 6 lists every eligible milestone, not only successors.
- **M15-b** — `docs/milestones/M15-b.md`. This milestone's direct successor: host-explained gaps and
  wake reconciliation. It inherits `record_only`, the `channel: ["record"]` value and the standing
  decision that `gap` is `human-required` until it can say which gaps the host accounts for.
- **M15-d** — `docs/milestones/M15-d.md`. The other direct successor, created by this session's
  split: the replan route. Its brief lands on `main` in this same close-out, so its blank `Status`
  is a row the next tick can dispatch rather than one that fails at the prompt stage.

**`CONTRACT.md` clause 3(c), read and not assumed.** The relay's lock was held for the first
nineteen minutes of the close-out by a live `baton tick` (pid 57347, `ELAPSED 18:56`) running a
standing check under it, which is D-148's ordinary cost and which D-174's `lock held` line reported
correctly rather than advising that anything be removed. Once it cleared, both verbs D-173 names
were run, because the rail denies a dispatched session the log the clause names:

```
last tick 2026-09-19T15:07:55-04:00 (14 s ago) · unattended needs the lid open or clamshell
in flight  Baton/M15 · cdbe7e79-9352-4bf0-b37f-c5184589e45a · opus · attempt 1 · 1 h 25 m
in flight  Baton/M14 · a6b48e5b-eb8e-493d-8450-ce5c37ab6854 · opus · attempt 1 · 15 s
offline  Baton/M17 · message Baton · wake, or baton wake Baton/M17
```

`baton plan Baton` agrees: `M14  in flight`, `M15  in flight`, `M15-b  waits on M15`. **No `parked`
line appears at all**, so there is no open park anywhere, on any project.

So **M14's refresh is withheld**: the tick dispatched it fifteen seconds before this reading, and a
prompt refreshed under a running session is a prompt that session is not using. It is still named
with disposition `run` and its brief pointer, because withholding a refresh changes neither
eligibility nor disposition. **M15-b's prompt is refreshed** — it has no open lane and no open park,
and it has never been dispatched, so D-173's remaining gap (a lane whose process is gone showing in
neither verb) cannot apply to it: there is no dispatch that could have gone missing. **M15-d's
prompt is the one written here**, so there is nothing to withhold.

The next step is the tick's: it reads this handover and dispatches whichever of M15-b and M15-d the
cap admits beside M14, which is already running.


## Copy-ready session prompt

```
You are implementing M15 of Baton at /Users/danny/Documents/Apps/Baton. Baton is a personal POSIX-shell relay on one Mac; its tick carries work between subscription Claude Code sessions through plans, mailbox artifacts and a single-writer log. Implement the four-disposition taxonomy, AI/replan routes and L33 repairs. HOST-EXPLAINED records without notifying. Size: Medium, 3–4 hours, high effort. Host gap classification is M15-b; the independent goal guard is M15-c. Model judgement is a CLI session, never a direct API call or inline tick judgement.

WHAT ELSE IS IN FLIGHT. Runs alone unless the dispatch says otherwise.

STARTUP ORDER. Read CLAUDE.md, CONTEXT.md, CONTRACT.md, this brief, SCOPE and §3's sources. M12 is complete and installed, and it is where your replan route goes: read lib/planning.sh before you classify anything. M10-b is also complete and installed and it touched the notification you are about to classify: read derivation 15 in docs/ARCHITECTURE.md, REQ-ESC-10, and D-174 and D-175 in docs/DECISIONS.md. A project that owes a plan carries plan_owed in its registration, planning_pass dispatches one planning session for it on the reserved lane id M00-plan, planning_validate measures what comes back, planning_adopt adopts it through onboard_commit's own seeding path, and the plan_generation event records adopted, refused or exhausted. A refusal writes its defects into plan_owed with their repairs and the next attempt's prompt carries them. Read M13's continuation interface if present without adding a dependency. Inspect status/working tree and follow Recovery procedure if work exists. Apply the two-existing-library split rule before editing.

WHAT TO SETTLE RATHER THAN INHERIT. Inventory every producer, preserving lane/project scope. Resolve L33's ambiguous qualified answers and orphaned rejection parks without notifying. Keep credentials/access/external facts human-required. Replan uses the stored goal and the generation path above, with M13's continuation boundary. Three of M12's decisions are yours to honour or to overturn deliberately, not to rediscover. **D-167**: the generation bound spends no new escalation class, because the project is already parked plan-unreadable and that park's repair — a person writes or fixes the plan, and the next tick re-reads it — is the same repair; your taxonomy has to say whether that is the general rule or the exception. **D-169**: a dispatch-failed park on the planning lane is released by the pass that would otherwise re-fail on it, once the condition it named reads clean again — which is an instance of the disposition you are classifying, already built, and the closest thing in the codebase to a park that resolves itself. **D-173**: CONTRACT.md clause 3(c) tells a session to read a log its own deny rules forbid it, and the verbs answer the question instead; the gap it leaves — a lane whose process is gone appears in neither baton status nor baton plan, and neither reports a dispatch no consumption closed — is a verb's worth of work in the files you are already opening. **D-174** is a fourth, and it is about the notification class your taxonomy owns: the `gap` report now measures from the clock the tick started with, so a stretch the tick spent working under its own lock is not a gap at all rather than a gap Baton chose not to send. The class therefore means exactly "Baton was not running", and you inherit a definition rather than a suppression to model — decide whether that is what your taxonomy wants the class to mean before M15-b adds host-explained gaps on top of it. `lock_stale_report` in the same decision is the shape worth copying: two lines, one of which states a condition and offers no act, which is the nearest thing already built to a record that does not ask for anything. Establish the silent-record contract, but do not guess host evidence or implement M15-b/M15-c. Read §4's verified finding and carry its corrections forward. Drift always reaches a person. Use the shared admission seam for pacing.

CONSTRAINTS. Never Python; POSIX shell with set -eu, jq -e, awk and git; no packages, Swift or SQLite. Never touch Reclaim. Keep deny rules, single-writer log, flagless resumes and existing escalation/answer/notifier machinery. No direct model API. Work in the assigned worktree, stage only own paths, accommodate concurrent changes. Existing git identity; neutral messages, no authorship trailers. Take the next free D-number. No routine human plan gate. Before you write any code, put backticks round every path this brief's §5 names: it currently writes SPEC, ARCHITECTURE §10 and DECISIONS as prose, and a completion is proved by at least one changed path falling inside the backticked spans of §5, so as it stands this milestone declares only `tests/scenarios/disposition-*` and an honest completion would be refused for having done nothing (D-150).

VERIFICATION. Follow §8; record exact output and passed/failed/unrun for each check. Run sh tests/run.sh. Host and guard acceptance are separate successor work.

CLOSE-OUT, once this bounded checklist is finished.
1. Run /review-2 with a hand pass, /address locally and sh tests/run.sh after fixes.
2. Append the ten-item plan handoff under Completion evidence; update SPEC/ARCHITECTURE interfaces and DECISIONS; commit on the assigned branch.
3. Merge to main in the canonical checkout. On failure write stopped/merge-failed. Run sh tests/run.sh on main; fix failures or write stopped/main-broken.
4. Mark only M15 done when §8 holds. Establish open lanes and open parks by baton status and CONTRACT.md clause 3(c)'s dispatch-log check. Refresh on main only listed prompts whose milestones have neither an open lane nor an open park with actual interfaces; preserve its slot paragraph. Keep the worktree. Commit own paths and run BATON_HOME=/Users/danny/.baton sh install.sh from the canonical checkout; record warnings.
5. Do not start another milestone. If the bounded work will not fit, follow the plan split rule with completion evidence and stopped/unfinished, not a false completion.
6. Immediately re-read the live plan and apply §11 to every direct successor and other eligible milestone. Refresh and commit only prompts whose milestones have neither an open lane nor an open park under CONTRACT.md clause 3(c) before handover. Write /Users/danny/.baton/inbox/M15-$CLAUDE_CODE_SESSION_ID.json atomically (.tmp then rename): baton 1, canonical project, milestone M15, session, outcome complete, merged_as the verified main merge, written_at and computed eligible entries with brief paths and Copy-ready session prompt heading. Follow installed completion rules. Print it verbatim last in a baton fence; if anything follows, handle it and print it again.
```
