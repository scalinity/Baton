# M14 — Budget-aware pacing

## 1. Identity

- **ID:** M14. **Objective:** pace unattended work against the subscription allowance, pause
  cleanly before exhaustion and resume automatically when its window rolls.
- **User-visible result:** `baton status` shows a budget pause and resume time instead of an
  apparently working lane whose dispatch has failed on quota.
- **Size:** Medium, about 3–4 hours; medium effort. Window behavior is tested with a simulated clock.

## 2. Dependencies and entry conditions

M13 complete and installed. Close-outs now admit all newly eligible work up to the existing cap,
and dispatch selects effort from Size through `claude_bg`. The status feed, usage holds and
flagless continuation machinery already exist. This milestone adds self-pacing; it does not
require M15 or make a new dependency on it.

## 3. Required reading

`CLAUDE.md`; `CONTEXT.md`; `CONTRACT.md`; `docs/v1.1/SCOPE.md` in full, especially §5 and §6 M14;
`docs/MILESTONES.md`; `docs/SPEC.md` §2.3, §2.4, §2.7, §2.9; `docs/ARCHITECTURE.md` §3,
§4.1 steps 5–8, §5.3, §6.2, §6.4, §7, §10; `docs/DECISIONS.md` D-072; M13 completion evidence
and its decisions; `lib/waits.sh`, `lib/status.sh`, `lib/tick.sh`, current admission code,
`hooks/statusline`; existing hold, reserve and window-reset fixtures.

## 4. Requirements, scope, non-goals

- **Covers:** SCOPE §6 M14 and §8 item 4; extends existing usage hold/admission behavior.
- **Scope:** additive `projects/<key>/budget.json` with `window_start`, `sessions_used`, `tier`;
  tier-aware pacing across the five-hour window; pre-exhaustion pause, recorded resume time,
  visible status and automatic window rollover without a failed dispatch.
- **Non-goals:** buying credits, metered API fallback, multiple accounts, a quota dashboard,
  replacing existing error waits or the cap, or deliberately exhausting the live subscription.

## 5. Expected files (proposed)

New `lib/budget.sh`; `lib/tick.sh` admission integration; `lib/status.sh`;
`tests/scenarios/budget-*`; `docs/SPEC.md`; `docs/ARCHITECTURE.md` §3 and §10 row M14;
`docs/DECISIONS.md`; this brief and successor prompt refreshes. Outside the repository:
`$BATON_HOME/projects/<key>/budget.json` and normal pause/resume log records.

## 6. Interfaces

- **Introduces:** budget record with `window_start`, `sessions_used`, `tier`; deterministic
  admission result carrying a pause reason and resume time; window rollover and status rendering.
- **Consumes:** M13's admission/effort seam, existing cap and holds, per-session status feed,
  tick clock and log writer. All sessions share the one subscription; per-project state does
  not grant each project a fresh account allowance.

## 7. Implementation checklist (ordered)

1. Inspect available usage/reset evidence and tier configuration on the installed CLI. Distinguish
   observed allowance from the `sessions_used` pacing estimate; do not invent a guaranteed number
   of sessions for a subscription tier or query a metered API for it.
2. Add clock-controlled fixtures and the three-field budget record. Settle the unit counted,
   failed dispatch accounting, resume accounting and window_start source; normal repeated ticks
   must not charge the same dispatch again.
3. Apply a conservative admission decision before dispatch would exhaust the allowance. Record
   the budget pause and resume time once; leave active work intact and do not spend a failure
   attempt, escalate, or send a dispatch that the pacing rule already knows it cannot admit.
4. Roll the window at its recorded boundary, reset the window's count and admit waiting work
   automatically subject to the existing cap and other holds. Apply pacing to every automatic
   session-start route, including planning and later judgement/replan sessions through the seam.
5. Show the pause reason and resume time in `baton status`; show the lift after rollover. Keep
   real usage-limit waits and the person's reserve effective alongside this policy.
6. Run §8, document the tier/estimation decisions and M16's observation procedure, review and
   close out. Live quota exhaustion is not a test requirement for this milestone.

## 8. Fixtures, verification, acceptance

- **Fixture-run:** allowance available, exhausted window, near-limit admission, repeated paused
  tick, failed-start accounting, same-window resume, boundary rollover, cap/hold after rollover,
  and shared-account accounting; each twice, then `sh tests/run.sh`.
- **Acceptance:** simulated exhaustion produces a clean pause with a recorded resume time and
  no failed dispatch; `baton status` exposes it; advancing the clock past the boundary resumes
  eligible work automatically, with consistent window/count state.

## 9. Definition of done and evidence

Ten-item handoff with budget record before/at/after the boundary, status output and log trace,
and calls proving no dispatch during the pause and successful admission afterward. Record the
tier source and the limits of the session-count estimate. Live boundary proof belongs to M16.

## 10. Risks and decisions this milestone settles

- **Sessions are not a fixed quota unit.** Usage depends on work and other account activity;
  state the conservative policy and how available actual usage/reset evidence constrains it.
- **A pause is not failure.** It must not feed the failure ladder or look like a running lane.
- **One account, several consumers.** Planning, judgement and work sessions share allowance;
  reuse admission and count evidence so a new route cannot bypass pacing.

## 11. Handoff and next milestone

Apply `CONTRACT.md` clause 3(c) to every refresh below: run `baton status` and check the
dispatch log for open lanes, including lanes absent from status. Refresh only a listed
milestone with no open lane; still name every eligible milestone with its correct disposition.

The direct successor in the current graph is **M16**, path `docs/milestones/M16.md`, heading
`Copy-ready session prompt`. M16 needs both M14 and M15-c done; do not assume which finishes first.

At close-out, after recording this milestone as done, re-read the plan's current Status cells,
dependencies and gates. List every direct successor in `eligible[]`, even if it cannot run yet,
and also every other milestone the plan then makes eligible. Derive each disposition at that
moment, with an open gate taking precedence:

- `held` — an open gate holds it; set `held_by` to that gate's exact name.
- `wait` — no open gate holds it and at least one dependency is not done; set `wait_for` to
  the IDs of all dependencies not yet done.
- `run` — every dependency reads done and no open gate holds it.

Use `docs/milestones/<ID>.md` and heading `Copy-ready session prompt` for each entry. Refresh
on main only listed prompts whose milestones have no open lane with the actual budget fields, tier configuration, status/resume
evidence and live observation instructions. Compute from the live plan, not an assumed sibling
order; never change dependencies or add a gate to obtain a disposition. The first of M14/M15-c
to close names M16 `wait` for the unfinished sibling; the second names it `run`, unless an open
gate holds it. Baton initiates successors; this session does not.

## Recovery procedure

Inspect budget state, authoritative reset evidence, dispatch counts and this brief's evidence.
Resume the first unfinished item; do not reset a live window or erase counts to make admission
succeed. Use the fixture clock for tests. Claim completion only when §8 holds.

## Completion evidence

## Copy-ready session prompt

```
You are implementing milestone M14 of Baton at /Users/danny/Documents/Apps/Baton. Baton is a personal POSIX-shell relay on one Mac; its tick dispatches Claude Code sessions through the subscription CLI using a plan, mailbox, log and cap. Add budget-aware pacing: projects/<key>/budget.json with window_start, sessions_used and tier, clean pre-exhaustion pause, visible resume time and automatic rollover. Size: Medium, about 3–4 hours, medium effort. No direct API or paid fallback.

WHAT ELSE IS IN FLIGHT. Runs alone unless the dispatch says otherwise.

STARTUP ORDER. Read CLAUDE.md, CONTEXT.md, CONTRACT.md, this brief in full, docs/v1.1/SCOPE.md and M14 §3's source/evidence. M13 must be complete and installed: autonomous cap-limited handoff and Size-driven effort exist. Existing status-feed usage, holds and flagless resumes remain. M15 may run independently. Inspect working tree/status and use Recovery procedure if budget code, records or evidence exist.

WHAT TO SETTLE RATHER THAN INHERIT. Inspect actual tier/reset evidence; sessions_used is a pacing estimate, not a guaranteed provider quota. Settle accounting for new sessions, failed starts and resumes, a conservative pre-exhaustion threshold and the window boundary. Every automatic model-session route uses the same subscription/admission seam. Record a pause and resume time without attempting a known-unaffordable dispatch; rollover re-admits through the cap and other holds. M16 will prove a live boundary; §8 here uses a simulated clock.

CONSTRAINTS. Never Python; POSIX shell with set -eu, jq -e, awk and git; no packages or rewrite. Do not deliberately exhaust live quota, start live sessions or touch Reclaim. Keep single-writer logs, deny rules, flagless resumes and the person's reserve. Work in the assigned worktree, accommodate concurrent edits, stage own paths by name, never git add -A. Commit locally with existing identity, neutral messages and no authorship trailers; take the next free D-number. No new human gate or budget approval loop.

VERIFICATION. Follow M14 §8; capture the status/log/budget sequence and absence of dispatch while paused. Run sh tests/run.sh. Every check is passed, failed or unrun with output; do not count unrun live work as passed.

CLOSE-OUT, in this order, once the M14 checklist is finished.
1. Run /review-2 with a hand pass; record findings, run /address locally, and sh tests/run.sh after fixes.
2. Append docs/MILESTONES.md's ten-item handoff under Completion evidence; update docs/ARCHITECTURE.md §10 row M14 and docs/DECISIONS.md, then commit on the assigned branch.
3. Merge into main at /Users/danny/Documents/Apps/Baton; on failure write stopped with reason merge-failed and stop. Run sh tests/run.sh on main; fix failures or write stopped with reason main-broken.
4. On main mark only M14 done after all acceptance holds, then re-read the current plan and derive the handover entries under §11. Establish open lanes by baton status and CONTRACT.md clause 3(c)'s dispatch-log check. Refresh only listed prompts whose milestones have no open lane, parts 1 and 3–7, part 4 additively, leaving part 2 verbatim; carry the actual interfaces and evidence named in §11 forward. Leave this worktree for the standing retention policy, commit only your paths, and run BATON_HOME=/Users/danny/.baton sh install.sh from the canonical checkout. Record any differing-agent or notifier warning.
5. Do not start M16 or M15. Do not mark M14 complete without every §8 criterion; apply the plan split rule and write stopped with reason unfinished when necessary.
6. Immediately before writing the handover, re-read the plan as it stands now, after this milestone is recorded done. Put every direct successor in eligible[], including successors not yet runnable, plus every other milestone the plan now makes eligible. Compute each disposition from current Status cells, dependencies and gates: an open gate takes precedence and means held with held_by naming that gate; otherwise any dependency not done means wait with wait_for listing all unfinished dependency IDs; otherwise every dependency is done and no open gate holds it, so use run. Do not assume sibling completion order or copy an earlier disposition. Use each docs/milestones/<ID>.md brief path and heading Copy-ready session prompt, and refresh and commit any newly listed prompt on main before writing only if its milestone has no open lane under CONTRACT.md clause 3(c). Follow §11 for the current direct successor names; use eligible [] only when no direct successors or other eligible milestones remain. Write /Users/danny/.baton/inbox/M14-$CLAUDE_CODE_SESSION_ID.json, .tmp first then rename, with baton 1, canonical project /Users/danny/Documents/Apps/Baton, milestone M14, session from CLAUDE_CODE_SESSION_ID, outcome complete, merged_as the merge commit on main, written_at and the computed eligible array. Follow the installed completion/guard rules without inventing Baton check results. Print the artifact verbatim, last, in a baton fence; deal with anything after it and print it again.
```
