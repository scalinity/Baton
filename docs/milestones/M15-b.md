# M15-b — Host-explained gaps and wake reconciliation

## 1. Identity

- **ID:** M15-b. **Objective:** explain missed ticks using host evidence and act on actual dead sessions.
- **User-visible result:** a sleep-explained gap is recorded and visible in status without a Mac
  notification; an unexplained outage still notifies; a dead lane uses existing recovery.
- **Size:** Medium, about 3–4 hours; high effort. Split from M15 before implementation.

## 2. Dependencies and entry conditions

M15 complete and installed, including the four dispositions and recorded-but-silent delivery.
Use its actual interfaces. M13/M14 may be independent; do not add dependencies on them.

## 3. Required reading

`CLAUDE.md`, `CONTEXT.md`, `CONTRACT.md`, `docs/MILESTONES.md` split rule;
M15 §4's verified September 17 finding and completion evidence; SCOPE §6 M15;
SPEC REQ-ESC-10, REQ-SETUP-07, REQ-STOP-08/10; ARCHITECTURE derivations 1 and 15 and tick order;
DECISIONS D-008, D-054, D-078; `lib/derive.sh`, `lib/rows.sh`, `lib/tick.sh`, `lib/status.sh`,
`lib/stops.sh`, and existing gap/crash/stall fixtures. Inspect M15's notification writer contract.

## 4. Requirements, scope, non-goals

Implement M15 §4's finding. Preserve threshold, marker deduplication, audit events and hardware
constraints. Introduce no new liveness probe, session ping, model judgement, sleep-prevention
workaround, root helper or power-management daemon. No real Mac sleep is needed for acceptance.

**Host evidence contract.** Read `/usr/bin/pmset -g log` once per needed tick through an injectable
command/input seam. Fixtures supply raw history, exit status and the measured gap. Parse actual
Sleep/Wake/DarkWake events with numeric UTC offsets into epochs; ignore Wake Requests, assertions
and summaries. Sort/deduplicate identical transitions; reject contradictory same-time transitions
and unaccounted boot/clock boundaries. Pair Sleep with the next Wake **or DarkWake**, treating
DarkWake as awake, never assuming the whole lid-closed period was continuously asleep. The parser
must retain sufficient boundary history to establish state at the marker and history through the
sampled endpoint. A successful command with empty or partial history does not prove no sleep.

**Coverage policy.** Let the interval be `[epoch(marker), sampled_endpoint]`, with endpoint captured
consistently with `gap_seconds`, not the later event-write time. Clip proven sleep intervals to
this window. Require a complete, unambiguous state timeline and some proven sleep. Classify
`HOST-EXPLAINED` only if every continuous awake portion (including the portions before sleep and
after wake) is **strictly less than two tick intervals**. This reuses the missed-tick threshold;
short dark wakes and tick latency do not require impossible literal coverage of both endpoints.
Do not subtract all sleep and sum disconnected awake fragments: separate short dark wakes are
not one long scheduling opportunity. Any awake portion at least two intervals remains
`UNEXPLAINED`, even if some sleep overlaps. This is a bounded explanation policy, not proof that
the relay was healthy during sleep. Fresh reads must establish the trailing state; an unmatched
Sleep at the history boundary alone cannot prove coverage through now.

**Unknown is not asleep.** Unreadable, failed, truncated, malformed, ambiguous or insufficient
history yields unknown evidence. Record the reason and keep the gap notification, explicitly
saying sleep history could not explain it; do not claim that the host reported no sleep. Complete
history with no sleep or enough awake time yields UNEXPLAINED and notifies. If the measured host
source ceases to support reliable attribution, document that plainly and retain this fallback;
do not replace evidence with a guessed cause or silence all gaps.

**Audit and delivery.** Both outcomes write the gap event under the existing single writer and
marker key, once. Retain duration, marker and detail; add disposition, measured endpoint and
bounded supporting sleep/awake evidence or unknown reason. HOST-EXPLAINED skips Mac delivery,
not event creation. Update SPEC/ARCHITECTURE to distinguish a recorded notification-class event
from actual delivery. `baton status` must show the latest recorded gap with its disposition and
time after `last-tick` advances, while retaining the current active-gap view without duplicating
that same marker. It must not notify or reread host history to display an existing record.

**Wake reconciliation.** Reuse the normal tick pipeline, not a second walker/ladder:

- Consume the inbox first. Read `claude agents --json` successfully; a failed listing is unknown,
  not an empty list, and must not produce crash sightings or dispatches.
- Use `derive_in_flight` and existing `takeover_check`/stand-off protection. A candidate has an
  absent row or `pid: null`, not merely `state: working`, stale output or “not connected.” Honor
  `inbox_holds`, `ended_on_disk`, consumed/rejected endings, current attempt/session and the
  dispatch/resume grace of two intervals. Parked/waiting/closed lanes are not new crashes.
- Death is the existing `crash_check` confirmation: two distinct tick sightings within two
  intervals, after the most recent dispatch/resume. A pre-sleep first sighting older than that
  expires; wake starts a new pair. A live pid and unchanged row across sleep is not a crash.
- Reuse transcript and subagent mtime checks for progress, and transcript readability/takeover
  guards. Age accumulated during proven sleep alone must not produce a wake-time stall message:
  use the same proven sleep intervals when assessing eligible awake inactivity against the
  existing stall threshold. Genuine awake inactivity still follows the existing stall route,
  separately from death. An unreadable transcript remains unknown/protected, not dead.
- Feed a confirmed death through existing `stops_run`/`ladder_step`: `transcript_of` decides
  flagless resume versus redispatch when the transcript is missing; it does not itself decide
  death. Preserve one resume, one redispatch, then actionable escalation. Do not notify merely
  because the host woke, immediately escalate the first missing row, or double-act on recovery.
  Actual credential/access or other existing actionable errors retain their normal routing.

D-078's missing-directory crash motivates this check but does not prove a sleep-caused crash.
No session restart is authorized merely by observing a gap.

## 5. Expected files (proposed)

Every path below is between backticks, because a completion is proved by at least one changed path
falling inside this section's backticked spans and a path named only in prose declares nothing
(D-150). The count was inventoried before any code was written; it came to three counted libraries
and the split was measured against and declined, which D-191 and `docs/MILESTONES.md`'s split rule
record. Do not hide extra scope in the new module or refactor unrelated tick machinery to meet the
count.

Introduced: `lib/host.sh` — the host's sleep history, the coverage policy and the gap record that
reads it. Wired into `bin/baton` and `tests/lib-load.sh`, neither of which is a counted library,
and carried by `install.sh`'s existing `lib/*.sh` glob without an edit. Its seam, `BATON_PMSET`, is
read in `bin/baton` and pointed at `tests/shim/pmset` by `tests/run.sh`.

Three counted libraries, and no more: `lib/disposition.sh` for the `gap` arm and
`record_notification`, `lib/rows.sh` for `gap_check`'s routing and `stall_check`'s awake age, and
`lib/status.sh` for the one recorded-gap line, five lines whose rendering lives in the introduced file.

Fixtures: `tests/scenarios/host-window`, `tests/scenarios/gap-host-explained`,
`tests/scenarios/gap-host-unexplained`, `tests/scenarios/gap-host-partial`,
`tests/scenarios/gap-host-unknown`, `tests/scenarios/gap-host-endpoint`,
`tests/scenarios/stall-sleep-not-a-stall`, `tests/scenarios/stall-awake-after-sleep`,
`tests/scenarios/crash-wake-sighting-expires` and `tests/scenarios/crash-wake-confirms`.

Documents: `docs/SPEC.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `docs/MILESTONES.md` and
this brief.

Six expectations already in the repository move with the behaviour they describe, and are named
here because a frozen expectation is evidence and not a by-product:
`tests/scenarios/gap-reported`, `tests/scenarios/gap-reported-long-check` and
`tests/scenarios/gap-rowless-open`, whose gap event gains the evidence and whose message gains its
clause; `tests/scenarios/disposition-table` and `tests/scenarios/disposition-silent-record`, which
gain the arm and the writer; and `tests/scenarios/install`, whose library listing gains one line.

## 6. Interfaces

Injectable host-history input plus explicit explained/unexplained/unknown result and evidence.
Consume gap marker/duration, current clock, M15 dispositions, existing rows/transcripts/ladder.
Record enough evidence for status and replay; do not store full host logs per tick or add packages.

## 7. Implementation checklist (ordered)

1. Recheck current launchd readability non-disruptively and freeze sanitized host examples as fixtures.
2. Implement/fixture parsing, boundary coverage and the three evidence outcomes defined in §4.
3. Wire gap recording and delivery; retain marker-key idempotence and latest-record status visibility.
4. Integrate wake handling with existing reconciliation and sleep-aware transcript age; preserve guards.
5. Verify §8, update current SPEC/ARCHITECTURE/DECISIONS, review and close out.

## 8. Fixtures, verification, acceptance

Use fixture history, gap, clock, rows, transcripts and a notifier spy; never sleep a real Mac.
For the same open lane and qualifying gap, assert one recorded event in each case and:

- Covering sleep with short awake margins/dark wakes: HOST-EXPLAINED, **zero notifier calls**.
- Complete awake history/no sleep: UNEXPLAINED, **one notifier call**.
- Partial overlap leaving >= two intervals awake: still one call; exactly two intervals is not silent.
- Failed read, empty/truncated history, missing boundary, malformed offset, conflicting transitions,
  reboot/clock discontinuity: unknown with recorded reason, one call. Wake Requests alone explain nothing.
- Multiple sleeps, offset changes, duplicate records and short dark wakes parse deterministically;
  marker + duration differs from event.at without changing the assessed interval.
- Second evaluation of the same marker creates no second event or message for every outcome;
  after marker advances, status still displays the recorded explained gap. A new qualifying gap re-arms.
- Live pid plus unchanged transcript after sleep: no crash and no sleep-only stall. Sufficient
  awake transcript inactivity still follows the existing stall check, not the crash ladder.
- Missing/null-pid row on one wake tick: no recovery; second qualifying tick: existing ladder once.
  Expired pre-sleep sighting cannot confirm on first wake tick. Missing transcript selects redispatch
  only after confirmed death; present transcript selects resume. Failed rows read, takeover,
  unscannable transcript, inbox artifact, consumed/rejected ending and closed lane keep their guards.

Run scenarios twice and `sh tests/run.sh`. Acceptance includes silent and notifying outcomes,
audit/status persistence, confirmed-death recovery and no real sleep. A live read-only LaunchAgent
probe can confirm access; it is not a replacement for deterministic fixtures.

## 9. Definition of done and evidence

Ten-item handoff; sanitized parser inputs and exact classification traces, notifier counts, latest
status after marker advance, row/transcript recovery traces and host access result. Report tests
passed/failed/unrun. M15 §4's prior measurements are not implementation test results.

## 10. Risks and decisions this milestone settles

History retention and formats can change: unknown must stay visible. Overlap is not coverage;
dark wakes can run ticks. A stale transcript is not a dead process. Do not suppress an unrelated
awake stall, count sleep as inactivity, or manufacture a person action for expected hardware behavior.

## 11. Handoff and next milestone

Apply `CONTRACT.md` clause 3(c) to every refresh below: run `baton status` and check the
dispatch log for open lanes, including lanes absent from status; status also lists open parks. Refresh only a listed
milestone with neither an open lane nor an open park; still name every eligible milestone with its correct disposition.

Direct successor **M15-c**, `docs/milestones/M15-c.md`, heading `Copy-ready session prompt`.
Refresh its prompt with actual interfaces. Derive every direct successor and all other eligible
work from the live plan: gate => held/held_by; unfinished dependencies => wait/wait_for; else run.
Do not assume M14's state or initiate successor work.

## Recovery procedure

Read existing host evidence, parser fixtures and recorded marker outcomes. Resume unfinished
work without duplicating a gap or death recovery. No sleep proof means no suppression.

## Completion evidence
### 1. What changed

- **A gap now carries the host's own account of itself, and an explained one raises no Mac message.**
  `lib/host.sh` reads `/usr/bin/pmset -g log` through the new `BATON_PMSET` seam, parses actual
  `Sleep`, `Wake` and `DarkWake` transitions into a timeline, and assesses the window the gap
  actually measured. `gap_check` asks the table what that evidence means and writes the event either
  way: `record_notification` with `channel: ["record"]` for a gap the host accounts for, and
  `notification_write` — the same call it always made — for every other.
- **Detection did not move, and is still asked first.** The threshold, the open-lane test and the
  marker key are unchanged and run before the host is consulted, so a gap already reported costs no
  host read and an ordinary tick costs none at all. What the evidence decides is the channel and
  nothing else; D-174's definition of the class is inherited rather than re-opened.
- **The window is the measured one.** `host_gap_evidence` derives the endpoint from the marker and
  the duration, never from the instant the event is written. `gap-host-endpoint` measures that on a
  tick whose own standing check carries the clock eleven minutes past the gap: the same history that
  explains the measured window would fail to explain eleven more minutes, so the scenario is silent
  under the right reading and would notify under the wrong one.
- **Unknown is never asleep.** Nine reasons — an unreadable read, no transitions, history that does
  not reach the marker, an unclosed `Sleep`, two transitions at one instant, a timestamp that will
  not parse, a reboot, a clock that moved backwards — each keeps the message and says which doubt it
  was, in as many words, rather than letting silence read as "the host reported no sleep".
- **`disposition_of` answers a reading rather than a constant for one class.** `notification:gap` is
  `HOST-EXPLAINED` for an event whose own `host.assessed` is `explained`, and `human-required`
  otherwise — including when it is asked about the class with no carries at all, which is what the
  table's own listing asks and what a gap nothing accounts for really needs. It reads the parser's
  field and never the answer the table itself wrote beside it.
- **`record_notification` is the silent record's notification half**, which M15 left undone because
  no notification class carried the disposition. It is a sibling of `record_only` rather than an arm
  of it, and its guard is the one that has to work hardest: it admits and refuses the same class on
  the fields alone.
- **Sleep is subtracted from a transcript's age before the stall rule fires.** A Mac that slept the
  night leaves every transcript older than the threshold; a session woken beside Baton has not
  stopped working, it has not been running. The subtraction is bounded by what the host proved, is
  not made at all where the evidence is unknown, and leaves `transcript_age_seconds` at the wall age
  with the awake share beside it.
- **`baton status` shows the newest recorded gap after the marker has advanced**, with its
  disposition, and reads no host history to do it. Without that line a silent gap would be visible
  nowhere but the log, which is worse than the message it replaced.
- **Wake reconciliation is the ordinary tick and not a second walker.** Nothing was added to it: the
  row, transcript, inbox and takeover guards, the two-tick crash confirmation and the recovery
  ladder all stand as they were, and two scenarios measure them across a night's sleep.
- **The traceability table was brought level.** It read `REQ-ESC | 01–11` and its own footer claims
  every requirement in §2 appears in it; M15 added 12 and 13 without updating it, and this milestone
  adds 14. The row now names 01–14 with M15 and M15-b against the ones they own, and the `REQ-STOP`
  row names M15-b against the 08 it amends. A claim the table makes about itself is worth keeping
  true.

### 2. Files added / modified / deleted

Added: `lib/host.sh`; `tests/shim/pmset`; `tests/scenarios/host-window/`,
`tests/scenarios/gap-host-explained/`, `tests/scenarios/gap-host-unexplained/`,
`tests/scenarios/gap-host-partial/`, `tests/scenarios/gap-host-unknown/`,
`tests/scenarios/gap-host-endpoint/`, `tests/scenarios/stall-sleep-not-a-stall/`,
`tests/scenarios/stall-awake-after-sleep/`, `tests/scenarios/crash-wake-sighting-expires/`,
`tests/scenarios/crash-wake-confirms/`.

Modified: `lib/disposition.sh`, `lib/rows.sh`, `lib/status.sh` (the three counted libraries);
`bin/baton` (the seam, two lines, plus one source line), `tests/lib-load.sh` (one source line) and
`tests/run.sh` (the seam export and its comment), none of which is a counted library; `docs/SPEC.md`,
`docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `docs/MILESTONES.md`, `docs/milestones/M15-b.md`.

Frozen expectations that moved with the behaviour they describe, named because a frozen expectation
is evidence and not a by-product: `gap-reported`, `gap-reported-long-check`, `gap-rowless-open` and
the eight `tick-step-failed-*` scenarios, whose gap event gains the evidence and whose message gains
its clause; `stall`, `notify-stall-session`, `notify-two-in-a-tick`, `notifications-after-resume`,
`remote-stall-rearms`, `remote-row-not-a-park`, `takeover-origin-not-an-object` and
`takeover-peer-not-a-person`, whose `calls.log` gains one `pmset -g log` line where the stall rule
now asks the host; `disposition-table` and `disposition-silent-record`, which gain the arm and the
writer; and `install`, whose installed library listing gains one line.

Deleted: nothing. `install.sh` was not edited — its existing `lib/*.sh` glob carries the new file.

### 3. Interfaces introduced or changed

In `docs/ARCHITECTURE.md` §10, row M15-b. `lib/host.sh` introduces `host_cache_file`,
`host_transitions`, `host_window`, `host_sleep_proven`, `host_gap_evidence`, `host_gap_detail`,
`host_gap_recorded` and `host_gap_status_line`, and the `BATON_PMSET` seam. `lib/disposition.sh`:
the `notification:gap` arm reads the carries, and `record_notification` is added beside
`record_only`. `lib/rows.sh`: `gap_check` routes on the disposition, `stall_check` subtracts proven
sleep and carries `awake_age_seconds`. `lib/status.sh`: one recorded-gap line. New requirement
`REQ-ESC-14`; `REQ-ESC-10`, `REQ-ESC-11`, `REQ-ESC-12`, `REQ-ESC-13`, `REQ-STOP-08` and
`REQ-SETUP-07` amended; derivation 15 amended. Events: a `notification` of class `gap` gains
`disposition` and `host`, and gains `channel: ["record"]` when it was recorded rather than
delivered; a `notification` of class `stall` gains `awake_age_seconds` when a subtraction was made.
### 4. Checks run, with outcomes

**The standing check, `sh tests/run.sh`, on the branch: PASSED — `415 scenarios, 0 failed`, `EXIT=0`**,
run after the review fixes against the tree commit `c0ad9bb` holds, and read to completion rather
than left in the background. The run is the
whole suite, each scenario run twice against a copy of its fixtures with the seams pointed at the
shims.

**The standing check on `main`, the combined tree, after the merge: @MAINRESULT@.**

**The ten new scenarios, and the twenty-two whose expectations moved**, each read and judged before it
was frozen rather than frozen and then trusted:

- `host-window` — the parser and the coverage policy, twenty-two histories against one two-hour window, plus a failed read. Covering sleep with short dark wakes, a marker inside an awake stretch, a complete
  timeline with no sleep, **exactly two intervals awake and one second under it**, two short awake
  stretches that are not summed, a UTC offset change read rather than assumed, and an unclosed
  `Sleep` on both sides of the two-interval line. Then every doubt: Wake Requests alone, an empty
  history, history starting after the marker, an unclosed trailing `Sleep`, a lost wake, a conflict
  inside the window and a conflict on the record the marker state is read from, a malformed
  timestamp as the last line of the history, a reboot, a clock that moved backwards, and a read that
  failed — beside two that correctly taint nothing, a malformed timestamp as the *first* line and a
  reboot a day before the window, because a doubt open backwards is still bounded forwards by the
  next good record. It ends by counting host
  reads: three windows under one lock make one, a fourth with the lock gone makes another, and two
  failing windows under one lock make one — the cached failure being read back as
  `history-unreadable` and not as an empty history.
- `gap-host-explained` — the acceptance. A two-hour gap the host accounts for: the event carries
  `channel: ["record"]`, `disposition: HOST-EXPLAINED` and the evidence, **`calls.log` holds one
  `pmset -g log` and no `osascript` line and no applet launch**, and the `status` below it shows the
  recorded gap after the tick has written a new marker — which is the second half, because a silent
  gap that also vanished from the view would be worse than the message it replaced.
- `gap-host-unexplained` — a complete history with no sleep in the window: one notifier call, and
  the Wake Requests line inside the window is not read as a wake.
- `gap-host-partial` — 118 minutes of sleep and one awake stretch of **exactly two intervals**: one
  notifier call. Overlap is not coverage, and the boundary is on this side of the line.
- `gap-host-unknown` — `pmset` exits non-zero: one notifier call, whose body says "the sleep history
  could not explain it (history-unreadable), which is not a claim that the host stayed awake", and
  an event carrying `host.assessed: unknown` with the reason.
- `gap-host-endpoint` — the endpoint rule, on a tick whose own standing check carries the clock
  eleven minutes past the gap. The event is written at 00:11:00; the window assessed is
  22:00:00–00:00:00. The host slept until forty seconds before the endpoint, so it is recorded
  silently; read the endpoint off the event's own time and the same history leaves eleven minutes
  unaccounted for and notifies.
- `stall-sleep-not-a-stall` — a forty-minute-old transcript across thirty-two minutes of proven
  sleep, a live pid and a fresh marker: no stall line, no crash line, no Mac message.
- `stall-awake-after-sleep` — the same age with ten minutes of sleep: the thirty that remain notify,
  and the event carries `transcript_age_seconds: 2400` beside `awake_age_seconds: 1800`.
- `crash-wake-sighting-expires` — the first tick after a night. The gap is recorded silently and the
  rowless lane is sighted for the **first** time although a sighting from ten hours earlier is in
  the log: a first sighting expires after two intervals, so the pair the night interrupted cannot be
  completed across it. Nothing resumed, redispatched or escalated.
- `crash-wake-confirms` — the tick after that: the pair made since the wake confirms, and the lane
  takes the existing ladder's first rung, one flagless resume under the same id.
- Moved expectations: `gap-reported`, `gap-reported-long-check`, `gap-rowless-open` and the eight
  `tick-step-failed-*` scenarios gain the evidence on the event and the clause on the message, all
  of them `unknown/no-transitions` because they carry no host fixture — which is itself the right
  answer for a history that holds nothing. `stall`, `notify-stall-session`, `notify-two-in-a-tick`,
  `notifications-after-resume`, `remote-stall-rearms`, `remote-row-not-a-park`,
  `takeover-origin-not-an-object` and `takeover-peer-not-a-person` gain one `pmset -g log` line
  where the stall rule now asks the host and gets no usable answer, so their behaviour is unchanged.
  `disposition-table` gains the gap arm driven across twelve carries shapes;
  `disposition-silent-record` gains `record_notification`, and its `calls.log` is still empty.
  `install` gains one line in the installed library listing.

**Six regressions were re-introduced on purpose, one at a time in a throwaway copy of the worktree,
to prove the new fixtures are load-bearing rather than decorative. All six were caught:**

```
endpoint from the write time       caught by gap-host-endpoint
two intervals read as still silent caught by gap-host-partial
two intervals read as still silent caught by host-window
table reads its own answer back    caught by disposition-table
DarkWake counted as still asleep   caught by host-window
Wake Requests read as a wake       caught by host-window
```

The first attempt at this proof reported the endpoint mutation as *not* caught, and the fault was in
the proof: it substituted with awk's `sub()`, whose first argument is a regular expression, against
lines full of `$`, `(` and `"`. The mutation had never applied. Redone with `index`/`substr` and a
post-condition that the mutated text is present, all six fail as they should.

**The run environment is worth recording, because it shaped the timing rather than the result.**
Three full-suite runs were live on this Mac at once during the branch check — this one and two
started by another session in the canonical checkout — and a scenario took about forty seconds
against the eleven M15 measured. A fourth was **this session's own orphan**: a run stopped through
the harness left a `sh tests/run.sh` reparented to launchd and still working, and it was killed by
pid once found. `tests/run.sh`'s own orphan guard exits 3 when its parent is pid 1 and is checked
before each scenario, so it had several chances in the two minutes the orphan lived; whether it
failed to fire or simply had not reached a boundary was not established, and nothing is claimed
about it here beyond that the process was there and was removed.

**Two checks outside the harness.** The date arithmetic `lib/host.sh` needs — `civil_from_days`,
the inverse of the `days_from_civil` `iso_epoch` already had — round-trips every one of fourteen
instants through `iso_epoch` and back: leap days in 2024 and 2000, the 2100 non-leap century, the
February/March boundary, `+05:30`, `-04:00`, the epoch itself and an instant before it. Zero
mismatches. And the parser was run against **the real `/usr/bin/pmset -g log`** on this Mac: 19,237
lines in, 950 transitions out, **zero anomalies**, and windows from ten minutes to five days assessed
without incident — the five-day one finding 312 sleep intervals and 205,997 seconds of sleep. The
read costs about one second and the awk pass about a tenth of one.

**Live host access, rechecked non-disruptively on 2026-09-19.** `/usr/bin/pmset -g log` was run
under a LaunchAgent submitted with a temporary label of its own, through
`/Users/danny/.baton/bin/sh` — the shell the real agent's `ProgramArguments` names — and reported
`PROBE-EXIT=0`, `UID=501 PPID=1`, empty stderr and 950 transition lines. The temporary job was
removed and `launchctl list` no longer holds it. **The real `com.baton.tick` agent was neither
changed nor invoked**, and no Mac was slept.

**`/review-2`: one of two reviewers reported, and the other is recorded as a timeout.** The
code-auditor (Fable) completed all six passes and raised **no Critical**. Its three warnings were
coverage holes and all three are closed: the cached-failure branch of `host_transitions` had no
fixture (now the last section of `host-window`); the `unexplained`-with-partial-sleep message arm
had none (now `gap-host-partial`, which this session had added independently before the report
arrived); and the anomaly brackets at the two ends of the history and the conflict-at-the-marker
clause had none (now three more `host-window` probes, one of which showed that a malformed *first*
line correctly does **not** taint a later window, because a doubt open backwards is still bounded
forwards by the next good record). Three of its six suggestions were applied — `status` now takes
its row kind from `channel`, the fact of what happened, rather than from `disposition`, the table's
answer written down; the inline `jq -r '.x // d'` calls became the `field` helper the rest of
`status.sh` uses; and `records` became `history_records`, which is what it counts. Its point that
the stall rule's host read is once per *tick* and not once per *outage* was right and the comment
overstated it: the comment now prices the real cost — a `pmset -g log` a minute, for at most
`stallMinutes` after a wake, per idle lane, at about a second each. Two suggestions are recorded and
not taken, in §6.

**The debugger did not report.** It ran for seventeen minutes, went idle, and did not answer four
requests for its findings. Nothing is claimed on its behalf: the bug-hunting lane was covered by
this session's own hand pass, by the two out-of-harness checks above and by the six regression
proofs, and that is a weaker substitute than an independent reader. It is named here rather than
left implicit.

### 5. Checks not run, and why

- **No real Mac was slept, and no Mac message was raised.** Every acceptance check is the fixture
  harness against the `pmset`, `claude`, `date`, `caffeinate`, `osascript` and `open` shims. The one
  outcome that changes delivery is proved by the *absence* of the `osascript` line in
  `gap-host-explained`'s and `gap-host-endpoint`'s `calls.log`, not by watching Notification Center.
- **No live gap was produced.** The relay was not stopped to manufacture one, and the real
  `log.jsonl` was not read — the deny rail forbids a dispatched session that file (D-173). The three
  outcomes are driven from a fixture marker and a fixture history; the only live reading taken is of
  `pmset -g log` itself, which writes nothing.
- **The debugger half of `/review-2` did not report**, so no independent bug-hunting pass is claimed.
  §4 says what stands in its place and why that is weaker.
- **`sh install.sh` is run at close-out on `main`**, after the standing check passes there, and its
  result is recorded in §4 rather than predicted here.
- **No M15-c acceptance is claimed.** The goal guard is successor work and nothing here stands in
  for it.

### 6. Known limitations

- **A malformed pmset line is only seen when it keeps the shape of a transition.** The domain is
  read as the fourth token onwards of the tab-preceding text, so a line whose *timestamp* is
  malformed is detected — `host-window` drives `-04X0` as the first line and as the last — but a
  line mangled badly enough that its fourth token is no longer `Sleep`, `Wake` or `DarkWake` is
  skipped in silence, like any other domain. A missing transition is not an anomaly Baton can see.
- **A reboot is only seen through the `Sleep/Wakes since boot` counter going backwards.** A reboot
  that left no such line between the two records around it, or a history that never prints one,
  reads as a stretch nothing covers — which is `unexplained` and notifies, the safe direction, but
  not the honest reason.
- **`host_gap_evidence` has its own grammar for the marker's UTC offset**, `[+-]HH:?MM$`, where
  `iso_epoch` accepts `+HH` and `Z` as well. Unreachable today, because `baton_now` always writes
  `+HH:MM` and the marker is Baton's own file; a `+HH` marker would get the right epoch and have its
  evidence rendered in UTC. Named rather than fixed: the repair belongs with `iso_epoch`, in
  `lib/derive.sh`, which is a fourth counted library.
- **Presentation and derivation share `lib/host.sh`.** `host_gap_detail`, `host_gap_recorded` and
  `host_gap_status_line` render, and call `duration` and `lib/render.sh`, while `status_render`
  calls back into this file; the load order shows one direction and not the other. The auditor
  proposed moving the two rendering halves into `rows.sh` and `status.sh`. Not taken, because it
  would make those two files carry the change rather than the introduced one and the count is
  already at three; recorded here so the next reader knows it was a choice.
- **`baton status` reads the log once more than it did.** `host_gap_recorded` calls `log_json`
  where `status_render` already calls it elsewhere. Consistent with the file, which re-reads per
  derivation, and `status` is a verb a person runs.
- **The explanation is bounded and is not a health claim.** That every awake portion was under two
  intervals says a tick had no room to run; it does not say launchd was loaded, the lock was free or
  the relay would have worked. A gap the host explains is a gap nobody could have prevented, not a
  gap that did nothing wrong.
- **Nothing here was proved against a live gap.** No relay was stopped to manufacture one and no Mac
  was slept; every acceptance check is the fixture harness against the shims.
### 7. Deviations from the brief, and the DECISIONS entries that record them

1. **The two-existing-library rule was crossed at three and the split was declined (D-191).** The
   brief's own §5 named the split point — "for example separate status/progress integration" — and
   it was measured against and declined, because §4 and §8 of that same brief make the status line
   this milestone's acceptance: splitting it out would have left a `stopped/unfinished` artifact for
   a milestone whose §8 could not hold, and a silent gap visible nowhere at all. The third file is
   held to five lines, with the rendering in the introduced file. Recorded in D-191 and in
   `docs/MILESTONES.md`'s split rule.
2. **An unclosed `Sleep` is `unknown` only when the unproven stretch inside the window is two
   intervals or longer (D-189).** §4 says "require a complete, unambiguous state timeline"; read
   literally, every `Sleep`-then-`Sleep` pair would make its window unknown, and the real history
   holds three of them, twelve to fourteen seconds apart, in seven days. Such a stretch is never
   counted as sleep — it falls out as an awake portion, which can only count against an explanation
   — so the conservative direction is kept while a doubt too short to have held a tick does not make
   a whole night unreadable.
3. **Transitions are never sorted; a record out of order is refused as `clock-discontinuity`
   (D-189).** §4 says "sort/deduplicate identical transitions". Deduplication is done. Sorting is
   not, because `pmset` prints chronologically and a record older than the one before it is the
   clock having moved, which is a thing to report rather than a thing to tidy away — sorting it into
   place would hide exactly the discontinuity §4 asks to be rejected.
4. **`record_notification` is a sibling and not an arm of `record_only` (D-190).** The prompt called
   it an arm; `escalate` takes a scope and `notification_write` takes a key, and one function
   pretending those were one field would read worse than two that each mirror their own writer.
   M15's own comment said "M15-b adds it *beside* this", which is what was built.
5. **`channel` is written on a `notification` event only by `record_notification` (D-190).**
   Backfilling `["notification"]` onto the delivered case would have moved forty-nine events across
   thirty-six frozen expectations without changing a decision, since nothing reads the field.
6. **§5 of this brief was rewritten before the merge** so that the declared scope names paths
   between backticks rather than in prose (D-150). As written it named none at all, so the
   completion check would have had no scope for a changed path to fall inside.
### 8. Commit information

On branch `m15-b`:

- `@C1@` — *Explain a gap with the host sleep history, and record an explained one silently*
- `@C2@` — this evidence, the decisions and the documents

- `@MERGE@` — *Merge branch 'm15-b'*, which is the handover artifact's `merged_as`
- the close-out commit on `main` below it, carrying `done`, the refreshed prompts and this paragraph

### 9. Working-tree state as observed

@TREE@

### 10. Exact next step, and the eligible milestones

The plan makes three milestones eligible once M15-b reads `done`, and all three are `run`; the gates
table is empty, so no disposition is `held`. M16 waits on M14 and M15-c, so it is not eligible and is
not named in the artifact.

- **M14** — `docs/milestones/M14.md`. Eligible because M13 is `done`. Not a successor of M15-b;
  named because clause 6 lists every eligible milestone and not only successors.
- **M15-c** — `docs/milestones/M15-c.md`. This milestone's direct successor, the independent scope
  guard. It inherits `lib/host.sh`, the `BATON_PMSET` seam, `record_notification` and the standing
  rule that `HOST-EXPLAINED` is now a reading of an event's own evidence for one class — which
  matters to it, because its own §4 says no disposition, `HOST-EXPLAINED` included, may clear drift.
- **M15-d** — `docs/milestones/M15-d.md`. The other eligible milestone, M15's replan split. Nothing
  here touches its route; `disposition_of` still assigns `replan` to `unfinished-twice` and
  `blocked`, and `disposition-table` still pins both.

**`CONTRACT.md` clause 3(c), read and not assumed.** The rail denies a dispatched session the log
the clause names, so both verbs D-173 substitutes were run (readings taken at 17:30 EDT):

```
last tick 2026-09-19T17:30:05-04:00 (0 s ago) · unattended needs the lid open or clamshell
in flight  Baton/M14 · a6b48e5b-eb8e-493d-8450-ce5c37ab6854 · opus · attempt 1 · 2 h 22 m
in flight  Baton/M15-b · 10f5a8a6-27e3-486e-a324-e5a560db48fc · opus · attempt 1 · 1 h 6 m
offline  Baton/M17 · message Baton · wake, or baton wake Baton/M17
```

`baton plan Baton` agrees: `M14  in flight`, `M15-b  in flight`, `M15-c  waits on M15-b`,
`M15-d  eligible`. **No `parked` line appears at all**, so there is no open park on any project.

So **M14's refresh is withheld**: its session has been running for over two hours and a prompt
refreshed under a running session is a prompt that session is not using. It is still named with
disposition `run` and its brief pointer, because withholding a refresh changes neither eligibility
nor disposition nor the artifact. **M15-c's and M15-d's prompts are refreshed**: neither has an open
lane in either verb, neither has ever been dispatched, and so D-173's remaining gap — a lane whose
process is gone showing in neither verb — cannot apply to either.

**M14 also took D-187 and D-188 on `main` while this session was working**, which is the collision
the plan's rule about re-reading the next free number at merge time exists for. This session had
read D-187 as free and written D-187 to D-189; all three were renumbered to **D-189 to D-191**, and
every cross-reference with them in `docs/DECISIONS.md`, `docs/SPEC.md`, `docs/ARCHITECTURE.md`,
`docs/MILESTONES.md`, `lib/disposition.sh` and this brief. Reading the number at the moment of
writing is not enough when another lane is writing too; the merge is where it has to be re-read.

The next step is the tick's: it reads this handover and dispatches whichever of M15-c and M15-d the
cap admits, leaving M14 alone because its lane is already open.

## Copy-ready session prompt

```
You are implementing M15-b of Baton at /Users/danny/Documents/Apps/Baton. Baton is a relay, a personal tool for one person on one Mac, on Claude Code 2.1.270, that carries a build from one Claude Code session to the next; a launchd-run tick every sixty seconds; it embeds no model call. Implement host-explained gaps and wake reconciliation exactly as §4 defines. Size: Medium, 3–4 hours, high effort. No goal-guard work: that is M15-c's.

WHAT ELSE IS IN FLIGHT. Runs alone unless the dispatch says otherwise.

STARTUP ORDER. Read CLAUDE.md, CONTEXT.md, CONTRACT.md, this brief and §3's sources, especially M15's verified finding, which is in docs/milestones/M15.md §4 and is investigation evidence rather than acceptance. M15 is complete and installed, and what it left you is smaller and sharper than "the taxonomy": read lib/disposition.sh in full before anything else. disposition_of <kind> <class> [<carries>] assigns every class class_or_fail accepts one of four dispositions and refuses one neither list holds; disposition_notifies <disposition> returns 0 for a class that reaches a person, 1 for HOST-EXPLAINED and 2 for a word that is not a disposition, which is why record_only matches the 1 positively; record_only <project> <milestone> <session> <attempt> <class> <scope> <carries> is escalate's arguments in escalate's order minus the message, writing the escalation event with channel: ["record"] and refusing any class the table does not call HOST-EXPLAINED. Then read tests/scenarios/disposition-table and disposition-silent-record, which are the two fixtures your change will move. Inspect status and the working tree and use the Recovery procedure if work exists. Inventory your edits and apply the split rule before a third existing library edit.

WHAT TO SETTLE RATHER THAN INHERIT. Three facts about M15's half are yours to change deliberately rather than to discover. First, record_only has NO notification arm, on purpose: no notification class carries HOST-EXPLAINED, so an arm for one would be a branch no caller could reach and no fixture could exercise, which is the rule-stated-but-not-measured shape D-171 rejects. Adding that arm is yours, and it is the smaller half of your change — the shape is already settled and will not move: the same event the notifier would have raised, with channel: ["record"] instead of ["notification"], and the class's own once-only key. Second, disposition_of currently answers human-required for notification:gap, and the comment at that arm says why: a gap is over when it is reported, which is half of HOST-EXPLAINED, but nothing yet explains it. You are the evidence that completes the other half, so you change that arm — but only for a gap the host accounts for, which means the disposition becomes a reading rather than a constant and the table has to keep saying something true about the class in general. Decide how before you write it. Third, tests/scenarios/disposition-table extracts the class list from class_or_fail AND the arm list from disposition_of and set-differences them, so it will move when you touch that arm; read what it proves before you re-freeze it, and do not weaken it to make it quiet. D-174 is the definition you inherit and should not re-open: the gap is measured from the clock the tick started with, so a stretch the tick spent working under its own lock is not a gap at all rather than a gap Baton chose not to send, which makes the class mean exactly "Baton was not running" with no cause attached — and that is precisely what leaves room for you to attach one. Read history once through a fixture seam. Use offset-aware actual transitions, including DarkWake, never Wake Requests. Match the measured gap endpoint, not the event's write time. Explain only complete sleep evidence with every awake portion shorter than two intervals. Unknown stays visible and notifies, explicitly marked unknown rather than falsely claiming no sleep. Record both outcomes once by marker, and retain the latest recorded gap in status after the marker advances — status_render currently re-derives only the active gap from last-tick, so preserving the log alone does not satisfy "baton status still shows it". Reuse the row, transcript, inbox and takeover guards, the two-tick crash confirmation and the existing recovery ladder. Sleep-only transcript age is not a wake stall; a stale transcript is not death; D-078 is evidence of a service restart plus a missing working directory, not of sleep killing sessions.

CONSTRAINTS. Never Python; POSIX shell with set -eu, jq -e, awk, git and native host tools; no packages, Swift, SQLite, privileged helper, direct API call or new liveness probe. Do not sleep the real Mac, alter the real tick agent, touch Reclaim, or restart a session just for a gap. Keep the deny rules, the single-writer log and flagless resumes. Work in the assigned worktree, stage only your own paths, never git add -A, and accommodate concurrent work: other lanes run beside you and append to the same documents. Existing git identity; neutral technical messages; no authorship trailers. Take the next free D-number, read from main at the moment you write it, and re-read it if your merge conflicts — two lanes took the same number during M15's close-out and one had to renumber.

VERIFICATION. Execute §8 with a fixture sleep history and gap, and with notifier call counts: show an explained gap recorded silently, an unexplained gap notifying, the unknown fallback notifying and marked unknown, the recorded gap still visible in status after the marker advances, and a confirmed dead session taking the existing recovery. Run sh tests/run.sh, read it to completion, and quote its scenario count and failure count; a run the harness moved to the background is not finished until its output has been read, and a check nobody read is unrun.

CLOSE-OUT, once this checklist is finished.
1. Run /review-2 with a hand pass, /address locally, and sh tests/run.sh after the fixes.
2. Append the ten-item handoff under Completion evidence; update SPEC, ARCHITECTURE §6.2 and §10, and DECISIONS; commit on the assigned branch.
3. Merge to main in the canonical checkout. If the merge fails, write stopped/merge-failed and go no further. Run sh tests/run.sh on main; fix it there or write stopped/main-broken. If another session has uncommitted work in the canonical checkout, do not commit, stash or clobber it: wait a bounded time, then stop and report rather than waiting forever.
4. Mark only M15-b done after §8 holds. Establish open lanes and open parks by baton status and CONTRACT.md clause 3(c)'s dispatch-log check, which a dispatched session answers with baton status and baton plan because the rail denies it the log the clause names (D-173). Refresh on main only the prompts of listed milestones with neither an open lane nor an open park, with the actual interfaces, preserving each slot paragraph verbatim. Keep the worktree. Commit your own paths and run BATON_HOME=/Users/danny/.baton sh install.sh from the canonical checkout; record any warning it prints.
5. Start no successor. If the work will not fit, follow the split rule and write completion evidence plus stopped/unfinished, never a false completion.
6. Re-read the live plan and apply §11 to every direct successor and every other eligible milestone, with held/wait/run dispositions. Refresh and commit only the eligible prompts clause 3(c) permits before writing /Users/danny/.baton/inbox/M15-b-$CLAUDE_CODE_SESSION_ID.json atomically (.tmp then rename): baton 1, canonical project, milestone M15-b, session, outcome complete, the verified merged_as, written_at, and eligible entries with brief paths and the Copy-ready session prompt heading. Follow installed completion rules. Print the artifact verbatim last in a fenced block whose info-string is baton; if anything lands after it, deal with that and print it again.
```
