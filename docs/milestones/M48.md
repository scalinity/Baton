# M48 — Codex unseen-project live acceptance

## 1. Identity

- **ID:** M48. **Objective:** Demonstrate the implemented Codex adapter carrying bounded unfamiliar-project work through question, verified result and continuation.
- **User-visible result:** Version/environment-bound observed Codex workflow, directory conflict evidence and sanitized replay captures.
- **Size:** Small, 2–3 hours of attended trials after prerequisites; split if external compatibility blocks. The estimate includes implementation, fixtures and close-out; it excludes waiting for missing external authority/access.

## 2. Dependencies and entry conditions

**Depends on:** M47. **PRECONDITIONS:** M47 is complete in the checkout used for this session, and the consumed interfaces below are present and working; run `test -f Sources/BatonCore/Reconciler.swift && test -f Sources/BatonAdapters/Codex.swift`, then `sh tests/v2/run.sh all` against the current source-matched V2 executable. A plan Status cell or a sibling brief is not proof. Inspect the files implementing §6's consumed interfaces if a check fails; do not proceed on a stub or a zero-scenario pass.

Run `git status --short` and `git branch --show-current`; preserve existing unrelated changes. V2 implementation must have been requested and the `V2 implementation` gate in `docs/MILESTONES.md` cleared by its recorded decision; check `sed -n '/^## Gates/,/^## Traceability/p' docs/MILESTONES.md`. The planning brief is not build/live permission. Before running a compiled V2 check, verify explicit compile/test authority and current executable provenance recorded in the repository or supplied for this session; never compile implicitly or use a stale binary to claim success. `BATON_V2_BIN` names the explicitly prepared current V2 executable; `test -n "${BATON_V2_BIN:-}" && test -x "$BATON_V2_BIN"` verifies availability, not provenance. No live V2 installation is required for fixture-only work.

This milestone additionally needs an explicitly authorized Codex, the actual required runtime/account or fresh-user access, and an explicitly built current release. Verify `"$BATON_V2_BIN" --version`, `"$BATON_V2_BIN" --home "$BATON_V2_TEST_HOME" capabilities --json`, and the available prerequisite evidence via `"$BATON_V2_BIN" --home "$BATON_V2_TEST_HOME" attention --all --json`; set BATON_V2_TEST_HOME to a new private temporary state root for this trial, never the production home. Setup/probes/install are mutating and run only under that trial's actual authority. Missing access or unsupported required guarantees leave the corresponding live checks unrun, not passed.

## 3. Required reading

`CLAUDE.md` (including its V2 authority notice); `CONTEXT.md`; `CONTRACT.md` (Baton development close-out only); this brief in full; `docs/v2/02-decisions.md` C01–C12 and user rulings O1–O8; `docs/ARCHITECTURE.md` §10 entries for the interfaces in §6.

**SPEC REFERENCES:** `docs/v2/03-spec-v2.md` §9.1–§9.2; §9.4; §10 AC01–AC02, AC06, AC09–AC10, AC13–AC14, AC16–AC23, AC28–AC29, AC33–AC34, AC50 (Codex live evidence). Also §7 items 5, 6, 7, 14, 29, 32, 49 and their corresponding §8 failure rows. Read §3.1 shared identity/durability rules and §4.1 transition discipline with every referenced entity. §11 remains the non-goal boundary. References narrowed to named rows/portions own that portion here, not the whole section's later implementation.

No sibling milestone or predecessor completion appendix is required. V2 decisions/specification govern product behavior; V1 shell-only, fixed paths, model aliases, target headings and mailbox authority are historical product contracts, not V2 requirements.

## 4. Requirements, scope, non-goals

- **Covers:** AC01, AC02, AC06, AC09, AC10, AC13, AC14, AC16, AC17, AC18, AC19, AC20, AC21, AC22, AC23, AC28, AC29, AC33, AC34, AC50 in the portions named in §3; §8 defines this milestone's proof, not a claim that every end-to-end AC is already held.
- **Scope:** Version/environment-bound observed Codex workflow, directory conflict evidence and sanitized replay captures. Create/modify only the paths proposed in §5 and necessary narrow connections to the interfaces in §6; consume the complete existing typed/persisted model, implement this feature's validation and public projections, and record any required schema correction rather than creating duplicate entities.
- **Non-goals:** Production target work, credential creation, cross-runtime live conversation transfer and deliberately consuming allocation. No unrelated target changes, production activation, hidden scope expansion or implementation of the next milestone.

## 5. Expected files (proposed)

`docs/v2/evidence/codex-live.md`; `tests/v2/captures/codex/`; `tests/v2/scenarios/codex-live-replay/`; `docs/ARCHITECTURE.md` §10 (actual paths/interfaces, invariant and runnable check); `docs/DECISIONS.md` (implementation decisions, next free D-number); `docs/MILESTONES.md` (this row/evidence status); this brief.

Paths are proposed because the implementation does not yet exist. If an equivalent interface already has a different location, use `rg --files Sources tests/v2` and the architecture registry, reuse it, and record the actual path; do not create a second implementation to match a filename. Changes to more than two existing implementation files beyond newly introduced files trigger the split review in §10. Outside the repository: only newly created temporary fixture roots, plus the explicitly authorized isolated trial environment; no production target or credential files.

## 6. Interfaces

- **Introduces:** Version/environment-bound observed Codex workflow, directory conflict evidence and sanitized replay captures.
- **Consumes:** Complete V2 implementation, current source-matched release and actual authorized Codex profile; live proof must be independently reproducible without Claude trial notes.
- Persist interface decisions, invariants and exact verification commands in `docs/ARCHITECTURE.md` §10 and tests before completion. These repository artifacts, rather than session memory, are the next session's contract.

## 7. Implementation checklist (ordered)

1. Confirm every entry condition in §2 and inspect the consumed interfaces; on a missing prerequisite, retain the command/output and stop the dependent work.
2. Record current release/profile/capabilities and trial authority; create an unfamiliar relocated dirty non-main Git fixture plus a non-Git fixture without Baton files.
3. Run explicit Codex intake, a real addressed question, independent verification and safe Git checkout application; preserve unrelated dirt.
4. Run non-Git intake through accepted output and either evidenced exclusive application or truthful retained-result/conflict, then exercise supported checkpoint/native continuation.
5. Retain sanitized actual captures and commands, labeling simulated failures/quota distinctly and exposing real unsupported guarantees.
6. Add the `codex-live-replay` scenarios with independent assertions and fault-boundary observations required by §8; run the focused suite twice and the standing checks.
7. Review, address findings, record the ten-part completion evidence and complete the ordered close-out in the prompt; do not start the successor.

## 8. Fixtures, verification, acceptance

- **No-build:** `git diff --check`; inspect changed commands to confirm argv/data transport and no implicit compile/sign/install in ordinary checks.
- **Fixture-run:** `sh tests/v2/run.sh codex-live-replay`, twice; `sh tests/v2/run.sh all`; `sh tests/run.sh`. The V2 runner and suite named here are deliverables (the runner already exists); missing executable/suite or zero executed scenarios is a failure. Fixtures use relocated temporary roots, deterministic adapters/clock/effect seams and inspect intermediate state, not just final output.
- **Manual, live:** Execute the exact bounded trial procedure written by this milestone in `docs/v2/evidence/codex-live.md` against the current release; retain commands, outputs, timestamps, versions and observations. Never replace a required live observation with a replay pass.
- **Acceptance:** Run the documented transcript in docs/v2/evidence/codex-live.md and sh tests/v2/run.sh codex-live-replay; observed question/handoff/verification/Git application/continuation are tied to this release.
- **Acceptance:** Non-Git work starts without initializing Git; unavailable exclusivity preserves accepted output and blocks only applied dependencies, with no fabricated native continuity or unsupported success claim.
- **Acceptance:** The focused and standing checks above pass on the resulting tree, and `git diff --check` is clean. Report every check as passed, failed or unrun with its output; an unrun required check prevents completion.

## 9. Definition of done and evidence

All §8 conditions hold. Append the existing ten-topic handoff in this exact order under Completion evidence: (1) what changed; (2) files added/modified/deleted; (3) interfaces and architecture pointer; (4) checks run with outcomes/output; (5) checks not run and why; (6) known limitations; (7) deviations and decision entry; (8) commit hash/message and merge commit on main if performed; (9) observed `git status --short`; (10) exact next step/eligible milestone. Include current executable/source provenance and this milestone's fixture/live captures. Do not substitute prose assurances for observed output. Commit only a coherent passing change; unresolved required checks keep the milestone incomplete.

## 10. Risks and decisions this milestone settles

- **Boundary risk.** Codex-specific capability limits must be surfaced rather than replaced with Claude behavior assumptions; unavailable live prerequisites leave this milestone incomplete.
- **Single-session limit.** No milestone is intended to need a second implementation session. If investigation makes the estimate too large, split before broadening: finish a coherent verified boundary, create peer `M48-b.md` depending on `M48`, update the single plan graph and record the exact unfinished scope. Apply the existing split rule: more than about 15 checklist items or more than two pre-existing implementation files beyond the introduced ones requires reconsideration. Never mark the original complete with unmet acceptance.
- **Authority.** Internal factoring is an engineering choice; changed product policy belongs in `docs/v2/02-decisions.md` OPEN DECISIONS with the concrete evidence. Do not silently narrow §3's guarantees.

## 11. Handoff and next milestone

M49 is next in the dependency chain; it is `run` only after this milestone's required evidence holds and all gates/entry conditions are clear, otherwise `held` by the named gate or `wait` for its dependency. The next coding session is initiated by the person or authorized controller, not by this session.

**HANDOFF NOTE:** None. Every fact needed later must already be in source, tests, `docs/ARCHITECTURE.md` §10, decisions or retained repository evidence before this milestone closes.

## Recovery procedure

Read this brief and inspect `git status --short`, actual §5 files, §6 interfaces and the repository evidence/check outputs. If Completion evidence is non-empty or any proposed file exists, run the relevant entry/focused checks and resume the first unfinished checklist item; do not redo an external action whose outcome is uncertain. Inspect its durable operation before any retry. No completion claim without every §8 criterion. No sibling brief or remembered conversation is needed.

## Completion evidence

## Copy-ready session prompt

```
You are implementing milestone M48 of Baton in this repository. Baton V2 is a personal local macOS controller with a deterministic Swift semantic core and implemented Claude Code and Codex adapters; it carries work through managed sessions, independent verification and safe local application. Demonstrate the implemented Codex adapter carrying bounded unfamiliar-project work through question, verified result and continuation. Implement only docs/milestones/M48.md.

WHAT ELSE IS IN FLIGHT. Runs alone unless the dispatch says otherwise.

STARTUP ORDER. Read CLAUDE.md, CONTEXT.md, CONTRACT.md as the Baton development workflow, this brief in full, then the exact spec/decision/architecture references in §3. Inspect the working tree and §6 interfaces. Verify every §2 precondition with its named commands before writing implementation. Do not read a sibling brief for missing context. If Completion evidence is non-empty or milestone files exist, follow Recovery procedure and resume only the unfinished work.

WHAT TO SETTLE RATHER THAN INHERIT. Codex-specific capability limits must be surfaced rather than replaced with Claude behavior assumptions; unavailable live prerequisites leave this milestone incomplete. Complete V2 implementation, current source-matched release and actual authorized Codex profile; live proof must be independently reproducible without Claude trial notes. Version/environment-bound observed Codex workflow, directory conflict evidence and sanitized replay captures. Use current repository code/tests and versioned evidence for physical paths and external capabilities. Persist any engineering choice in the architecture registry and decision entry; §3 names the exact product contract and §10 names when a policy question or split is required.

CONSTRAINTS. Never use Python. Never run build/compile commands without explicit authorization; tests must not compile/sign/install implicitly and a stale executable cannot verify current source. Use deterministic Swift control logic and system facilities; no speculative packages or framework. Preserve unrelated edits, stage only your own paths by name, never git add -A. Use existing Git identity, neutral messages and no authorship trailers. Never change Reclaim or another real target; only this brief's isolated fixtures/trials are in scope. Do not activate V2, migrate live V1 state, invent credentials, publish/push or send external messages. Production target work, credential creation, cross-runtime live conversation transfer and deliberately consuming allocation. Exact input is data. Fixtures are not live provider evidence. A missing required permission/access/check is recorded unrun; do not bypass it.

VERIFICATION. Execute §8 in order with the explicitly prepared current executable. Record each outcome and output as passed, failed or unrun; unrun never means passed. Verify intermediate invariants, not just final goldens, and use separate live captures wherever required.

CLOSE-OUT, in this order, once the M48 checklist is finished.
1. Run /review-2 on this session's changes and a hand pass over §7–§8; then /address using local commits and no remote. Rerun the checks affected by fixes.
2. Append the ten-topic handoff under Completion evidence, update docs/ARCHITECTURE.md §10 with discoverable interface/check facts and add the next free decision entry in docs/DECISIONS.md. Commit only this milestone's paths with the existing identity.
3. Follow CONTRACT.md clause 3 for an authorized Baton development close-out: merge to main in the actual canonical checkout, run sh tests/run.sh and sh tests/v2/run.sh all against its current explicitly prepared executable; record merge-failed or main-broken if unresolved. Do not infer compile authority from this step.
4. Only with every §8 criterion held, update this milestone's Status to done in docs/MILESTONES.md. Refresh any eligible successor prompt from facts already persisted in the repository, retaining its exact slot paragraph; no private handoff knowledge. Keep the worktree. Commit by named paths. Installation requires its actual current authority: do not activate V2 or invoke compile/sign/install implicitly as part of this documentation convention.
5. Do not start M49 or target work. If incomplete or oversized, follow §10 and the existing split rule, retaining coherent work and naming the remainder; never report done for missing evidence.
6. If this development session is actually managed by V1 Baton, write its handover per CONTRACT.md using the real injected session ID, canonical checkout and observed merge hash, .tmp then rename; list every now-eligible milestone with gate/dependency disposition and brief pointer, and print it verbatim last in a baton fence. The planned successor is M49, docs/milestones/M49.md, heading Copy-ready session prompt. Never invent a V1 session ID for a manual/Codex session; retain the same handoff facts in this brief and return the observed next step locally. This repository development handover is not V2 target reporting authority.
```

