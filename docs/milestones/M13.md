# M13 — Autonomous handoff, and more than one at a time

## 1. Identity

- **ID:** M13. **Objective:** let close-out dispatch all newly eligible work up to the cap,
  without a human gate, and select reasoning effort from the brief's declared Size.
- **User-visible result:** an independent pair overlaps in time and its dependent successor
  finishes without human action; small work receives an appropriate effort through the existing seam.
- **Size:** Medium, about 3–4 hours; high effort for admission and parallel close-out correctness.

## 2. Dependencies and entry conditions

M12 complete and installed. A confirmed goal can already produce a plan, conforming cold-session
briefs and an automatically seeded first milestone. Dispatch already uses managed worktrees and
records baseline, changed paths and a Baton-produced check result. The existing cap and candidate
intersection remain the foundation. M15 can run independently after M12; it is not an entry condition.

## 3. Required reading

`CLAUDE.md`; `CONTEXT.md`; `CONTRACT.md`; `docs/v1.1/SCOPE.md` in full, especially §6 M13 and §7;
`docs/MILESTONES.md`; `docs/SPEC.md` §2.3, §2.6, §2.7, §2.9; `docs/ARCHITECTURE.md` §4.1 steps
6–8, §6.2, §6.4, §7, §10; `docs/DECISIONS.md` D-071, D-072, D-095, D-096; M12 completion
evidence and its decisions; `lib/candidates.sh`, `lib/tick.sh`, `lib/dispatch.sh`;
existing cap, wait, omitted and repeat fixtures; current plan-generation handover template.

## 4. Requirements, scope, non-goals

- **Covers:** SCOPE §6 M13 and §8 item 5; extends REQ-DISPATCH-01, -02 and REQ-TICK admission.
- **Scope:** automatic close-out-to-candidate progression; all newly eligible work up to the
  existing cap; independent branches overlapping; effort selected from the existing brief's Size
  declaration and passed to `claude_bg`.
- **Non-goals:** a second scheduler, changing genuine target gates without authority, budget
  pacing (M14), escalation classification or goal checking (M15), and cross-project dependencies.

## 5. Expected files (proposed)

`lib/candidates.sh`; `lib/dispatch.sh`; a narrow `lib/tick.sh` integration only if required
(assess the split rule before a third existing library edit); `tests/scenarios/autonomous-*`,
`tests/scenarios/effort-size-*`; generation/close-out templates where they emit successor names;
`docs/SPEC.md`; `docs/ARCHITECTURE.md` §10 row M13; `docs/DECISIONS.md`; this brief and successor
prompt refreshes. Outside the repository: only fixture worktrees and normal Baton run records.

## 6. Interfaces

- **Introduces:** Size-to-effort selection and automatic admission of the complete newly
  eligible set after close-out, using the existing dependency graph and cap.
- **Consumes:** `dispositions_in_force`, `dispositions_intersect`, `cap_order`, `dispatch_run`,
  `claude_bg <worktree> <name> <model> <effort> <settings> <prompt>`, plan Effort tokens and
  brief Size. Record the actual selected effort in the dispatch evidence.

## 7. Implementation checklist (ordered)

1. Trace a consumed completion to admission. Build a three-milestone fixture with independent
   A and B and C depending on both; record the current behavior and the gap to unattended execution.
2. Feed every newly eligible named milestone through the current intersection and cap. Keep
   explicit successor names in generated close-outs so no eligible work disappears into `omitted`;
   retain repeat protection, done filtering and dependency checks.
3. Make slot context and in-flight accounting include work admitted earlier in the same tick.
   An independent pair must get separate worktrees and correct parallel-run constraints; each
   close-out rechecks combined main before claiming completion.
4. Settle a deterministic mapping from declared Small/Medium/Large Size to supported effort
   tokens, how an explicit plan Effort overrides it, and the fallback for older briefs with no
   Size. Keep the existing model selection; do not build a new model router.
5. Run the fixture chain without a human action after its initial seed. Capture both start and
   completion times proving overlap, then C's completion after both dependencies.
6. Run §8, document the delivered autonomous continuation interface for M15's replan integration,
   review, fix and close out.

## 8. Fixtures, verification, acceptance

- **Fixture-run:** independent pair/join, cap one versus cap two, multiple eligible items,
  repeated close-out, successor omission, same-tick slot context, explicit effort override,
  every Size mapping and missing-Size fallback; each twice, then `sh tests/run.sh`.
- **Manual, live:** one disposable three-milestone project through Baton, with an independent
  pair and its join. After setup/initial seed no person starts, resumes, merges or approves a lane.
- **Acceptance:** all three milestones complete without human action, the independent pair
  overlaps, no cap is exceeded, and the selected effort reaches the existing dispatch seam.

## 9. Definition of done and evidence

Ten-item handoff with the fixture graph, start/end timestamps and session IDs, selected effort
and its source, handovers and verified merge/check records. A serial trace or mocked overlap is
not the live overlap proof; record unrun live work as unrun.

## 10. Risks and decisions this milestone settles

- **Eligibility is not a handover disposition.** The current relay escalates omitted names;
  preserve complete successor naming while making the chain automatic.
- **Parallel does not mean shared writes.** Separate worktrees and staged paths remain standing
  rules; the cap is a ceiling, not permission to ignore a declared conflict.
- **Effort precedence.** Explain Size mapping, explicit token overrides and legacy fallback
  in one place; later pacing must consume this seam, not invent another dispatcher.

## 11. Handoff and next milestone

Apply `CONTRACT.md` clause 3(c) to every refresh below: run `baton status` and check the
dispatch log for open lanes, including lanes absent from status; status also lists open parks. Refresh only a listed
milestone with neither an open lane nor an open park; still name every eligible milestone with its correct disposition.

The direct successor in the current graph is **M14**, path `docs/milestones/M14.md`, heading
`Copy-ready session prompt`. M15 was named by M12 and may be running; do not restart it.

At close-out, after recording this milestone as done, re-read the plan's current Status cells,
dependencies and gates. List every direct successor in `eligible[]`, even if it cannot run yet,
and also every other milestone the plan then makes eligible. Derive each disposition at that
moment, with an open gate taking precedence:

- `held` — an open gate holds it; set `held_by` to that gate's exact name.
- `wait` — no open gate holds it and at least one dependency is not done; set `wait_for` to
  the IDs of all dependencies not yet done.
- `run` — every dependency reads done and no open gate holds it.

Use `docs/milestones/<ID>.md` and heading `Copy-ready session prompt` for each entry. Refresh
on main only listed prompts whose milestones have neither an open lane nor an open park with the admission and effort interfaces, including M15's replan
continuation seam when it is listed. Compute from the live plan, not an assumed sibling order;
never change dependencies or add a gate to obtain a disposition. Baton initiates successors;
this session does not.

## Recovery procedure

Inspect the current candidate/cap code, Size mapping, run records and this brief's evidence.
Resume only unfinished checks or implementation. Do not seed or dispatch a lane whose run is
already recorded, and do not count a serial or manually advanced trace as §8 acceptance.

## Completion evidence

1. **What changed.**
   - The dispatched effort gained a second source. `dispatch_one` falls back from a blank plan
     `Effort` cell to the Size the milestone's brief declares in its `## 1.` section on `main`,
     mapped `Small` → `medium` and `Medium` or `Large` → `high`. The cell still wins. A Size that is
     not one of the three — absent, misspelt, or a phrase such as M16's "Live trial" — yields no
     effort, `claude_bg` leaves the flag off, and the project is not parked (D-174, REQ-DISPATCH-13).
   - The chain's admission needed no change and got none. A handover naming both halves of an
     independent pair already produced two candidates that the existing cap admitted in one tick,
     and `intersect_verdicts`' `wait` branch already promoted the join the moment its named waits
     read `done`. `lib/candidates.sh` is untouched; §5 listed it as expected and it was not needed.
   - `dispatch_run` re-reads `claude agents --json` before each dispatch after the first, so the
     second of a co-dispatched pair names the first in its slot line. Before this, the pass read the
     listing once and both halves were told "Nothing else is in flight" about each other — which is
     `docs/v2/01-findings.md` finding 24, whose recorded evidence was three of the expectations this
     milestone moves. A listing that cannot be re-read ends the pass rather than being replaced by
     the stale one (D-175).
   - The slot line names a peer's whole worktree path instead of its basename, which under M09's
     managed root is the milestone's own id and so said nothing (D-176, amending REQ-DISPATCH-05's
     "never paths").
   - Seven scenarios, and `tests/shim/claude` gained `agents.fail-at` so the refused re-read could
     be driven at all.

2. **Files added / modified / deleted.**
   - Modified: `lib/dispatch.sh` (adds `brief_size`, `size_effort`; the effort fallback in
     `dispatch_one`; the `do_also` path), `lib/tick.sh` (`dispatch_run`'s per-dispatch listing and
     `drn_started`), `tests/shim/claude` (`agents.fail-at`), `docs/SPEC.md` (REQ-DISPATCH-13 added,
     REQ-DISPATCH-05 amended), `docs/ARCHITECTURE.md` (§4.1 step 8, §4.3, §10 row M13),
     `docs/DECISIONS.md` (D-174 to D-176), this brief.
   - Added: `tests/scenarios/{autonomous-pair-join,autonomous-join,autonomous-cap-one,autonomous-omitted-successor,autonomous-listing-refused,effort-size-mapping,effort-size-precedence}/`.
   - Modified expectations, each for one of exactly two reasons and read before freezing:
     `cap-two-of-three`, `omitted-resolves-on-handover` (one extra listing call);
     `held-plan-wins`, `fable-reserve-holds` (a co-dispatched peer now named — finding 24's own
     evidence); `dispatch-also-in-flight`, `main-broken-cascade`, `omitted-edit-dispatches`
     (a worktree path where a basename stood).
   - Deleted: nothing. `lib/candidates.sh` unchanged.
   - Two counted `lib/` files, at the split rule's budget; `lib/planning.sh` was deliberately not
     touched, for the reason D-174 records.

3. **Interfaces introduced or changed.** `brief_size <checkout> <milestone>`,
   `size_effort <size>` — `docs/ARCHITECTURE.md` §10 row M13. `dispatch_one` and `dispatch_run`
   change behaviour without changing signature; no new event, field or config key. **M14's seam is
   `size_effort` and the `Effort`-cell-then-Size order in `dispatch_one`**: pacing consumes that,
   never a second resolution beside it. **M15's replan seam is unchanged** — eligibility still comes
   from the plan and dispositions from the handover, and a replan that rewrites the plan is admitted
   by the same intersection.

4. **Checks run, with outcomes.**
   - `sh tests/run.sh` on the branch: **passed**, `398 scenarios, 0 failed`. Run four times across
     the work; the last, after the review fixes, is the one that stands.
   - Every §8 fixture case, each run twice by the harness: independent pair (`autonomous-pair-join`),
     join (`autonomous-join`), cap one versus cap two (`autonomous-cap-one` against
     `autonomous-pair-join`), successor omission (`autonomous-omitted-successor`), same-tick slot
     context (`autonomous-pair-join`, and negatively `cap-two-of-three` where the peer is another
     project's and is not named), every Size mapping (`effort-size-mapping`), explicit effort
     override and missing-Size fallback (`effort-size-precedence`), the refused re-read
     (`autonomous-listing-refused`). **All passed.**
   - `brief_size` against all 24 of this repository's own briefs: **passed**. Every brief that
     declares a Size and an effort agrees with the mapping, and the mapping reproduces every
     non-blank `Effort` cell in `docs/MILESTONES.md`. M14, the one row where Size and cell differ,
     takes the cell. M16's "Live trial" and M01's absent Size both yield no effort.
   - awk interval-expression portability, checked empirically on this Mac's `/usr/bin/awk`
     (`awk version 20200816`): intervals are supported, and the `\*{0,2}` form was removed anyway
     because `[-*+ \t]*` already allows what it allowed.
   - **Manual, live (§8): passed.** A disposable three-milestone project `Trio` at
     `/Users/danny/.claude/jobs/487f37f5/tmp/live/Trio`, graph `M01` and `M02` independent and `M03`
     depending on both, registered in a disposable `BATON_HOME` with this branch installed into it
     (`BATON_HOME=<tmp>/live/home BATON_INSTALL_TEST=1 sh install.sh`, the seam D-113 documents). A
     separate home because the production cap was full with `Baton/M10-b` and this session; the
     production home was not written, and the deny rail was composed from **both** homes' rules so a
     live session could not reach the real one either. Driven only by `baton tick` on the same sixty
     seconds launchd uses. After the registration's seed no person started, resumed, merged or
     approved a lane.

     | | dispatched (Baton's clock) | effort | its own `written_at` | `merged_as` |
     |---|---|---|---|---|
     | M01 | 2026-09-19T17:00:32Z | `medium`, from Size `Small` | 17:01:44Z | `e055fc2` |
     | M02 | 2026-09-19T17:00:36Z | `medium`, from Size `Small` | 17:01:45Z | `838c8c1` |
     | M03 | 2026-09-19T17:02:46Z | `high`, from Size `Medium` | 17:04:06Z | `5ccf60a` |

     Session ids `48486317-ac62-414e-9c35-cb5b1962a9d8`, `e9e7d7a4-8bd3-493f-a3cc-e4f97facd6af`,
     `e38e53eb-fa94-47f4-a902-867b17c08a54`. **Overlap:** M02 started at 17:00:36Z, 68 seconds
     before M01 ended at 17:01:44Z — two live sessions, not a serial trace and not a mock. Separate
     worktrees `<home>/worktrees/Trio/{M01,M02,M03}` on branches `m01`, `m02`, `m03`, all from one
     baseline `387df60`. M02's slot line named `M01 (worktree <home>/worktrees/Trio/M01, brief
     docs/milestones/M01.md)`; M01's said nothing else was in flight, which at 17:00:32Z was true.
     Tick 1 dispatched the pair; tick 2 printed nothing, because both sessions were between writing
     `done` in the plan and writing their artifact and the `midrun` suspension withheld the
     `omitted` park rather than paging a person; tick 3 consumed both and dispatched M03; a later
     tick consumed M03. The whole log is **three `dispatch` and three `consumed` events and nothing
     else** — no escalation, park, resume, redispatch, rejected artifact or `plan_override`, cap 2
     never exceeded. Every completion was proved by Baton itself: baseline → candidate →
     integration, one in-scope changed path each, and `sh check.sh` re-run by Baton on each merge
     commit, `outcome: passed`. Transcript at `<tmp>/live/transcript.txt`.

5. **Checks not run, and why.**
   - **`/review-2` did not deliver a report: unrun, not passed.** Three reviewer subagents were
     spawned (a debugger and a code-auditor in parallel, then one more after the first two failed to
     report). All three ran to completion and reached `idle`, and not one report reached this
     session: no task notification arrived for any of them over ~40 minutes, and two rounds of
     `SendMessage` asking each to reply returned success with no reply. The `review-2` skill's own
     Read-Only Protocol instructs agents to send to "the team lead", which is not a valid recipient
     name, and that may explain the first two; the third was told explicitly to return its report as
     its final message and did not arrive either, so it does not explain all three. **The hand pass
     required beside `/review-2` did run, and is the review of record.** It found and fixed two
     things, both in commit `b5f6fcc`: the Size matcher taking the first §1 line holding `Size:`
     anywhere rather than one that declares it, and the refused re-read having no fixture. It also
     confirmed, by running them: that `brief_size` returning non-zero does not abort `dispatch_one`
     under `set -eu`; that `size_effort`'s `case` treats a `*` argument as data; that `/^## 1\./`
     does not match `## 10.`; that `plan_row`'s `--arg effort` makes a blank cell `""` and never jq
     `null`, which is what the fallback triggers on; and that `do_rows` reaches exactly one place
     inside `dispatch_one`, so the re-read cannot move a cap or ordering decision.
   - No second live trial, and no live trial of the refused re-read: the fixture drives that branch
     deterministically and a live one would need the CLI to fail on a chosen call.
   - Nothing was run against Reclaim.

6. **Known limitations.**
   - The `Size:` label is matched case-sensitively, so a brief writing `size:` declares none. This
     matches M12's generation check, which greps the same spelling and would report the brief as
     defective.
   - `brief_size` costs one `git show` per dispatch, duplicating a read `prompt_from_brief` makes a
     moment later. One process per dispatch against a document already in git's object cache; not
     worth a shared read that would tie the two functions together.
   - The mapping has no route to `low`, `xhigh` or `max` from a Size. Only the `Effort` cell reaches
     those, which is what the column is for. Nothing in this repository's briefs asks for more.
   - A refused re-read ends the whole dispatch pass, including candidates of other projects that
     would have been admitted. The cap is the Mac's and is counted against that same listing, so the
     conservative reading is the deliberate one; the cost is one tick.
   - The live proof ran in a disposable `BATON_HOME` rather than the production one, because the
     production cap was full. What it therefore does not exercise is two projects' lanes competing
     under one cap — which `cap-two-of-three` covers as a fixture.

7. **Deviations from the brief, with their decision entries.**
   - `lib/candidates.sh`, named first in §5, is unchanged: §7 item 2 says "keep" the existing
     intersection and cap, and measurement showed they already fed every named successor through.
     `lib/tick.sh` was the third counted file §5 anticipated; the count stands at two, so no split
     was needed.
   - `lib/planning.sh`'s generation template, also named in §5, was deliberately left alone. D-174
     records why: telling a planning session that the Size picks the effort invites it to write
     `Large` to obtain `high` rather than to describe the work, corrupting the one field the mapping
     reads, and a truthful Size already yields the right effort.
   - One incidental documentation correction outside the milestone's subject: `docs/ARCHITECTURE.md`
     §4.1 step 8 still described worktree creation at the pre-M09 sibling path
     `../<Project>-M<nn>`. It is two lines above a paragraph this milestone rewrote, REQ-DISPATCH-03
     already states the current design, and specs state the current design only. No decision entry:
     nothing was decided, a stale sentence was corrected.
   - `tests/shim/claude` gained a knob, which §5 did not name. It is what makes the refused re-read
     testable; `agents.fail-at` is inert in all 397 other scenarios.

8. **Commit information.** `5e18142` "Select the dispatched effort from the brief's Size, and name a
   co-dispatched peer"; `b5f6fcc` "Read the Size off a line that declares one, and cover the refused
   re-read"; plus the commit carrying this evidence and the decisions. Branch `m13`. The merge commit
   on `main` is the handover artifact's `merged_as`.

9. **Working-tree state as observed.** Recorded from `git status --short` at close-out, in the
   handover section below, never assumed.

10. **Exact next step, and the next eligible milestone.** Nothing is started by this session. Baton
    initiates successors. The plan's direct successor of M13 is **M14** (`Budget-aware pacing`,
    `docs/milestones/M14.md`, heading `Copy-ready session prompt`), whose only dependency is M13, so
    with M13 `done` it reads `run`. The `eligible[]` array of the handover artifact is computed from
    the plan as it stands after M13 is marked `done`, and matches this.

## Copy-ready session prompt

```
You are implementing milestone M13 of Baton at /Users/danny/Documents/Apps/Baton. Baton is a personal POSIX-shell relay on one Mac; its tick moves work between Claude Code sessions using a mailbox, log, plan and existing cap. Make close-out dispatch every newly eligible milestone up to that cap without a human gate, and select effort from declared Size through the existing claude_bg seam. Size: Medium, about 3–4 hours, high effort. Keep all model work on the subscription CLI, no direct API calls.

WHAT ELSE IS IN FLIGHT. Runs alone unless the dispatch says otherwise.

STARTUP ORDER. Read CLAUDE.md, CONTEXT.md, CONTRACT.md, this brief in full, docs/v1.1/SCOPE.md, and M13 §3's source and evidence. Then read lib/planning.sh and lib/tick.sh's self-check loop, because M12 put a second producer of candidates into the tick and you are about to change what happens to them. M12 is complete and installed: a project that owes a plan has one planning session dispatched for it on the reserved lane id M00-plan, its output is measured and adopted through onboard_commit's own seeding path, and the tick's list now carries candidates from two places — planning_pass, called before each project's self-check, and dispositions_intersect, called after it. Managed worktrees and baseline/scoped-change/Baton-check completion are already present. M15 may run independently. Inspect working tree and status; follow Recovery procedure if evidence or autonomous/effort-size work exists.

WHAT TO SETTLE RATHER THAN INHERIT. Use the existing graph, dispositions, cap and dispatch_run. Explicit handover names remain necessary because omitted eligible work parks. Settle Size-to-effort mapping, explicit plan Effort precedence and missing-Size fallback; pass the result into claude_bg, which already accepts effort. **The precedent for an effort that does not come from a plan row already exists and is yours to match rather than invent**: dispatch_one takes the planning lane's model and effort from config.json through planning_model and planning_effort (planningModel, planningEffort), branching on the lane's id, and the dispatch event records what actually ran. A Size-derived effort is the same shape of decision made from a different source, so put it where that branch already is rather than beside it. Two more things M12 settled that you must not undo: a candidate is withheld while a lane-scope park stands on its lane, for the planning lane as for a milestone's (D-169), and a dispatch that produced no session writes dispatch_failed and not dispatch, so attempt_of does not move and a bound counted on attempts does not bound a failing dispatch. Keep dependency and repeat checks, same-tick in-flight context, separate worktrees and merged-main verification. Prove A and B overlap and their dependent C completes with no human advancement. Budget pacing is M14; escalation/scope checks are M15.

CONSTRAINTS. Never Python; POSIX shell with set -eu, jq -e, awk and git, no packages or rewrite. Only the disposable three-milestone project's live sessions in §8 may run; never touch Reclaim. Keep the log single-writer, deny rules, flagless resumes and subscription pricing. Work in your assigned worktree, stage own paths by name, never git add -A; accommodate concurrent edits. Commit locally with existing identity, neutral messages and no authorship trailers; take the next free D-number. Do not add an approval gate or change genuine target holds to make a proof pass.

VERIFICATION. Follow M13 §8; record start/end timestamps, IDs and effort. Run sh tests/run.sh. Every check is passed, failed or unrun with output; no unrun proof is passed.

CLOSE-OUT, in this order, once the M13 checklist is finished.
1. Run /review-2 with a hand pass; record findings and run /address with local fixes/commits, then sh tests/run.sh.
2. Append the ten-item docs/MILESTONES.md handoff under Completion evidence, update docs/ARCHITECTURE.md §10 row M13 and docs/DECISIONS.md, and commit on the assigned branch.
3. Merge into main at /Users/danny/Documents/Apps/Baton. On failure write stopped with reason merge-failed and stop. Run sh tests/run.sh on main; fix failures or write stopped with reason main-broken.
4. On main mark only M13 done after all acceptance holds, then re-read the current plan and derive the handover entries under §11. Establish open lanes and open parks by baton status and CONTRACT.md clause 3(c)'s dispatch-log check. Refresh only listed prompts whose milestones have neither an open lane nor an open park, parts 1 and 3–7, part 4 additively, leaving part 2 verbatim; carry the actual interfaces and evidence named in §11 forward. Leave this worktree for the standing retention policy, commit only your paths, and run BATON_HOME=/Users/danny/.baton sh install.sh from the canonical checkout. Record any differing-agent or notifier warning.
5. Do not start M14 or M15. Do not mark M13 complete without every §8 criterion; follow the plan split rule and write stopped with reason unfinished if required.
6. Immediately before writing the handover, re-read the plan as it stands now, after this milestone is recorded done. Put every direct successor in eligible[], including successors not yet runnable, plus every other milestone the plan now makes eligible. Compute each disposition from current Status cells, dependencies and gates: an open gate takes precedence and means held with held_by naming that gate; otherwise any dependency not done means wait with wait_for listing all unfinished dependency IDs; otherwise every dependency is done and no open gate holds it, so use run. Do not assume sibling completion order or copy an earlier disposition. Use each docs/milestones/<ID>.md brief path and heading Copy-ready session prompt, and refresh and commit any newly listed prompt on main before writing only if its milestone has neither an open lane nor an open park under CONTRACT.md clause 3(c). Follow §11 for the current direct successor names; use eligible [] only when no direct successors or other eligible milestones remain. Write /Users/danny/.baton/inbox/M13-$CLAUDE_CODE_SESSION_ID.json, .tmp first then rename, with baton 1, canonical project /Users/danny/Documents/Apps/Baton, milestone M13, session from CLAUDE_CODE_SESSION_ID, outcome complete, merged_as the merge commit on main, written_at and the computed eligible array. Follow the installed completion/guard rules without inventing Baton check results. Print the artifact verbatim, last, in a baton fence; deal with anything after it and print it again.
```
