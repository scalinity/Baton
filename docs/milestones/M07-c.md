# M07-c — The Mac message: Claude's icon, and a click that opens the session

## 1. Identity

- **ID:** M07-c. **Objective:** the Mac message Baton raises for a park or a notification carries
  Claude's icon and, when clicked, opens the session it is about in Claude.app — instead of arriving
  as Script Editor's notification and opening a blank AppleScript document.
- **User-visible result:** an escalation or notification appears under a "Baton" sender with Claude's
  icon; clicking it opens the lane's session in Claude.app's Code tab (or, with several open, the
  newest park's session, or Claude.app's own "needs input" view — whichever §7 item 1 settles).

## 2. Dependencies and entry conditions

M07-b complete and installed (both edit `install.sh`, so they run one after the other). Every
dispatched session is on Remote Control (D-081), so every session has a claude.ai session id.

## 3. Required reading

`CLAUDE.md`; `CONTEXT.md`; `CONTRACT.md`; `docs/SPEC.md` §2.5 (REQ-ESC-02, 03), §2.11 (REQ-SETUP-05,
08), §4 item 7, §6; `docs/ARCHITECTURE.md` §2 (the installed relay), §4.3; `docs/DECISIONS.md` D-005,
D-018, D-079, D-081; `lib/notify.sh` in full.

## 4. Requirements, scope, non-goals

- **Covers:** REQ-ESC-02 (how the Mac message is raised), REQ-SETUP-05 and 08 (what the install
  builds and the one notification permission a fresh Mac grants), SPEC §4 item 7 amended to let
  Baton read one more thing from a transcript — the newest `remote_session_change` URL.
- **Scope:** a notifier applet built by `install.sh`; `notify` posting through it; the click opening
  the session; fixtures; the live proofs.
- **Non-goals:** a third-party package (`terminal-notifier` and the like); committing Claude's icon
  to the repository — it is copied from the installed Claude.app at install time, because it is
  Anthropic's mark and this is a personal tool on one Mac; changing what a message says (REQ-ESC-03
  stays).

## 5. Expected files (proposed)

`notify/Baton.applescript` (the applet's source); `install.sh` (build the applet with `osacompile`,
copy the icon, set the bundle id, sign it ad hoc); `lib/notify.sh`; the osascript seam's shim
(`tests/shim/`); `tests/scenarios/{notify-*}/` and the `install` scenario; `docs/SPEC.md`;
`docs/ARCHITECTURE.md` §2, §4.3, §10 row M07-c; `docs/DECISIONS.md`; this brief.

## 6. Interfaces

- **Introduces:** `~/.baton/bin/Baton.app`; a per-message spool the applet reads; `session_url
  <session>` (the claude.ai URL from the transcript, empty when there is none).
- **Consumes:** `notify`, `notification_write`, `escalate`, `message_render`, the transcript glob.

## 7. Implementation checklist (ordered)

1. Measure before building, live, with the person at the Mac: (a) a notification posted by an applet
   built with `osacompile` shows the applet's name and icon, not Script Editor's; (b) clicking it
   launches or activates the applet, and the applet can tell a click launch from a post launch; (c)
   `open 'claude://code/session_<id>'` opens that Remote Control session in Claude.app — the app
   gates it behind a feature flag — and `https://claude.ai/code/session_<id>` is the fallback; (d)
   what the first notification from a new applet asks macOS for. If (a) or (b) fails, stop and ask the
   person in the session with `AskUserQuestion` (D-089): the fallback is a small Swift helper using
   `UNUserNotificationCenter`, which is D-005's named moment and a build step, so it is the person's
   call.
2. The applet: on launch, post every spooled message and record each one's target; on a launch with
   nothing spooled — a click — open the target of the newest message still standing, or
   `claude://code/needs-input` when there is none.
3. `install.sh`: build `~/.baton/bin/Baton.app` from `notify/Baton.applescript`, copy
   `/Applications/Claude.app/Contents/Resources/electron.icns` into it when that file exists (and keep
   the applet's own icon when it does not), set a bundle identifier, sign ad hoc, idempotently.
4. `notify`: spool the message with its target and launch the applet; keep `osascript` as the path
   when the applet is missing, so a failed install never silences the relay.
5. `session_url`: the newest `remote_session_change` URL in the session's transcript, found by the
   same glob derivation 3 uses; SPEC §4 item 7 and a decision entry say so.
6. The fixtures, twice each: a message with a session URL, one without, the applet missing.
7. `sh tests/run.sh`, `/review-2`, `/address`, completion evidence, the handover.

## 8. Fixtures, verification, acceptance

- **Fixture-run:** every scenario in §7 item 6, twice; the `install` scenario.
- **Manual, live:** §7 item 1, then a real park on a fixture session raising the new message, and a
  click opening that session.
- **Acceptance:** the message shows Claude's icon under Baton, and a click opens the session.

## 9. Definition of done and evidence

Handoff per template with a description of the notification as shown, the click's result, and the
applet's `Info.plist` identifier.

## 10. Risks and decisions this milestone settles

- **The icon is Anthropic's mark.** Copied at install from the Claude.app on this Mac and never
  committed; the applet is not distributed.
- **One target per click.** A notification posted through `display notification` carries no data,
  so a click cannot say which message it was; settle what the applet opens when several stand.
- **A new sender needs the person's permission once** (System Settings › Notifications); REQ-SETUP-08
  records it.

## 11. Handoff and next milestone

M07-d with disposition `run`, and nothing else.

## Recovery procedure

Inspect `notify/`, `install.sh` and `lib/notify.sh`; resume from the first unfinished checklist item;
no completion claim without every acceptance criterion.

## Completion evidence

## Copy-ready session prompt

```
You are implementing milestone M07-c of Baton at /Users/danny/Documents/Apps/Baton. Baton is a relay: a personal tool for one person on one Mac, on Claude Code 2.1.270, that carries a build from one Claude Code session to the next — a launchd-run tick every sixty seconds that reads its inbox, its dispatch log, the target project's plan file and one git check, then dispatches, resumes, waits and escalates by fixed rules; it embeds no model call. M07-c makes the Mac message carry Claude's icon and open the session it is about when clicked.

WHAT ELSE IS IN FLIGHT. Runs alone unless the dispatch says otherwise.

STARTUP ORDER. Read CLAUDE.md, then CONTEXT.md, then CONTRACT.md, then docs/milestones/M07-c.md in full, then the SPEC, ARCHITECTURE and DECISIONS sections listed in M07-c §3 and lib/notify.sh in full, then docs/milestones/M07-b.md's completion evidence. Then inspect the working tree before writing anything. If M07-c "Completion evidence" is non-empty or notify/Baton.applescript exists, follow M07-c's Recovery procedure and resume only the unfinished portion.

WHAT TO SETTLE RATHER THAN INHERIT. (1) Whether an applet built with osacompile posts under its own name and icon and is relaunched by a click on its notification — measure it with the person at the Mac before building anything; if not, ask the person in this session with AskUserQuestion, because the fallback is a Swift helper using UNUserNotificationCenter, which is D-005's named moment and a build step. (2) What a click opens when several messages stand. (3) Whether open 'claude://code/session_<id>' opens the session in Claude.app on this account. Facts you do not re-derive: today notify runs osascript -e 'display notification …', which macOS attributes to Script Editor, so the message shows Script Editor's icon and a click opens a blank AppleScript document; Claude.app (bundle id com.anthropic.claudefordesktop) registers the claude:// scheme and its URL handler routes claude://code/<id> to a Code session when <id> matches ^(cse|session)_[A-Za-z0-9_-]+$, gated by a feature flag, and also knows claude://code/needs-input and claude://code/continue?session=<local_…|last>; every dispatched session is on Remote Control (D-081) and its transcript records remote_session_change attachments whose url is https://claude.ai/code/session_<id>, written at the next turn boundary; Claude.app's icon is /Applications/Claude.app/Contents/Resources/electron.icns; /usr/bin/osacompile, /usr/bin/swiftc and /usr/bin/codesign are present. Claude's icon is Anthropic's mark: copy it from the installed Claude.app at install time and never commit it. From M07-b (docs/milestones/M07-b.md completion evidence, D-085 to D-090): a background session's claude.ai id is in ~/.claude/jobs/<job>/state.json as bridgeSessionId (cse_…, the same suffix as session_…) from its first turn, while its transcript recorded the remote_session_change URL only at a later turn boundary — measured on fixture 0801ecf4, no URL until its third turn — so a session_url read from the transcript alone can be empty for a new session; a stopped session's thread is archived on claude.ai until a flagless resume brings it back, so a click on a message about an offline finished session opens an archived thread, and the way back is the Baton · wake session or baton wake <milestone>; the question park's Mac message still tells the person to answer in place with claude attach <job>, which predates every session being on Remote Control and is this milestone's to point at Claude.app if the click makes that the natural route; bin/baton drops every CLAUDE* variable but CLAUDE_CONFIG_DIR before any verb, because a claude process started under a parent session's variables failed to create its Remote Control session; Claude.app groups sessions by repository and lists Baton's under Other, and claude --bg --project is rejected as an unknown option on 2.1.270, so nothing groups them. A question for the person is asked in the session with AskUserQuestion, which the tick parks as question and the person answers in Claude.app; an asking artifact stops and archives the session (D-089).

CONSTRAINTS. Never use Python. Shell only: /bin/sh with set -eu, jq -e, awk, git, and AppleScript compiled by the system's osacompile; no third-party packages; sh tests/run.sh is the standing check and every check is reported as passed, failed or unrun with its output. Never touch /Users/danny/Documents/Apps/Reclaim. Never start, stop, attach to or resume any session except fixture-project sessions this brief's live proofs need, and never one named Baton · Reclaim · anything, nor Baton · wake. A question for the person is asked in this session with AskUserQuestion, never as an asking artifact the milestone could have avoided, because an asking artifact stops and archives the session and leaves the question readable only in JSON. Every resume is flagless. The log has one writer. You are in a worktree Baton created; stage only your own paths by name, never git add -A; merge into main at close-out and refresh there. Commit locally with the existing git identity, a neutral technical message, no authorship trailers, never addressing a person; take the next free D-number at the moment you write the entry.

VERIFICATION. Per M07-c §8. Report every check as passed, failed or unrun with its output; an unrun check is never reported as passed.

CLOSE-OUT, in this order, once the M07-c checklist is finished.
1. Run /review-2 on this session's changes and do a hand pass over the same checklist while the reviewers work. Launch the two reviewers as background agents pinned to Fable rather than as teammates. A fixture you write for a finding should be shown failing before the fix. Record what was run and what returned.
2. When the review report is in, run /address. No git remote; commit fixes locally. Run sh tests/run.sh again after the fixes.
3. Append the handoff to docs/milestones/M07-c.md under "Completion evidence" using the docs/MILESTONES.md template. Add the docs/ARCHITECTURE.md §10 row M07-c detail and the docs/DECISIONS.md entries for what you settled. Commit on m07-c.
4. Merge m07-c into main in /Users/danny/Documents/Apps/Baton; if the merge fails, write ~/.baton/inbox/M07-c-$CLAUDE_CODE_SESSION_ID.json with outcome stopped and reason merge-failed and go no further. Run sh tests/run.sh on main; fix main if it fails, else write stopped with reason main-broken. On main: write done in M07-c's Status cell in docs/MILESTONES.md; refresh the copy-ready prompt of docs/milestones/M07-d.md (parts 1 and 3–7, part 4 additively, part 2 left as the slot line); leave the worktree ../Baton-M07-c in place so this session can be resumed later; commit, staging only your own paths. Then run BATON_HOME=/Users/danny/.baton sh install.sh from /Users/danny/Documents/Apps/Baton, so the tick that consumes your handover runs the relay you merged — the home is named because this session inherits the background service's environment, which carried the M03 probe's BATON_HOME=/Users/danny/.baton/m03-probe/home into M07-b's session and sent its first install there (D-091); if it prints that the installed launchd agent differs, quote that line, because copying the agent and reloading it is a person's act.
5. Do not start M07-d. Do not mark M07-c complete unless every acceptance criterion in §8 holds; a split follows the split rule in docs/MILESTONES.md.
6. Write ~/.baton/inbox/M07-c-$CLAUDE_CODE_SESSION_ID.json (.tmp first, then rename) with baton 1, project /Users/danny/Documents/Apps/Baton, milestone M07-c, your session id, outcome complete, merged_as the merge commit on main, written_at, and eligible listing M07-d with disposition run and brief path docs/milestones/M07-d.md heading "Copy-ready session prompt". Print it verbatim, last, in a fenced block whose info-string is baton; if anything lands after it, deal with it and print it again.
```
