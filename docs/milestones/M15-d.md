# M15-d — Replan: when the plan is what is wrong

## 1. Identity

- **ID:** M15-d. **Objective:** give the `replan` disposition a route, so that a lane whose plan is
  wrong is replanned from the confirmed goal rather than handed to a person.
- **User-visible result:** a milestone that comes back unfinished twice, or one blocked on a
  dependency nothing will ever deliver, produces a new plan instead of a Mac message — and the
  milestones already marked `done` stay `done`.
- **Size:** Medium, about 3–4 hours; high effort. One counted `lib/` file beyond the one M15
  introduced. Recheck that count against the then-current interfaces before editing, as M15 did,
  and split rather than cross it.

## 2. Dependencies and entry conditions

M15 complete and installed: `disposition_of` assigns `replan` to `unfinished-twice` and to `blocked`,
and nothing acts on it yet. M12 complete and installed: `planning_owed`, `planning_pass`,
`planning_validate`, `planning_adopt` and the `plan_generation` event are the generation path this
milestone routes into, unchanged in shape.

M13 is complete and merged as of 2026-09-19 and **is still not an entry dependency**, because what
it settled is not what replan needs settling. Replan asks for a plan to be rewritten while other
lanes of the same project may be running under the plan it replaces. M13 made that situation
ordinary rather than rare — newly eligible work now runs up to the cap, an independent pair
overlaps, and `dispatch_run` re-reads `claude agents --json` before each dispatch after the first so
a co-dispatched sibling is named in the slot line (D-177) — and it is the admission seam to use. But
it says nothing about a lane whose plan is being replaced underneath it, which is this milestone's
question to answer and not M13's.

So: use `dispatch_run`'s re-read as the admission seam rather than adding a second one, and gate the
request on the project having no other open lane, readable from `derive_in_flight`. That gate is
conservative and it is the thing to revisit first if it proves too strict; say in the evidence what
it cost, because a milestone that never replans because a sibling is always running is the failure
mode nobody would notice.

## 3. Required reading

`CLAUDE.md`; `CONTEXT.md`; `CONTRACT.md`; `docs/v1.1/SCOPE.md` §6 M15 and §7;
`docs/MILESTONES.md` including its split rule; `docs/milestones/M15.md` in full, especially §4's
split paragraph and its completion evidence; `docs/SPEC.md` REQ-GENERATE-01 to REQ-GENERATE-10 and
REQ-ESC-12; `docs/ARCHITECTURE.md` §4.5 (the two session roles), §6.2's `plan_generation` row and
§10's M12 and M15 rows; `docs/DECISIONS.md` D-156, D-158, D-161, D-164, D-167, D-169 and M15's own
entries; `lib/planning.sh` in full, `lib/disposition.sh`, `declared_step` in `lib/declared.sh`, and
`onboard_commit` and `onboard_start` in `lib/onboard.sh`;
`tests/scenarios/plan-generate-adopt/`, `plan-generate-brief-defects/`, `plan-generate-exhausted/`
and `unfinished-twice/`; M13's `autonomous-pair-join/` and `autonomous-cap-one/` for what more than one lane at a time now does.

## 4. Requirements, scope, non-goals

- **Scope:** the `replan` route, end to end — the request, what the generating session is told, what
  the validator will accept back, and the record of the replacement.
- **The obstacle, measured by M15 before this brief was written.** The generation path as M12 left
  it cannot preserve completed work, and it is not close to being able to. `planning_validate`
  raises a defect for **every** row whose `Status` is not blank — "`<plan>` row N marks M05 `done`
  before anybody has done it, and Baton never dispatches a row that is not blank" — with the repair
  "leave every Status cell in `<plan>` blank and commit it on main". `planning_prompt` says the same
  thing twice: in `WHO IS ASKING` it opens "This project is registered with Baton and has no plan
  Baton can read", and in `WHAT THE PLAN MUST SATISFY` it states "Status is blank on every row.
  Nothing is done yet." A replan of a project with `done` rows routed through that path unchanged
  would be refused row by row, and its next attempt's prompt would carry, as a concrete repair, the
  instruction to erase exactly the work this milestone exists to preserve.
- **So the first requirement is a distinction the code does not currently make:** between a `done`
  cell that is a claim nobody has earned — which is what M12 is right to refuse from a plan written
  for a project with no history — and a `done` cell the replan is carrying forward, which Baton can
  check against its own record rather than take the session's word for. The dispatch log knows which
  milestones completed: a `consumed` event with outcome `complete` that Baton proved. That is the
  evidence, and it is Baton's, which is the same principle CONTRACT clause 4 applies to
  `check_result` — a session's word about its own work is what Baton is checking.
- **Which conditions ask for a replan.** `disposition_of` already answers this and must not be
  re-derived: `unfinished-twice` and `blocked`. Both are lane parks, so the request is made from a
  standing park rather than from the ending that raised it, which also makes it idempotent — the
  park is the once-key, exactly as it is for `person_acted`.
- **No person approval gate.** The confirmed intent record was the one routine human touchpoint and
  it has already been taken; asking again for permission to act on its consequences is the review
  that gets rubber-stamped, which `planning_prompt` already tells the generating session not to add.
- **Preserve the park until the plan lands.** The provoking park stands while generation runs, so the
  lane does not redispatch under the plan that was wrong. It is closed by the adoption, and by
  nothing else — which is `park_resolve`'s shape and not a new mechanism.
- **Non-goals:** the second judgment role and the `AI-resolvable` route (see §11); host-explained
  gaps (`docs/milestones/M15-b.md`); the scope guard (`docs/milestones/M15-c.md`); replanning a
  project that owes a plan already, which is M12's path and is untouched; any change to what
  `record_only` writes or to the four-disposition table; a second way to compute eligibility.

## 5. Expected files (proposed)

Every path is between backticks, because a completion is proved by at least one changed path falling
inside these spans and a path named only in prose declares nothing (D-150).

One counted library: `lib/planning.sh` — `planning_prompt`'s two hard-coded sentences and
`planning_validate`'s Status rule, both of which have to learn the difference named in §4.
The request itself belongs beside the disposition that names it, in `lib/disposition.sh`, which M15
introduced and which this milestone is free to extend.

Fixtures: `tests/scenarios/replan-request`, `tests/scenarios/replan-preserves-done`,
`tests/scenarios/replan-refused-no-goal`, `tests/scenarios/replan-park-stands` and
`tests/scenarios/replan-adopted`. `tests/scenarios/unfinished-twice` and
`tests/scenarios/blocked-escalates` hold the endings that now route, and move with them.

Documents: `docs/SPEC.md`, `docs/ARCHITECTURE.md` §6.2 and §10, `docs/DECISIONS.md`,
`docs/MILESTONES.md` and this brief.

`bin/baton` and `tests/lib-load.sh` are wiring and are not counted libraries. If anything else under
`lib/` turns out to be required, that is two counted files and the split rule's limit; inventory
before editing and split rather than cross, which is what M15 did to produce this brief.

## 6. Interfaces

- **Introduces:** the replan request — from a standing `replan`-dispositioned park to `plan_owed`
  and the `plan_generation` record — and the preserved-`done` rule in the prompt and the validator.
- **Consumes:** `disposition_of` from `lib/disposition.sh`; `planning_owed`, `planning_pass`,
  `planning_validate`, `planning_adopt`, `planning_record` and `PLANNING_ID` from `lib/planning.sh`;
  the confirmed intent record; `derive_parked`, `derive_in_flight` and the dispatch log's `consumed`
  events; M13's continuation seam when it is available.

## 7. Implementation checklist (ordered)

1. Read `planning_validate` and `planning_prompt` end to end and write down, before changing either,
   what each currently guarantees about a generated plan. The Status rule is not the only assumption
   that a project with history breaks; find the others first.
2. Establish the preserved-`done` set from Baton's own record — proved `complete` consumptions — and
   not from the plan document or from anything a session writes. Fixture a plan that claims `done`
   for a milestone Baton never proved, and require it to be refused exactly as it is today.
3. Teach `planning_prompt` the replan case: what is already built and must not be re-planned, what
   is wrong and why it is being replanned, and that the existing briefs of preserved milestones stay.
   Keep the confirmed intent paragraph verbatim; it is the authority in both cases.
4. Make the request: from a standing park whose disposition is `replan`, write `plan_owed` with the
   reason and the preserved set, record the replacement, and leave the park standing. Repeat ticks
   must make no second request — the standing park and the recorded `plan_generation` are the keys.
5. Close the park on adoption and not before. Prove that a refused generation leaves both the park
   and the old plan exactly as they were, and that the bound in D-167 still applies unchanged.
6. Run §8, document, review and close out.

## 8. Fixtures, verification, acceptance

Fixture: the request from each of the two dispositioned classes; a repeated tick making no second
request; a project with no confirmed goal refusing the request and saying which verb supplies one; a
generation that preserves `done` rows being adopted; one that claims a `done` Baton never proved
being refused with that row named; the park standing across a refusal and closing on adoption. Run
each twice, then `sh tests/run.sh`.

Acceptance: a lane whose plan is wrong is replanned without a Mac message; every milestone Baton
proved complete is still `done` in the adopted plan; the confirmed intent is unchanged; and a
replan cannot clear a drift escalation, which stays `docs/milestones/M15-c.md`'s.

## 9. Definition of done and evidence

Ten-item handoff with the request trace from park to `plan_owed` to dispatch to adoption, the
preserved-`done` evidence and where it came from, the before/after of both `planning_validate` and
`planning_prompt`, the repeated-tick idempotence trace, and the test output. State plainly whether
M13's continuation was integrated or fixtured, and if fixtured, what it would change.

## 10. Risks and decisions this milestone settles

- Preserving completed work must rest on Baton's evidence, never on a plan document's own claim;
  the failure mode is a session marking its own work `done` and being believed.
- A replan while other lanes run is M13's question. Gate conservatively rather than guess.
- Replan preserves confirmed intent and completed work, and it cannot clear a drift escalation.
- More than one counted library beyond `lib/disposition.sh` triggers a split before implementation.

## 11. Handoff and next milestone

Apply `CONTRACT.md` clause 3(c) to every refresh: run `baton status` and check the dispatch log for
open lanes, including lanes absent from status; status also lists open parks. Refresh only a listed
milestone with neither an open lane nor an open park; still name every eligible milestone with its
correct disposition.

M15-d has no direct successor. It is a leaf: `docs/milestones/M15-b.md` depends on M15 and not on
this, and M16 waits on M14 and `docs/milestones/M15-c.md`. Name every other eligible milestone with
its correct disposition and brief pointer. Do not add a dependency to obtain one.

**The second judgment role is deliberately unscheduled.** M15 measured what the `AI-resolvable`
route costs — `lib/dispatch.sh` for a lane with no plan row, `lib/completion.sh` for a declared scope
that is Baton's own, and `lib/inbox.sh` to turn the judgment's artifact into a ruling, three counted
libraries — and recorded it rather than creating a row, so that starting it stays a decision. Do not
create that row as part of this milestone's close-out. If this session learns something that changes
the measurement, record it as a decision entry and say so; that is what a later person would read.

## Recovery procedure

Inspect `lib/planning.sh` against `git diff main`, the registration's `plan_owed`, and the
`plan_generation` events for the fixture project. Resume the first unfinished checklist item without
repeating a request already recorded. Do not claim M15-b or M15-c acceptance as part of this.

## Completion evidence

## Copy-ready session prompt

```
You are implementing M15-d of Baton at /Users/danny/Documents/Apps/Baton. Baton is a relay, a personal tool for one person on one Mac, on Claude Code 2.1.270, that carries a build from one Claude Code session to the next; a launchd-run tick every sixty seconds; it embeds no model call. Implement the replan route: a lane whose plan is wrong is replanned from the confirmed goal instead of being handed to a person, and every milestone Baton proved complete stays done. Size: Medium, 3–4 hours, high effort. The second judgment role and the AI-resolvable route are deliberately unscheduled and are not yours to start.

WHAT ELSE IS IN FLIGHT. Runs alone unless the dispatch says otherwise.

STARTUP ORDER. Read CLAUDE.md, CONTEXT.md, CONTRACT.md, this brief in full, and §3's sources. M15 is complete and installed and is where your entry point is: read lib/disposition.sh before anything else, because disposition_of already assigns `replan` to unfinished-twice and to blocked, and that assignment is yours to act on rather than to re-derive. M15-b has since changed that file twice and neither change is on your route: disposition_of now reads the carries on a second arm, notification:gap, so a disposition is a reading for some classes and a constant for the rest — replan is still a constant for both of your classes and stays one — and record_notification sits beside record_only as the silent record's notification half, taking notification_write's arguments in its order minus the message. tests/scenarios/disposition-table still extracts the class list from class_or_fail and the arm list from disposition_of and set-differences them, so it still pins both of your assignments and will move if you touch an arm. M12 is complete and installed and is the path you route into: read lib/planning.sh in full. Then read M15's completion evidence, which measured the obstacle you are about to remove. Inspect the working tree and follow the Recovery procedure if work exists. Apply the split rule before editing: one counted lib/ file beyond lib/disposition.sh, and the moment a second is required, split instead of crossing.

WHAT TO SETTLE RATHER THAN INHERIT. The generation path cannot preserve completed work as M12 left it, and this is measured rather than suspected: planning_validate raises a defect for every row whose Status is not blank, with the repair "leave every Status cell blank", and planning_prompt states "Status is blank on every row. Nothing is done yet" and opens by telling the session the project "has no plan Baton can read". Routed through that unchanged, a replan is refused row by row and its next attempt is instructed to erase the work you are preserving. What you settle is the distinction the code does not make: a `done` cell nobody earned, which M12 is right to refuse, against a `done` cell the replan carries forward. Establish the second from Baton's own record — a consumed handover with outcome complete that Baton proved — and never from the plan document or from anything a session writes, for the reason CONTRACT clause 4 gives about check_result: a session's word about its own work is the thing being checked. Make the request from the standing park rather than from the ending that raised it, so repeated ticks are idempotent by the park rather than by a new counter. Leave the park standing until adoption closes it, so the lane cannot redispatch under the plan that was wrong. M13 is complete and merged and is still not an entry dependency, because what it settled is not what replan needs settling: it made more than one lane at a time ordinary and gave you the admission seam — dispatch_run re-reads claude agents --json before each dispatch after the first (D-177) — but it says nothing about a lane whose plan is being replaced underneath it, which is your question. Use that seam rather than adding a second one, and gate the request on the project having no other open lane, readable from derive_in_flight; say in the evidence what that gate cost, because a milestone that never replans because a sibling is always running is the failure nobody would notice. D-167's bound applies unchanged and spends no new escalation class. Add no person approval gate: the confirmed intent was the one routine touchpoint and re-approving its consequences is the review that gets rubber-stamped.

CONSTRAINTS. Never Python; POSIX shell with set -eu, jq -e, awk and git; no packages, Swift or SQLite. Never touch Reclaim. Keep the deny rules, the single-writer log, flagless resumes and the existing escalation/answer/notifier machinery. No direct model API: judgement is a dispatched session and never an inline call in the tick. Work in the assigned worktree, stage only your own paths, never git add -A. Existing git identity; neutral technical messages; no authorship trailers. Take the next free D-number, read from main at the moment you write it, because other lanes append too.

VERIFICATION. Follow §8. Run each new fixture twice, then sh tests/run.sh. Record the exact output and passed, failed or unrun for each check; an unrun check is never reported as passed.

CLOSE-OUT, once this bounded checklist is finished.
1. Run /review-2 with a hand pass, /address locally, and sh tests/run.sh after the fixes.
2. Append the ten-item handoff under Completion evidence; update SPEC, ARCHITECTURE §6.2 and §10, and DECISIONS; commit on the assigned branch.
3. Merge to main in the canonical checkout. On failure write stopped/merge-failed and go no further. Run sh tests/run.sh on main; fix it there or write stopped/main-broken.
4. Mark only M15-d done when §8 holds. Establish open lanes and open parks by baton status and CONTRACT.md clause 3(c)'s dispatch-log check. Refresh on main only the prompts of listed milestones with neither an open lane nor an open park, with the actual interfaces; preserve each slot paragraph verbatim. Keep the worktree. Commit your own paths and run BATON_HOME=/Users/danny/.baton sh install.sh from the canonical checkout; record any warning it prints.
5. Do not start another milestone, and do not create a row for the second judgment role. If the bounded work will not fit, follow the plan split rule with completion evidence and a stopped/unfinished artifact, never a false completion.
6. Re-read the live plan and apply §11 to every eligible milestone. Write /Users/danny/.baton/inbox/M15-d-$CLAUDE_CODE_SESSION_ID.json atomically (.tmp then rename): baton 1, the canonical project, milestone M15-d, session, outcome complete, merged_as the verified main merge, written_at, and the computed eligible entries with brief paths and the Copy-ready session prompt heading. Print it verbatim last in a fenced block whose info-string is baton; if anything lands after it, deal with that and print it again.
```
