# M17 — Dispatch preconditions before worktree creation

## 1. Identity

- **ID:** M17. **Objective:** report the knowable dispatch defects together and refuse them
  before creating a milestone's branch or worktree.
- **User-visible result:** `baton plan <project>` names every unmet precondition for every
  eligible milestone, with a repair; dispatch refuses the same defects before worktree creation.
- **Size:** Medium, about 3–4 hours; **Effort: high**. Two existing library files:
  `lib/plan.sh` and `lib/dispatch.sh`. A small new shared module is permitted; a third existing
  library edit requires a further split before implementation, not an exception to the rule.

## 2. Dependencies and entry conditions

None. M17 is the single V1.1 root and the only milestone a person hand-dispatches. M17-b follows
it; M09 and M10 follow M17-b. The V1 relay is installed. Read the live plan and status first;
eligibility and a lane's park are different facts. Do not dispatch this milestone or any other
from an authoring session.

The authoring baseline at `e165c75` was measured in the canonical checkout with
`LC_ALL=en_US.UTF-8 sh tests/run.sh`: **246 scenarios, 0 failed**. This is a starting point,
not implementation acceptance. D-099 establishes that linked worktrees resolve the canonical
checkout through `install.sh`, while a detached clone cannot satisfy the two install scenarios'
absolute-path assertions. Use the canonical checkout or its linked worktree; do not relax those
assertions or promise detached-clone portability here.

## 3. Required reading

`CLAUDE.md`; `CONTEXT.md`; `CONTRACT.md`; `docs/MILESTONES.md`, including its split rule;
`docs/v1.1/SCOPE.md` §6 M17/M17-b, §7, §8 and §10; `docs/SPEC.md` §2.6–2.10 and §5;
`docs/ARCHITECTURE.md` §3, §4.1–4.2, §6.2, §7 and §10;
`docs/DECISIONS.md` D-030, D-048, D-049, D-098–D-104;
commit `7dd3ee6`, including its three new scenarios;
`bin/baton`, `lib/plan.sh`, `lib/dispatch.sh`, and the read-only callers in `lib/tick.sh`,
`lib/inbox.sh` and `lib/escalate.sh`; `tests/run.sh`, `tests/lib-load.sh`, and the
`plan-*`, `dispatch-*`, `tick-dispatch-fails-twice`, `held-plan-dispatch-fails` scenarios.
There is no predecessor milestone completion evidence for this root.

## 4. Requirements, scope, non-goals

**Already built; extend it.** `7dd3ee6` made `prompt_from_brief` distinguish a brief absent
everywhere, present on disk but absent from `main`, and on `main` without a complete fenced
block at the requested heading. Each message explains the need and names a commit command.
`dispatch_one` also refuses a worktree without a readable copy of its own brief. The scenarios
`dispatch-brief-absent`, `dispatch-brief-uncommitted` and `dispatch-worktree-missing-brief`
pin these cases. The existing guard runs **after** `worktree_ensure` and `settings_compose`;
it prevents a bad session, but still leaves creation side effects. Do not rewrite working code
or its useful messages to match a preference.

**Remaining work.** Share one read-only dispatch-precondition result between `verb_plan` and
`dispatch_one`. Put it in a new `lib/preconditions.sh` if needed to keep the existing modules
small; source it from `bin/baton` and `tests/lib-load.sh`. It may call `prompt_from_brief` after
the libraries have been loaded. It must not copy that parser or call `worktree_ensure`,
`settings_compose`, a verb, or a session operation. Its checks are:

1. The registered checkout has a usable `main`, and the milestone's brief is readable from
   `main` at `Copy-ready session prompt` with the complete fenced block the dispatch reads.
   Use the existing three-case messages. The slot paragraph must also be replaceable: run the
   existing `slot_line` validation without creating a worktree. A missing slot is knowable now.
2. Each literal `docs/…` reference in that fenced kickoff prompt exists on `main`. Inspect the
   fenced prompt, not the whole explanatory brief or its historical evidence. Deduplicate paths;
   remove Markdown delimiters and sentence punctuation, preserve the path's spelling, accept
   an explicitly named directory only when Git has a tree there. Template examples such as
   `docs/milestones/<ID>.md` are not literal filenames. Expand finite brace alternatives if a
   real prompt uses them; never evaluate shell text or silently discard an ambiguous reference.
   Report an ambiguous reference as needing a concrete path. Do not recursively follow links.
3. If the milestone branch already exists, it contains the latest commit on `main` that changed
   the milestone's brief (`git log -1 main -- <brief>`), verified by ancestry against that branch.
   Also require a readable brief in an existing milestone worktree. Check the actual worktree
   HEAD when it exists, not merely the expected branch name. A readable old copy alone does not
   prove the brief's commit arrived. Do not require every unrelated `main` commit to be merged,
   demand a clean worktree, discard edits, or forbid a later branch-local completion-evidence edit.
4. The project's `permissions.json` is present and parses, and satisfies the existing
   `settings_compose` deny-rule guard. Inspect without composing or writing settings. Do not
   widen the allowlist, invent new deny classes, or count bypass mode as a substitute for them.

Use `plan_eligible` unchanged to choose the milestones for the report. Preserve the current
plan parser and all Model-cell validation; a plan that cannot parse still reports its existing
parse failure rather than inventing an eligible set. For a parsed plan, collect every independent
failure for each eligible milestone in plan order, then emit the whole report. An unreadable
prompt prevents inspecting its references; report that dependency once, not guessed filenames.
Still check its branch and permissions. A shared permissions defect names every affected milestone.
Runtime rows, holds and cap continue to govern admission in their existing callers; the report
does not turn plan eligibility into a promise of dispatch or alter any of those decisions.

Each diagnostic names the milestone, what Baton needed, what it found and one concrete repair
command. Reuse the existing add-by-name/commit command for a brief after the named authoring fix;
use `git -C <worktree> merge main` for a stale checked-out branch, with no automatic merge.
For a branch with no worktree, name the branch and a deliberate checkout/worktree-and-merge repair;
never advise a forced reset. Missing referenced documents need authoring/restoration before their
named add/commit command. For permissions, point to the exact file and an editor command to
restore the owner's deny rules, not an invented permissive JSON value. Quote every path as shell
data, including apostrophes and spaces; suggested commands are printed and never executed.

The plan verb remains read-only, prints its graph and widening provenance, appends the complete
precondition report, and returns 1 if any precondition failed, 0 otherwise; retain its existing
registration/parse error codes. `dispatch_one` checks the same result **before**
`worktree_ensure`, including on redispatch. On failure it records through existing
`dispatch_failed` with the applicable existing stage (`prompt`, `worktree`, or `settings`) and
returns failure. Check all defects for the report; the existing single failure event may carry
the first defect deterministically. No new event kind or stage is needed. Keep the later
readability guard as a final race check unless its removal is proved safe, not merely redundant.

**Creating nothing** means no new branch, worktree, settings file, prompt sidecar, session or
caffeinate process. The lock and existing failure log/escalation machinery still operate.
Do not claim the failed command writes no event. Do not turn this precheck into a standing-check
run: running a project's tests is not a dispatch prerequisite owned by M17.

**Explicit deferrals.** Leave `dispatch_try`'s retry-then-escalate bound untouched, including
precondition failures; first-occurrence escalation is for M15 to classify. D-098's unification
with `brief_pointer_check` is deferred, despite its older assignment to M17. Do not edit
`lib/inbox.sh`, change its archived detail strings, or migrate existing archives. Rendering is
M17-b. No new verbs, dependencies, packages, plan format, on-main rule, event-log line format,
or unrelated dispatch/admission decision changes. D-101/D-102 are findings, not permission to
add pause machinery or change the source from which the plan is read.

## 5. Expected files (proposed)

Existing libraries, **count 2**: `lib/plan.sh`, `lib/dispatch.sh`.
New library: `lib/preconditions.sh` if the shared reader needs its own file.
Other paths: `bin/baton`, `tests/lib-load.sh`, named scenario inputs/expectations and new
precondition scenarios; this brief; `docs/SPEC.md`; `docs/ARCHITECTURE.md` §10 row M17;
`docs/DECISIONS.md`; `docs/MILESTONES.md` at close-out; M17-b's prompt refresh on main.
No `lib/tick.sh` or `lib/inbox.sh` change. Count an extra existing-library edit before making
it; stop and split if one is necessary. Do not move functions merely to evade the count.

## 6. Interfaces

- **Introduces:** `dispatch_preconditions <project> <milestone>`: read-only, one JSON result
  with an ordered `failures` array; each failure carries its existing dispatch `stage`, a stable
  check name, the affected path and the human detail/repair. Successful inspection returns the
  same shape with an empty array. Distinguish a failure to collect the result from a valid result
  naming unmet preconditions; never treat empty or malformed output as success. Settle exact
  field names in implementation and carry them into ARCHITECTURE and M17-b's prompt.
- **Consumes unchanged:** `plan_of_project`, `plan_eligible`, `project_path`, `prompt_from_brief`,
  `slot_line`, `dispatch_failed`, branch/worktree naming, `settings_compose`'s deny guard.
- **Preserves:** all machine-return values and the event writer. Render human text only at
  the plan/dispatch boundary; no terminal styling in shared data or stored log fields.

## 7. Implementation checklist (ordered)

1. Inspect the assigned checkout, current status and the three existing brief scenarios; read
   `7dd3ee6`. Confirm the two-file budget and record the prior baseline separately from new checks.
2. Implement the shared read-only precondition reader and fixtures for every §4 case. Reuse
   existing parsing/messages; keep D-098's inbox implementation untouched.
3. Append the aggregate report to `verb_plan`, inspecting every plan-eligible milestone even
   after a defect is found. Preserve graph, gates, widening provenance and their ordering.
4. Invoke the reader in `dispatch_one` before creation; preserve retry/escalation and transient
   handling. Verify both a new branch and a reused branch/worktree without altering either.
5. Review exactly which old expectations change for earlier refusal, run §8, update current
   documentation and interfaces, then review and close out. Do not implement M17-b.

## 8. Fixtures, verification, acceptance

- **No-build:** inspect the diff and count existing library edits; confirm no call path to
  worktree/settings/session creation precedes the check. Inspect committed brief and branch
  ancestry, not just filesystem presence. Run the standing check under `LC_ALL=en_US.UTF-8`.
- **Fixture-run:** multiple eligible milestones, multiple independent defects on one milestone,
  shared permissions failure, and a valid eligible peer all reported in one pass; noneligible,
  held and done rows unchanged. Cover absent/on-disk-only/malformed-fence/missing-heading/missing-slot
  briefs; valid/missing/deduplicated/template/ambiguous documentation references; empty/malformed/
  missing permissions; no branch, stale branch with no worktree, stale readable brief, missing
  worktree brief, current branch with retained local work, and a successful dispatch.
- **No-creation proof:** snapshot refs, worktree inventory, settings, sidecars and shim calls
  before and after each precondition refusal. The only expected state changes are the existing
  failure record and, on the second tick, existing escalation/notification state. No Claude start
  or caffeinate call. Run each fixture twice, including the repaired case and transient launch failure.
- **Existing behaviour contracts:** retain `plan-*` parse/eligibility checks, live-row refusal,
  slot replacement, settings deny guard, session names/argv, log schema, retry bound and all
  `reject-brief-pointer` archive behaviour. Keep `tests/run.sh`'s full diff and both runs.
  The intentional timing correction changes old expectations that contain settings/worktrees
  created before a brief refusal. In `dispatch-worktree-missing-brief/cmd`, replace its assumption
  that `Fixture-M03` was created with a stronger assertion that the previously absent worktree
  remains absent; preserve the unchanged branch assertion and add a separate pre-existing-worktree
  case. Do not describe this necessary correctness change as a presentation-only refreeze.
- **Manual, read-only:** `baton plan Baton` and a fixture plan with several failures. No real
  dispatch, tick, session, install or notification is authorized as a verification probe.
- **Acceptance:** the complete report names every inspectable unmet precondition; the earliest
  dispatch check refuses it without creation; no later milestone's eligibility is changed; the
  unchanged retry bound and deferred inbox strings are proved by fixtures; the full standing check
  passes. New scenarios increase the count; 246 is the authoring baseline, not a target to freeze.

## 9. Definition of done and evidence

Append the ten-item `docs/MILESTONES.md` handoff, including exact report output, before/after
creation inventories, negative shim calls, test output, the two-file count and interface names.
Explain each changed expectation as the intended early-refusal effect or presentation; attach
no blanket freeze. Report checks passed, failed or unrun. A proposal or this authoring baseline
does not establish implementation acceptance.

## 10. Risks and decisions this milestone settles

- The live plan is read from the working tree while briefs come from `main` (D-102). This check
  limits damage when the two disagree; it does not make publication atomic or add pause (D-101).
- A missing prompt limits what references can be inspected. Report that limitation rather than
  declaring the milestone ready or inventing its dependencies. Quote repairs, never execute them.
- Git ancestry and readable worktree content answer different questions; test both without
  requiring unrelated new main commits or destroying resumable work.
- `dispatch_failed` caps detail at 2,000 bytes and marks `detail_truncated`; `log_event` refuses
  lines over 4 KB. Preserve those bounds. The older scope's claim that stderr was kept in full
  was wrong. Do not introduce unbounded aggregate diagnostics into one event.
- First-failure escalation belongs to M15; D-098 remains deferred. Record implementation
  decisions at the next free D-number, without quietly taking either scope back.

## 11. Handoff and next milestone

Apply `CONTRACT.md` clause 3(c) to every refresh below: run `baton status` and check the
dispatch log for open lanes, including lanes absent from status; status also lists open parks. Refresh only a listed
milestone with neither an open lane nor an open park; still name every eligible milestone with its correct disposition.

Direct successor **M17-b**, `docs/milestones/M17-b.md`, heading `Copy-ready session prompt`.
After M17 is done it is `run` under the current graph. M09 and M10 still wait for M17-b;
they are not this milestone's direct successors. Refresh M17-b's prompt on main with the
actual precondition interface and changed-output fixtures so its rendering work consumes them.

Immediately before writing the artifact, re-read live Status cells, dependencies and gates.
List every direct successor plus every other milestone then eligible. An open gate takes
precedence: `held` with its exact `held_by`; otherwise unfinished dependencies mean `wait` with
all their IDs in `wait_for`; otherwise `run`. Give each entry its `docs/milestones/<ID>.md`
path and heading `Copy-ready session prompt`, refresh only listed prompts whose milestones have neither an open lane nor an open park and commit on main.
Do not change the graph to obtain a disposition or start the next milestone. The tick owns it;
an omitted entry can park the chain.

## Recovery procedure

Read completion evidence, the working tree, existing helper and fixture output first. Determine
which checks are already wired before editing. Resume only unfinished checklist items, preserving
the 7dd3ee6 messages, other sessions' work and every existing branch/worktree. Do not retry a real
dispatch to discover whether a previous failure created anything. No completion without §8.

## Completion evidence

### 1. What changed

- A new read-only library, `lib/preconditions.sh`. `dispatch_preconditions <project> <milestone>`
  reads every dispatch precondition Baton can know without creating anything and returns one JSON
  result; `verb_plan` and `dispatch_one` share that one result, so the report and the refusal
  cannot disagree about a milestone.
- `baton plan <project>` appends a precondition report over every plan-eligible milestone in plan
  order, collecting each milestone's independent failures rather than stopping at the first, and
  returns 1 when anything is unmet. A shared defect — permission rules without deny rules — names
  every milestone it would refuse, not only the first.
- `dispatch_one` calls the reader **before** `worktree_ensure`, on the hand-run verb, the tick's
  `dispatch_try` and the ladder's `redispatch` alike, so a refusal creates no branch, worktree,
  settings file, prompt sidecar, session or `caffeinate` process. It records the first failure in
  the result's fixed order through the existing `dispatch_failed`, at that failure's existing
  stage. No new verb, event kind or stage.
- The checks, in the order `failures` carries them: the checkout has a usable `main`; the brief is
  on `main` at `## Copy-ready session prompt` with a complete fenced block; that prompt carries the
  slot paragraph; every literal `docs/…` path the fenced prompt names exists on `main`; an existing
  branch or worktree carries the latest `main` commit that changed the brief and a readable copy of
  it; and `permissions.json` parses and carries deny rules.
- `worktree_of` was factored out of `worktree_ensure`, so the branch and worktree naming is written
  once and the reader inspects exactly what the creator would create.
- The final readability guard in `dispatch_one` is kept as a race check. Its message no longer
  names a branch behind the brief's commit as the cause: `behind-brief` refuses that before this
  point, so a message naming it would send a person the wrong way (D-140).

### 2. Files added / modified / deleted

**Added.** `lib/preconditions.sh`. Twelve scenarios: `plan-preconditions`,
`plan-preconditions-none`, `plan-preconditions-permissions`, `plan-preconditions-no-main`,
`preconditions-references`, `preconditions-result-shape`, `dispatch-precondition-no-creation`,
`dispatch-precondition-repaired`, `dispatch-worktree-stale-branch`,
`dispatch-worktree-brief-unreadable`, `dispatch-worktree-stale-and-absent`,
`tick-precondition-fails-twice`.

**Modified.** `lib/plan.sh` and `lib/dispatch.sh` (the two counted existing libraries);
`bin/baton` and `tests/lib-load.sh` (one source line each, plus the library count in a comment);
`install.sh` (the same count in a comment); `docs/SPEC.md`, `docs/ARCHITECTURE.md`,
`docs/DECISIONS.md`, `docs/MILESTONES.md`; and the expectations of `plan-parses`,
`dispatch-brief-absent`, `dispatch-brief-uncommitted`, `dispatch-empty-deny`,
`dispatch-failed-prompt`, `dispatch-no-slot-line`, `dispatch-worktree-missing-brief`, `install`
and `install-bundle-flip`.

**Deleted.** The `home/settings/Fixture-M0x.json` files from four refusal scenarios' expectations,
which is the timing correction itself — see item 7.

Not changed, and checked: `lib/tick.sh`, `lib/inbox.sh`, `lib/stops.sh`, `AGENTS.md`, `CLAUDE.md`.

```
$ git diff --stat main -- 'lib/*.sh'
 lib/dispatch.sh      |  48 +++++++++-
 lib/plan.sh          |  65 ++++++++++++-
 lib/preconditions.sh | 261 +++++++++++++++++++++++++++++++++++++++++++++++++++
 3 files changed, 370 insertions(+), 4 deletions(-)
```

Existing-library count: **2** (`lib/dispatch.sh`, `lib/plan.sh`). `lib/preconditions.sh` is the new
shared module §5 permits. `cmp AGENTS.md CLAUDE.md` is silent and neither appears in `git status`,
so D-100 holds without either being touched.

### 3. Interfaces introduced or changed

`docs/ARCHITECTURE.md` §10, row M17, holds the full list. In summary:

- `lib/preconditions.sh`: `dispatch_preconditions <project> <milestone>` — read-only; prints
  `{project, milestone, checkout, brief, worktree, branch, references_inspected,
  failures: [{check, stage, path, detail, repair}]}` with status 0 whatever it found, and the
  detail with status 1 when the inspection could not be made at all. `permissions_inspect
  <project>`; `prompt_references` (a prompt on stdin, `path <p>` or `ambiguous <t>` per line);
  `dp_add <check> <stage> <path> <detail> <repair>`.
- Check names, stable: `checkout`, `brief`, `slot`, `reference`, `reference-ambiguous`,
  `not-a-worktree`, `behind-brief`, `worktree-brief`, `permissions`. `behind-brief` is named for
  what it checks — the branch carries the brief's own latest commit, not every unrelated commit on
  `main`.
- A failure's `path` is checkout-relative for stage `prompt`, which is how the brief and the
  commands repairing it spell it, and absolute otherwise; `checkout` is in the result so a reader
  wanting the absolute form joins the two instead of resolving the project again.
- `lib/plan.sh`: `plan_preconditions_report <project> <plan json>`; `verb_plan` returns 1 when a
  precondition is unmet.
- `lib/dispatch.sh`: `worktree_of <path> <milestone>` prints `{worktree, branch}`.

### 4. Checks run, with outcomes and attached output

**The standing check, on the branch and again on `main` — passed both times.**

```
=en_US.UTF-8 sh tests/run.sh          # branch m17
319 scenarios, 0 failed

=en_US.UTF-8 sh tests/run.sh          # main, after the merge, the combined tree
319 scenarios, 0 failed
```

The authoring baseline recorded in §2 of the brief was 246 scenarios at `e165c75`; this worktree's
`main` stood at 307 before the milestone, and the twelve new scenarios take it to 319. The
baseline is a starting point, not acceptance.

**No-build, by inspection — passed.** `git diff --stat main -- 'lib/*.sh'` above gives the
two-file count. Reading `dispatch_one` top to bottom, `project_path` and `dispatch_preconditions`
precede `plan_row`, `attempt_of`, `worktree_ensure`, `settings_compose`, `prompt_from_brief`,
`claude_bg`, `sidecar_write`, `caffeinate_hold` and `log_event`; `verb_dispatch`, the tick's
`dispatch_run` and the ladder's `redispatch` reach `dispatch_one` having done only reads.
`grep` over `lib/preconditions.sh` finds no redirection, `mkdir`, `rm`, `mv`, `cp` or `log_event`.
`sh -n` passes on `lib/preconditions.sh`, `lib/plan.sh`, `lib/dispatch.sh` and `bin/baton`.

**Fixture-run — passed.** Every §8 case has a scenario:

| §8 case | Scenario |
|---|---|
| Multiple eligible milestones, multiple independent defects on one, a valid peer, in one pass | `plan-preconditions` (6 eligible; M04 carries `slot`, `reference` and `reference-ambiguous`; M02 `ready`) |
| Non-eligible, held, done and gate-held rows unchanged | `plan-preconditions` (M01 done, M06 held, M07 waiting, M08 gate-held: none reported), `plan-preconditions-none` |
| Shared permissions failure naming every affected milestone | `plan-preconditions-permissions` (M02 and M07 both) |
| Absent / on-disk-only / malformed-fence / missing-heading / missing-slot briefs | `plan-preconditions` M05 / M10 / M03 / M09 / M04; `dispatch-brief-absent`, `dispatch-brief-uncommitted`, `dispatch-failed-prompt`, `dispatch-no-slot-line` |
| Valid / missing / deduplicated / template / ambiguous documentation references | `preconditions-references` (every classifier branch), `plan-preconditions` M02 and M04 |
| Empty / malformed / missing permissions | `plan-preconditions-permissions`, `dispatch-empty-deny` |
| No branch | `dispatch-precondition-repaired` (first half), `plan-preconditions` |
| Stale branch with no worktree | `dispatch-worktree-missing-brief` |
| Stale branch with a readable old brief | `dispatch-worktree-stale-branch` |
| Missing worktree brief | `dispatch-worktree-brief-unreadable`, `dispatch-worktree-stale-and-absent` |
| Current branch with retained local work | `dispatch-precondition-repaired` (the redispatch over `scratch.txt`) |
| A successful dispatch | `dispatch-precondition-repaired`, and the unchanged `dispatch-first` and `dispatch-reused-worktree` |
| A usable `main` absent | `plan-preconditions-no-main` (branch renamed, and the repository removed) |
| A directory that is not a worktree | `dispatch-worktree-stage` (unchanged: the message is `worktree_ensure`'s, verbatim) |
| The result shape, and a result that could not be collected | `preconditions-result-shape` |

**No-creation proof — passed.** `dispatch-precondition-no-creation` snapshots refs, the worktree
inventory, the siblings beside the checkout, the settings directory, the sidecar directory and the
shim's `--bg`, `claude stop` and `caffeinate` call counts before and after a prompt-stage refusal
and a settings-stage refusal. Every line is identical across all three snapshots, and the scenario
asserts `test "$before" = "$after"` in its own `cmd` as well as freezing it, so a blanket
re-freeze cannot accept a regression:

```
--- before
ref refs/heads/main
worktree Fixture
beside Fixture
beside home
beside jobs
beside out
beside shim
beside transcripts
settings 0
sidecars 0
bg launches 0
claude stops 0
caffeinate calls 0
--- after a prompt-stage refusal (exit 1)
   … identical …
--- after a settings-stage refusal (exit 1)
   … identical …
two refusals, two failure records, nothing created
```

The positive control is in `dispatch-precondition-repaired`: the same inventory function, over the
same milestone once its brief is committed, reports `ref refs/heads/m03`, `beside Fixture-M03`,
`settings 1`, `sidecars 1`, `bg launches 1` and `caffeinate calls 1`. The proof is therefore not
vacuous. Every scenario runs twice, the harness's own rule, and the repaired case and the existing
transient-launch-failure cases (`dispatch-failed-launch`, `tick-dispatch-fails-twice`,
`held-plan-dispatch-fails`) are among them.

**The retained retry bound — passed.** `tick-precondition-fails-twice` puts a precondition defect
in front of the tick. Its expected log shows one `dispatch_failed` and no escalation after the
first tick, and a second `dispatch_failed` with one `dispatch-failed` lane escalation after the
second — `dispatch_try` unchanged, a precondition failure climbing the same ladder as any other.
The `cmd` asserts the counts itself, deriving which tick it is from the log:

```
dispatch  Fixture/M02 · 2 consecutive failures · the lane is parked
```

`dispatch-failed-handback` exercises the same bound over a worktree-stage precondition failure and
is unchanged.

**The untouched inbox strings — passed.** `lib/inbox.sh` does not appear in
`git diff --name-only main`, and every `reject-brief-pointer` and `consume-*` scenario passes
unmodified.

**Manual, read-only — passed.** `baton plan Baton`, run from this worktree's `bin/baton` against
the live `BATON_HOME`, exit 0:

```
…
M17  in flight  model opus  effort high  remote no
M17-b  waits on M17  model opus  effort high  remote no
widenings: none
preconditions: 1 eligible, 0 with unmet preconditions
  M17  ready
```

A fixture plan with several failures is `plan-preconditions`, whose whole report is in its
expectation. No real dispatch, tick, session, install or notification was run as a probe.

### 5. Checks not run, and why

- **No live dispatch, tick, session start, stop, resume or notification.** §8 authorises none as a
  verification probe, and the constraints forbid them. Every dispatch path is exercised through
  the shim.
- **Reclaim untouched**, read or written, in any form.
- **`sh install.sh` not yet run** at the time this evidence was written: the close-out runs it on
  `main`, after the standing check passes there. Its result is recorded in the final message.
- **The `not inspected` branch of the plan report has no scenario.** It is defensive: reaching it
  needs a `project.json` that `dispatch_preconditions` cannot read but `plan_of_project` could,
  and `plan_of_project` fails first on the same file. The function's own status-1 path is covered
  by `preconditions-result-shape`, which asserts the status, the plain-text detail, and that the
  output is not JSON that could be mistaken for a result.

### 6. Known limitations

1. **A redispatch stops the outgoing session before the preconditions can refuse.** `redispatch`
   in `lib/stops.sh` runs `claude stop` and `stop_settle`, then `dispatch_try`. A knowable refusal
   therefore still costs the old session. It is not creation, so it is within this milestone's
   scope statement, and `lib/stops.sh` would be a third counted existing library. A later
   milestone can call the reader before the stop.
2. **A mark glued inside a reference stays part of the spelling.** A trailing dash, section mark or
   sentence mark is trimmed and a `#fragment` is dropped for the lookup, but `docs/SPEC.md§3` is
   looked up as written and reported missing. That is a typo rather than a convention, and the
   diagnostic quoting it exactly is what makes it visible.
3. **`git cat-file -e main:<p>` does not normalise `..`.** A brief writing `docs/adr/../SPEC.md` is
   reported missing.
4. **A multi-line git error carried into a `not-a-worktree` detail wraps in the report.** The
   detail is `worktree_ensure`'s own text, kept verbatim; rendering is M17-b's.
5. **A different git repository sitting at the worktree path** would be read at its own HEAD and
   reported `behind-brief` with a merge repair that would not apply. The `not-a-worktree` check
   catches a directory that is no repository at all, not one that is the wrong repository.
6. **The two install scenarios still assert the canonical absolute path** (D-099): they work from
   the canonical checkout or a linked worktree, not a detached clone. Unchanged by this milestone.

### 7. Deviations from the brief, and the decisions recording them

- **Four refusal scenarios lost a settings file from their expectations**, and
  `dispatch-worktree-missing-brief` lost its assumption that the worktree was created. This is the
  timing correction itself, not presentation: an expectation recording creation before a knowable
  refusal recorded the defect this milestone removes. `dispatch-worktree-missing-brief` now
  asserts the previously absent worktree is still absent, keeps its unchanged branch assertion, and
  a separate pre-existing-worktree case was added (`dispatch-worktree-stale-branch`). **D-140.**
- **The slot and permissions diagnostics grew a need and a repair**, because §4 requires every
  diagnostic to name the milestone, what Baton needed, what it found and one concrete repair
  command. The three `7dd3ee6` brief messages and `worktree_ensure`'s "exists but is not a
  worktree" are verbatim; the latter carries no repair, because what to do with a directory Baton
  did not create is a person's judgement and scripting a move would be advising a destructive act.
  **D-140.**
- **The race guard's message changed.** It named a branch predating the brief's commit as the
  cause, which `behind-brief` now refuses earlier. D-140 protects the old messages from preference,
  not from asserting a cause known to be false. **D-140.**
- **The result carries `checkout` and each failure a `repair`**, beyond §6's minimum shape. §6 says
  to settle exact field names in implementation and carry them into ARCHITECTURE and M17-b's
  prompt; both spare the renderer re-deriving the checkout and recovering a command from prose.
  **D-138.**
- **`install-bundle-flip`'s `cmd` asserts nineteen libraries rather than eighteen**, and the same
  count in comments in `bin/baton` and `install.sh` was corrected. Neither is a counted library and
  the assertion is not weakened.
- Decisions taken: **D-138** (the shared reader, the refusal point and the result's shape),
  **D-139** (what counts as a literal `docs/…` reference), **D-140** (the changed expectations and
  the messages kept). The next free number is now **D-141**.

### 8. Commit information

On the branch `m17`:

- `3784ca2` Refuse a dispatch's knowable defects before anything is created
- `b2c7a2e` Settle the precondition contract and record the milestone's evidence

On `main`:

- `e1f7bb0` Merge M17: every eligible milestone's unmet dispatch preconditions reported in one pass, and the same result refusing a dispatch before any branch, worktree or settings file exists — **this is the artifact's `merged_as`**, verified as an ancestor of `main`.
- `ecf319d` Mark M17 done and carry the precondition interface into M17-b's prompt

The install was run from the canonical checkout on `main` once the standing check had passed
there: the relay is under `/Users/danny/.baton/bin`, and the launchd agent was reported current,
so no differing-agent warning arose and none is quoted.

### 9. Working-tree state as observed

`git status --short` in the milestone worktree `/Users/danny/Documents/Apps/Baton-M17` prints
nothing: it is clean, and it is left in place so the session can be resumed.

In the canonical checkout `/Users/danny/Documents/Apps/Baton`, `git status --short` prints one
line, `?? "Review Reports/baton-v1.1-proposal-review.md"` — an untracked file this milestone did
not create and did not stage. Every commit above staged its paths by name.

### 10. Exact next step and the next eligible milestone

**M17-b**, `docs/milestones/M17-b.md`, heading `Copy-ready session prompt`, disposition `run`: it
depends on M17 alone and the plan's gates table holds nothing. Its prompt is refreshed on `main`
with the precondition interface above and the changed-output fixtures, so its rendering work
consumes them. M09 and M10 still wait for M17-b and are not eligible. The artifact's `eligible[]`
names M17-b and nothing else.

## Copy-ready session prompt

```
You are implementing milestone M17 of Baton at /Users/danny/Documents/Apps/Baton. Baton is a relay, a personal tool for one person on one Mac, on Claude Code 2.1.270, that carries a build from one Claude Code session to the next; a launchd-run tick every sixty seconds; it embeds no model call. Implement only the dispatch preconditions in M17 §4: the complete plan report and refusal before worktree creation. Size: Medium, 3–4 hours; high effort. V1.1 extends V1 in POSIX shell on the Claude subscription.

WHAT ELSE IS IN FLIGHT. Runs alone unless the dispatch says otherwise.

STARTUP ORDER. Read CLAUDE.md, CONTEXT.md, CONTRACT.md, docs/milestones/M17.md in full, docs/v1.1/SCOPE.md, then the sections, decisions, commit 7dd3ee6 and files named in M17 §3. There is no predecessor completion evidence for this root. Run baton status and inspect the working tree and current plan. Confirm only lib/plan.sh and lib/dispatch.sh require existing-library edits; a new shared helper does not authorize a third. If Completion evidence is non-empty or milestone files exist, follow the Recovery procedure and resume only unfinished work.

WHAT TO SETTLE RATHER THAN INHERIT. 7dd3ee6 already supplies the three brief-failure messages and the final worktree readability guard; extend these, do not rewrite them for preference. Share read-only checks between plan and dispatch without copying the brief parser. Collect all eligible milestones' independent failures; check main's fenced prompt and slot, literal docs references, existing branch ancestry and worktree readability, and permissions with deny rules. Plan reads the working tree; briefs read main. Refuse before worktree_ensure and settings_compose, preserve existing failure logging and deliberately leave dispatch_try's retry-then-escalate bound unchanged. First-occurrence escalation is M15's classification work. D-098's inbox unification and archived strings remain deferred. D-099 requires en_US.UTF-8 and the canonical checkout or a linked worktree, not a detached clone. The measured authoring baseline is 246 scenarios, 0 failed, not proof of this implementation.

CONSTRAINTS. Never Python; POSIX shell with set -eu, jq -e, awk and git; no packages, new dependency, new verb, language rewrite or direct API calls. Do not change the plan format, on-main rule, event-log line format or unrelated verb decisions. Do not touch Reclaim or start, stop, attach to, respawn or resume real sessions for verification; use only shell fixture shims. The log has one writer and every resume is flagless. A necessary question is asked in this session with AskUserQuestion, not an avoidable asking artifact. Work in the assigned worktree; stage only owned paths by name, never git add -A; preserve concurrent work. Commit locally using existing identity, neutral messages and no authorship trailers. Take the next free D-number. If instruction files must change, AGENTS.md and CLAUDE.md must remain byte-identical (D-100). Do not add pause machinery or change plan authority under cover of D-101/D-102.

VERIFICATION. Execute M17 §8, including the aggregate report, no-creation snapshots, retained retry bound and untouched inbox strings. Keep the standing-check harness and behaviour assertions except the explicitly named stronger no-creation replacement for the old worktree-creation assumption. Run LC_ALL=en_US.UTF-8 sh tests/run.sh. Report every check passed, failed or unrun with output; never report an unrun check as passed.

CLOSE-OUT, in this order, once the checklist is finished.
1. Run /review-2 with a hand pass against §8; address findings with /address, local commits only. Run LC_ALL=en_US.UTF-8 sh tests/run.sh after fixes.
2. Append the ten-item docs/MILESTONES.md handoff under Completion evidence; update SPEC, ARCHITECTURE §10 row M17 and DECISIONS. Commit on the assigned branch.
3. Merge into main in /Users/danny/Documents/Apps/Baton. If the merge fails, write stopped with reason merge-failed and go no further. Run LC_ALL=en_US.UTF-8 sh tests/run.sh on main; fix failures there, or write stopped with reason main-broken if they cannot be fixed.
4. On main mark only M17 done after acceptance. Re-read the plan and derive entries by §11. Establish open lanes and open parks by baton status and CONTRACT.md clause 3(c)'s dispatch-log check. Refresh only listed prompts whose milestones have neither an open lane nor an open park, parts 1 and 3–7 and part 4 additively, preserving part 2 verbatim. Carry the actual precondition interface into M17-b's prompt. Leave the assigned worktree in place, commit owned paths, and run BATON_HOME=/Users/danny/.baton sh install.sh from the canonical checkout. Quote any differing-agent warning in evidence and the final message; a person loads an agent.
5. Start no successor. If the work does not fit, follow the split rule, write evidence and stopped with reason unfinished and a bounded remainder; do not mark M17 done or relax the two-library limit.
6. Immediately before the handover, re-read the live plan after the done commit. List §11's direct successor M17-b and every other eligible milestone, deriving held/held_by, wait/wait_for or run from current gates and Status cells. Refresh and commit any newly listed prompt first only if its milestone has neither an open lane nor an open park under CONTRACT.md clause 3(c). Write /Users/danny/.baton/inbox/M17-$CLAUDE_CODE_SESSION_ID.json, .tmp first then rename, with baton 1, canonical project /Users/danny/Documents/Apps/Baton, milestone M17, session from CLAUDE_CODE_SESSION_ID, outcome complete, merged_as the merge commit on main, written_at, and eligible entries with brief path and heading Copy-ready session prompt. Follow the installed completion rules without inventing check results. Print the artifact verbatim last in a baton fence; if anything follows, handle it and print it again.
```
