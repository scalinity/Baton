# M07 — Remote dispatch and acceptance

## 1. Identity

- **ID:** M07. **Objective:** The phone, and the proof. `Remote: yes` dispatches in the verified
  two-step way and a question parked in such a session is answered from the Claude mobile app.
  (Item 40 collapsed the two-step dispatch into one command, D-080, and every session is on Remote
  Control, D-081; the completion evidence records what was built.)
  Then the acceptance evidence: the nights Baton has driven itself since M06, read from the log
  and the archive, written up against `docs/SPEC.md` §1.4.
- **User-visible result:** A fixture-project milestone marked `Remote: yes` starts idle with
  `--remote-control`, is stopped, and is woken flagless with its prompt; its `AskUserQuestion`
  pushes the phone and the answer clears it. The acceptance write-up shows one lane in flight,
  one wait and one park resolved with one command, and the double-tick property holding on the
  real log.

## 2. Dependencies and entry conditions

M06 complete and installed; dispatched by the tick, unattended. `DISABLE_TELEMETRY` unset in the
launching environment (REQ-SETUP-06) — the plist's `EnvironmentVariables` does not set it, and
launchd does not read `~/.zshrc`, so the job is clean; the person's terminal may not be.

## 3. Required reading

`CLAUDE.md`; `CONTEXT.md`; `CONTRACT.md`; `docs/SPEC.md` §1.4, §2.5 (REQ-ESC-08), §2.7
(REQ-DISPATCH-07), §2.11 (REQ-SETUP-06), §7; `docs/ARCHITECTURE.md` §4.1 step 8 (the remote
branch); `docs/DECISIONS.md` D-010, D-014, D-019;
`.scratch/baton/issues/11-watching-a-dispatched-session.md` "Addendum" and items 39, 40;
`.scratch/baton/issues/09-dispatching-more-than-one-at-once.md` §7.

## 4. Requirements, scope, non-goals

- **Covers:** REQ-DISPATCH-07; REQ-ESC-08; REQ-SETUP-06; live items 39 and 40; the acceptance
  of REQ-TICK-02 and §1.4 on the real log.
- **Scope:** `claude_bg_remote` (three commands, one `dispatch` event with `remote: true`); the
  remote lane's row treated as not a park detector; items 39 and 40 measured; the acceptance
  write-up in this brief's completion evidence, drawn from `~/.baton/log.jsonl` and
  `~/.baton/archive/` since M06's dispatch.
- **Non-goals:** collapsing the two-step dispatch into one unless item 40 proves it (then it is a
  decision entry, not a rewrite); anything Reclaim.

## 5. Expected files (proposed)

`lib/dispatch.sh` (`claude_bg_remote`); `tests/scenarios/{remote-two-step,remote-row-not-a-park}/`;
`docs/ARCHITECTURE.md` §10 row M07; `docs/DECISIONS.md`; this brief; `docs/milestones/M08.md`'s
prompt at refresh.

## 6. Interfaces

- **Introduces:** `claude_bg_remote`; the `remote` field's `true` path.
- **Consumes:** M01–M06.

## 7. Implementation checklist (ordered)

1. `claude_bg_remote`: `--remote-control` with `-n`, `--model`, `--effort`, `--permission-mode`,
   `--settings` and no prompt; parse the id; `claude stop <id>`; flagless
   `--bg --resume <uuid> "<prompt>"`; one `dispatch` event.
2. Step 3 skips the row-based park detection for lanes whose `dispatch` carries `remote: true`.
3. The fixtures, twice each; the shim gains the idle-start mode.
4. Live, once, fixture project: item 39 (the flags given at the idle start are restored by the
   flagless resume — read the `woke session` line's list) and item 40 (`remoteControlAtStartup`
   in `--settings` with a positional prompt: does the prompt survive?). A question answered from
   the phone.
5. The acceptance write-up: from the log since M06's dispatch — every event kind that occurred,
   every wait and its length, every park and its resolution, the double-tick check run against a
   copy of the real `~/.baton/` with the shim; each §1.4 criterion marked held or not held with
   the lines that show it.
6. `sh tests/run.sh`, `/review-2`, `/address`, completion evidence, the handover.

## 8. Fixtures, verification, acceptance

- **Fixture-run:** the two scenarios, twice.
- **Manual, live:** item 4 with the phone in hand.
- **Acceptance:** REQ-DISPATCH-07 and REQ-ESC-08 hold; items 39 and 40 recorded; §1.4 assessed
  line by line against the real log.

## 9. Definition of done and evidence

Handoff per template with the live captures and the acceptance write-up.

## 10. Risks and decisions this milestone settles

- **Item 40 is a simplification to try, not a design to rest on.** If both halves hold, the
  two-step path is deleted with a decision entry; if not, nothing changes.
- **The phone push needs the Claude app registered and `DISABLE_TELEMETRY` unset** in the
  process tree that starts the session; a tick under launchd is clean, a terminal may not be.

## 11. Handoff and next milestone

M07-b with disposition `run`, and nothing else: M07-b was split from M07 (D-081), and M08 now depends
on M07-d behind M07-b and M07-c (D-083), so the plan makes one milestone eligible.

## Recovery procedure

Inspect `claude_bg_remote` and the scenarios; resume from the first unfinished checklist item; no
completion claim without every acceptance criterion.

## Completion evidence

Session `e7f442dc-179b-4535-8992-ae5447ebe3c1`, dispatched into `/Users/danny/Documents/Apps/Baton-M07` on
branch `m07`, 2026-09-12, on Claude Code 2.1.270 (the CLI updated itself from 2.1.269 at 15:48 local,
before this dispatch). Attempt 1; resumed twice — once by the crash rule after the background service
was stopped from a terminal, once by the ruling on this session's `asking` park. Every item of §8 is
assessed below with its output; §1.4 is assessed line by line and two of its three criteria are not
held, which is recorded rather than smoothed over. The scope grew during the session on the person's
direction and by one ruling; §7 lists every change to the brief.

### 1. What changed

- **`Remote: yes` is one command** (D-080). Live item 40 showed `remoteControlAtStartup: true` in a
  `--settings` file connects a `--bg` session and keeps its positional prompt, so the brief's two-step
  path — built first, as §7 ordered, with its fixtures — was deleted. `dispositions_intersect` no longer
  skips a remote candidate; `dispatch_one` no longer refuses one and records the plan's `remote` on the
  `dispatch` event.
- **Every dispatched session is on Remote Control** (D-081, superseding D-077). `settings_compose` writes
  `remoteControlAtStartup: true` for every dispatch. Every `--bg` session Baton dispatched since M02 had
  been connected by the account's default; a session started with the key `false` is absent from
  Claude.app. `Remote: yes` keeps only its escalation meaning.
- **A remote lane stalls whether its row reads waiting or not** (REQ-ESC-08): `question_check` never
  parks a remote lane, so `stall_check` now judges one whose row reads `waiting`.
- **Milestone worktrees are kept** (D-078). `worktree_prune`, its call in `tick_project` and its twelve
  scenarios are deleted; CONTRACT clause 3(c), REQ-DISPATCH-03, CONTEXT and this repository's close-out
  leave the worktree in place and remove only build products.
- **The Stop gate and the StopFailure hook stand down on a closed lane** (D-078): when an archived file
  has this session, this milestone and `outcome: complete` — matched by content, as the first rule —
  both exit 0 writing nothing, so a finished session a person resumes is neither asked for a handover
  again nor turned into an ending the tick acts on.
- **A remote lane's stall re-arms once its transcript moves after the notification**, so a second
  unanswered phone prompt in one attempt still surfaces.
- **The close-out runs `sh install.sh`** (D-079), on `main`, after the standing check passes there and
  before the artifact is written; CLAUDE.md's hard rules carry the exception. `install.sh` copies the
  launchd agent only when none is installed and leaves one that differs in place with a line.
- **Three milestones added**, by the ruling and at the person's direction, and the plan is one lane
  again: M07-b (a finished session offline when idle and awake when messaged, project grouping, the
  takeover rule seeing Remote Control input; D-081, D-084), M07-c (the Mac message under a notifier
  with Claude's icon whose click opens the session; D-082), M07-d (a handover acted on once), with M08
  behind M07-d (D-083).
- REQ-SETUP-06 names the push setting the phone relies on (`agentPushNotifEnabled: true`).

### 2. Files

Added: `docs/milestones/M07-b.md`, `docs/milestones/M07-c.md`, `docs/milestones/M07-d.md`; scenarios
`remote-dispatch-tick`, `remote-row-not-a-park`, `remote-stall-rearms`, `stop-gate-closed-lane`,
`stop-failure-closed-lane`, `stop-gate-closed-lane-misnamed`, `stop-failure-closed-lane-misnamed`,
`stop-gate-archived-stopped`, `stop-failure-archived-stopped`, `install-plist-differs`;
`dispatch-remote-yes`'s `project/docs/milestones/M06.md` and its new expected state. Modified:
`lib/{dispatch,candidates,rows,tick}.sh`; `hooks/{stop-gate,stop-failure}`; `install.sh`;
`docs/milestones/M08.md` (§2, M07-d); the `install` scenario's expected second run;
`CLAUDE.md`, `CONTEXT.md`, `CONTRACT.md`, `docs/{SPEC,ARCHITECTURE,DECISIONS,MILESTONES}.md`, this brief;
the `expected/home/settings/*.json` of forty scenarios, each differing by the `remoteControlAtStartup`
line alone (checked mechanically before every freeze). Deleted: `tests/scenarios/prune-*` (twelve).

### 3. Interfaces

As `docs/ARCHITECTURE.md` §10 row M07 lists them. Changed from §6 of this brief: `claude_bg_remote` is
not introduced (D-080); `settings_compose` takes no `remote` argument; `worktree_prune` is removed.

### 4. Checks run

- **`sh tests/run.sh` on `m07`**, in order:
  - with the key written from the `Remote` cell and before the prune's removal: `199 scenarios, 41
    failed`, every failure the one new settings line (`41 scenarios differ only by the key`; `40 ×
    false, 1 × true`, the `true` in `dispatch-remote-yes`); refrozen;
  - on the one-command design, hooks and prune removal: `192 scenarios, 0 failed`;
  - after every session on Remote Control and the forty refreezes: `192 scenarios, 0 failed`;
  - after `/address`: `197 scenarios, 0 failed`.
- **Shown failing before the fix** (run against the code as it stood, or against an export of `54c07ee`):
  - `stop-gate-closed-lane` — the gate printed `{"decision":"block",…}` to a session whose handover was
    archived; `stop-failure-closed-lane` — the hook wrote an `api-error` artifact into the inbox;
  - `remote-row-not-a-park` on `54c07ee` — a remote lane waiting forty minutes produced no event at all;
  - `remote-dispatch-tick` on `54c07ee` — the `Remote: yes` candidate with `run` was never dispatched;
  - `stop-gate-closed-lane-misnamed` and `stop-failure-closed-lane-misnamed` — the gate blocked and
    StopFailure wrote an inbox artifact while the complete handover sat archived under another name;
  - `remote-stall-rearms` — a remote lane quiet for forty minutes after an answered prompt wrote nothing;
  - `install-plist-differs` — the person's edited agent was replaced by the repository's.
- **`stop-gate-archived-stopped`** freezes identical to `stop-gate-first` but for the archive: an archived
  `stopped` handover does not close the lane.
- **Plan file**: `baton plan Baton` against this worktree prints `M07  eligible`, `M07-b  waits on
  M07`, `M07-c  waits on M07-b`, `M07-d  waits on M07-c`, `M08  held by gate "Reclaim migrated"`.

#### 4a. Live, fixture project `Probe07` under `/Users/danny/.claude/jobs/e7f442dc/tmp/live/`

Every session was started with launchd's environment (`env -i` with `PATH`, `LC_ALL`, `HOME`, `USER`) and
a scratch Baton home; none touched `~/.baton/` or a real project.

- **Item 40 — holds, both halves.** `claude --bg -n "Baton · Probe07 · M03" … --settings <file with
  remoteControlAtStartup: true> "<prompt>"` at 21:42:41Z: the first `user` record is the prompt, the
  session wrote `"detail":"item 40: the positional prompt arrived"`, and the transcript records
  `remote_session_change` with `url: https://claude.ai/code/session_REDACTED` at
  21:43:07Z.
- **Item 39 — superseded, and its successor holds.** The idle `--remote-control` start it asked about is
  no longer used. What Baton relies on instead: a flagless resume of that session printed
  `note: woke session 84a92139 with its saved options (-n, --effort, --permission-mode, --settings,
  --model).`, and after its first tool result (21:47:25.317Z) the transcript recorded the same claude.ai
  URL again (21:47:25.335Z).
- **The control, key `false`** (`baton dispatch Probe07 M02`, session `64863e5e`): one
  `remote_session_change` with `url: null` at start and none after, across eight tool results; zero
  `bridge-session` records; absent from Claude.app's sidebar while its `true` siblings were listed.
- **REQ-ESC-08's phone proof, through `baton dispatch Probe07 M01`** (one command, session `141143f7`):
  `AskUserQuestion` at 21:45:20.409Z; row `blocked/waiting, waitingFor: "input needed"` by 21:45:24Z; the
  push reached the Claude mobile app and was answered `alpha.txt`, recorded as the tool result at
  21:45:50.694Z; row `working/busy` at 21:45:51Z; artifact `"detail":"answered: alpha.txt"`.
- **The cost of a session is its process, not its connection:** idle `466 MB` RSS with the key `false`,
  `454 MB` with `true`; 12 and 15 HTTPS sockets. The service did not stop M03's finished session
  (`a1f6772a`) in six hours.
- **Claude.app cannot wake a stopped session; a message typed to it is held.** `claude stop` makes
  claude.ai archive the session; after unarchiving, `hey` sat at "Sending…". A flagless resume at
  22:12:09.8Z delivered it one second later as a `queued_command` with `origin: {kind: "human"}`.
- **Grouping:** a session in a repository whose `origin` is `git@github.com:scalinity/probe07.git`
  (push URL set to fail) connected and still listed under "Other" (ruling A).
- **The takeover rule does not see Remote Control input:** every message the person sent from Claude.app
  into this session arrived as a `queued_command` attachment (`absorbed_mid_turn`); the only three
  `typed` records are Baton's prompts. Carried to M07-b.
- **The question park, live, three times:** `launchd.out` shows `question  Baton/M07 · … · waiting for
  input` followed by `unparked  … · the question was answered in place`, once for each question this
  session asked the person, then `consumed  M07-e7f442dc-….json → … (asking, stopped e7f442dc)` at
  18:16:11.
- **Live item 38's cold half** — run by the person with `proof-38-cold.sh` after `claude daemon stop
  --any`: `the service is down, which is the precondition`, then `stopped/other: the granted shell
  reached this file under ~/Documents`. It passed. The same `daemon stop` killed this session, and the
  tick confirmed the crash on two sightings and resumed it: `crash?` 16:57:29, `crash` 16:58:30,
  `resume … continue · delivered` 16:58:31.

#### 4b. The review

`/review-2` ran on `54c07ee..82b05cb…515039b` with two reviewers pinned to Fable, launched as background
agents: **DB1 (debugger)** returned in 5 m 28 s, **CA1 (code auditor)** in 5 m 54 s. A hand pass ran
beside them. The unified report had no critical findings, eight warnings and nine suggestions; every
one was addressed, each fix committed on its own:

- **W — hooks rebuilt the archive name from its parts** (DB1). Nothing refuses an inbox name that is
  not `<milestone>-<session>.json`, so a complete handover archived as `T01-handover-<at>.json` left
  the gate blocking a finished session and StopFailure writing `api-error` for a closed lane. Both
  hooks now match archived files by their fields, as their first rule — `e591ba8`, fixtures
  `stop-gate-closed-lane-misnamed` and `stop-failure-closed-lane-misnamed`, both shown failing first.
  The loop variable no longer reads as `done` and the check sits first in both hooks (suggestions,
  both reviewers and CA1).
- **W — a remote lane's second unanswered prompt passed in silence** (DB1). The stall key re-arms only
  on a dispatch, a takeover or a session-written consume, and a prompt answered from the phone writes
  none. A remote lane's key is now spent only while its transcript has not moved since the
  notification — `6a34537`, fixture `remote-stall-rearms`, shown writing nothing before the fix.
- **W — `install.sh` replaced the launchd agent** (CA1). launchd reads it at every login, so under
  D-079 a session could change what the tick runs, and with which shell, with no person's act. The
  agent is now copied only when none is installed; one that differs is left with a line — `3497ca9`,
  fixture `install-plist-differs`, shown overwriting the edited agent before the fix.
- **W — no `stop-failure-archived-stopped`** (both) — `6b2d7da`.
- **W — M07's handover described one entry while the plan made M07-b and M08 eligible** (CA1): a
  handover listing only M08 would have parked M07-b as `omitted`. Resolved in the plan: M08 now
  depends on M07-d (D-083), so M07's handover lists M07-b alone — `0f2815b`, `0d90091`.
- **W — "eighteen event kinds"** (CA1): seventeen after `worktree_pruned`'s removal, which is noted
  as historical — `0d90091`.
- **W — §10's M07 row and a comment in `dispositions_intersect` named the deleted two-step path**
  (both) — `f9b3cb5`, `0d90091`.
- **Suggestions** — a Remote Control glossary entry; the M06 plan row marked for the prune's
  removal; M07's brief noting D-080 and D-081; §4.4 naming a flagless resume as the way back into a
  finished session, since a flagged resume's copy escapes the stand-down (DB1); M08 beside M07-b now
  moot under D-083; Claude Code 2.1.270 in the product definition and the kickoff identity line (hand
  pass) — `0d90091`. The takeover rule missing Remote Control input (DB1) was already carried to M07-b.
- **Found by the address pass and not by the review:** the install scenario's second run had been
  copying the agent again on every install, silently; it now reports "is current".

Two additions came from the person during the address pass, each with its own decision: the
duplicate-handover guard as milestone M07-d with M08 behind it (D-083), and M07-b's wake made a
message sent to the session rather than a typed command, measured before building (D-084, with the
2.1.270 binary's Remote Control server strings as evidence).

#### 4c. The acceptance write-up: `~/.baton/` since M06's dispatch

The live log is closed to a dispatched session by the deny list (REQ-PERM-04), so the person copied
`~/.baton/` to `/Users/danny/.claude/jobs/e7f442dc/tmp/baton-home` at 17:25; later events are read from
`launchd.out`, which the tick writes beside the log.

**Every event since M06's dispatch** (`at`, `kind`, milestone, session):

```
15:29:10 dispatch        M06 c7cf6a1a attempt 1
15:32:12 consumed        M05 5fbd3e3f complete (second copy of the 15:29:08 handover; see §6)
16:49:31 consumed        M06 c7cf6a1a complete, merged_as f1e8b59
16:49:32 dispatch        M07 e7f442dc attempt 1
16:57:29 crash_sighting  M07 e7f442dc sighting 1
16:58:30 crash_sighting  M07 e7f442dc sighting 2
16:58:31 resume          M07 e7f442dc continue, class "process gone", delivered
```
then, from `launchd.out`: three `question` parks each resolved `answered in place`; `consumed` of
M07's `asking` artifact at 18:16:11 with the session stopped; the ruling delivered by the person's
`baton answer M07 "A: Other. B: 3"`, which resumed this session.

**Kinds that occurred:** `dispatch`, `consumed`, `crash_sighting`, `resume`, `escalation` (`question`,
`asking`), `resolution` (`answered in place`, `ruling`). **Waits:** none — no `api-error` ending and no
hold occurred. **Parks and their resolutions:** three `question` parks, each resolved in place within
the minute of the person's answer; one `asking` park, resolved by one ruling. **Kinds M06 introduced
that did not occur:** `plan_override`, `hold` for `fableReserve`, `omitted`, `disagreement`,
`main-broken`; `worktree_pruned` did not occur and no longer exists.

**§1.4, line by line:**

- **"A night with one lane in flight, one usage-limit wait and one `asking` artifact ends with the
  milestone merged, the ruling delivered, and every event in the log — with the person having typed one
  command." — not held.** No usage-limit wait occurred in the window, so the night it describes has not
  happened. What did: M06 ran unattended from 15:29:10 to 16:49:31 and merged (`f1e8b59`), one lane in
  flight; M07's `asking` park was resolved by one typed command and the ruling delivered (the resume
  that carried it is this session's resume 2). The person also typed into sessions through Claude.app,
  which Baton does not count and cannot yet see (M07-b).
- **"`baton tick` run twice against the same inputs changes nothing the second time." — held.** Against
  a copy of the snapshot, the Baton project pointed at a throwaway clone of the canonical checkout, the
  shim for `claude`, `date`, `osascript` and `caffeinate`, the M07 row as it stood, and M07's transcript
  cut at the snapshot's moment: `tick 1 exit 0`, `tick 2 exit 0`, `log lines: snapshot 17, after run 1
  17, after run 2 17`, `marker: 2026-09-12T17:26:00-04:00 / 2026-09-12T17:26:00-04:00`, and `diff -r
  after1 after2` — `NO DIFFERENCE`. A first attempt that read today's transcript against the 17:25
  snapshot wrote a `takeover` on the ruling's own resume message, whose sidecar postdates the snapshot;
  the second run still changed nothing.
- **"A fresh Mac needs exactly the setup facts in §2.11 and nothing that is not written down." — not
  held as written, and not assessable on this Mac.** One fact was missing and is now in REQ-SETUP-06:
  the phone is pushed only with `agentPushNotifEnabled: true`, which this Mac sets in
  `~/.claude/settings.json`. One behaviour belongs beside the setup facts and is recorded in §6: the
  background service takes its environment from whatever starts it, so a service cold-started by
  another launchd job hands that job's variables (here `BATON_HOME=/Users/danny/.baton/m03-probe/home`)
  to every session it later runs.

### 5. Checks not run

- **A usage-limit wait on the real log** — none occurred; §1.4's first criterion stays unobserved.
- **A fresh Mac** — not available.
- launchd is not in the tests (D-006).

### 6. Known limitations

- **A handover delivered twice is consumed twice.** M05's session wrote its artifact again after the
  tick had consumed it; the identical file was consumed at 15:32:12 into a second archive entry. Nothing
  ran twice, because the lane was closed and its successor in flight, but no rule refuses the repeat,
  and a repeated older handover would rank as the newest in force. Carried to M07-d (D-083).
- **The background service's environment is its starter's.** After the probe's launchd job cold-started
  the service, every session it ran — this one after its resume — carried `BATON_HOME` and the other
  seams of the probe. The hooks name `BATON_HOME` on their own command lines, so nothing Baton writes
  went astray; a session's own `baton` verb would read the wrong home.
- **Every `asking` park archives its session in Claude.app**, because the consume stops it; the person
  unarchived this one to type. Carried to M07-b.
- **A person typing through Remote Control is not seen by the takeover rule** (§4a). Carried to M07-b.
- **Old settings files** (M02–M06) carry no `remoteControlAtStartup`; those sessions follow the
  account's default when resumed.
- **The notice at the top of `CLAUDE.md`** concerns M03's integration with the
  `codex/architecture-foundation` branch, still not merged into `main`; this milestone did not act on
  it, as it names neither M07 nor this session's work.

### 7. Deviations from the brief

- **The two-step dispatch was deleted** (D-080), as §10 allowed on item 40's result.
- **Remote Control is on for every session** (D-081), by the person's choice after the facts in §4a; the
  brief's "per-dispatch, never default" premise did not hold on this account.
- **Worktrees kept, the prune deleted, the hooks' stand-down, the install at close-out** (D-078, D-079) —
  added at the person's direction, beyond §5.
- **M07-b, M07-c and M07-d added** to the plan with briefs, and M08 moved behind M07-d (D-081 to D-084);
  the fixes from the review in §4b; `tests/shim/claude` gained and then
  lost an idle-start mode, and ends unchanged.
- **`tests/scenarios/remote-two-step` is not written**; `remote-dispatch-tick` covers the path that exists.

### 8. Commits, on `m07`

- `82b05cb` Dispatch Remote: yes as one command, keep milestone worktrees, and stand the hooks down on a closed lane
- `d1df8e4` Record what M07 settled so far: one-command remote dispatch, kept worktrees, the install at close-out
- `a72026c` Put every dispatched session on Remote Control
- `515039b` Split the session lifecycle and the Mac message into M07-b and M07-c
- `f9b3cb5` docs(candidates): describe the remote candidate by the one-command dispatch
- `e591ba8` fix(hooks): close the lane by the archived handover's fields, not its file name
- `6b2d7da` test(hooks): pin that an archived stopped handover leaves StopFailure writing its artifact
- `6a34537` fix(rows): re-arm a remote lane's stall once its transcript moves after the notification
- `3497ca9` fix(install): leave an installed launchd agent that differs in place
- `0f2815b` docs(plan): add M07-d for handovers delivered twice, and make M07-b wake a session on a message
- `0d90091` docs: bring the architecture, spec, glossary and plan into line with M07's code
- the close-out commit carrying this evidence.

The merge of `m07` into `main` is the handover artifact's `merged_as`.

### 9. Working tree as observed

`git status --short` on `m07` before the close-out commit is empty.

### 10. Exact next step, and the next eligible milestone

M07-b, brief `docs/milestones/M07-b.md` under "Copy-ready session prompt", disposition `run`, dispatched by
the tick after this handover is consumed; the relay it runs is installed by this close-out. Nothing
else is eligible: M07-c, M07-d and M08 wait on it in turn, and M08 stays behind the gate "Reclaim migrated".

## Copy-ready session prompt

```
You are implementing milestone M07 of Baton at /Users/danny/Documents/Apps/Baton. Baton is a relay: a personal tool for one person on one Mac, on Claude Code 2.1.268, that carries a build from one Claude Code session to the next — a launchd-run tick every sixty seconds that reads its inbox, its dispatch log, the target project's plan file and one git check, then dispatches, resumes, waits and escalates by fixed rules; it embeds no model call. M07 is the phone and the proof: the two-step Remote: yes dispatch, and the acceptance write-up of the nights Baton has driven itself since M06.

WHAT ELSE IS IN FLIGHT. Runs alone unless the dispatch says otherwise.

STARTUP ORDER. Read CLAUDE.md, then CONTEXT.md, then CONTRACT.md, then docs/milestones/M07.md in full, then the SPEC, ARCHITECTURE and DECISIONS sections listed in M07 §3, then the addendum of the ticket "Watching a dispatched session", then docs/milestones/M06.md's completion evidence — §4b for what its review found and §6 for its known limitations — and docs/ARCHITECTURE.md §10 row M06 and D-070 to D-076 for what M06 actually built. Then inspect the working tree and read ~/.baton/log.jsonl since M06's dispatch event before writing anything. If M07 "Completion evidence" is non-empty or claude_bg_remote exists, follow M07's Recovery procedure and resume only the unfinished portion.

WHAT TO SETTLE RATHER THAN INHERIT. (1) Item 40: whether remoteControlAtStartup in a --settings file starts Remote Control on a --bg session and whether such a session keeps a positional prompt — if both hold, Remote: yes becomes one command and the two-step path is deleted with a decision entry; if not, nothing changes and the two-step path stays, because a mechanism seen working beats one that might. (2) Item 39: which flags a flagless resume restores after an idle --remote-control start — read the woke session line's list rather than assuming. (3) The acceptance write-up's form: each criterion of docs/SPEC.md §1.4 marked held or not held with the log lines that show it, and the double-tick check run against a copy of the real ~/.baton/ through the shim, never against the live one. Facts you do not re-derive: --remote-control at dispatch discards the positional prompt and starts the session idle with no transcript written; a flagless resume against the live idle session forks a copy, so the stop is required; the phone was pushed and answered twice in the prototype, clearing the row within a second; a remote session's row read working/idle with waitingFor null through a whole park, so it is not a park detector and its parks are artifact-borne; Remote Control stores the transcript on Anthropic's servers while connected, which is why it is per-dispatch and never default. Facts M06 settled that you build on: step 8 no longer lives in tick_project — dispatch_run in lib/tick.sh dispatches once per tick across every project, in cap_order's order, after dispositions_intersect in lib/candidates.sh has produced each project's candidates; the Remote: yes skip that M07 replaces is in dispositions_intersect's final filter (".remote != true"), and dispatch_one still refuses a remote row with status 2, so both places change together. The cap counts derivation 1 across every project, so a remote lane counts once its session has a live row. A plan_override, a worktree_pruned, a fableReserve hold and the escalations omitted, disagreement and main-broken are new kinds and classes the acceptance write-up may meet in the real log, and a line that says "held … the project is parked" or "prune … left in place" is launchd.out, not the log. omitted is judged only once a project has an archived complete handover and never while a dependency's session is mid-run (D-071); your handover's M08 held by "Reclaim migrated" agrees with the plan's uncleared gate, so it writes no plan_override and escalates nothing until a person clears the gate, and then the tick dispatches M08 with a plan_override. stops_standing_by stands step 4 down for any park naming a milestone, project scope included (8879d8b). The claude shim answers a resume per session with resume.note.<session> and resume.status.<session>, and a scenario can carry a second committed fixture project under other/ (D-076). The standing check is 199 scenarios and takes about eleven minutes, so run it in the background; BATON_TESTS_ONLY=<glob> runs matching scenarios alone while you iterate, and BATON_TESTS_FREEZE=<name> refreezes one you have read.

CONSTRAINTS. Never use Python. Shell only: /bin/sh with set -eu, jq -e, awk, git; no third-party packages; sh tests/run.sh is the standing check and every check is reported as passed, failed or unrun with its output. Never touch /Users/danny/Documents/Apps/Reclaim. Never start, stop, attach to or resume any session except the fixture-project remote session item 4 names, and never one named Baton · Reclaim · anything. Every resume is flagless. The log has one writer. You are in a worktree Baton created; stage only your own paths by name, never git add -A; merge into main at close-out and refresh there. Commit locally with the existing git identity, a neutral technical message, no authorship trailers, never addressing a person; take the next free D-number at the moment you write the entry.

VERIFICATION. Per M07 §8. Report every check as passed, failed or unrun with its output; an unrun check is never reported as passed.

CLOSE-OUT, in this order, once the M07 checklist is finished.
1. Run /review-2 on this session's changes and do a hand pass over the same checklist while the reviewers work. Launch the two reviewers as background agents pinned to Fable rather than as teammates: on M05 and M06 both reports came back that way, one of M06's in eleven minutes, after the teammate channel had dropped every report on M02 to M04. A fixture you write for a finding should be shown failing before the fix; on M06 that is how a defect neither reviewer named was found. Record what was run and what returned.
2. When the review report is in, run /address. No git remote; commit fixes locally. Run sh tests/run.sh again after the fixes.
3. Append the handoff to docs/milestones/M07.md under "Completion evidence" using the docs/MILESTONES.md template, with the acceptance write-up. Add the docs/ARCHITECTURE.md §10 row M07 detail and the docs/DECISIONS.md entries for what you settled. Commit on m07.
4. Merge m07 into main in /Users/danny/Documents/Apps/Baton; if the merge fails, write ~/.baton/inbox/M07-$CLAUDE_CODE_SESSION_ID.json with outcome stopped and reason merge-failed and go no further. Run sh tests/run.sh on main; fix main if it fails, else write stopped with reason main-broken. On main: write done in M07's Status cell in docs/MILESTONES.md; refresh docs/milestones/M08.md's copy-ready prompt (parts 1 and 3–7, part 4 additively, part 2 left as the slot line); remove the worktree ../Baton-M07; commit, staging only your own paths.
5. Do not start M08. Do not mark M07 complete unless every acceptance criterion in §8 holds.
6. Write ~/.baton/inbox/M07-$CLAUDE_CODE_SESSION_ID.json (.tmp first, then rename) with baton 1, project /Users/danny/Documents/Apps/Baton, milestone M07, your session id, outcome complete, merged_as the merge commit on main, written_at, and eligible listing M08 with disposition held and held_by "Reclaim migrated" and brief path docs/milestones/M08.md heading "Copy-ready session prompt" — the plan's gate holds it until the person clears it, and the artifact mirrors the plan. Print it verbatim, last, in a fenced block whose info-string is baton; if anything lands after it, deal with it and print it again.
```
