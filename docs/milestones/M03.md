# M03 — Safe reconciliation tick

**In progress** on `m03` in `/Users/danny/Documents/Apps/Baton-M03`. Tick, row reconciliation,
notification and launchd code already exist there, with additional changes underway. The inspected
head was `c93098386bd54b2f7324dca220e4bc156d6aea72`; re-read its head/dirty state before integration.

M02-b is a combined acceptance prerequisite, not an instruction to restart this milestone.
Preserve the existing work and follow `docs/M03-RECONCILIATION.md`; do not dispatch a duplicate M03.

## Required reading

CLAUDE.md, docs/GOVERNANCE.md, CONTEXT.md, CONTRACT.md, docs/SPEC.md, docs/ARCHITECTURE.md,
docs/FOUNDATION.md, docs/MIGRATION.md, and the predecessor's completion evidence.
Read docs/M03-RECONCILIATION.md and M03's own branch-local decisions/evidence as well.
Historical prototype captures can inform an adapter experiment; they are not current guarantees.

## Scope

Compose one coherent journal/plan snapshot and tagged provider observation. Reconcile pending actions first. Respect human ownership before any candidate action. Consume immutable messages. Dispatch from the current committed graph only when existing capacity and safety checks permit. Add launchd and a completed-tick marker under the lock; never execute target checks in the tick. Verify FDA and notification behavior with explicitly authorized fixture sessions before unattended use.

Adapt the existing M03 implementation rather than starting over. Preserve its timing fixtures,
unavailable-row safeguard, notification escaping and shell-signing evidence. Replace old protocol
assumptions at the boundaries named in the reconciliation table. Reuse the complete dispatch
admission checks rather than calling the raw launch helper. Failed/partial work cannot publish a
fully completed marker; notification intent is not delivery proof; detached holders close FD 9.

## Acceptance

Unavailable observations do not advance crash counters; uncertain launches never duplicate; human-owned runs receive no process action; second reconciliation is state-idempotent; interruption before completion leaves the old marker; no permissive placeholders bypass cap, ownership or receipt checks.

F-39 also requires separate acknowledged-run and observed-process state: a historical active run
must not be displayed or acted on as definitely working. This is a repository-wide state-model
obligation; M03's specific reconciliation checklist is one application of it.

Run sh tests/run.sh and add behavior assertions for the new transitions. Use fault injection at
new effect boundaries, not only snapshot comparisons. Use two independent reviews and address
concrete findings. No build, live session, launchd or target-project operation is implicit in the
fixture test suite. Obtain any required explicit authorization before a live acceptance action.

## Recovery procedure

Inspect existing code, durable action records, candidate work and prior evidence. Resume only the
unfinished part. Do not reset failure budgets, duplicate external effects, overwrite human work,
or weaken an invariant to make a continuation succeed. Split scope into a real plan row if needed.

## Completion evidence

Implementation is underway on the separate `m03` workstream. The foundation's 31/61 result does not
certify it. The branch-local Completion evidence heading was empty at inspection; its decisions
report host findings, which must retain their provenance rather than be inferred as rerun here.
Combined compatibility, test/review output and outstanding live proofs remain to be recorded.

## Copy-ready session prompt

```
Continue the existing M03 work: Safe reconciliation tick. Read the Required reading, Scope and
docs/M03-RECONCILIATION.md before changes. Preserve the m03 worktree and uncommitted work. Do not
restart the milestone, discard its tests, or dispatch a duplicate M03 session.

WHAT ELSE IS IN FLIGHT. Runs alone unless the dispatch says otherwise.

Follow the current contract-2 architecture, P-01–P-12 and applicable F-01–F-45 requirements. Implement only this
milestone. Treat unsupported provider behavior and missing observations as uncertainty. Keep
ownership, identity, journal recovery and exclusive integration intact. No safety branch is a
permissive placeholder. Never use Python, add packages, or run builds without explicit instruction.
Run shell fixtures, review with two independent reviewers, fix findings and record actual evidence.
Prepare close-out and the done cell on your candidate branch. Do not edit canonical main directly,
remove worktrees, or recursively refresh successor prompts. Use baton integrate for verified
promotion, then publish one immutable contract-2 handover naming its exact receipt. Do not start
the next milestone. A direct maintenance session without a run ID reports to the person and does
not invent a managed completion artifact.
```
