# M15-c — The independent scope guard

## 1. Identity

- **ID:** M15-c. **Objective:** always bring goal drift to the person through an independent check.
- **User-visible result:** a deliberately drifted milestone is stopped by a guard that knows
  the confirmed goal, not the plan's rationale. Aligned work advances without a routine human gate.
- **Size:** Medium, about 3–4 hours; high effort. This is M15's original natural split after replan.

## 2. Dependencies and entry conditions

M15-b complete and installed (and therefore M15/M12). Registration stores a confirmed goal;
completion records baseline, actual changes and Baton-produced standing-check evidence. Use
M15's human-required route and M12's adoption boundary. Read M13/M14's interfaces when available;
use the shared admission seam without adding entry dependencies.

## 3. Required reading

`CLAUDE.md`, `CONTEXT.md`, `CONTRACT.md`, `docs/MILESTONES.md`, SCOPE §6 M15 and §7;
SPEC §2.4, §2.5, §2.8, §2.9, §5; ARCHITECTURE §4.1–4.3, §5.2, §6.2, §6.4, §7, §10;
M12, M15 and M15-b completion evidence and decisions; current planning/adoption, completion,
escalation and CLI admission interfaces. Read M15's original finding for disposition boundaries.

## 4. Requirements, scope, non-goals

Independent goal-only checks before plan adoption and before each close-out advances successors.
Drift is always human-required and notifies. Neither AI resolution, replan nor HOST-EXPLAINED
can clear it automatically. No generic managed review framework, direct API call, new person
approval gate, inherited plan authority or replay of completed taxonomy/host work.

## 5. Expected files (proposed)

New `lib/scope.sh`; narrow planning/adoption and close-out integration seams;
`tests/scenarios/scope-guard-*`; SPEC, ARCHITECTURE §10, DECISIONS and this brief's evidence.
Inventory actual files first; more than two existing library edits requires another preimplementation
split per the plan. Outside the repository: CLI judgement prompts/results under existing state paths.

## 6. Interfaces

Independent goal-check request/result consuming only stored goal and proposed plan or actual
just-completed work/evidence. Use subscription CLI sessions via the shared admission seam and
existing records; explicit pass, drift or unresolved outcome. Missing/failed checks never pass.

## 7. Implementation checklist (ordered)

1. Establish an independent session context; verify that cwd, inherited instructions and sibling
   history cannot smuggle planning rationale or previous approval into the guard.
2. Before plan adoption, supply only goal and proposed plan; block advancement pending a result.
3. Before close-out advances successors, supply only goal and actual change/evidence; not the
   plan author's explanation. Make request/result consumption idempotent across ticks/restarts.
4. Route drift to a human-required escalation and Mac notification. AI/replan cannot approve or
   clear its own drift. Aligned work passes; unavailable/failed checks remain unresolved.
5. Run §8, document exact input isolation, review and close out.

## 8. Fixtures, verification, acceptance

- Fixture aligned/drifted plans and close-outs, failed/unavailable checks, exact input isolation,
  repeated ticks, and refusal of AI/replan/host disposition to clear drift; each twice, then
  `sh tests/run.sh`.
- Live: disposable fixture projects only, with aligned and deliberately drifted work checked in
  separate cold CLI sessions. Capture exact goal/work-only prompts and the drift Mac message.
- Acceptance: both boundaries checked independently; aligned work advances, drift reaches a
  person, unresolved checks block advancement, and the guard cannot approve its own escalation.

## 9. Definition of done and evidence

Ten-item handoff with exact guard input sidecars, isolation proof, fixture and live results,
human-directed drift record/message, and current boundary interfaces. Note any M13/M14 integration
proved only by fixture. Combined M15 acceptance is not complete until this part is done.

## 10. Risks and decisions this milestone settles

Review can inherit a false premise. Only confirmed intent and actual work belong in this check.
A model in the target cwd can inherit instructions: verify isolation, do not merely call it cold.
Replan preserves completed work and goal; it never expands the goal or clears drift by itself.

## 11. Handoff and next milestone

Apply `CONTRACT.md` clause 3(c) to every refresh below: run `baton status` and check the
dispatch log for open lanes, including lanes absent from status; status also lists open parks. Refresh only a listed
milestone with neither an open lane nor an open park; still name every eligible milestone with its correct disposition.

Direct successor **M16**, `docs/milestones/M16.md`, heading `Copy-ready session prompt`.
M16 requires both M14 and M15-c. Refresh it with actual guard/disposition/host interfaces. Re-read
live Status cells, dependencies and gates. Include every direct successor and all other eligible
work: gate => held/held_by; unfinished dependencies => wait/wait_for; else run. The first of
M14/M15-c to close names M16 wait for its unfinished sibling; the second names run unless gated.
Do not assume order or start M16.

## Recovery procedure

Inspect request/result records, input sidecars and completion evidence; resume the first unfinished
item without duplicate guards. Never treat missing evidence as approval or clear drift automatically.

## Completion evidence

## Copy-ready session prompt

```
You are implementing M15-c of Baton at /Users/danny/Documents/Apps/Baton. Baton is a relay, a personal tool for one person on one Mac, on Claude Code 2.1.270, that carries a build from one Claude Code session to the next; a launchd-run tick every sixty seconds; it embeds no model call in the tick, so judgement is dispatched as a session and returns as an artifact. Add the independent goal-only scope guard at plan adoption and at every close-out. Drift always reaches a person. Size: Medium, 3–4 hours, high effort.

WHAT ELSE IS IN FLIGHT. Runs alone unless the dispatch says otherwise.

STARTUP ORDER. Read CLAUDE.md, CONTEXT.md, CONTRACT.md, this brief and §3's sources. M15-b is complete and installed; read its completion evidence in docs/milestones/M15-b.md before the rest, because two of the things it settled are yours to consume rather than rediscover. Then read lib/disposition.sh in full: disposition_of <kind> <class> [<carries>] now reads the carries on two arms rather than one — escalation:asking for options, and notification:gap for host.assessed — so a disposition is a reading for some classes and a constant for the rest, and the table still answers human-required for a class asked with no carries at all. record_only <project> <milestone> <session> <attempt> <class> <scope> <carries> writes the silent escalation and record_notification <project> <milestone> <session> <attempt> <class> <key> <fields> is its notification sibling, taking notification_write's arguments in notification_write's order minus the message; both ask disposition_of and refuse anything the table does not call HOST-EXPLAINED. Read tests/scenarios/disposition-table, which extracts the class list from class_or_fail and the arm list from disposition_of and set-differences them, so any arm you add or remove shows there. Inspect baton status and the working tree and use the Recovery procedure if work exists. Inventory your edits and apply the split rule before a third existing library edit.

WHAT TO SETTLE RATHER THAN INHERIT. The guard receives only the confirmed goal and either the proposed plan or the actual just-completed work and evidence. Prove independence from sibling transcripts, inherited instructions, the plan author's rationale and any previous approval. Both boundaries wait for the result; a missing or failed check is unresolved and never a pass. Drift is human-required and nothing clears it automatically — and that now has a sharper edge than when this brief was written: HOST-EXPLAINED is no longer a property of a class alone. M15-b made notification:gap HOST-EXPLAINED for an event whose own evidence explains it, so "no disposition can clear drift" has to be enforced on the class and not on any evidence a caller attaches; a drift escalation must be refused by record_only whatever its carries hold, and tests/scenarios/disposition-silent-record is where that refusal is shown. Do not re-open D-189's coverage policy, D-190's writer pair or D-191's file-count judgement. Use the shared admission seam so M14 pacing applies when installed, and remember that a judgment session is a lane like any other — a name, an attempt, a dispatch event, an injected Stop gate and an owed artifact — while a judgment request is a foreground claude -p under a deadline that takes no slot; CONTEXT.md defines both and the difference decides whether the cap can deadlock.

CONSTRAINTS. Never Python; POSIX shell with set -eu, jq -e, awk, git and native host tools; no packages, Swift, SQLite, privileged helper or direct API call. Only disposable fixture judgement sessions under §8; never touch Reclaim and never dispatch into it. Keep the deny rules, the single-writer log, flagless resumes and the existing escalation machinery. Work in the assigned worktree, stage only your own paths, never git add -A, and accommodate concurrent work: other lanes run beside you and append to the same documents. Existing git identity; neutral technical messages; no authorship trailers. Take the next free D-number, read from main at the moment you write it, and re-read it if your merge conflicts — M14 and M15-b took the same three numbers during M15-b's close-out and one lane renumbered.

VERIFICATION. Execute §8. Show the exact input the guard was given at both boundaries, an aligned plan passing, a drifted one notifying, and a failed or unavailable check staying unresolved rather than passing. Run sh tests/run.sh, read it to completion, and quote its scenario count and failure count; a run the harness moved to the background is not finished until its output has been read, and a check nobody read is unrun.

CLOSE-OUT, once this checklist is finished.
1. Run /review-2 with a hand pass, /address locally, and sh tests/run.sh after the fixes.
2. Append the ten-item handoff under Completion evidence; update SPEC, ARCHITECTURE §10, and DECISIONS; commit on the assigned branch.
3. Merge to main in the canonical checkout. If the merge fails, write stopped/merge-failed and go no further. Run sh tests/run.sh on main; fix it there or write stopped/main-broken. If another session has uncommitted work in the canonical checkout, do not commit, stash or clobber it: wait a bounded time, then stop and report rather than waiting forever.
4. Mark only M15-c done after §8 holds. Establish open lanes and open parks by baton status and CONTRACT.md clause 3(c)'s dispatch-log check, which a dispatched session answers with baton status and baton plan because the rail denies it the log the clause names (D-173). Refresh on main only the prompts of listed milestones with neither an open lane nor an open park, with the actual interfaces, preserving each slot paragraph verbatim. Keep the worktree. Commit your own paths and run BATON_HOME=/Users/danny/.baton sh install.sh from the canonical checkout; record any warning it prints.
5. Start no successor. If the work will not fit, follow the split rule and write completion evidence plus stopped/unfinished, never a false completion.
6. Re-read the live plan and apply §11 to every direct successor and every other eligible milestone, with held/wait/run dispositions. Refresh and commit only the eligible prompts clause 3(c) permits before writing /Users/danny/.baton/inbox/M15-c-$CLAUDE_CODE_SESSION_ID.json atomically (.tmp then rename): baton 1, canonical project, milestone M15-c, session, outcome complete, the verified merged_as, written_at, and eligible entries with brief paths and the Copy-ready session prompt heading. Follow installed completion rules. Print the artifact verbatim last in a fenced block whose info-string is baton; if anything lands after it, deal with that and print it again.
```
