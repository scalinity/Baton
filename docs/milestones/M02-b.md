# M02-b — Durable orchestration foundation

Authorized directly by the user after accepting the architectural audit, 2026-09-12.
This maintenance work spans the original milestone boundaries and corrects future plans before they
are implemented. It does not install a live release or claim M03–M08 acceptance.

## Scope

All remediation items in docs/FOUNDATION.md, the contract-2 runtime, new fault tests, explicit
migration, immutable installation and rewritten future milestone briefs.

## Completion evidence

Delivered contract-2 run/message identity, kernel locking, atomic journal replacement, provider
observations, claim/ack/archive recovery, explicit ownership, verified integration/retry/reconciliation,
legacy adoption, immutable installation and corrected future milestone requirements.

Validation: 31 retained historical regression scenarios and 61 foundation behavior/fault checks
passed, with shell/schema syntax and git diff --check. Full output is docs/validation/foundation.txt;
two independent reviews and finding closure are recorded in docs/reviews/foundation.md.

This was direct user-authorized maintenance on codex/architecture-foundation, not a managed Baton
run. No integration receipt or handover artifact is invented for it. The installed relay, live
sessions and target projects were not changed. No builds or packages were used. Live provider,
launchd/FDA, remote and unattended acceptance remain unrun and belong to the later milestones.

M03 is already in progress in its own worktree. Reconcile the workstreams using
docs/M03-RECONCILIATION.md, then validate the combined candidate before activation under
docs/MIGRATION.md. Foundation changes are preserved on `codex/architecture-foundation` for
review and deliberate reconciliation with M03; they must not be assumed present on `main`.

## Copy-ready session prompt

```
Maintain M02-b: the orchestration foundation. Read CLAUDE.md, CONTRACT.md, docs/FOUNDATION.md,
docs/SPEC.md, docs/ARCHITECTURE.md and docs/MIGRATION.md. Inspect existing implementation and
fault tests. Preserve completed work; address only remaining evidence-backed gaps.

WHAT ELSE IS IN FLIGHT. Runs alone unless the dispatch says otherwise.

Run sh tests/run.sh. Review changes independently, fix findings and record actual evidence.
Do not install, dispatch live sessions, run builds or touch target projects as a testing side effect.
Follow the current contract for managed close-out; a direct maintenance session reports to the
person without inventing a run or handover.
```
