echo "== an archived file no event claims and that cannot be read back into a lane is named and left exactly as it is, because guessing its lane would write an event about a milestone nothing establishes =="
sh "$ROOT/tests/completion-fixture.sh" valid
sh "$ROOT/tests/consume-once.sh"
