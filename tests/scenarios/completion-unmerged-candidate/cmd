echo "== the claimed commit descends from the baseline and a separate branch holds scoped work, which is the ancestry that used to be enough; the candidate is not inside the claim, so it is refused =="
sh "$ROOT/tests/completion-fixture.sh" unmerged-candidate
sh "$ROOT/tests/consume-once.sh"
