# M07 — A Size only the prompt states

## 1. Identity

- **ID:** M07. **Objective:** stand in for a real milestone in Baton's own tests.
- **User-visible result:** its §1 section states no Size at all.

## 5. Expected files (proposed)

`docs/milestones/M07.md`; `docs/M07.md`.

## Completion evidence

## Copy-ready session prompt

```
You are implementing milestone M07 of Fixture. One line that never changes. Size: Large, about 5–7 hours

WHAT ELSE IS IN FLIGHT. Runs alone unless the dispatch says otherwise.

STARTUP ORDER. Read CLAUDE.md, then the brief.

## 1. Identity
- **Size:** Large, about 5–7 hours; high effort.

The two lines above are inside this fenced block on purpose. A reader that tracked headings
but not fences would re-enter section 1 here and read that Size as the declaration, which is
the one place the dispatch must never take it from.

CONSTRAINTS. Stage only your own paths; never git add -A.

CLOSE-OUT. Write ~/.baton/inbox/M07-$CLAUDE_CODE_SESSION_ID.json and print it last in a baton fence.
```
