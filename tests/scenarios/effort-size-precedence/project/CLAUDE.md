# Fixture

A fixture project for Baton's tests, whose briefs declare every Size a dispatch has to read and
two it cannot. The plan file is `docs/MILESTONES.md`; briefs are `docs/milestones/M<nn>.md`; the
contract is `CONTRACT.md` in Baton.
