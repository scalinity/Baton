echo "== after the stop the first listing fails and the next two still show the pid: the resume waits for"
echo "   the stop to land, because a resume into a session still running starts a copy =="
"$BATON" answer M02 "expose it, and say why in the brief" || echo "status $?"
