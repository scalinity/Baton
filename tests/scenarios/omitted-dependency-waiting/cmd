echo "== M01 reads done and its session is waiting out a rate limit, not finished: the hook's api-error artifact is not the session's handover =="
echo "== so M02 does not escalate as omitted, and the omitted park already on M07 stays open rather than clearing itself =="
"$BATON" tick
