== the standing check failed and the consumed event says so, but the tick was killed before the project park was appended: the receipt exists, nothing re-derives the park, and the chain would run on over a tree Baton has already recorded as broken — so the reconciliation owes that park and writes it ==
