echo "== M02 and M03 both merged onto a broken main: two project parks =="
"$BATON" tick
echo "== one ruling after the fix resumes M02, then M03, with the same words, and closes both parks =="
"$BATON" answer M02 "main fixed; finish the close-out from step (c)" || echo "status $?"
echo "== the project is no longer held, and with the cap at three M04 dispatches beside the two resumed sessions =="
"$BATON" tick
