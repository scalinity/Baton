echo "== the planning handover Baton already consumed is delivered again: it is archived as a repeat, no chain is checked a second time, and the plan the first one produced is not adopted twice =="
sh "$ROOT/tests/completion-fixture.sh" planning
sh "$ROOT/tests/consume-once.sh"
