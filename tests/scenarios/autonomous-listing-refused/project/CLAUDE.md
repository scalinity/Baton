# Fixture

A fixture project for Baton's tests, shaped as a graph rather than a lane: `M02` and `M03` are
independent of each other and `M04` joins them. The plan file is `docs/MILESTONES.md`; briefs are
`docs/milestones/M<nn>.md`; the contract is `CONTRACT.md` in Baton.
