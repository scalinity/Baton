echo "== the same independent pair, with the listing refused on the fifth agents --json call of the scenario — the one dispatch_run takes again before its second dispatch. M02 is already started and stays started; M03 is not dispatched onto a listing Baton could not read, and the tick reports itself incomplete, so the marker is not advanced. The second run of this cmd is the next tick: the listing answers again and M03 goes, which is what "the candidate waits sixty seconds" costs =="
"$BATON" tick
echo "== tick exit: $? =="
"$BATON" status
