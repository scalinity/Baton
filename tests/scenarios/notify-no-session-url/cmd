# The applet is installed and the transcript holds no usable URL — a null one, and one that is not
# exactly a claude.ai session URL — so the message is spooled with an empty target, which a click
# opens as Claude.app's needs-input view.
"$BATON" tick
