# M01 — Read the fixture note

## 1. Identity

- **ID:** M01. **Objective:** read `docs/notes.md` and record what it says in the fixture's own log.
- **User-visible result:** `notes/read.txt` exists and quotes the note.
- **Size:** Small, about two hours; medium effort.

## 2. Dependencies and entry conditions

None. This is the first milestone; the repository is checked out on `main`.

## 3. Required reading

`CLAUDE.md`, `docs/MILESTONES.md`, this brief in full.

## 4. Requirements, scope, non-goals

- **Scope:** read the note and write what it says.
- **Non-goals:** changing the note; M02 says what the note said.

## 5. Expected files (proposed)

`notes/read.txt`, `docs/milestones/M01.md`.

## 6. Interfaces

- **Introduces:** `notes/read.txt`.
- **Consumes:** `docs/notes.md`.

## 7. Implementation checklist (ordered)

1. Read `docs/notes.md`.
2. Write `notes/read.txt` quoting it.
3. Verification, review and close-out.

## 8. Fixtures, verification, acceptance

- **Verification:** `test -s notes/read.txt`.
- **Acceptance:** `notes/read.txt` quotes the note.

## 9. Definition of done and evidence

The completing session appends what changed and the merge commit under Completion evidence.

## 10. Risks and decisions this milestone settles

- **The note may be empty.** Then `notes/read.txt` says so, rather than being absent.

## 11. Handoff and next milestone

Nothing follows. The chain ends here.

## Recovery procedure

Look at `notes/read.txt` and this brief's Completion evidence; resume the first unfinished item of
section 7 rather than starting over. No completion claim without section 8.

## Completion evidence

## Copy-ready session prompt

```
You are implementing milestone M01 of Fixture at the checkout this session was dispatched from, a fixture repository for Baton's tests. Read the note and record what it says. Size: Small, about two hours, medium effort.

WHAT ELSE IS IN FLIGHT. Runs alone unless the dispatch says otherwise.

STARTUP ORDER. Read CLAUDE.md, docs/MILESTONES.md and docs/milestones/M01.md in full, then the working tree. If this brief's Completion evidence is not empty, follow its Recovery procedure and resume only the unfinished part.

WHAT TO SETTLE RATHER THAN INHERIT. Whether an empty note is recorded as empty or as absent: record it as empty, because absent and empty are different facts and only one of them is true.

CONSTRAINTS. Shell only; no packages. Stage your own paths by name and never git add -A.

VERIFICATION. Follow section 8 of docs/milestones/M01.md. Report every check as passed, failed or unrun with its output; an unrun check is never reported as passed.

CLOSE-OUT, in this order.
1. Append completion evidence to docs/milestones/M01.md and commit on this branch.
2. Merge into main, keeping the branch and its commits.
3. Write done in M01's Status cell in docs/MILESTONES.md on main and commit.
4. Write the handover artifact naming M02 as eligible, then print it last in a baton fence.
```
