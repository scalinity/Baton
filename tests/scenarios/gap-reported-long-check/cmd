echo "== a real outage, reported from inside a tick whose own standing check then spends eleven minutes: the gap is the two hours Baton was actually not running, not those two hours plus the length of the check =="
sh "$ROOT/tests/completion-fixture.sh" check-clock
"$BATON" tick
