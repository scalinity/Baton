# M06 — Remote one

## Completion evidence

## Copy-ready session prompt

```
You are implementing milestone M06 of Fixture. One line that never changes.

WHAT ELSE IS IN FLIGHT. Runs alone unless the dispatch says otherwise.

STARTUP ORDER. Read CLAUDE.md, then the brief.

CONSTRAINTS. Stage only your own paths; never git add -A.

CLOSE-OUT. Write ~/.baton/inbox/M06-$CLAUDE_CODE_SESSION_ID.json and print it last in a baton fence. Do not start M07.
```

Text after the block, with a second fence that must not be read:

```
not the prompt
```
