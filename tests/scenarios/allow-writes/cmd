echo "== the rule as typed, into the project's allowlist and the live settings file in place =="
"$BATON" allow M02 'Bash(xcodebuild:*)' || echo "status $?"
echo "== a rule both files already carry widens nothing, so it writes no widening either =="
"$BATON" allow M02 'Bash(jq:*)' || echo "status $?"
