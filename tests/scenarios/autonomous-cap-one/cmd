echo "== the same pair under a cap of one: M02 takes the slot on its handover's own order and M03 waits for one, unparked and unescalated. The cap is a ceiling on what runs at once, never a verdict on what is eligible =="
"$BATON" tick
echo "== status: one in flight, and nothing says M03 is held =="
"$BATON" status
