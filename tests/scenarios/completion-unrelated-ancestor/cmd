echo "== L13: the artifact names the repository's first commit, which is ancestral to main and passes merged_as verification; the branch it was dispatched on is not inside it, so the chain refuses it =="
sh "$ROOT/tests/completion-fixture.sh" valid
sh "$ROOT/tests/consume-once.sh"
