echo "== another lane's work landed on main after the merge: the changed paths are still the branch's own, and the check names the merge commit rather than whatever main now points at =="
sh "$ROOT/tests/completion-fixture.sh" main-moved-on
sh "$ROOT/tests/consume-once.sh"
