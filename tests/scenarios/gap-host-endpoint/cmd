echo '== the endpoint is the one the gap measured, not the one the event was written at. This tick starts at 00:00:00, runs a standing check that carries the clock to 00:11:00, and writes the gap event there; the gap itself is the two hours from 22:00 to 00:00. The host slept from 21:58 to 23:59:20, so the window the gap measured is covered with forty seconds awake and the event is recorded silently. Take the endpoint from the event at-time instead and the eleven minutes the check spent become eleven minutes the host has to account for and cannot: the same history would notify. calls.log carries no osascript line, which is the whole of the assertion =='
sh "$ROOT/tests/completion-fixture.sh" check-clock
"$BATON" tick
echo
"$BATON" status
