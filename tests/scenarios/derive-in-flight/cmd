. "$ROOT/tests/lib-load.sh"
# Historical read-model fixture, not a current provider-contract observation.
rows_json() { "$BATON_CLAUDE" agents --json; }
echo "== every project =="
derive_in_flight "" "$(rows_json)" | jq .
echo "== Other alone =="
derive_in_flight Other "$(rows_json)" | jq -c .
echo "== the fork chain =="
current_session Fixture M02 2
