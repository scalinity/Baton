echo "== one parked lane: the ruling is delivered under the label, and the park is closed =="
"$BATON" answer M02 "expose it, and say why in the brief" || echo "status $?"
echo "== a ruling lands once: the second run has nothing waiting =="
