echo "== the pair has both closed out. M02's handover said M04 waits on M03 and M03's said run; the newest word that names M04 decides, and the join is dispatched with no person in it =="
"$BATON" tick
